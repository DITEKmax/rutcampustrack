---
phase: 29-pwa-scaffold-auth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontends/pwa/package.json
  - frontends/pwa/vite.config.ts
  - frontends/pwa/tsconfig.json
  - frontends/pwa/tsconfig.app.json
  - frontends/pwa/tsconfig.node.json
  - frontends/pwa/index.html
  - frontends/pwa/src/main.tsx
  - frontends/pwa/src/App.tsx
  - frontends/pwa/src/index.css
  - frontends/pwa/src/sw.ts
  - frontends/pwa/src/vite-env.d.ts
  - frontends/pwa/components.json
  - frontends/pwa/public/manifest.webmanifest
  - frontends/pwa/public/icons/icon-192.png
  - frontends/pwa/public/icons/icon-512.png
  - frontends/pwa/src/shared/types/pwa.d.ts
  - frontends/pwa/src/shared/lib/queryClient.ts
  - frontends/pwa/src/shared/lib/axios.ts
  - frontends/pwa/src/shared/hooks/useNetworkStatus.ts
  - frontends/pwa/src/shared/components/OfflineBanner.tsx
  - frontends/pwa/src/shared/components/LoadingSpinner.tsx
  - frontends/pwa/vitest.config.ts
  - frontends/pwa/src/test/setup.ts
autonomous: true
requirements:
  - PWA-04
  - PWA-05

must_haves:
  truths:
    - "npm run build produces a working Vite production build in frontends/pwa/dist/"
    - "manifest.webmanifest contains name RutCampusTrack, short_name RutTrack, display standalone, 192 and 512 icons"
    - "Service Worker sw.ts uses injectManifest strategy and calls precacheAndRoute"
    - "vitest runs and passes with jsdom environment"
    - "Tailwind CSS + shadcn/ui initialized with slate theme"
    - "TanStack Query client configured with Axios transport"
  artifacts:
    - path: "frontends/pwa/package.json"
      provides: "React PWA project with all dependencies"
      contains: "vite-plugin-pwa"
    - path: "frontends/pwa/src/sw.ts"
      provides: "Custom Service Worker with precache manifest injection"
      contains: "precacheAndRoute"
    - path: "frontends/pwa/vite.config.ts"
      provides: "Vite config with PWA plugin injectManifest strategy"
      contains: "injectManifest"
    - path: "frontends/pwa/components.json"
      provides: "shadcn/ui configuration"
      contains: "shadcn"
    - path: "frontends/pwa/src/shared/lib/axios.ts"
      provides: "Axios instance with interceptor infrastructure"
      contains: "withCredentials"
  key_links:
    - from: "frontends/pwa/vite.config.ts"
      to: "frontends/pwa/src/sw.ts"
      via: "vite-plugin-pwa injectManifest srcDir + filename"
      pattern: "filename.*sw\\.ts"
    - from: "frontends/pwa/src/main.tsx"
      to: "frontends/pwa/src/shared/lib/queryClient.ts"
      via: "QueryClientProvider import"
      pattern: "QueryClientProvider"
---

<objective>
Scaffold the React PWA project with Vite, Tailwind, shadcn/ui, TanStack Query, Axios, and Service Worker.

Purpose: PWA-04 and PWA-05 require a working PWA project with manifest, Service Worker precaching, and the UI/data-fetching foundation that Plan 03 builds auth features on. This plan creates the entire frontends/pwa/ project from scratch (currently only placeholder dist/ files from Phase 28 exist).

Output: A buildable React + TypeScript + Vite project with PWA manifest, custom Service Worker (injectManifest), Tailwind CSS + shadcn/ui (slate theme), TanStack Query + Axios, and test infrastructure (vitest).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-pwa-scaffold-auth/29-CONTEXT.md
@.planning/phases/29-pwa-scaffold-auth/29-RESEARCH.md
@.planning/phases/29-pwa-scaffold-auth/29-UI-SPEC.md
@.planning/phases/29-pwa-scaffold-auth/29-VALIDATION.md
@frontends/pwa/nginx.conf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create React + Vite project with Tailwind + shadcn/ui</name>
  <files>
    frontends/pwa/package.json,
    frontends/pwa/vite.config.ts,
    frontends/pwa/tsconfig.json,
    frontends/pwa/tsconfig.app.json,
    frontends/pwa/tsconfig.node.json,
    frontends/pwa/index.html,
    frontends/pwa/src/main.tsx,
    frontends/pwa/src/App.tsx,
    frontends/pwa/src/index.css,
    frontends/pwa/src/vite-env.d.ts,
    frontends/pwa/components.json,
    frontends/pwa/public/manifest.webmanifest,
    frontends/pwa/public/icons/icon-192.png,
    frontends/pwa/public/icons/icon-512.png,
    frontends/pwa/src/shared/types/pwa.d.ts,
    frontends/pwa/.gitignore
  </files>
  <read_first>
    frontends/pwa/nginx.conf,
    frontends/pwa/dist/index.html,
    docs/design-decisions.md,
    .planning/phases/29-pwa-scaffold-auth/29-RESEARCH.md,
    .planning/phases/29-pwa-scaffold-auth/29-UI-SPEC.md
  </read_first>
  <action>
    Per D-05, D-07, D-08, D-09, D-14 from CONTEXT.md:

    **Step 1: Initialize Vite project.** Run from repo root:
    ```bash
    cd frontends/pwa
    npm create vite@latest . -- --template react-ts
    ```
    This creates the project scaffold. The existing `dist/` and `nginx.conf` must NOT be deleted — they are from Phase 28. Add `dist/` to `.gitignore` with an exception comment (Phase 28 tracked placeholder files — the real build output is gitignored).

    **Step 2: Install all dependencies.** Per RESEARCH.md standard stack:
    ```bash
    cd frontends/pwa
    npm install react-router@7 @tanstack/react-query axios motion @phosphor-icons/react
    npm install -D tailwindcss @tailwindcss/vite vite-plugin-pwa workbox-precaching
    ```
    Use Vite 7.x (NOT Vite 8) per RESEARCH.md Pitfall 1 — vite-plugin-pwa 1.2.0 peer dep requires `^7.0.0`. If `npm create vite@latest` installs Vite 8, downgrade: `npm install -D vite@7`.

    **Step 3: Initialize shadcn/ui.** Per D-05 and UI-SPEC:
    ```bash
    cd frontends/pwa
    npx shadcn@latest init
    ```
    When prompted, select: TypeScript, slate theme (default), CSS variables, src/shared/components/ui as components path. This creates `components.json`. Then install Phase 29 components:
    ```bash
    npx shadcn@latest add button input label card alert separator
    ```

    **Step 4: Configure Tailwind CSS.** Tailwind 4 uses `@tailwindcss/vite` plugin. In `src/index.css`:
    ```css
    @import "tailwindcss";
    ```
    The Vite plugin handles the rest. No separate `tailwind.config.ts` needed with Tailwind 4 + @tailwindcss/vite.

    **Step 5: Configure vite.config.ts.** Per D-14 and RESEARCH.md Pattern 4:
    ```typescript
    import { defineConfig } from 'vite'
    import react from '@vitejs/plugin-react'
    import tailwindcss from '@tailwindcss/vite'
    import { VitePWA } from 'vite-plugin-pwa'
    import path from 'path'

    export default defineConfig({
      plugins: [
        react(),
        tailwindcss(),
        VitePWA({
          strategies: 'injectManifest',
          srcDir: 'src',
          filename: 'sw.ts',
          registerType: 'autoUpdate',
          injectRegister: false,
          manifest: {
            name: 'RutCampusTrack',
            short_name: 'RutTrack',
            display: 'standalone',
            start_url: '/',
            background_color: '#ffffff',
            theme_color: '#0f172a',
            orientation: 'portrait',
            icons: [
              { src: '/icons/icon-192.png', sizes: '192x192', type: 'image/png', purpose: 'any maskable' },
              { src: '/icons/icon-512.png', sizes: '512x512', type: 'image/png', purpose: 'any maskable' }
            ]
          },
          devOptions: {
            enabled: true,
            type: 'module'
          }
        })
      ],
      resolve: {
        alias: {
          '@': path.resolve(__dirname, './src')
        }
      },
      server: {
        port: 5173,
        proxy: {
          '/api': {
            target: 'http://localhost:8080',
            changeOrigin: true
          }
        }
      }
    })
    ```

    **Step 6: Create Service Worker.** `src/sw.ts` per RESEARCH.md Pattern 4:
    ```typescript
    /// <reference lib="webworker" />
    import { precacheAndRoute } from 'workbox-precaching'

    declare const self: ServiceWorkerGlobalScope

    precacheAndRoute(self.__WB_MANIFEST)

    // Phase 31 will add push event handler here
    ```

    **Step 7: Create manifest.webmanifest.** Place at `public/manifest.webmanifest` exactly as specified in UI-SPEC Manifest Contract section. The vite-plugin-pwa `manifest` config in vite.config.ts generates the manifest automatically — but also have a static fallback in public/ for reference. Actually, vite-plugin-pwa generates the manifest from config — do NOT create a separate public/manifest.webmanifest as it would conflict. Remove this step; the manifest is generated by the plugin.

    **Step 8: Create placeholder icons.** Generate 192x192 and 512x512 solid slate (#0f172a) PNG rectangles at `public/icons/icon-192.png` and `public/icons/icon-512.png`. Use a Node.js script or canvas approach, or create minimal valid PNG files. Simplest: create them with a small script using `sharp` (install as devDep) or create manually. For placeholder purposes, a 1x1 PNG scaled won't work — create actual-size solid color PNGs. Install `sharp` temporarily:
    ```bash
    npx -y sharp-cli -i /dev/null -o public/icons/icon-192.png --create 192,192,4 --flatten --background "#0f172a"
    npx -y sharp-cli -i /dev/null -o public/icons/icon-512.png --create 512,512,4 --flatten --background "#0f172a"
    ```
    Or write a small Node script that creates the PNGs. Any approach that produces valid PNG files of the correct size with slate-900 (#0f172a) fill color works.

    **Step 9: Create PWA type declarations.** `src/shared/types/pwa.d.ts`:
    ```typescript
    interface BeforeInstallPromptEvent extends Event {
      prompt(): Promise<void>
      userChoice: Promise<{ outcome: 'accepted' | 'dismissed' }>
    }

    declare global {
      interface WindowEventMap {
        beforeinstallprompt: BeforeInstallPromptEvent
      }
    }

    export {}
    ```

    **Step 10: Create minimal App.tsx.** Placeholder that just renders "RutTrack" heading to verify the build works:
    ```tsx
    export default function App() {
      return <div className="min-h-screen bg-background text-foreground flex items-center justify-center">
        <h1 className="text-3xl font-semibold">RutTrack</h1>
      </div>
    }
    ```

    **Step 11: Configure main.tsx.** Minimal entry with QueryClientProvider (router added in Plan 03):
    ```tsx
    import { StrictMode } from 'react'
    import { createRoot } from 'react-dom/client'
    import { QueryClientProvider } from '@tanstack/react-query'
    import { queryClient } from './shared/lib/queryClient'
    import App from './App'
    import './index.css'

    createRoot(document.getElementById('root')!).render(
      <StrictMode>
        <QueryClientProvider client={queryClient}>
          <App />
        </QueryClientProvider>
      </StrictMode>
    )
    ```

    **Step 12: Ensure .gitignore excludes node_modules, build output, etc.** but does NOT exclude `dist/` (Phase 28 tracked placeholder files there). Actually, better to gitignore dist/ and let Phase 28's files stay tracked (git won't untrack them). Add standard Vite .gitignore contents.

    **Step 13: tsconfig.json** — Ensure `compilerOptions.paths` has `"@/*": ["./src/*"]` alias matching vite.config.ts resolve.alias. Ensure `lib` includes `ES2020` and `DOM` (NOT `WebWorker` — that's for sw.ts only, handled by the triple-slash reference).
  </action>
  <verify>
    <automated>cd frontends/pwa && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontends/pwa/package.json contains "vite-plugin-pwa", "react-router", "@tanstack/react-query", "axios", "motion", "@phosphor-icons/react", "tailwindcss", "@tailwindcss/vite"
    - frontends/pwa/package.json does NOT contain "vite": "8" (must be vite 7.x)
    - frontends/pwa/vite.config.ts contains `strategies: 'injectManifest'` and `filename: 'sw.ts'`
    - frontends/pwa/vite.config.ts contains manifest with `name: 'RutCampusTrack'` and `short_name: 'RutTrack'`
    - frontends/pwa/vite.config.ts contains `theme_color: '#0f172a'`
    - frontends/pwa/src/sw.ts contains `precacheAndRoute(self.__WB_MANIFEST)`
    - frontends/pwa/src/sw.ts contains `/// <reference lib="webworker" />`
    - frontends/pwa/components.json exists (shadcn initialized)
    - frontends/pwa/src/shared/components/ui/button.tsx exists (shadcn button installed)
    - frontends/pwa/src/index.css contains `@import "tailwindcss"` or `@tailwind` directives
    - frontends/pwa/public/icons/icon-192.png exists and is a valid PNG
    - frontends/pwa/public/icons/icon-512.png exists and is a valid PNG
    - frontends/pwa/src/shared/types/pwa.d.ts contains `BeforeInstallPromptEvent`
    - `npm run build` exits 0 and produces frontends/pwa/dist/index.html
  </acceptance_criteria>
  <done>
    React + Vite project builds successfully with PWA manifest, Service Worker (injectManifest), Tailwind CSS + shadcn/ui (slate theme), and all Phase 29 dependencies installed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create shared infrastructure — Axios, QueryClient, hooks, OfflineBanner, test setup</name>
  <files>
    frontends/pwa/src/shared/lib/axios.ts,
    frontends/pwa/src/shared/lib/queryClient.ts,
    frontends/pwa/src/shared/hooks/useNetworkStatus.ts,
    frontends/pwa/src/shared/components/OfflineBanner.tsx,
    frontends/pwa/src/shared/components/LoadingSpinner.tsx,
    frontends/pwa/vitest.config.ts,
    frontends/pwa/src/test/setup.ts
  </files>
  <read_first>
    frontends/pwa/package.json,
    frontends/pwa/vite.config.ts,
    frontends/pwa/tsconfig.json,
    .planning/phases/29-pwa-scaffold-auth/29-RESEARCH.md,
    .planning/phases/29-pwa-scaffold-auth/29-UI-SPEC.md
  </read_first>
  <action>
    Per D-03, D-06, D-12 from CONTEXT.md:

    **Step 1: Install vitest + testing deps.**
    ```bash
    cd frontends/pwa
    npm install -D vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom @types/react @types/react-dom
    ```
    Add to package.json scripts: `"test": "vitest run"`, `"test:watch": "vitest"`

    **Step 2: Create vitest.config.ts:**
    ```typescript
    import { defineConfig } from 'vitest/config'
    import react from '@vitejs/plugin-react'
    import path from 'path'

    export default defineConfig({
      plugins: [react()],
      test: {
        environment: 'jsdom',
        globals: true,
        setupFiles: ['./src/test/setup.ts'],
        css: false,
      },
      resolve: {
        alias: {
          '@': path.resolve(__dirname, './src')
        }
      }
    })
    ```

    **Step 3: Create test setup.** `src/test/setup.ts`:
    ```typescript
    import '@testing-library/jest-dom'
    ```

    **Step 4: Create Axios instance.** `src/shared/lib/axios.ts` per RESEARCH.md Pattern 2:
    ```typescript
    import axios from 'axios'

    let isRefreshing = false
    let pendingQueue: Array<{ resolve: (token: string) => void; reject: (err: unknown) => void }> = []

    const flushQueue = (token: string | null, error: unknown = null) => {
      pendingQueue.forEach(({ resolve, reject }) => {
        if (token) resolve(token)
        else reject(error)
      })
      pendingQueue = []
    }

    export const apiClient = axios.create({
      baseURL: '/api',
      withCredentials: true,
    })

    // Token getter — set by AuthProvider
    let getAccessToken: () => string | null = () => null
    export const setAccessTokenGetter = (fn: () => string | null) => {
      getAccessToken = fn
    }

    // Token setter — called by interceptor after refresh
    let onTokenRefreshed: ((token: string) => void) | null = null
    export const setTokenRefreshCallback = (fn: (token: string) => void) => {
      onTokenRefreshed = fn
    }

    // Logout callback — called when refresh fails
    let onAuthLogout: (() => void) | null = null
    export const setAuthLogoutCallback = (fn: () => void) => {
      onAuthLogout = fn
    }

    // Request interceptor: attach Bearer token
    apiClient.interceptors.request.use((config) => {
      const token = getAccessToken()
      if (token) {
        config.headers['Authorization'] = `Bearer ${token}`
      }
      return config
    })

    // Response interceptor: silent refresh on 401
    apiClient.interceptors.response.use(
      (response) => response,
      async (error) => {
        const original = error.config
        if (error.response?.status === 401 && !original._retry) {
          original._retry = true

          if (isRefreshing) {
            return new Promise((resolve, reject) => {
              pendingQueue.push({
                resolve: (token) => {
                  original.headers['Authorization'] = `Bearer ${token}`
                  resolve(apiClient(original))
                },
                reject,
              })
            })
          }

          isRefreshing = true
          try {
            // withCredentials: true sends the httpOnly refresh_token cookie automatically
            const { data } = await axios.post('/api/auth/refresh', {}, { withCredentials: true })
            const newToken = data.accessToken
            onTokenRefreshed?.(newToken)
            flushQueue(newToken)
            original.headers['Authorization'] = `Bearer ${newToken}`
            return apiClient(original)
          } catch (refreshError) {
            flushQueue(null, refreshError)
            onAuthLogout?.()
            return Promise.reject(refreshError)
          } finally {
            isRefreshing = false
          }
        }
        return Promise.reject(error)
      }
    )
    ```

    **Step 5: Create QueryClient.** `src/shared/lib/queryClient.ts`:
    ```typescript
    import { QueryClient } from '@tanstack/react-query'

    export const queryClient = new QueryClient({
      defaultOptions: {
        queries: {
          staleTime: 5 * 60 * 1000,     // 5 minutes
          gcTime: 10 * 60 * 1000,        // 10 minutes garbage collection
          retry: 1,
          refetchOnWindowFocus: false,
        },
      },
    })
    ```

    **Step 6: Create useNetworkStatus hook.** `src/shared/hooks/useNetworkStatus.ts`:
    ```typescript
    import { useSyncExternalStore } from 'react'

    function subscribe(callback: () => void) {
      window.addEventListener('online', callback)
      window.addEventListener('offline', callback)
      return () => {
        window.removeEventListener('online', callback)
        window.removeEventListener('offline', callback)
      }
    }

    function getSnapshot() {
      return navigator.onLine
    }

    function getServerSnapshot() {
      return true
    }

    export function useNetworkStatus() {
      const isOnline = useSyncExternalStore(subscribe, getSnapshot, getServerSnapshot)
      return { isOnline }
    }
    ```

    **Step 7: Create OfflineBanner.** `src/shared/components/OfflineBanner.tsx` per UI-SPEC:
    ```tsx
    import { AnimatePresence, motion } from 'motion/react'
    import { useNetworkStatus } from '@/shared/hooks/useNetworkStatus'

    export function OfflineBanner() {
      const { isOnline } = useNetworkStatus()

      return (
        <AnimatePresence>
          {!isOnline && (
            <motion.div
              initial={{ y: -32 }}
              animate={{ y: 0 }}
              exit={{ y: -32 }}
              transition={{ duration: 0.2, ease: 'easeOut' }}
              className="fixed top-0 left-0 right-0 z-50 h-8 bg-slate-100 text-slate-600 text-sm flex items-center justify-center"
            >
              Нет подключения к интернету
            </motion.div>
          )}
        </AnimatePresence>
      )
    }
    ```

    **Step 8: Create LoadingSpinner.** `src/shared/components/LoadingSpinner.tsx`:
    ```tsx
    export function LoadingSpinner() {
      return (
        <div className="flex items-center justify-center min-h-[200px]">
          <div className="h-8 w-8 animate-spin rounded-full border-4 border-muted border-t-primary" />
        </div>
      )
    }
    ```
  </action>
  <verify>
    <automated>cd frontends/pwa && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontends/pwa/src/shared/lib/axios.ts contains `withCredentials: true` on apiClient.create()
    - frontends/pwa/src/shared/lib/axios.ts contains `isRefreshing` flag and `pendingQueue` array (race condition prevention)
    - frontends/pwa/src/shared/lib/axios.ts contains `setAccessTokenGetter` and `setTokenRefreshCallback` exports
    - frontends/pwa/src/shared/lib/axios.ts interceptor calls `/api/auth/refresh` with `withCredentials: true`
    - frontends/pwa/src/shared/lib/queryClient.ts exports `queryClient` with `staleTime` and `gcTime` configured
    - frontends/pwa/src/shared/hooks/useNetworkStatus.ts exports `useNetworkStatus` using `useSyncExternalStore`
    - frontends/pwa/src/shared/components/OfflineBanner.tsx contains text "Нет подключения к интернету"
    - frontends/pwa/src/shared/components/OfflineBanner.tsx uses `AnimatePresence` from motion
    - frontends/pwa/src/shared/components/LoadingSpinner.tsx contains `animate-spin`
    - frontends/pwa/vitest.config.ts contains `environment: 'jsdom'`
    - frontends/pwa/src/test/setup.ts contains `@testing-library/jest-dom`
    - `npx vitest run` exits 0 (no test failures — may have 0 tests, that's OK at this stage)
  </acceptance_criteria>
  <done>
    Axios instance with silent refresh interceptor, TanStack Query client, network status hook, OfflineBanner component, LoadingSpinner, and vitest test infrastructure are all in place. Build still succeeds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PWA JS -> API Gateway | Axios sends withCredentials: true; cookie handled by browser |
| Service Worker -> Network | SW only precaches static assets; never caches /api/** |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-06 | Information Disclosure | Access token in memory | mitigate | Token stored in React closure/state, never written to localStorage or sessionStorage; cleared on logout and page unload |
| T-29-07 | Tampering | SW fetch interception | mitigate | sw.ts only calls `precacheAndRoute(self.__WB_MANIFEST)` — no custom fetch handler; API requests pass through to network |
| T-29-08 | Denial of Service | Concurrent 401 refresh storm | mitigate | `isRefreshing` flag + pendingQueue ensures only one /auth/refresh call at a time; others queued and resolved with same token |
</threat_model>

<verification>
1. `cd frontends/pwa && npm run build` — Vite build succeeds
2. `cd frontends/pwa && npx vitest run` — test runner works (0 or more tests pass)
3. Built `dist/` contains `sw.js` (compiled from sw.ts)
4. Built `dist/` contains `manifest.webmanifest` with RutTrack metadata
5. `public/icons/icon-192.png` and `icon-512.png` exist as valid PNG files
</verification>

<success_criteria>
Complete React PWA project scaffold that builds, has SW precaching via injectManifest, manifest with RutTrack branding, Tailwind + shadcn/ui (slate), Axios with interceptor infrastructure, TanStack Query, and vitest test setup. Ready for Plan 03 to add auth flow and UI screens.
</success_criteria>

<output>
After completion, create `.planning/phases/29-pwa-scaffold-auth/29-02-SUMMARY.md`
</output>
