---
phase: 53
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontends/web-panel/src/app/features/student/shared/student-schedule.types.ts
  - frontends/web-panel/src/app/features/student/shared/student-api.service.ts
  - frontends/web-panel/src/app/app.routes.ts
  - frontends/web-panel/src/app/layout/sidebar/sidebar.component.ts
  - frontends/web-panel/src/app/features/student/excuses/student-excuses.component.spec.ts
  - frontends/web-panel/src/app/features/student/late-checkin/student-late-checkin.component.spec.ts
  - frontends/web-panel/src/app/layout/shell/student-pwa-banner/student-pwa-banner.component.spec.ts
autonomous: true
requirements:
  - STU-WEB-07
  - STU-WEB-08
  - STU-WEB-10

must_haves:
  truths:
    - "Маршруты /student/excuses и /student/late-checkin зарегистрированы в app.routes.ts"
    - "Sidebar содержит два новых nav-item для STUDENT (Пропуски, Запрос отметки)"
    - "StudentApiService имеет методы getStudentRecords, submitExcuse, requestLateCheckin"
    - "student-schedule.types.ts содержит типы AttendanceRecord, ExcuseTicket, ExcuseSubmitRequest"
    - "Все три spec-файла Wave 0 существуют с failing-заглушками"
    - "cd frontends/web-panel && npm test проходит (265+ тестов, новые spec компилируются)"
  artifacts:
    - path: "frontends/web-panel/src/app/features/student/shared/student-schedule.types.ts"
      provides: "Новые типы AttendanceRecord, ExcuseTicket, ExcuseTicketStatus, ExcuseSubmitRequest"
      contains: "AttendanceRecord"
    - path: "frontends/web-panel/src/app/features/student/shared/student-api.service.ts"
      provides: "HTTP-методы для excuses и late-checkin"
      exports: ["getStudentRecords", "submitExcuse", "requestLateCheckin"]
    - path: "frontends/web-panel/src/app/app.routes.ts"
      provides: "Lazy-loaded маршруты excuses и late-checkin"
      contains: "excuses"
    - path: "frontends/web-panel/src/app/layout/sidebar/sidebar.component.ts"
      provides: "Nav items Пропуски и Запрос отметки"
      contains: "ph-file-text"
  key_links:
    - from: "app.routes.ts student children"
      to: "student-excuses.component.ts"
      via: "loadComponent lazy import"
      pattern: "import.*student-excuses"
    - from: "sidebar.component.ts allNavItems"
      to: "/student/excuses route"
      via: "route: '/student/excuses'"
      pattern: "student/excuses"
---

<objective>
Создать фундамент для фазы 53: расширить типы, API-сервис, маршруты и боковое меню, а также добавить Wave 0 spec-файлы-заглушки для трёх новых компонентов.

Purpose: Последующие планы 02, 03, 04 зависят от этих типов и маршрутов — Wave 2 начнётся немедленно после Wave 1.
Output: Расширенный student-schedule.types.ts, student-api.service.ts, app.routes.ts, sidebar.component.ts, три spec-файла.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/53/53-RESEARCH.md
@.planning/phases/53/53-UI-SPEC.md

<interfaces>
<!-- Текущие типы и экспорты — executor не должен искать в кодовой базе -->

Из frontends/web-panel/src/app/features/student/shared/student-schedule.types.ts (текущее):
```typescript
export type LessonStatus = 'PLANNED' | 'ACTIVE' | 'CLOSED' | 'CANCELLED';
export type AttendanceStatus = 'present' | 'absent' | 'excused' | 'free_attendance';
export interface PagedResponse<T> {
  _embedded?: Record<string, T[]>;
  page?: { totalElements: number; totalPages: number; size: number; number: number };
}
```

Из frontends/web-panel/src/app/features/student/shared/student-api.service.ts (текущее):
```typescript
@Injectable({ providedIn: 'root' })
export class StudentApiService {
  private readonly http = inject(HttpClient);
  // Импорты: Injectable, inject, HttpClient, HttpParams, Observable, map
  // Уже есть: getWeekLessons, getStudentStats, resolveGlobalThreshold, resolveGroupThreshold,
  //           checkin, getHomeworks, markHomeworkComplete, unmarkHomeworkComplete, getActiveSemesterId
}
```

Из frontends/web-panel/src/app/layout/sidebar/sidebar.component.ts (текущее):
```typescript
interface NavItem {
  label: string;
  icon: string;  // Phosphor icon class name
  route: string;
  roles: ('TEACHER' | 'ADMIN' | 'STUDENT')[];
}
readonly allNavItems: NavItem[] = [
  // последние student items:
  { label: 'Уведомления', icon: 'ph-bell', route: '/student/notifications', roles: ['STUDENT'] },
  { label: 'Профиль', icon: 'ph-user-circle', route: '/student/profile', roles: ['STUDENT'] },
  // сюда добавить два новых
];
```

Из frontends/web-panel/src/app/app.routes.ts (текущее):
```typescript
// student children заканчиваются на:
{ path: 'profile', loadComponent: () => import(...StudentProfileComponent) },
{ path: '', redirectTo: 'dashboard', pathMatch: 'full' },
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Расширить типы и API-сервис (Wave 0 + Wave 1)</name>

  <read_first>
    - frontends/web-panel/src/app/features/student/shared/student-schedule.types.ts
    - frontends/web-panel/src/app/features/student/shared/student-api.service.ts
    - .planning/phases/53/53-RESEARCH.md (Architecture Patterns §1-3)
  </read_first>

  <files>
    frontends/web-panel/src/app/features/student/shared/student-schedule.types.ts,
    frontends/web-panel/src/app/features/student/shared/student-api.service.ts,
    frontends/web-panel/src/app/features/student/excuses/student-excuses.component.spec.ts,
    frontends/web-panel/src/app/features/student/late-checkin/student-late-checkin.component.spec.ts,
    frontends/web-panel/src/app/layout/shell/student-pwa-banner/student-pwa-banner.component.spec.ts
  </files>

  <behavior>
    - Test student-schedule.types.ts: экспортирует AttendanceRecord, ExcuseTicket, ExcuseSubmitRequest
    - Test student-api.service.ts: getStudentRecords() вызывает GET /api/attendance/reports/student/records
    - Test student-api.service.ts: submitExcuse() при HTTP 404 возвращает Observable<void> без ошибки (graceful degradation)
    - Test student-api.service.ts: requestLateCheckin() при HTTP 404 возвращает Observable<void> без ошибки
    - Spec-заглушки компилируются и содержат placeholder-тест `it('should create', ...)`
  </behavior>

  <action>
**1. student-schedule.types.ts — добавить в конец файла (после NotificationItem):**

```typescript
/** Attendance record returned by GET /api/attendance/reports/student/records */
export interface AttendanceRecord {
  lessonId: number;
  subjectId: number;
  lessonDate: string;      // YYYY-MM-DD
  lessonNumber: number;
  status: string;          // 'present' | 'absent' | 'excused' | 'free_attendance' | 'cancelled'
  symbol: string;          // 'б' | 'н' | 'у' | 'сп' | '--'
  source: string;          // 'manual' | 'auto' | 'checkin'
}

export type ExcuseTicketStatus = 'pending' | 'approved' | 'rejected' | 'cancelled';

export interface ExcuseTicket {
  id: number;
  createdAt: string;         // ISO-8601
  subjectNames: string[];
  status: ExcuseTicketStatus;
  lessonIds: number[];
}

export interface ExcuseSubmitRequest {
  lessonIds: number[];
  comment: string | null;
  // files отправляются через FormData отдельно
}
```

**2. student-api.service.ts — добавить три метода + импорт catchError/throwError/of в начало файла:**

Добавить в импорты из 'rxjs': `catchError`, `of`, `throwError`
Добавить в импорты из '@angular/common/http': `HttpErrorResponse`
Добавить типы из student-schedule.types.ts: `AttendanceRecord`, `ExcuseSubmitRequest`

Добавить три метода в класс StudentApiService (после `getActiveSemesterId`):

```typescript
/**
 * Fetch the student's attendance records (all lessons with status).
 * Backend: GET /api/attendance/reports/student/records
 * HATEOAS embedded key: attendanceRecordEntryList (defensive fallback uses Object.values).
 */
getStudentRecords(subjectId?: number): Observable<AttendanceRecord[]> {
  let params = new HttpParams();
  if (subjectId != null) params = params.set('subjectId', String(subjectId));
  return this.http
    .get<PagedResponse<AttendanceRecord>>(
      '/api/attendance/reports/student/records',
      { params },
    )
    .pipe(
      map(resp => {
        const embedded = resp._embedded;
        if (!embedded) return [];
        // Defensive: try known key first, then first array value
        return (
          embedded['attendanceRecordEntryList'] ??
          (Object.values(embedded)[0] as AttendanceRecord[]) ??
          []
        );
      }),
    );
}

/**
 * Submit an excuse ticket with optional file attachments.
 * Backend endpoint (POST /api/attendance/excuses) is deferred from v5.0.
 * HTTP 404 → graceful degradation (treated as success).
 */
submitExcuse(
  lessonIds: number[],
  comment: string | null,
  files: File[],
): Observable<void> {
  const body = new FormData();
  body.append('lessonIds', JSON.stringify(lessonIds));
  if (comment) body.append('comment', comment);
  files.forEach(f => body.append('files', f));
  return this.http.post<void>('/api/attendance/excuses', body).pipe(
    catchError((err: HttpErrorResponse) => {
      if (err.status === 404) return of(undefined);
      return throwError(() => err);
    }),
  );
}

/**
 * Request a late check-in for a specific absent lesson.
 * Backend endpoint (POST /api/attendance/late-checkin/{lessonId}) is deferred.
 * HTTP 404 → graceful degradation (treated as success).
 */
requestLateCheckin(lessonId: number): Observable<void> {
  return this.http
    .post<void>(`/api/attendance/late-checkin/${lessonId}`, {})
    .pipe(
      catchError((err: HttpErrorResponse) => {
        if (err.status === 404) return of(undefined);
        return throwError(() => err);
      }),
    );
}
```

**3. Создать Wave 0 spec-файл для excuses** (создать директорию `features/student/excuses/` если не существует):

`frontends/web-panel/src/app/features/student/excuses/student-excuses.component.spec.ts`:
```typescript
import { describe, it, expect, vi } from 'vitest';
import { render } from '@testing-library/angular';
import { StudentExcusesComponent } from './student-excuses.component';
import { HttpClientTestingModule } from '@angular/common/http/testing';
import { provideAnimations } from '@angular/platform-browser/animations';

describe('StudentExcusesComponent', () => {
  // Wave 0 stub — компонент ещё не существует
  it('stub: placeholder for STU-WEB-07 tests', () => {
    expect(true).toBe(true);
  });
});
```

**4. Создать Wave 0 spec-файл для late-checkin** (создать директорию `features/student/late-checkin/`):

`frontends/web-panel/src/app/features/student/late-checkin/student-late-checkin.component.spec.ts`:
```typescript
import { describe, it, expect } from 'vitest';

describe('StudentLateCheckinComponent', () => {
  // Wave 0 stub — компонент ещё не существует
  it('stub: placeholder for STU-WEB-08 tests', () => {
    expect(true).toBe(true);
  });
});
```

**5. Создать Wave 0 spec-файл для PWA banner** (создать директорию `layout/shell/student-pwa-banner/`):

`frontends/web-panel/src/app/layout/shell/student-pwa-banner/student-pwa-banner.component.spec.ts`:
```typescript
import { describe, it, expect } from 'vitest';

describe('StudentPwaBannerComponent', () => {
  // Wave 0 stub — компонент ещё не существует
  it('stub: placeholder for STU-WEB-10 tests', () => {
    expect(true).toBe(true);
  });
});
```
  </action>

  <verify>
    <automated>cd frontends/web-panel && npm test 2>&1 | tail -20</automated>
  </verify>

  <done>
    - student-schedule.types.ts экспортирует AttendanceRecord, ExcuseTicket, ExcuseTicketStatus, ExcuseSubmitRequest
    - student-api.service.ts содержит getStudentRecords, submitExcuse, requestLateCheckin
    - Три spec-файла существуют и компилируются без ошибок
    - npm test проходит (265+ тестов)
  </done>
</task>

<task type="auto">
  <name>Task 2: Добавить маршруты и sidebar nav items</name>

  <read_first>
    - frontends/web-panel/src/app/app.routes.ts
    - frontends/web-panel/src/app/layout/sidebar/sidebar.component.ts
    - .planning/phases/53/53-RESEARCH.md (Architecture Patterns §Routes, §Sidebar)
  </read_first>

  <files>
    frontends/web-panel/src/app/app.routes.ts,
    frontends/web-panel/src/app/layout/sidebar/sidebar.component.ts
  </files>

  <action>
**1. app.routes.ts — добавить два маршрута в student children перед `{ path: '', redirectTo: 'dashboard' }`:**

```typescript
{
  path: 'excuses',
  loadComponent: () =>
    import('./features/student/excuses/student-excuses.component')
      .then(m => m.StudentExcusesComponent),
  data: { title: 'Пропуски', eyebrow: 'Студент' },
},
{
  path: 'late-checkin',
  loadComponent: () =>
    import('./features/student/late-checkin/student-late-checkin.component')
      .then(m => m.StudentLateCheckinComponent),
  data: { title: 'Запрос отметки', eyebrow: 'Студент' },
},
```

Вставить ПЕРЕД строкой `{ path: '', redirectTo: 'dashboard', pathMatch: 'full' }` в блоке student children.

**2. sidebar.component.ts — добавить два nav item в allNavItems ПОСЛЕ Профиль (после `ph-user-circle`):**

```typescript
{
  label: 'Пропуски',
  icon: 'ph-file-text',
  route: '/student/excuses',
  roles: ['STUDENT'],
},
{
  label: 'Запрос отметки',
  icon: 'ph-clock-countdown',
  route: '/student/late-checkin',
  roles: ['STUDENT'],
},
```

Вставить после объекта с `route: '/student/profile'` и до закрывающей скобки `]` массива `allNavItems`.
  </action>

  <acceptance_criteria>
    - grep "student/excuses" frontends/web-panel/src/app/app.routes.ts → находит строку с loadComponent
    - grep "student/late-checkin" frontends/web-panel/src/app/app.routes.ts → находит строку с loadComponent
    - grep "ph-file-text" frontends/web-panel/src/app/layout/sidebar/sidebar.component.ts → находит nav item
    - grep "ph-clock-countdown" frontends/web-panel/src/app/layout/sidebar/sidebar.component.ts → находит nav item
    - grep "Пропуски" frontends/web-panel/src/app/layout/sidebar/sidebar.component.ts → найдено
    - grep "Запрос отметки" frontends/web-panel/src/app/layout/sidebar/sidebar.component.ts → найдено
  </acceptance_criteria>

  <verify>
    <automated>cd frontends/web-panel && npm test 2>&1 | tail -10</automated>
  </verify>

  <done>
    - app.routes.ts содержит маршруты /student/excuses и /student/late-checkin с lazy loadComponent
    - sidebar.component.ts содержит два новых nav item для STUDENT с иконками ph-file-text и ph-clock-countdown
    - npm test продолжает проходить
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Types only | Plan 01 не имеет UI или HTTP-вызовов — только типы, методы API и конфиг маршрутов |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01-01 | Tampering | submitExcuse FormData | accept | Plan 01 только объявляет метод; file validation реализуется в Plan 02 (ExcuseFormDialog) |
| T-53-01-02 | Information Disclosure | catchError 404 degradation | accept | Низкий риск: 404 трактуется как успех в соответствии с ROADMAP Phase 53 Notes; backend deferred |
</threat_model>

<verification>
После выполнения обоих задач:
1. `cd frontends/web-panel && npm test` — все тесты зелёные
2. `grep -r "getStudentRecords\|submitExcuse\|requestLateCheckin" frontends/web-panel/src/app/features/student/shared/student-api.service.ts` → три метода найдены
3. `grep "AttendanceRecord\|ExcuseTicket\|ExcuseSubmitRequest" frontends/web-panel/src/app/features/student/shared/student-schedule.types.ts` → три типа найдены
4. `grep "student/excuses\|student/late-checkin" frontends/web-panel/src/app/app.routes.ts` → два маршрута найдены
5. Три spec-файла Wave 0 существуют
</verification>

<success_criteria>
- student-schedule.types.ts: добавлены AttendanceRecord, ExcuseTicket, ExcuseTicketStatus, ExcuseSubmitRequest
- student-api.service.ts: добавлены getStudentRecords (с defensive embedded-key), submitExcuse (graceful 404), requestLateCheckin (graceful 404)
- app.routes.ts: /student/excuses и /student/late-checkin зарегистрированы как lazy-loaded в student children
- sidebar.component.ts: два новых nav item (ph-file-text Пропуски, ph-clock-countdown Запрос отметки) для роли STUDENT
- Три Wave 0 spec-файла существуют и npm test проходит
</success_criteria>

<output>
После завершения создать `.planning/phases/53/53-01-SUMMARY.md`
</output>
