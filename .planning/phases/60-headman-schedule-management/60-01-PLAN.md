---
phase: 60-headman-schedule-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/academic-service/academic-app/src/main/resources/db/migration/V12__subjects_group_id.sql
  - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/entity/Subject.java
  - services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/dto/subject/CreateSubjectRequest.java
  - services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/dto/subject/SubjectResponse.java
  - services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/api/SubjectApi.java
  - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/subject/SubjectService.java
  - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/subject/SubjectAssembler.java
  - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/repository/SubjectRepository.java
  - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/subject/SubjectController.java
  - services/academic-service/academic-app/src/test/java/ru/rutcampustrack/academic/subject/SubjectServiceIT.java
autonomous: true
requirements: [AC-01, AC-02]

must_haves:
  truths:
    - "POST /api/academic/subjects с телом {name, type, teacherIds:[...]} атомарно создаёт 1 запись subjects + N записей teacher_subject_groups; при сбое после subjects.save — откат всей транзакции"
    - "subjects.group_id колонка существует, NOT NULL, имеет FK на groups(id)"
    - "GET /api/academic/subjects возвращает только предметы group старосты (не глобально)"
    - "POST /api/academic/subjects/{id}/teachers/{teacherId} добавляет запись в teacher_subject_groups; DELETE удаляет"
  artifacts:
    - path: "services/academic-service/academic-app/src/main/resources/db/migration/V12__subjects_group_id.sql"
      provides: "Flyway migration: DELETE subjects, ADD group_id NOT NULL FK"
    - path: "services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/dto/subject/CreateSubjectRequest.java"
      provides: "record с полями name, type, teacherIds (List<Long>)"
    - path: "services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/entity/Subject.java"
      provides: "entity с полем groupId"
    - path: "services/academic-service/academic-app/src/test/java/ru/rutcampustrack/academic/subject/SubjectServiceIT.java"
      provides: "IT тест атомарности + rollback"
  key_links:
    - from: "SubjectService.createSubject"
      to: "TeacherSubjectGroupRepository.saveAll"
      via: "@Transactional — единая транзакция"
      pattern: "tsgrRepository\\.saveAll"
    - from: "SubjectService.listSubjects"
      to: "SubjectRepository.findByGroupId"
      via: "requestContext.getGroupId() для фильтрации"
      pattern: "findByGroupId"
---

<objective>
Починить модель Subject в academic-service: добавить `subjects.group_id NOT NULL` через Flyway V12, переработать `CreateSubjectRequest` для приёма `teacherIds`, реализовать атомарное создание Subject + N TeacherSubjectGroup в одной транзакции, добавить endpoints управления преподавателями существующего предмета (POST/DELETE `/api/academic/subjects/{id}/teachers/{teacherId}`), отфильтровать listSubjects по группе старосты.

Purpose: Устранить несоответствие между фронтом (шлёт teacherId) и бэкендом (игнорирует его). После фазы предмет привязан к группе и имеет N равноправных преподавателей-наблюдателей (D-01, D-02, D-15).

Output: V12 миграция, обновлённые DTO/entity/service/controller, IT-тесты атомарности.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/60-headman-schedule-management/60-CONTEXT.md
@.planning/phases/60-headman-schedule-management/60-RESEARCH.md

<interfaces>
<!-- Текущие файлы, которые executor читает перед изменением -->

From services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/entity/Subject.java:
```java
// Текущее состояние: НЕТ groupId — нужно добавить
@Entity @Table(name = "subjects")
public class Subject {
    private Long id;
    private String name;       // @Column(nullable = false, length = 255)
    private SubjectType type;  // @Column(nullable = false)
    private OffsetDateTime createdAt;
    // groupId ОТСУТСТВУЕТ — добавить @Setter @Column(name="group_id", nullable=false) private Long groupId;
}
```

From services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/entity/TeacherSubjectGroup.java:
```java
@Entity @Table(name = "teacher_subject_groups", uniqueConstraints = @UniqueConstraint(columnNames = {"teacher_id","subject_id","group_id","semester_id"}))
public class TeacherSubjectGroup {
    // Конструктор: public TeacherSubjectGroup(Long teacherId, Long subjectId, Long groupId, Long semesterId)
}
```

From services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/subject/SubjectService.java:
```java
// Текущий createSubject: ИГНОРИРУЕТ teacherId (per задача фазы 60 — починить):
public Subject createSubject(CreateSubjectRequest request) {
    requireHeadman();
    Subject subject = new Subject();
    subject.setName(request.name());
    subject.setType(request.type());
    return subjectRepository.save(subject);  // НЕТ group_id, НЕТ TeacherSubjectGroup
}
```

From services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/dto/subject/CreateSubjectRequest.java:
```java
// Текущее: record CreateSubjectRequest(String name, SubjectType type)
// Нужно: добавить List<Long> teacherIds (nullable — N >= 0)
```

From services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/repository/SemesterRepository.java (для active semester lookup):
// Используй academicGrpcClient или SemesterService для получения активного семестра
// В academic-service используй SemesterRepository.findByIsActiveTrue() напрямую

From services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/security/RequestContext.java:
// requestContext.isHeadman() — проверка роли
// requestContext.getGroupId() — Long groupId из JWT claim (X-Group-Id header)
// requestContext.getUserId() — Long userId
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flyway V12 + Subject entity (group_id)</name>
  <read_first>
    - services/academic-service/academic-app/src/main/resources/db/migration/V11__enum_equality_operators.sql (предыдущая миграция — убедиться в номере V12)
    - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/entity/Subject.java
    - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/repository/SubjectRepository.java
    - .planning/phases/60-headman-schedule-management/60-CONTEXT.md (D-01: DELETE + ADD NOT NULL, потому что тестовые данные)
  </read_first>
  <files>
    services/academic-service/academic-app/src/main/resources/db/migration/V12__subjects_group_id.sql,
    services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/entity/Subject.java,
    services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/repository/SubjectRepository.java,
    services/academic-service/academic-app/src/test/java/ru/rutcampustrack/academic/subject/SubjectSchemaIT.java
  </files>
  <behavior>
    - Test 1: SubjectSchemaIT.groupIdIsNotNull — попытка INSERT без group_id бросает DataIntegrityViolationException
    - Test 2: SubjectSchemaIT.groupIdHasForeignKey — INSERT с несуществующим group_id бросает DataIntegrityViolationException
    - Test 3: findByGroupId(groupId) возвращает только предметы данной группы
  </behavior>
  <action>
    1. Создать `V12__subjects_group_id.sql`:
    ```sql
    BEGIN;
    -- D-01: удалить тестовые данные перед NOT NULL constraint
    DELETE FROM teacher_subject_groups;
    DELETE FROM subjects;
    -- Добавить group_id NOT NULL с FK
    ALTER TABLE subjects ADD COLUMN group_id BIGINT NOT NULL;
    ALTER TABLE subjects ADD CONSTRAINT fk_subjects_group FOREIGN KEY (group_id) REFERENCES groups(id);
    COMMIT;
    ```
    2. В `Subject.java` добавить поле:
    ```java
    @Setter
    @Column(name = "group_id", nullable = false)
    private Long groupId;
    ```
    3. В `SubjectRepository.java` добавить:
    ```java
    Page<Subject> findByGroupId(Long groupId, Pageable pageable);
    boolean existsByIdAndGroupId(Long id, Long groupId);
    ```
    4. Создать `SubjectSchemaIT.java` с @SpringBootTest + Testcontainers (PostgreSQL), проверяющим constraints.

    ВАЖНО: Lombok используется только в `*-app` модуле. Subject.java находится в academic-app — Lombok допустим.
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && ./gradlew.bat :services:academic-service:academic-app:test --tests "ru.rutcampustrack.academic.subject.SubjectSchemaIT" -x javadoc 2>&1 | tail -20</automated>
  </verify>
  <done>
    V12 миграция применяется без ошибок; subjects.group_id существует как NOT NULL с FK; SubjectSchemaIT все тесты зелёные; `findByGroupId` объявлен в репозитории.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CreateSubjectRequest + SubjectService атомарность + teacher endpoints</name>
  <read_first>
    - services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/dto/subject/CreateSubjectRequest.java
    - services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/api/SubjectApi.java
    - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/subject/SubjectService.java
    - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/entity/TeacherSubjectGroup.java
    - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/repository/TeacherSubjectGroupRepository.java
    - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/security/RequestContext.java
    - .planning/phases/60-headman-schedule-management/60-RESEARCH.md (Pattern 3: Transaction Atomicity)
  </read_first>
  <files>
    services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/dto/subject/CreateSubjectRequest.java,
    services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/dto/subject/SubjectResponse.java,
    services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/api/SubjectApi.java,
    services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/subject/SubjectService.java,
    services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/subject/SubjectAssembler.java,
    services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/subject/SubjectController.java,
    services/academic-service/academic-app/src/test/java/ru/rutcampustrack/academic/subject/SubjectServiceIT.java
  </files>
  <behavior>
    - Test 1: SubjectServiceIT.createSubject_withTwoTeachers — после вызова subjects таблица имеет 1 запись с group_id=headman.groupId; teacher_subject_groups имеет 2 записи с subject_id, group_id, semesterId (активный семестр)
    - Test 2: SubjectServiceIT.createSubject_rollbackOnTeacherSaveFail — если второй saveAll бросает исключение, subjects таблица остаётся пустой (полный откат)
    - Test 3: SubjectServiceIT.addTeacher_and_removeTeacher — POST .../teachers/{teacherId} создаёт TSG запись; повторный POST возвращает 409; DELETE удаляет запись
    - Test 4: SubjectServiceIT.listSubjects_filteredByGroup — GET /api/academic/subjects возвращает только предметы группы старосты, не чужих групп
  </behavior>
  <action>
    1. В `CreateSubjectRequest.java` (в academic-api-contract, БЕЗ Lombok):
    ```java
    public record CreateSubjectRequest(
        @NotBlank(message = "Название предмета обязательно") String name,
        @NotNull(message = "Тип предмета обязателен") SubjectType type,
        @NotNull(message = "Список преподавателей обязателен") List<Long> teacherIds
    ) {}
    ```
    teacherIds может быть пустым списком (не null).

    2. В `SubjectResponse.java` добавить поле `groupId` и `List<Long> teacherIds` (per D-15 — для UI отображения назначенных преподавателей):
    ```java
    public class SubjectResponse extends RepresentationModel<SubjectResponse> {
        private Long id;
        private String name;
        private SubjectType type;
        private Long groupId;
        private List<Long> teacherIds;
        // геттеры, сеттеры (не Lombok — контракт!)
    }
    ```
    ВНИМАНИЕ: `*-api-contract` — БЕЗ Lombok. Геттеры и сеттеры вручную или заменить на record с namedField (но SubjectResponse наследует RepresentationModel — должен быть class).

    3. В `SubjectApi.java` добавить endpoints (маппинги ТОЛЬКО в интерфейсе):
    ```java
    @PostMapping("/{id}/teachers/{teacherId}")
    @Operation(summary = "Добавить преподавателя к предмету (HEADMAN/ADMIN)")
    ResponseEntity<Void> addTeacher(@PathVariable Long id, @PathVariable Long teacherId);

    @DeleteMapping("/{id}/teachers/{teacherId}")
    @Operation(summary = "Удалить преподавателя из предмета (HEADMAN/ADMIN)")
    ResponseEntity<Void> removeTeacher(@PathVariable Long id, @PathVariable Long teacherId);
    ```
    Также обновить listSubjects — убрать pagination по всем предметам и добавить фильтр по groupId старосты.

    4. В `SubjectService.java`:
    - `createSubject`: добавить `subject.setGroupId(requestContext.getGroupId())`, lookup активного семестра через `SemesterRepository.findByIsActiveTrue()`, создать TSG записи: `request.teacherIds().stream().map(tid -> new TeacherSubjectGroup(tid, subject.getId(), groupId, semesterId)).toList()`, затем `tsgrRepository.saveAll(assignments)`. Всё в одной `@Transactional`.
    - `listSubjects`: фильтровать через `subjectRepository.findByGroupId(requestContext.getGroupId(), pageable)` если роль HEADMAN; ADMIN видит всё.
    - `addTeacher(Long subjectId, Long teacherId)`: проверить subject.groupId == headman.groupId; проверить уникальность через `tsgrRepository.existsByTeacherIdAndSubjectIdAndGroupIdAndSemesterId`; создать TSG запись.
    - `removeTeacher(Long subjectId, Long teacherId)`: удалить TSG запись для текущего семестра.
    - `updateSubject`: сохранить update без изменения groupId (groupId immutable после создания).

    5. В `SubjectController.java` реализовать новые методы из SubjectApi.

    6. В `SubjectAssembler.java` добавить маппинг `groupId` и `teacherIds` в SubjectResponse (для teacherIds загружать из tsgrRepository по subjectId).

    7. Создать `SubjectServiceIT.java` с @SpringBootTest + Testcontainers (PostgreSQL), покрывающим все 4 behavior.
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && ./gradlew.bat :services:academic-service:academic-app:test --tests "ru.rutcampustrack.academic.subject.SubjectServiceIT" -x javadoc 2>&1 | tail -25</automated>
  </verify>
  <done>
    CreateSubjectRequest принимает teacherIds; SubjectService.createSubject атомарно создаёт Subject + N TSG; addTeacher/removeTeacher endpoints работают; listSubjects фильтрует по groupId; SubjectServiceIT все тесты зелёные; `./gradlew.bat :services:academic-service:academic-app:build` зелёный.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→academic-service | teacherIds[] приходят из JWT-аутентифицированного запроса; groupId берётся из JWT (X-Group-Id) |
| academic-service→PostgreSQL | JPA parameterized queries предотвращают SQL injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Elevation of Privilege | SubjectService.createSubject | mitigate | `requireHeadman()` проверяет isHeadman() через RequestContext перед созданием; groupId берётся из JWT, не из body |
| T-60-02 | Tampering | SubjectService.createSubject | mitigate | `@Transactional` — atomicity гарантирует: либо Subject + все N TSG созданы, либо откат |
| T-60-06 | Tampering | SubjectRepository.findByGroupId | accept | JPA parameterized query (`WHERE group_id = :groupId`) — SQL injection невозможен |
</threat_model>

<verification>
После выполнения обоих tasks:
1. `./gradlew.bat :services:academic-service:academic-app:build` — BUILD SUCCESS
2. `grep -r "group_id" services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/entity/Subject.java` — содержит `group_id`
3. `grep -r "teacherIds" services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/dto/subject/CreateSubjectRequest.java` — содержит поле
4. `grep -rn "saveAll" services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/subject/SubjectService.java` — содержит tsgrRepository.saveAll
5. `grep -rn "Transactional" services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/subject/SubjectService.java | grep createSubject` — метод @Transactional
</verification>

<success_criteria>
- V12 применяется без ошибок: `subjects.group_id BIGINT NOT NULL REFERENCES groups(id)` в схеме
- POST /api/academic/subjects принимает `{name, type, teacherIds:[1,2]}` и создаёт 1 запись subjects + 2 записи teacher_subject_groups
- При ошибке во время saveAll — откат: subjects таблица пуста
- POST /api/academic/subjects/{id}/teachers/{teacherId} → 201 (или 409 при дубле)
- DELETE /api/academic/subjects/{id}/teachers/{teacherId} → 204
- SubjectServiceIT все тесты зелёные; `./gradlew.bat build` BUILD SUCCESS
</success_criteria>

<output>
После завершения создать `.planning/phases/60-headman-schedule-management/60-01-SUMMARY.md` с:
- Реализованные файлы (entity, DTO, service, Flyway)
- Добавленные endpoints
- Результат тестов (SubjectServiceIT, SubjectSchemaIT)
- Любые отклонения от плана
</output>
