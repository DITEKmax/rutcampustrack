---
phase: 60-headman-schedule-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/schedule-service/schedule-app/src/main/resources/db/migration/V3__drop_teacher_id.sql
  - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/item/entity/ScheduleItem.java
  - services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/item/CreateScheduleItemRequest.java
  - services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/item/UpdateScheduleItemRequest.java
  - services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/item/ScheduleItemResponse.java
  - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/item/ScheduleItemService.java
  - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/item/ScheduleItemAssembler.java
  - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/item/ScheduleItemController.java
  - services/schedule-service/schedule-app/src/test/java/ru/rutcampustrack/schedule/item/ScheduleItemEntityTest.java
autonomous: true
requirements: [AC-03, AC-04]

must_haves:
  truths:
    - "`schedule_items.teacher_id` колонка удалена из БД (Flyway V3)"
    - "ScheduleItem entity, CreateScheduleItemRequest, UpdateScheduleItemRequest, ScheduleItemResponse не содержат teacherId"
    - "Препод видит журнал через JOIN ScheduleItem × TeacherSubjectGroup WHERE TSG.teacher_id = :me"
    - "HEADMAN guard в ScheduleItemService.requireHeadmanForGroup() корректно проверяет groupId через gRPC isHeadman()"
  artifacts:
    - path: "services/schedule-service/schedule-app/src/main/resources/db/migration/V3__drop_teacher_id.sql"
      provides: "DROP COLUMN teacher_id из schedule_items"
    - path: "services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/item/entity/ScheduleItem.java"
      provides: "entity без teacherId поля"
  key_links:
    - from: "ScheduleItemService.updateScheduleItem"
      to: "existing.setSubjectId"
      via: "PUT /api/schedule/schedule-items/{id} — teacherId убран из запроса"
      pattern: "setTeacherId"
---

<objective>
Удалить `ScheduleItem.teacherId` из entity, DTO (CreateScheduleItemRequest, UpdateScheduleItemRequest, ScheduleItemResponse) и БД (Flyway V3 миграция). Все существующие references на teacherId устранить. Гарантировать что teacher-journal read-path использует JOIN через TeacherSubjectGroup (уже описан в attendance-service ReportService через gRPC — убедиться что нет зависимостей от ScheduleItem.teacherId).

Purpose: Реализовать D-16 — слот хранит только {subject_id, day, lesson_number, classroom, week_type}. Препод получает доступ к журналу через JOIN, не через teacherId слота.

Output: V3 миграция, очищенные DTO и entity, тест ScheduleItemEntityTest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60-headman-schedule-management/60-CONTEXT.md
@.planning/phases/60-headman-schedule-management/60-RESEARCH.md

<interfaces>
<!-- Текущее состояние ScheduleItem — teacherId удаляем -->
From services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/item/entity/ScheduleItem.java:
```java
// УДАЛИТЬ это поле:
@Setter
@Column(name = "teacher_id", nullable = false)
private Long teacherId;
```

From services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/item/ScheduleItemService.java:
```java
// УДАЛИТЬ строки вида:
item.setTeacherId(request.teacherId());
existing.setTeacherId(request.teacherId());
// и scheduleAffected проверку на teacherId если есть
```

From services/schedule-service/schedule-app/src/main/resources/db/migration/V2__add_enum_casts.sql:
// Следующая миграция — V3
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flyway V3 + удаление teacherId из entity и DTO</name>
  <read_first>
    - services/schedule-service/schedule-app/src/main/resources/db/migration/V1__baseline.sql (текущая схема с teacher_id)
    - services/schedule-service/schedule-app/src/main/resources/db/migration/V2__add_enum_casts.sql (убедиться в номере V3)
    - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/item/entity/ScheduleItem.java
    - services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/item/CreateScheduleItemRequest.java (найти в glob)
    - services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/item/UpdateScheduleItemRequest.java
    - services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/item/ScheduleItemResponse.java
    - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/item/ScheduleItemService.java
    - .planning/phases/60-headman-schedule-management/60-CONTEXT.md (D-16)
    - .planning/phases/60-headman-schedule-management/60-RESEARCH.md (Pitfall 2: удалить ПОСЛЕ переделки read-path)
  </read_first>
  <files>
    services/schedule-service/schedule-app/src/main/resources/db/migration/V3__drop_teacher_id.sql,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/item/entity/ScheduleItem.java,
    services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/item/CreateScheduleItemRequest.java,
    services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/item/UpdateScheduleItemRequest.java,
    services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/item/ScheduleItemResponse.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/item/ScheduleItemService.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/item/ScheduleItemAssembler.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/item/ScheduleItemController.java,
    services/schedule-service/schedule-app/src/test/java/ru/rutcampustrack/schedule/item/ScheduleItemEntityTest.java
  </files>
  <action>
    1. Создать `V3__drop_teacher_id.sql`:
    ```sql
    BEGIN;
    -- D-16: teacher_id удаляется из schedule_items (препод видит журнал через JOIN с TeacherSubjectGroup)
    ALTER TABLE schedule_items DROP COLUMN IF EXISTS teacher_id;
    COMMIT;
    ```
    Использовать `DROP COLUMN IF EXISTS` чтобы миграция была идемпотентной.

    2. В `ScheduleItem.java` удалить:
    ```java
    // УДАЛИТЬ:
    @Setter
    @Column(name = "teacher_id", nullable = false)
    private Long teacherId;
    ```

    3. В `CreateScheduleItemRequest.java` (record в schedule-api-contract, БЕЗ Lombok) удалить поле `teacherId`.
       Контракт в schedule-api-contract — убедиться что нет Lombok импортов.

    4. В `UpdateScheduleItemRequest.java` удалить поле `teacherId`.

    5. В `ScheduleItemResponse.java` удалить поле `teacherId` (или если это class — удалить геттер/сеттер).

    6. В `ScheduleItemService.java` удалить все ссылки:
       - `item.setTeacherId(request.teacherId())` в createScheduleItem
       - `existing.setTeacherId(request.teacherId())` в updateScheduleItem
       - в scheduleAffected проверке убрать `teacherId` из сравнения (если была)

    7. В `ScheduleItemAssembler.java` удалить маппинг `teacherId` в response.

    8. В `ScheduleItemController.java` убедиться нет прямых ссылок на teacherId.

    9. Поиск всех оставшихся ссылок:
       `grep -rn "teacherId\|teacher_id" services/schedule-service/schedule-app/src/main/java/`
       Должны остаться ТОЛЬКО в V1__baseline.sql (исторически) и V3__drop_teacher_id.sql.

    10. Создать `ScheduleItemEntityTest.java` (unit test, без Spring context):
    ```java
    @Test void scheduleItem_hasNoTeacherIdField() {
        // Reflection: ScheduleItem.class.getDeclaredFields() — нет поля teacherId
        var fieldNames = Arrays.stream(ScheduleItem.class.getDeclaredFields())
            .map(Field::getName).collect(Collectors.toSet());
        assertFalse(fieldNames.contains("teacherId"), "ScheduleItem must not have teacherId field after D-16");
    }
    ```
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && ./gradlew.bat :services:schedule-service:schedule-app:test --tests "ru.rutcampustrack.schedule.item.ScheduleItemEntityTest" -x javadoc 2>&1 | tail -20</automated>
  </verify>
  <done>
    V3 миграция создана; ScheduleItem.java не содержит teacherId поля; CreateScheduleItemRequest и UpdateScheduleItemRequest не содержат teacherId; ScheduleItemEntityTest зелёный; `grep -rn "setTeacherId" services/schedule-service/schedule-app/src/main/java/` возвращает 0 результатов.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard headmanForGroup — проверка groupId ресурса против группы старосты</name>
  <read_first>
    - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/item/ScheduleItemService.java (метод requireHeadmanForGroup — уже реализован)
    - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/LessonService.java (метод requireHeadmanForGroup — паттерн)
    - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/security/RequestContext.java
    - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/grpc/AcademicGrpcClient.java (isHeadman)
    - .planning/phases/60-headman-schedule-management/60-CONTEXT.md (D-11, D-20)
  </read_first>
  <files>
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/item/ScheduleItemService.java,
    services/schedule-service/schedule-app/src/test/java/ru/rutcampustrack/schedule/item/ScheduleItemSecurityTest.java
  </files>
  <behavior>
    - Test 1: ScheduleItemSecurityTest.requireHeadmanForGroup_headmanOfCorrectGroup_allowed — headman userId=1, groupId=1 → isHeadman mock returns true → нет исключения
    - Test 2: ScheduleItemSecurityTest.requireHeadmanForGroup_headmanOfWrongGroup_throws403 — headman userId=1, groupId=2 → isHeadman mock returns false → AccessDeniedException
    - Test 3: ScheduleItemSecurityTest.requireHeadmanForGroup_notHeadman_throws403 — requestContext.isHeadman()=false → AccessDeniedException (до gRPC)
    - Test 4: ScheduleItemSecurityTest.requireHeadmanForGroup_adminBypasses — role=ADMIN → нет gRPC вызова, нет исключения
  </behavior>
  <action>
    1. Прочитать текущий `requireHeadmanForGroup` в `ScheduleItemService.java`. Он уже реализован корректно (проверяет ADMIN bypass → isHeadman check → gRPC isHeadman). Если реализация корректна — только добавить unit test.

    2. Убедиться что в `LessonService.requireHeadmanForGroup` (уже используется для отмены) — та же логика.

    3. Создать `ScheduleItemSecurityTest.java` — unit тест с Mockito:
    - Мокировать `RequestContext`, `AcademicGrpcClient`
    - Проверить все 4 случая из behavior
    - Тест проходит без Spring context (чистый Mockito unit test)

    ЗАМЕЧАНИЕ: Согласно D-20 — backend отмены уже реализован и проверен. Этот task только гарантирует что guard корректен для ScheduleItem operations.
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && ./gradlew.bat :services:schedule-service:schedule-app:test --tests "ru.rutcampustrack.schedule.item.ScheduleItemSecurityTest" -x javadoc 2>&1 | tail -20</automated>
  </verify>
  <done>
    ScheduleItemSecurityTest все 4 теста зелёные; guard корректно: ADMIN bypass, isHeadman=false → 403, чужая группа → 403; `./gradlew.bat :services:schedule-service:schedule-app:build` зелёный.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→schedule-service | Запросы CRUD расписания от headman/admin |
| schedule-service→academic-service (gRPC) | isHeadman() check — проверка что headman владеет группой |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Broken Access Control | ScheduleItemService.requireHeadmanForGroup | mitigate | Двухэтапная проверка: (1) requestContext.isHeadman() — флаг из JWT; (2) gRPC academicGrpcClient.isHeadman(userId, targetGroupId) — верификация владения группой; возвращает 403 при отказе |
| T-60-06 | Injection | ScheduleItemRepository queries | accept | Spring Data JPA использует parameterized queries; нет JPQL конкатенации строк |
</threat_model>

<verification>
1. `./gradlew.bat :services:schedule-service:schedule-app:build` — BUILD SUCCESS
2. `grep -rn "teacherId\|teacher_id" services/schedule-service/schedule-app/src/main/java/` — 0 результатов (только V1/V3 SQL)
3. `grep -rn "requireHeadmanForGroup" services/schedule-service/schedule-app/src/main/java/` — вызывается в createScheduleItem, updateScheduleItem, deleteScheduleItem
4. ScheduleItemEntityTest и ScheduleItemSecurityTest зелёные
</verification>

<success_criteria>
- V3 миграция применяется; `schedule_items` таблица не содержит `teacher_id` колонки
- ScheduleItem.java, CreateScheduleItemRequest.java, UpdateScheduleItemRequest.java не содержат teacherId поля
- `./gradlew.bat :services:schedule-service:schedule-app:build` BUILD SUCCESS
- ScheduleItemEntityTest зелёный: reflection подтверждает отсутствие teacherId
- ScheduleItemSecurityTest зелёный: все 4 guard сценария
</success_criteria>

<output>
После завершения создать `.planning/phases/60-headman-schedule-management/60-02-SUMMARY.md` с реализованными изменениями и результатами тестов.
</output>
