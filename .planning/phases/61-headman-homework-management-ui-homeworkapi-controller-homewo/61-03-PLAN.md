---
phase: 61-headman-homework-management-ui-homeworkapi-controller-homewo
plan: 03
type: execute
wave: 3
depends_on: [61-02]
files_modified:
  - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/homework/HomeworkService.java
  - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/homework/HomeworkController.java
  - services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/api/HomeworkApi.java
  - services/academic-service/academic-app/src/test/java/ru/rutcampustrack/academic/homework/HomeworkServiceTest.java
  - services/academic-service/academic-app/src/test/java/ru/rutcampustrack/academic/homework/HomeworkControllerIT.java
autonomous: true
requirements: [HW-VAL-DATE, HW-VAL-LESSON, HW-AUTHOR, HW-ROLE]

must_haves:
  truths:
    - "POST /api/academic/homeworks с lessonDate < today → 400 (D-03)"
    - "POST с (groupId, lessonDate, lessonNumber) не существующим в расписании → 400 (D-04)"
    - "POST с subjectId ≠ lesson.subject_id → 400 (D-04)"
    - "PUT/DELETE /api/academic/homeworks/{id} от НЕ автора → 403 (D-05)"
    - "POST от STUDENT БЕЗ is_headman → 403 (D-06)"
    - "POST от ADMIN → 403 (D-06: ADMIN убран из разрешённых ролей для create/update/delete)"
    - "На одну пару (group_id, date, lesson_number) можно создать ≥2 ДЗ подряд — нет UNIQUE constraint"
  artifacts:
    - path: "services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/homework/HomeworkService.java"
      provides: "валидации D-03/04/05/06 + injection Clock + ScheduleGrpcClient"
      contains: "isBefore(LocalDate.now(clock))"
    - path: "services/academic-service/academic-app/src/test/java/ru/rutcampustrack/academic/homework/HomeworkServiceTest.java"
      provides: "unit тесты для всех 6 сценариев валидации"
  key_links:
    - from: "HomeworkService.createHomework"
      to: "scheduleGrpcClient.resolveLesson"
      via: "D-04 проверка существования пары"
      pattern: "scheduleGrpcClient\\.resolveLesson"
    - from: "HomeworkService.createHomework"
      to: "requestContext.isHeadman()"
      via: "D-06 guard"
      pattern: "isHeadman"
    - from: "HomeworkService.updateHomework/deleteHomework"
      to: "homework.getPublishedBy().equals(requestContext.getUserId())"
      via: "D-05 guard"
      pattern: "getPublishedBy"
---

<objective>
Реализовать бэкенд-валидации и guards для ДЗ: D-03 (дата ≥ today), D-04 (пара существует + subject match), D-05 (только автор update/delete), D-06 (только HEADMAN create). Убрать ADMIN из разрешённых ролей для write-операций.

Purpose: Реализует D-03, D-04, D-05, D-06. Использует инфраструктуру из PLAN-02 (Clock, ScheduleGrpcClient, DTO с полями). Полное тест-покрытие: 6 сценариев в unit-тестах + 3 в integration.

Output: обновлённый HomeworkService + HomeworkController + HomeworkApi (annotations), unit-тесты (Mockito), controller IT (MockMvc + Testcontainers).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-headman-homework-management-ui-homeworkapi-controller-homewo/61-CONTEXT.md
@.planning/phases/61-headman-homework-management-ui-homeworkapi-controller-homewo/61-RESEARCH.md
@CLAUDE.md

<interfaces>
From services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/homework/HomeworkService.java:
```java
// Текущее createHomework (строки 68-82) — просто сохраняет и публикует event.
// Нужно добавить валидации D-03/04/06 ПЕРЕД save.
// Необходимо внедрить: Clock clock, ScheduleGrpcClient scheduleGrpcClient, RequestContext requestContext.
```

From services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/homework/HomeworkController.java:
```java
// Текущий @RequireRole аспект пропускает ADMIN+HEADMAN на create.
// По D-06 убрать ADMIN из create/update/delete.
// Для list (GET) оставить как есть — student видит свои ДЗ.
```

From shared RequestContext:
```java
// requestContext.getUserId() — Long
// requestContext.getRole() — UserRole enum (ADMIN/TEACHER/STUDENT)
// requestContext.isHeadman() — boolean (STUDENT + is_headman=true)
```

From services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/exception/:
```java
// BadRequestException → 400 через @ControllerAdvice
// AccessDeniedException (Spring Security) → 403
// ScheduleServiceUnavailableException (PLAN-02) → 503
```

From services/academic-service/academic-app/src/test/java/ru/rutcampustrack/academic/group/GroupServiceTest.java:
// Паттерн unit-теста с Mockito
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HomeworkService — D-03/D-04/D-06 guards в createHomework + unit-тесты</name>
  <read_first>
    - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/homework/HomeworkService.java (текущая реализация)
    - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/event/HomeworkPublishedEvent.java (добавим lessonDate/lessonNumber здесь же — или в PLAN-04, решить per current signature; researcher: в PLAN-04)
    - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/shared/context/RequestContext.java (API)
    - services/academic-service/academic-app/src/test/java/ru/rutcampustrack/academic/group/GroupServiceTest.java (шаблон unit-тестов)
    - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/exception/BadRequestException.java
  </read_first>
  <files>
    services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/homework/HomeworkService.java,
    services/academic-service/academic-app/src/test/java/ru/rutcampustrack/academic/homework/HomeworkServiceTest.java
  </files>
  <behavior>
    - `createHomework_throwsForbidden_whenNotHeadman()` — requestContext.isHeadman()=false → AccessDeniedException (D-06).
    - `createHomework_throwsForbidden_whenAdmin()` — role=ADMIN, isHeadman=false → AccessDeniedException (D-06).
    - `createHomework_throwsBadRequest_whenDateInPast()` — lessonDate=yesterday → BadRequestException (D-03). Clock зафиксирован на "2026-05-01T12:00:00 Europe/Moscow".
    - `createHomework_throwsBadRequest_whenLessonNotFound()` — scheduleGrpcClient.resolveLesson → Optional.empty() → BadRequestException (D-04).
    - `createHomework_throwsBadRequest_whenSubjectMismatch()` — resolveLesson возвращает LessonResponse с subjectId=99, request.subjectId=42 → BadRequestException (D-04).
    - `createHomework_persistsAndPublishesEvent_whenValid()` — happy path: staroста, дата завтра, lesson found с подходящим subject → saved + eventPublisher вызван.
  </behavior>
  <action>
    В `HomeworkService.createHomework(CreateHomeworkRequest req)` добавить в порядке:

    ```java
    // D-06: только HEADMAN
    if (!requestContext.isHeadman()) {
        throw new AccessDeniedException("Только староста может создавать ДЗ");
    }

    // D-03: дата не в прошлом
    if (req.lessonDate().isBefore(LocalDate.now(clock))) {
        throw new BadRequestException("Нельзя создать ДЗ на прошедшую дату");
    }

    // D-04: пара существует + subject match
    LessonResponse lesson = scheduleGrpcClient
        .resolveLesson(req.groupId(), req.lessonDate(), req.lessonNumber())
        .orElseThrow(() -> new BadRequestException(
            "На эту пару нельзя назначить ДЗ — пары нет в расписании"));
    if (!Long.valueOf(lesson.getSubjectId()).equals(req.subjectId())) {
        throw new BadRequestException("ДЗ можно задать только по предмету этой пары");
    }

    // затем existing save + publishEvent (с lessonDate+lessonNumber в payload — будет в PLAN-04)
    Homework homework = new Homework(
        req.groupId(), req.subjectId(), req.semesterId(),
        req.title(), req.description(), req.link(),
        requestContext.getUserId(),
        req.lessonDate(), req.lessonNumber()
    );
    ```

    Инжектить через конструктор: `Clock clock`, `ScheduleGrpcClient scheduleGrpcClient`, `RequestContext requestContext` (если не инжектировано уже).

    Unit-тесты (Mockito + @ExtendWith(MockitoExtension.class)):
    - 6 тестов из `<behavior>`
    - Использовать `Clock.fixed(Instant.parse("2026-05-01T09:00:00Z"), ZoneOffset.UTC)` для детерминированных дат
    - Mock `ScheduleGrpcClient.resolveLesson(...)` возвращает нужный `LessonResponse` (билдер из proto) или empty

    **НЕ делать:** не править event payload в этом плане (PLAN-04), не трогать update/delete (Task 2).
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && ./gradlew.bat :services:academic-service:academic-app:test --tests 'HomeworkServiceTest'</automated>
  </verify>
  <done>
    - 6 unit-тестов зелёные
    - existing HomeworkService-тесты (если есть) тоже зелёные
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HomeworkService — D-05 publishedBy guard + HomeworkController @RequireRole правки + controller IT</name>
  <read_first>
    - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/homework/HomeworkController.java (текущий @RequireRole на create/update/delete)
    - services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/api/HomeworkApi.java
    - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/shared/security/RequireRole.java (понять семантику)
    - services/academic-service/academic-app/src/test/java/ru/rutcampustrack/academic/ (любой controller IT с MockMvc + Testcontainers)
  </read_first>
  <files>
    services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/homework/HomeworkService.java,
    services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/homework/HomeworkController.java,
    services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/api/HomeworkApi.java,
    services/academic-service/academic-app/src/test/java/ru/rutcampustrack/academic/homework/HomeworkServiceTest.java,
    services/academic-service/academic-app/src/test/java/ru/rutcampustrack/academic/homework/HomeworkControllerIT.java
  </files>
  <behavior>
    - `updateHomework_throwsForbidden_whenNotAuthor()` — homework.publishedBy=10, requestContext.userId=20 → AccessDeniedException (D-05).
    - `deleteHomework_throwsForbidden_whenNotAuthor()` — та же логика для delete (D-05).
    - `updateHomework_succeeds_whenAuthor()` — userId совпадает → сохранение + event.
    - Controller IT: `POST /api/academic/homeworks` от ADMIN → 403 (D-06). От STUDENT без is_headman → 403. От STUDENT с is_headman → 201 (при валидных данных с mock-ScheduleGrpcClient).
  </behavior>
  <action>
    1. **HomeworkService.updateHomework + deleteHomework** — добавить D-05 guard:
    ```java
    @Transactional
    public Homework updateHomework(Long id, UpdateHomeworkRequest req) {
        Homework hw = getHomework(id); // существующий helper
        if (!hw.getPublishedBy().equals(requestContext.getUserId())) {
            throw new AccessDeniedException("Редактировать ДЗ может только автор");
        }
        // existing update-logic + publish HomeworkUpdatedEvent (D-06 НЕ создаёт новую пару — lessonDate/lessonNumber не меняются)
    }

    @Transactional
    public void deleteHomework(Long id) {
        Homework hw = getHomework(id);
        if (!hw.getPublishedBy().equals(requestContext.getUserId())) {
            throw new AccessDeniedException("Удалять ДЗ может только автор");
        }
        homeworkRepository.delete(hw);
    }
    ```

    2. **HomeworkController @RequireRole правки** (D-06):
    - `createHomework` — было `@RequireRole({ADMIN, HEADMAN})` (или аналог), убрать ADMIN. Остаётся `HEADMAN` только. Точный механизм зависит от существующего `@RequireRole` enum — проверить контракт и использовать соответствующий флаг.
    - `updateHomework`, `deleteHomework` — аналогично: убрать ADMIN.
    - `listHomeworks` (GET) — не трогать: student/teacher/admin продолжают читать.

    3. **HomeworkApi.java (contract)** — если аннотации `@PreAuthorize` или `@RequireRole` объявлены на интерфейсе, правки идут сюда (contract-first). Если только в контроллере — там.

    4. Добавить в **HomeworkServiceTest** тесты `update*` и `delete*` (4 новых теста).

    5. **HomeworkControllerIT.java** — новый MockMvc IT с Testcontainers Postgres + RabbitMQ:
    - 3 POST сценария (ADMIN→403, обычный STUDENT→403, HEADMAN→201)
    - Mock `ScheduleGrpcClient` через `@MockBean` либо in-process gRPC server (проще `@MockBean`).
    - Паттерн — скопировать из любого существующего `*ControllerIT` в academic-app.

    **НЕ делать:** не править event payload здесь (PLAN-04).
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && ./gradlew.bat :services:academic-service:academic-app:test --tests 'HomeworkServiceTest' --tests 'HomeworkControllerIT'</automated>
  </verify>
  <done>
    - HomeworkServiceTest: 10 тестов (6 из Task 1 + 4 новых) зелёные
    - HomeworkControllerIT: 3 теста зелёные
    - academic-app полный test suite проходит без regression
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JWT-claims → RequestContext | untrusted (верифицировано Gateway) |
| HomeworkService ← RequestContext | trusted (in-process) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-07 | Elevation | обычный student создаёт ДЗ | mitigate | D-06 `isHeadman()` + @RequireRole убран ADMIN |
| T-61-08 | Elevation (IDOR) | чужой user редактирует/удаляет ДЗ | mitigate | D-05 `publishedBy.equals(userId)` guard |
| T-61-09 | Tampering | lessonDate в прошлом | mitigate | D-03 `LocalDate.now(clock)` check |
| T-61-10 | Tampering | subjectId не матчит паре | mitigate | D-04 проверка lesson.subjectId |
| T-61-11 | DoS | flood requests на resolveLesson | accept | deadline 3s + существующий rate-limit на Gateway |
</threat_model>

<verification>
- `./gradlew :academic-app:test` зелёный
- Нет regression в существующих тестах
- `HomeworkServiceTest` + `HomeworkControllerIT` покрывают D-03/04/05/06
</verification>

<success_criteria>
1. POST с датой в прошлом → 400
2. POST с несуществующей парой → 400
3. POST с subjectId ≠ lesson.subject_id → 400
4. PUT/DELETE от не-автора → 403
5. POST от STUDENT без is_headman → 403
6. POST от ADMIN → 403
7. На одну пару (groupId, date, lessonNumber) создаются ≥2 ДЗ (проверяется в IT sanity-тесте)
</success_criteria>

<output>
После завершения создать `.planning/phases/61-headman-homework-management-ui-homeworkapi-controller-homewo/61-03-SUMMARY.md`.
</output>
