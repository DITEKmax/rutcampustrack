---
phase: 59-excuses-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/attendance-service/attendance-api-contract/src/main/java/ru/rutcampustrack/attendance/contract/api/ExcuseApi.java
  - services/attendance-service/attendance-api-contract/src/main/java/ru/rutcampustrack/attendance/contract/dto/excuse/CreateExcuseRequest.java
  - services/attendance-service/attendance-api-contract/src/main/java/ru/rutcampustrack/attendance/contract/dto/excuse/ExcuseTicketResponse.java
  - services/attendance-service/attendance-api-contract/src/main/java/ru/rutcampustrack/attendance/contract/dto/excuse/UpdateExcuseStatusRequest.java
  - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/excuse/entity/ExcuseTicket.java
  - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/excuse/ExcuseRepository.java
  - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/config/MongoConvertersConfig.java
autonomous: true
requirements:
  - FR-1
  - FR-3
  - FR-4
  - NFR-3

must_haves:
  truths:
    - "ExcuseApi интерфейс определён с 5 эндпоинтами (POST, GET /me, GET /group/{groupId}, GET /{id}, PATCH /{id}/status)"
    - "ExcuseTicket MongoDB document содержит все поля из D-02 (id, studentId, groupId, studentName, lessonIds, excuseType, comment, status, decisionBy, decisionComment, decisionAt, createdAt, updatedAt)"
    - "ExcuseTicketStatus в Mongo сериализуется как lowercase через MongoCustomConversions"
    - "ExcuseType в Mongo сериализуется как lowercase через MongoCustomConversions"
    - "ExcuseRepository имеет метод existsByStudentIdAndLessonIdsInAndStatusIn для D-11"
    - "CreateExcuseRequest — Java record, НЕТ Lombok в контрактном модуле"
    - "ExcuseTicketResponse — класс extends RepresentationModel, НЕТ Lombok"
  artifacts:
    - path: "services/attendance-service/attendance-api-contract/src/main/java/ru/rutcampustrack/attendance/contract/api/ExcuseApi.java"
      provides: "REST contract interface с маппингами и Swagger-аннотациями"
    - path: "services/attendance-service/attendance-api-contract/src/main/java/ru/rutcampustrack/attendance/contract/dto/excuse/CreateExcuseRequest.java"
      provides: "Request record для POST /excuses"
    - path: "services/attendance-service/attendance-api-contract/src/main/java/ru/rutcampustrack/attendance/contract/dto/excuse/ExcuseTicketResponse.java"
      provides: "HATEOAS response class"
    - path: "services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/excuse/entity/ExcuseTicket.java"
      provides: "MongoDB @Document для коллекции excuse_tickets"
    - path: "services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/excuse/ExcuseRepository.java"
      provides: "Spring Data Mongo repository"
  key_links:
    - from: "ExcuseController (план 02)"
      to: "ExcuseApi"
      via: "implements ExcuseApi"
      pattern: "implements ExcuseApi"
    - from: "MongoConvertersConfig"
      to: "ExcuseTicket.excuseType / status"
      via: "ExcuseTypeWriter/Reader + ExcuseTicketStatusWriter/Reader"
      pattern: "ExcuseType|ExcuseTicketStatus"
---

<objective>
Создать доменные контракты и слой данных для тикетов о пропуске занятий (Фаза 59, Wave 1).

Purpose: Все последующие планы зависят от этих интерфейсов — контракт API, DTO, MongoDB-документ и репозиторий с нужными методами. Это "interface-first" Wave 0 для фазы.

Output:
- `ExcuseApi.java` — интерфейс с 5 эндпоинтами (контрактный модуль)
- `CreateExcuseRequest.java`, `ExcuseTicketResponse.java`, `UpdateExcuseStatusRequest.java` — DTO (контрактный модуль)
- `ExcuseTicket.java` — MongoDB document с Lombok (app-модуль)
- `ExcuseRepository.java` — Spring Data Mongo repository
- Расширить `MongoConvertersConfig` — добавить конвертеры для `ExcuseType` и `ExcuseTicketStatus`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/59-excuses-backend/59-CONTEXT.md
@.planning/phases/59-excuses-backend/59-RESEARCH.md

<interfaces>
<!-- Существующие паттерны для точного соблюдения -->

Из CheckinApi.java (контрактный модуль — образец):
```java
@Tag(name = "...", description = "...")
@RequestMapping("/attendance")
public interface CheckinApi {
    @Operation(...)
    @ApiResponses({...})
    @PostMapping("/checkin")
    ResponseEntity<EntityModel<CheckinResponse>> checkin(@Valid @RequestBody CheckinRequest request);
}
```

Из CheckinController.java (app-модуль — образец):
```java
@RestController
public class CheckinController implements CheckinApi { ... }
```

Из AttendanceDocument.java (MongoDB document — образец, Lombok в app-модуле):
```java
@Data @Builder @NoArgsConstructor @AllArgsConstructor
@Document(collection = "attendances")
public class AttendanceDocument {
    @Id private String id;
    @Field("lesson_id") private Long lessonId;
    // ...
}
```

Из MongoConvertersConfig.java (паттерн конвертеров):
```java
@WritingConverter static class AttendanceStatusWriter implements Converter<AttendanceStatus, String> {
    public String convert(AttendanceStatus source) { return source.name().toLowerCase(); }
}
@ReadingConverter static class AttendanceStatusReader implements Converter<String, AttendanceStatus> {
    public AttendanceStatus convert(String source) { return AttendanceStatus.valueOf(source.toUpperCase()); }
}
```

Из RequestContext.java (JWT claims):
```java
private Long userId; private UserRole role; private Long groupId; private boolean headman;
```

ExcuseType enum (уже существует, не менять):
```java
public enum ExcuseType { ILLNESS, SUMMONS, UNIVERSITY_ORDER, EXEMPTION, FREE_ATTENDANCE, OTHER; }
```

ExcuseTicketStatus enum (уже существует, ТОЛЬКО ЭТИ 4 значения):
```java
public enum ExcuseTicketStatus { DRAFT, SUBMITTED, APPROVED, REJECTED; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MongoDB document ExcuseTicket + Repository</name>
  <files>
    services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/excuse/entity/ExcuseTicket.java
    services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/excuse/ExcuseRepository.java
    services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/config/MongoConvertersConfig.java
  </files>
  <read_first>
    - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/checkin/AttendanceDocument.java (образец MongoDB document с Lombok)
    - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/config/MongoConvertersConfig.java (РЕДАКТИРОВАТЬ — добавить конвертеры)
    - services/attendance-service/attendance-api-contract/src/main/java/ru/rutcampustrack/attendance/contract/enums/ExcuseType.java
    - services/attendance-service/attendance-api-contract/src/main/java/ru/rutcampustrack/attendance/contract/enums/ExcuseTicketStatus.java
    - .planning/phases/59-excuses-backend/59-CONTEXT.md (D-01, D-02, D-11)
  </read_first>
  <behavior>
    - ExcuseTicket создан в пакете ru.rutcampustrack.attendance.excuse.entity, @Document(collection = "excuse_tickets")
    - Все поля из D-02: id (String @Id), studentId (Long), groupId (Long), studentName (String), lessonIds (List&lt;Long&gt;), excuseType (ExcuseType), comment (String), status (ExcuseTicketStatus, default SUBMITTED), decisionBy (Long), decisionComment (String), decisionAt (Instant), createdAt (Instant), updatedAt (Instant)
    - Lombok @Data @Builder @NoArgsConstructor @AllArgsConstructor (только в app-модуле — OK)
    - @Field("snake_case") для всех полей (пример: @Field("student_id"))
    - ExcuseRepository extends MongoRepository&lt;ExcuseTicket, String&gt; с методами:
      - existsByStudentIdAndLessonIdsInAndStatusIn(Long studentId, List&lt;Long&gt; lessonIds, List&lt;ExcuseTicketStatus&gt; statuses) — для D-11 duplicate check
      - findByStudentId(Long studentId, Pageable pageable) — для GET /me
      - findByGroupIdAndStatus(Long groupId, ExcuseTicketStatus status, Pageable pageable) — для headman list
      - findByGroupId(Long groupId, Pageable pageable) — для headman all tickets
    - MongoConvertersConfig: добавить 4 новых конвертера (ExcuseTypeWriter, ExcuseTypeReader, ExcuseTicketStatusWriter, ExcuseTicketStatusReader) в существующий mongoCustomConversions() bean список — Pattern: source.name().toLowerCase() / valueOf(source.toUpperCase())
    - ВАЖНО: ExcuseTicketStatus.SUBMITTED используется как default (не PENDING, которого нет в enum — D-02 говорит «pending» — это SUBMITTED в существующем enum)
  </behavior>
  <action>
    1. Создать пакет excuse/entity/. Создать ExcuseTicket.java с @Data @Builder @NoArgsConstructor @AllArgsConstructor @Document(collection = "excuse_tickets"). Все поля с @Field("snake_case"). Для status — java-default в builder = ExcuseTicketStatus.SUBMITTED.
    2. Создать ExcuseRepository.java в пакете ru.rutcampustrack.attendance.excuse, extends MongoRepository&lt;ExcuseTicket, String&gt;. Добавить 4 query-метода описанные выше.
    3. Открыть существующий MongoConvertersConfig.java. В mongoCustomConversions() добавить 4 новых inner static class конвертера для ExcuseType и ExcuseTicketStatus по образцу существующих AttendanceStatus конвертеров. Добавить их в List.of(...) в bean.
    4. Создать тест services/attendance-service/attendance-app/src/test/java/ru/rutcampustrack/attendance/excuse/ExcuseRepositoryTest.java — unit-тест с @DataMongoTest или Testcontainers, проверяет existsByStudentIdAndLessonIdsInAndStatusIn возвращает true/false корректно.
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && ./gradlew.bat :services:attendance-service:attendance-app:compileJava 2>&1 | tail -20</automated>
  </verify>
  <done>Компиляция зелёная; ExcuseTicket.java существует с 14 полями; ExcuseRepository.java с 4 методами; MongoConvertersConfig содержит ExcuseTypeWriter</done>
</task>

<task type="auto">
  <name>Task 2: API Contract — ExcuseApi + DTO (контрактный модуль)</name>
  <files>
    services/attendance-service/attendance-api-contract/src/main/java/ru/rutcampustrack/attendance/contract/api/ExcuseApi.java
    services/attendance-service/attendance-api-contract/src/main/java/ru/rutcampustrack/attendance/contract/dto/excuse/CreateExcuseRequest.java
    services/attendance-service/attendance-api-contract/src/main/java/ru/rutcampustrack/attendance/contract/dto/excuse/ExcuseTicketResponse.java
    services/attendance-service/attendance-api-contract/src/main/java/ru/rutcampustrack/attendance/contract/dto/excuse/UpdateExcuseStatusRequest.java
  </files>
  <read_first>
    - services/attendance-service/attendance-api-contract/src/main/java/ru/rutcampustrack/attendance/contract/api/CheckinApi.java (образец API интерфейса)
    - services/attendance-service/attendance-api-contract/src/main/java/ru/rutcampustrack/attendance/contract/dto/checkin/CheckinRequest.java (образец record DTO)
    - services/attendance-service/attendance-api-contract/src/main/java/ru/rutcampustrack/attendance/contract/dto/report/StudentStatsResponse.java (образец Response с RepresentationModel)
    - .planning/phases/59-excuses-backend/59-CONTEXT.md (D-04..D-09, D-15)
  </read_first>
  <action>
    1. Создать пакет dto/excuse/. Создать CreateExcuseRequest.java как Java record (NO Lombok):
       - Поля: List&lt;Long&gt; lessonIds (не null, не empty — @NotEmpty), ExcuseType excuseType (@NotNull), String comment (nullable, @Size(max=1000))
       - jakarta.validation аннотации на полях
    2. Создать ExcuseTicketResponse.java как класс (NO Lombok) extends RepresentationModel&lt;ExcuseTicketResponse&gt;:
       - Поля: String id, Long studentId, Long groupId, String studentName, List&lt;Long&gt; lessonIds, ExcuseType excuseType, String comment, String status (lowercase string), Long decisionBy, String decisionComment, Instant decisionAt, Instant createdAt, Instant updatedAt
       - Конструктор + геттеры (или использовать public поля — предпочтительно конструктор + геттеры для HATEOAS)
    3. Создать UpdateExcuseStatusRequest.java как Java record (NO Lombok):
       - Поля: ExcuseTicketStatus status (@NotNull, только APPROVED или REJECTED), String decisionComment (nullable, @Size(max=1000))
       - ВАЖНО: status должен быть ExcuseTicketStatus, не String
    4. Создать ExcuseApi.java как interface (NO Lombok, NO Spring @RequireRole — роль проставляется в контроллере):
       - @Tag(name = "Excuses", description = "Тикеты о пропуске занятий")
       - @RequestMapping("/attendance/excuses")
       - 5 методов с @Operation, @ApiResponses, HTTP маппингами:
         - @PostMapping → ResponseEntity&lt;EntityModel&lt;ExcuseTicketResponse&gt;&gt; createExcuse(@Valid @RequestBody CreateExcuseRequest)
         - @GetMapping("/me") с Pageable параметром → ResponseEntity&lt;PagedModel&lt;EntityModel&lt;ExcuseTicketResponse&gt;&gt;&gt; getMyTickets(Pageable, @RequestParam(required=false) ExcuseTicketStatus status)
         - @GetMapping("/group/{groupId}") → ResponseEntity&lt;PagedModel&lt;EntityModel&lt;ExcuseTicketResponse&gt;&gt;&gt; getGroupTickets(@PathVariable Long groupId, Pageable, @RequestParam(required=false) ExcuseTicketStatus status)
         - @GetMapping("/{id}") → ResponseEntity&lt;EntityModel&lt;ExcuseTicketResponse&gt;&gt; getTicketById(@PathVariable String id)
         - @PatchMapping("/{id}/status") → ResponseEntity&lt;EntityModel&lt;ExcuseTicketResponse&gt;&gt; updateStatus(@PathVariable String id, @Valid @RequestBody UpdateExcuseStatusRequest)
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && ./gradlew.bat :services:attendance-service:attendance-api-contract:compileJava 2>&1 | tail -20</automated>
  </verify>
  <done>Контрактный модуль компилируется; grep "interface ExcuseApi" выдаёт результат; CreateExcuseRequest — record; ExcuseTicketResponse — class extends RepresentationModel</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | HTTP запросы со student/headman JWT, lessonIds могут быть произвольными |
| API → MongoDB | Сохранение документов; enum поля должны быть строго валидированы |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01-01 | Tampering | CreateExcuseRequest.lessonIds | mitigate | @NotEmpty валидация + проверка принадлежности группе в сервисе (план 02) |
| T-59-01-02 | Tampering | ExcuseType enum deserialization | mitigate | Конвертер ExcuseTypeReader.valueOf() бросит IllegalArgumentException → 400 |
| T-59-01-03 | Information Disclosure | ExcuseTicketResponse содержит studentId | accept | Видно только владельцу или headman своей группы (контроль в сервисе, план 02) |
</threat_model>

<verification>
- `./gradlew.bat :services:attendance-service:attendance-api-contract:compileJava` → BUILD SUCCESSFUL
- `./gradlew.bat :services:attendance-service:attendance-app:compileJava` → BUILD SUCCESSFUL
- `grep -r "interface ExcuseApi" services/attendance-service/` → находит файл
- `grep -r "ExcuseTypeWriter" services/attendance-service/attendance-app/` → находит в MongoConvertersConfig.java
- `grep -r "record CreateExcuseRequest" services/attendance-service/attendance-api-contract/` → находит файл
</verification>

<success_criteria>
- ExcuseApi.java — интерфейс с 5 методами, маппинги, Swagger аннотации
- CreateExcuseRequest — Java record с @NotEmpty lessonIds, @NotNull excuseType, @Size(max=1000) comment
- ExcuseTicketResponse — class extends RepresentationModel, конструктор + геттеры
- UpdateExcuseStatusRequest — record с ExcuseTicketStatus status, String decisionComment
- ExcuseTicket — @Document(collection = "excuse_tickets") с 14 полями, Lombok OK
- ExcuseRepository — MongoRepository с 4 методами включая existsByStudentIdAndLessonIdsInAndStatusIn
- MongoConvertersConfig — 8 конвертеров (4 существующих + 4 новых для ExcuseType/ExcuseTicketStatus)
- Оба модуля компилируются без ошибок
</success_criteria>

<output>
После завершения создать `.planning/phases/59-excuses-backend/59-01-SUMMARY.md`
</output>
