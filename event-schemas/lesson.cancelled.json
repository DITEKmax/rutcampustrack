{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "lesson.cancelled",
  "description": "Пара отменена старостой. Генерируется Schedule Service.",
  "type": "object",
  "required": ["event_type", "event_id", "occurred_at", "payload"],
  "properties": {
    "event_type": { "const": "lesson.cancelled" },
    "event_id": { "$ref": "_common.json#/$defs/eventId" },
    "occurred_at": { "$ref": "_common.json#/$defs/occurredAt" },
    "payload": {
      "type": "object",
      "required": ["lesson_id", "group_id", "subject_id", "date"],
      "properties": {
        "lesson_id": { "type": "integer" },
        "group_id": { "type": "integer" },
        "subject_id": { "type": "integer" },
        "date": { "type": "string", "format": "date" },
        "cancel_reason": { "type": "string" }
      }
    }
  }
}
