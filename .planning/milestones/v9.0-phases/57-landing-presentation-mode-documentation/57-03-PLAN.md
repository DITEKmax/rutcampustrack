---
phase: 57-landing-presentation-mode-documentation
plan: 03
type: execute
wave: 2
depends_on:
  - 57-01
  - 57-02
files_modified:
  - docs/phase-57-report.md
  - frontends/landing/dist/index.html
autonomous: false
requirements:
  - LAND-v9-04
  - LAND-v9-02
  - DOCS-v9-01
tags:
  - verification
  - responsive
  - dark-mode
  - report
must_haves:
  truths:
    - "Лендинг `/presentation/` корректно рендерится на 360px, 768px, 1440px viewport широтах (нет горизонтального overflow, нет cut-off текста)"
    - "Лендинг корректно рендерится в dark и light темах — все элементы новой секции `#architecture-flow` читаемы в обеих"
    - "При `prefers-reduced-motion: reduce` секция `#architecture-flow` отображается статично (все шаги и стрелки видны без скролла)"
    - "Scroll-driven анимация секции работает на desktop (≥1024px) без визуальных артефактов (pin layout не ломается)"
    - "docs/phase-57-report.md существует и описывает результаты фазы 57"
  artifacts:
    - path: docs/phase-57-report.md
      provides: "Отчёт о завершении Phase 57"
      min_lines: 40
      contains: "Phase 57"
  key_links:
    - from: docs/phase-57-report.md
      to: CLAUDE.md + 57-01-SUMMARY.md + 57-02-SUMMARY.md
      via: явные ссылки на обновлённые артефакты
      pattern: "57-01-SUMMARY|57-02-SUMMARY|CLAUDE\\.md"
---

<objective>
Верификация полного результата Phase 57 (LAND-v9-04 responsive + dark mode регрессия; LAND-v9-02 визуальный UAT; DOCS-v9-01 docs → phase-57-report), фикс любых найденных регрессий в landing и создание `docs/phase-57-report.md` — обязательного по CLAUDE.md инструкции артефакта.

Purpose: Закрывает LAND-v9-04 (responsive + dark mode — это регрессия, её нельзя проверить в plan 01 пока секция не существует), добавляет обязательный phase report, даёт финальный human UAT gate для всей фазы 57.
Output: Manual UAT checkpoint + потенциальные CSS-фиксы в `index.html` + новый `docs/phase-57-report.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-landing-presentation-mode-documentation/57-RESEARCH.md
@.planning/phases/57-landing-presentation-mode-documentation/57-01-SUMMARY.md
@.planning/phases/57-landing-presentation-mode-documentation/57-02-SUMMARY.md
@frontends/landing/dist/index.html
@CLAUDE.md

<interfaces>
Доступные для использования skills (из docs/skills-inventory.md):
  - responsiveness-check (agent) — тест адаптивности по viewport breakpoints
  - baseline-ui — валидация анимаций и accessibility в Tailwind (применимо только как reference)
  - fixing-accessibility — аудит ARIA / keyboard / contrast
  - ux-audit — dogfood тестирование UX

Существующий responsive baseline в landing index.html:
  - @media (min-width: 640px) — tablet
  - @media (min-width: 1024px) — desktop (используется для pin-трigger новой секции)
  - Design tokens автоматически переключаются через [data-theme="light"] / [data-theme="dark"]
  - Theme toggle handler — строки 23-25 inline script

Шаблон phase report (из phase-5-report.md, phase-7-report.md):
  # Phase N Report: <Name>
  ## Goal
  ## Completed requirements
  ## Key decisions
  ## Artifacts changed
  ## Verification results
  ## Notes / gotchas
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Автоматическая responsive + dark/light regression проверка (LAND-v9-04)</name>
  <files>frontends/landing/dist/index.html</files>
  <read_first>
    - frontends/landing/dist/index.html (финальное состояние после plan 01)
    - .planning/phases/57-landing-presentation-mode-documentation/57-01-SUMMARY.md
  </read_first>
  <action>
    Запустить автоматические smoke-проверки на уровне HTML/CSS (без браузера — это проверки структурной целостности):

    1. Проверить, что в файле нет хард-кодед hex цветов внутри новых `.arch-*` CSS правил (только `var(--...)`):
       ```bash
       awk '/ARCHITECTURE FLOW/,/\/\* ============/' frontends/landing/dist/index.html | grep -E '#[0-9a-fA-F]{3,6}' | grep -v 'var(--' | wc -l
       ```
       Ожидаемо: 0.

    2. Проверить балланс `<section>` / `</section>`:
       ```bash
       [ "$(grep -c '<section' frontends/landing/dist/index.html)" = "$(grep -c '</section>' frontends/landing/dist/index.html)" ]
       ```

    3. Проверить наличие `@media (min-width: 1024px)` блока ВНУТРИ `.arch-flow` CSS (для responsive grid):
       ```bash
       awk '/ARCHITECTURE FLOW/,/\/\* ============/' frontends/landing/dist/index.html | grep -c '@media (min-width: 1024px)'
       ```
       Ожидаемо: ≥ 1.

    4. Проверить, что в `mm.add('(prefers-reduced-motion: reduce)'` блоке есть gsap.set для `.arch-step`:
       ```bash
       awk '/prefers-reduced-motion: reduce/,/\}\);/' frontends/landing/dist/index.html | grep -c 'arch-step'
       ```
       Ожидаемо: ≥ 1.

    5. Проверить, что все 4 role cards на месте после правок plan 01 Task 3:
       ```bash
       for role in student headman teacher admin; do grep -c "data-role=\"$role\"" frontends/landing/dist/index.html; done
       ```
       Ожидаемо: `1 1 1 1`.

    Если любая проверка не прошла — немедленно зафиксировать дефект в `docs/phase-57-report.md` (Task 3) и сделать минимальный фикс в `index.html` (НЕ расширять scope). Типичные фиксы:
    - Хард-кодед hex → заменить на ближайший токен из `:root`
    - Не хватает reduced-motion gsap.set → добавить одну строку в `mm.add('(prefers-reduced-motion: reduce)'` блок
    - Нет mobile fallback grid → добавить `grid-template-columns: 1fr` снаружи @media

    НЕ ДОБАВЛЯТЬ новые фичи. Только фикс регрессий, обнаруженных на этом шаге.
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && bash -c '[ "$(grep -c "<section" frontends/landing/dist/index.html)" = "$(grep -c "</section>" frontends/landing/dist/index.html)" ]' && bash -c '[ "$(awk "/ARCHITECTURE FLOW/,/\\/\\* ============/" frontends/landing/dist/index.html | grep -cE "#[0-9a-fA-F]{3,6}" | grep -v "var(--")" = "0" ]' && bash -c '[ "$(grep -c "data-role=\"student\"" frontends/landing/dist/index.html)" = "1" ]' && bash -c '[ "$(grep -c "data-role=\"headman\"" frontends/landing/dist/index.html)" = "1" ]' && bash -c '[ "$(grep -c "data-role=\"teacher\"" frontends/landing/dist/index.html)" = "1" ]' && bash -c '[ "$(grep -c "data-role=\"admin\"" frontends/landing/dist/index.html)" = "1" ]'</automated>
  </verify>
  <acceptance_criteria>
    - Нет хард-кодед hex цветов внутри `.arch-*` CSS блока (все через `var(--...)`)
    - `<section>` / `</section>` count совпадает (валидный HTML)
    - Внутри `.arch-flow` CSS есть `@media (min-width: 1024px)` директива
    - В reduced-motion блоке есть gsap.set для `.arch-step`
    - Все 4 role cards ровно по 1 разу присутствуют
    - Если был нужен фикс — в `docs/phase-57-report.md` есть запись о дефекте и фиксе
  </acceptance_criteria>
  <done>
    Все 5 авто-проверок зелёные. Структурная регрессия отсутствует или исправлена минимальной правкой `index.html`. Файл готов к human UAT в Task 2.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — responsive (360/768/1440) + dark/light + reduced-motion + scroll animation</name>
  <what-built>
    Phase 57 landing changes:
    - Новая секция `#architecture-flow` «От коммита до вашего устройства» с 6-шаговой GSAP scroll-driven анимацией (plan 01)
    - Обновлённая карточка Староста (5 буллетов v9.0) в `#roles` (plan 01)
    - Вся документация синхронизирована: CLAUDE.md, url-layout.md, job-stories.md, PROJECT.md (plan 02)
  </what-built>
  <how-to-verify>
    Перед началом: поднять production билд локально или открыть staging.
    Вариант A (локальный preview без сборки): открыть файл прямо в Chrome:
    ```
    start chrome "file:///C:/Users/maksd/IntelliJIDEA/rutcampustrack/frontends/landing/dist/index.html"
    ```
    Вариант B (через nginx контейнер, если запущен): `https://ruttrack.site/presentation/`.

    Затем пройти чек-лист:

    **A. Responsive (LAND-v9-04)** — Chrome DevTools → Toggle device toolbar:
    1. **360px width** (iPhone SE):
       - [ ] Нет горизонтального overflow (scrollbar снизу отсутствует)
       - [ ] Секция `#architecture-flow`: 6 шагов рендерятся ВЕРТИКАЛЬНО в 1 колонку (не pinned)
       - [ ] Все тексты шагов читаемы, не обрезаны
       - [ ] Role cards (4 шт.) рендерятся в 1 колонку
    2. **768px width** (iPad):
       - [ ] Секция `#architecture-flow`: всё ещё 1-колоночный batch reveal (не pinned — брейкпоинт 1024px)
       - [ ] Role cards: 2 колонки
    3. **1440px width** (desktop):
       - [ ] Секция `#architecture-flow`: горизонтальная цепочка из 6 шагов + 5 стрелок
       - [ ] При прокрутке секция ПИНИТСЯ к вьюпорту, каждый шаг анимируется последовательно со scrub'ом
       - [ ] После завершения timeline секция «отпускает» и скролл продолжается к `#roles`

    **B. Theme (LAND-v9-04)**:
    1. [ ] Переключить через theme-toggle в хедере: light ↔ dark
    2. [ ] В dark теме: `.arch-step` имеют тёмный фон, светлый текст, видны границы
    3. [ ] В light теме: `.arch-step` имеют светлый фон, тёмный текст, видны границы
    4. [ ] Стрелки `.arch-arrow` — градиент виден в обеих темах
    5. [ ] Иконки Phosphor рендерятся в обеих темах (цветом `--accent-primary`)

    **C. Reduced Motion (LAND-v9-02)**:
    1. DevTools → Rendering panel → Emulate CSS media feature `prefers-reduced-motion` → `reduce`
    2. Перезагрузить страницу
    3. [ ] Секция `#architecture-flow` полностью видна без скролла (все шаги opacity:1, стрелки scaleX:1)
    4. [ ] Нет pin'а — страница скроллится непрерывно
    5. Вернуть в `no-preference`

    **D. Scroll Animation (LAND-v9-02) на desktop 1440px + no reduced motion**:
    1. [ ] Секция пинится примерно на 5 высот вьюпорта
    2. [ ] При скролле первый шаг появляется, затем стрелка→2, второй шаг, и т.д. — 6 шагов + 5 стрелок
    3. [ ] Нет рывков/скачков layout при входе/выходе pin'а
    4. [ ] После последнего шага pin отпускает — можно прокрутить к `#roles`

    **E. Role cards (LAND-v9-05)**:
    1. [ ] Секция `#roles` содержит 4 карточки: Студент, Староста, Преподаватель, Администратор (в любом порядке)
    2. [ ] Карточка «Староста» содержит 5 буллетов, один из которых упоминает «вкладка Группа» или «Web-кабинет»
    3. [ ] Карточки Студент/Преподаватель/Администратор выглядят идентично pre-Phase-57 (только быстрое визуальное сравнение)

    **F. Smoke (docs sync)**:
    1. `grep -q "## URL Layout" CLAUDE.md` → true
    2. `grep -q "## Production Path Routing" docs/url-layout.md` → true
    3. `grep -cE "JS-(STUDENT|HEADMAN)-WEB-" docs/job-stories.md` ≥ 18
    4. `grep -q "ship-ready after Phase 57" .planning/PROJECT.md` → true
  </how-to-verify>
  <resume-signal>
    Ответьте одним из:
    - "approved" — всё прошло, переходим к Task 3 (phase report)
    - "fix: <описание>" — указать регрессию, её зафиксит следующий auto task (я добавлю Task 2.5 в revision plan)
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Создать docs/phase-57-report.md</name>
  <files>docs/phase-57-report.md</files>
  <read_first>
    - docs/phase-5-report.md (структура-образец)
    - docs/phase-7-report.md (структура-образец)
    - .planning/phases/57-landing-presentation-mode-documentation/57-01-SUMMARY.md
    - .planning/phases/57-landing-presentation-mode-documentation/57-02-SUMMARY.md
    - .planning/REQUIREMENTS.md (LAND-v9-02, 04, 05 + DOCS-v9-01..04)
    - .planning/ROADMAP.md (Phase 57 Goal + Success Criteria)
  </read_first>
  <action>
    Создать новый файл `docs/phase-57-report.md` со структурой (текст на русском, следуя паттерну phase-5/7 reports):

    ```markdown
    # Phase 57 Report: Landing Presentation Mode + Documentation

    **Завершена:** <YYYY-MM-DD — поставить дату выполнения задачи>
    **Milestone:** v9.0 Frontend Unification
    **Requirements закрыты:** LAND-v9-02, LAND-v9-04, LAND-v9-05, DOCS-v9-01, DOCS-v9-02, DOCS-v9-03, DOCS-v9-04

    ## Цель

    Добавить на лендинг `/presentation/` GSAP scroll-driven анимацию «Как работает система», отражающую путь данных git push → CI → backend → RabbitMQ → WebSocket/Push → устройство. Описать в ролях всех 4 участников (включая HEADMAN). Синхронизировать всю project documentation с v9.0 state.

    ## Результаты

    ### Landing (LAND-v9-02, 04, 05)

    - Новая секция `#architecture-flow` в `frontends/landing/dist/index.html` — 6 шагов + 5 стрелок, Phosphor duotone иконки, token-based CSS
    - GSAP timeline: `pin + scrub` на desktop ≥1024px; `ScrollTrigger.batch` на mobile; статический fallback для `prefers-reduced-motion: reduce`
    - Роль «Староста» обновлена 5 буллетами, отражающими v9.0 (web-кабинет /headman/*, PWA «Группа» tab)
    - Все 4 роли (ADMIN, TEACHER, STUDENT, HEADMAN) видны в `#roles`
    - Dark/light темы работают через design tokens без хард-кодед цветов
    - Responsive на 360px / 768px / 1440px подтверждён в Human UAT

    ### Documentation (DOCS-v9-01..04)

    - `CLAUDE.md`: статус-блок синхронизирован (v1.0-v8.0 ЗАВЕРШЕНА, v9.0 В РАБОТЕ); добавлен раздел `URL Layout (v9.0)` с таблицей production routes
    - `docs/url-layout.md`: добавлен раздел `Production Path Routing (v9.0)` с полной таблицей 8+ маршрутов, маппингом роль→dashboard, списком deprecated путей
    - `docs/job-stories.md`: добавлены 18 новых историй — 10 JS-STUDENT-WEB-01..10 и 8 JS-HEADMAN-WEB-01..08 — каждая с traceability ссылкой `(STU-WEB-NN)` / `(HEAD-WEB-NN)`
    - `.planning/PROJECT.md`: v9.0 помечен ship-ready; Recently Validated v9.0 список расширен phase 50-57 summaries

    ## Ключевые решения

    1. **GSAP без апгрейда стека** — использован уже подключённый GSAP 3.12.5 + ScrollTrigger через CDN. Нет перехода на build pipeline (контракт лендинга: static HTML).
    2. **Pin + scrub только для desktop** — на mobile (<1024px) используется обычный `ScrollTrigger.batch()` reveal, чтобы избежать perceived-DoS «страница зависла».
    3. **Reduced-motion fallback обязателен** — вся секция `#architecture-flow` получает статическое состояние при `prefers-reduced-motion: reduce` через существующий `mm.add` блок.
    4. **v9.0 ship-ready, не shipped** — PROJECT.md помечает v9.0 как готовый к `/gsd-close-milestone`, но не перемещает его в `## Shipped Milestones` — это работа следующей команды.

    ## Изменённые файлы

    - `frontends/landing/dist/index.html` — HTML секции `#architecture-flow`, CSS `.arch-*`, GSAP таймлайн внутри `mm.add` блока; обновлена карточка `data-role="headman"`
    - `CLAUDE.md` — статус-блок + раздел URL Layout
    - `docs/url-layout.md` — раздел Production Path Routing
    - `docs/job-stories.md` — 18 новых web-cabinet историй
    - `.planning/PROJECT.md` — ship-ready mark + Recently Validated расширен
    - `docs/phase-57-report.md` — этот файл

    ## Верификация

    - Все grep-based smoke tests из PLAN-01 и PLAN-02 зелёные
    - Human UAT на 360/768/1440px + dark/light + reduced-motion: approved
    - Scroll-driven анимация на desktop работает плавно, pin/unpin без layout-скачков
    - Существующий landing код (hero, features, how-it-works, CTA) не тронут

    ## Nuances / gotchas

    - Если какая-то Phosphor иконка из набора (`ph-git-branch`, `ph-gear`, `ph-cube`, `ph-paper-plane-tilt`, `ph-wifi-high`, `ph-device-mobile`) в будущем исчезнет — заменить на ближайший duotone-аналог. В Phosphor 2.1.1 все 6 подтверждены.
    - `end: '+=500%'` pin длительности — можно настроить по UX-ощущениям (исследование предлагало 500-600%). Текущее значение — 500%.
    - CI path filters для `rct-landing-nginx` image не проверялись отдельной задачей (см. RESEARCH Q4). Если pipeline rebuild не запустится при изменении только `frontends/landing/dist/**` — потребуется отдельный фикс workflow в следующей фазе.

    ## Следующие шаги

    - `/gsd-verify-work phase 57` — финальная верификация всех requirement IDs
    - `/gsd-close-milestone v9.0` — перенос v9.0 в `## Shipped Milestones` секцию `.planning/PROJECT.md` и `ROADMAP.md`
    - v10.0 planning (OTP UI, PWA TEACHER/ADMIN — см. REQUIREMENTS.md «Future Requirements»)
    ```

    НЕ изменять другие `docs/phase-*-report.md` файлы. НЕ трогать CLAUDE.md строку про отчёты (CLAUDE.md раздел «Инструкция для Claude Code» уже требует создание этого файла).
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && test -f docs/phase-57-report.md && bash -c '[ "$(wc -l < docs/phase-57-report.md)" -ge 40 ]' && grep -q "Phase 57 Report" docs/phase-57-report.md && grep -q "LAND-v9-02" docs/phase-57-report.md && grep -q "DOCS-v9-04" docs/phase-57-report.md && grep -q "JS-STUDENT-WEB" docs/phase-57-report.md && grep -q "architecture-flow" docs/phase-57-report.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/phase-57-report.md` существует
    - ≥ 40 строк
    - Содержит заголовок `# Phase 57 Report`
    - Упоминает все 7 requirement IDs: LAND-v9-02, LAND-v9-04, LAND-v9-05, DOCS-v9-01..04
    - Упоминает `#architecture-flow` (новая секция)
    - Упоминает `JS-STUDENT-WEB` (новые job stories)
    - Упоминает «ship-ready» или следующий шаг `/gsd-close-milestone`
    - Остальные `docs/phase-*-report.md` файлы не тронуты
  </acceptance_criteria>
  <done>
    `docs/phase-57-report.md` создан с полной структурой: цель, результаты по треккам, ключевые решения, изменённые файлы, верификация, nuances, следующие шаги. Все 7 requirement IDs упомянуты.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI pipeline ↔ updated landing image | Правки `frontends/landing/dist/index.html` попадают в production через существующий GitHub Actions pipeline (v8.0) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-09 | Denial of Service | Сломанный HTML (несбалансированные секции) сломает рендеринг на production | mitigate | Task 1 проверяет `<section>`/`</section>` balance перед human UAT |
| T-57-10 | Tampering | Реверсируемый regression в dark mode (hex-цвет вместо токена) | mitigate | Task 1 grep на hex внутри `.arch-*` CSS |
| T-57-11 | Repudiation | Phase 57 закрыт без report — невозможно восстановить контекст | mitigate | Task 3 создаёт `docs/phase-57-report.md` с явным upgrade path к `/gsd-close-milestone` |
</threat_model>

<verification>
1. Все 5 авто-проверок Task 1 зелёные
2. Human UAT Task 2 approved (responsive + dark/light + reduced-motion + scroll animation)
3. `docs/phase-57-report.md` существует с ≥ 40 строк и всеми 7 requirement IDs
4. Никаких правок вне frontmatter `files_modified` не произведено
</verification>

<success_criteria>
- [ ] LAND-v9-04 подтверждён на 360/768/1440px в обеих темах
- [ ] LAND-v9-02 scroll-анимация работает на desktop, reduced-motion даёт статику
- [ ] `docs/phase-57-report.md` существует и полный
- [ ] Все задачи plan 01 и plan 02 прошли финальную UAT
- [ ] Фаза 57 готова к `/gsd-verify-work` и `/gsd-close-milestone v9.0`
</success_criteria>

<output>
After completion, create `.planning/phases/57-landing-presentation-mode-documentation/57-03-SUMMARY.md`
</output>
