---
phase: 50
plan: 02
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - frontends/web-panel/src/app/core/auth/student.guard.ts
  - frontends/web-panel/src/app/core/auth/student.guard.spec.ts
  - frontends/web-panel/src/app/core/auth/headman.guard.ts
  - frontends/web-panel/src/app/core/auth/headman.guard.spec.ts
  - frontends/web-panel/src/app/core/auth/guest.guard.ts
  - frontends/web-panel/src/app/core/auth/guest.guard.spec.ts
autonomous: true
requirements:
  - AUTH-v9-04
  - AUTH-v9-05
must_haves:
  truths:
    - "studentGuard пропускает любого STUDENT (включая headman), блокирует ADMIN/TEACHER/unauthenticated"
    - "headmanGuard пропускает только STUDENT+isHeadman=true, блокирует plain STUDENT/ADMIN/TEACHER/unauthenticated"
    - "guestGuard пропускает unauthenticated, редиректит authenticated на их dashboard"
    - "Все 3 guard'а следуют паттерну CanActivateFn + inject() + createUrlTree (never router.navigate)"
  artifacts:
    - path: "frontends/web-panel/src/app/core/auth/student.guard.ts"
      provides: "studentGuard named CanActivateFn"
      contains: "export const studentGuard"
    - path: "frontends/web-panel/src/app/core/auth/headman.guard.ts"
      provides: "headmanGuard named CanActivateFn"
      contains: "export const headmanGuard"
    - path: "frontends/web-panel/src/app/core/auth/guest.guard.ts"
      provides: "guestGuard named CanActivateFn"
      contains: "export const guestGuard"
  key_links:
    - from: "student.guard.ts"
      to: "auth.service.ts resolveDashboardFor"
      via: "inject(AuthService).resolveDashboardFor(user)"
      pattern: "resolveDashboardFor"
    - from: "headman.guard.ts"
      to: "user.isHeadman from JWT"
      via: "user.role === 'STUDENT' && user.isHeadman"
      pattern: "isHeadman"
---

<objective>
Создать три новых функциональных guard (`studentGuard`, `headmanGuard`, `guestGuard`) и их полные spec-файлы. Guards используют `AuthService.resolveDashboardFor` (из Plan 01) как единственный источник истины для fallback редиректов.

Purpose: Guards — это access control для `/student/*`, `/headman/*` и `/login` маршрутов, которые регистрируются в Plan 03. Без этих функций `app.routes.ts` не сможет применить `canActivate`. Guards также реализуют D-08 (guestGuard) и закрывают AUTH-v9-04 (headmanGuard) и AUTH-v9-05 (studentGuard).
Output:
- 3 новых guard файла (~15 строк каждый) в `core/auth/`
- 3 новых spec файла, каждый с ≥ 4 it-блоками
- Все guards протестированы через `TestBed.runInInjectionContext` (стандартный паттерн проекта)
- Никаких изменений в существующих файлах
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-basehref-migration-unified-login/50-CONTEXT.md
@.planning/phases/50-basehref-migration-unified-login/50-RESEARCH.md
@frontends/web-panel/src/app/core/auth/auth.guard.ts
@frontends/web-panel/src/app/core/auth/auth.guard.spec.ts
@frontends/web-panel/src/app/core/auth/auth.service.ts

<interfaces>
<!-- AuthService API (from Plan 01) — guards consume this -->
```typescript
// frontends/web-panel/src/app/core/auth/auth.service.ts (after Plan 01)
export interface AuthUser {
  id: number;
  role: 'TEACHER' | 'ADMIN' | 'STUDENT';
  isHeadman: boolean;
  groupId: number | null;
}

export class AuthService {
  readonly isAuthenticated: Signal<boolean>;
  readonly currentUser: Signal<AuthUser | null>;
  resolveDashboardFor(user: AuthUser | null): string;
  // ... existing methods
}
```

<!-- Reference guard pattern — studentGuard/headmanGuard/guestGuard MUST follow this shape -->
```typescript
// frontends/web-panel/src/app/core/auth/auth.guard.ts (reference)
import { CanActivateFn } from '@angular/router';
import { inject } from '@angular/core';
import { Router } from '@angular/router';
import { AuthService } from './auth.service';

export const authGuard: CanActivateFn = () => {
  const auth = inject(AuthService);
  const router = inject(Router);

  if (auth.isAuthenticated()) return true;
  return router.createUrlTree(['/login']);
};
```

<!-- Reference test pattern — new spec files MUST follow this TestBed.runInInjectionContext shape -->
```typescript
// frontends/web-panel/src/app/core/auth/auth.guard.spec.ts (reference pattern)
TestBed.configureTestingModule({
  providers: [
    AuthService,
    {
      provide: Router,
      useValue: {
        createUrlTree: vi.fn((commands: string[]) => ({ commands }) as unknown as UrlTree),
        navigate: vi.fn(),
      },
    },
  ],
});

it('...', () => {
  authService.setTokens(TOKEN, REFRESH_TOKEN);
  const result = TestBed.runInInjectionContext(() =>
    myGuard({} as any, {} as any)
  );
  expect(result).toBe(true); // or .toHaveBeenCalledWith([...])
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать studentGuard + spec (AUTH-v9-05)</name>
  <files>frontends/web-panel/src/app/core/auth/student.guard.ts, frontends/web-panel/src/app/core/auth/student.guard.spec.ts</files>
  <read_first>
    - frontends/web-panel/src/app/core/auth/auth.guard.ts (reference pattern)
    - frontends/web-panel/src/app/core/auth/auth.guard.spec.ts (test boilerplate: makeJwt, TestBed setup)
    - frontends/web-panel/src/app/core/auth/auth.service.ts (AuthUser interface после Plan 01)
    - .planning/phases/50-basehref-migration-unified-login/50-RESEARCH.md §Code Examples `studentGuard` block
  </read_first>
  <behavior>
    - `studentGuard()` с неаутентифицированным пользователем → возвращает `UrlTree['/login']`
    - `studentGuard()` с TEACHER токеном → возвращает `UrlTree['/teacher/dashboard']` (fallback через resolveDashboardFor)
    - `studentGuard()` с ADMIN токеном → возвращает `UrlTree['/admin/dashboard']`
    - `studentGuard()` с plain STUDENT токеном → возвращает `true`
    - `studentGuard()` с headman STUDENT (isHeadman=true) → возвращает `true` (headman passes studentGuard per HEAD-WEB-01)
  </behavior>
  <action>
    **ШАГ 1 — RED: создать spec файл сначала.**

    Создать `frontends/web-panel/src/app/core/auth/student.guard.spec.ts` с содержимым:
    ```typescript
    import { TestBed } from '@angular/core/testing';
    import { describe, it, expect, beforeEach, vi } from 'vitest';
    import { Router, UrlTree } from '@angular/router';
    import { AuthService } from './auth.service';
    import { studentGuard } from './student.guard';

    const makeJwt = (payload: object): string => {
      const header = btoa(JSON.stringify({ alg: 'HS256', typ: 'JWT' }));
      const body = btoa(JSON.stringify(payload));
      return `${header}.${body}.signature`;
    };

    const TEACHER_TOKEN = makeJwt({ sub: '1', role: 'TEACHER', is_headman: false, group_id: null, exp: 9999999999 });
    const ADMIN_TOKEN = makeJwt({ sub: '2', role: 'ADMIN', is_headman: false, group_id: null, exp: 9999999999 });
    const STUDENT_TOKEN = makeJwt({ sub: '3', role: 'STUDENT', is_headman: false, group_id: 5, exp: 9999999999 });
    const HEADMAN_TOKEN = makeJwt({ sub: '4', role: 'STUDENT', is_headman: true, group_id: 5, exp: 9999999999 });
    const REFRESH_TOKEN = 'refresh-token-abc';

    describe('studentGuard', () => {
      let authService: AuthService;
      let router: Router;

      beforeEach(() => {
        TestBed.configureTestingModule({
          providers: [
            AuthService,
            {
              provide: Router,
              useValue: {
                createUrlTree: vi.fn((commands: string[]) => ({ commands }) as unknown as UrlTree),
                navigate: vi.fn(),
              },
            },
          ],
        });
        authService = TestBed.inject(AuthService);
        router = TestBed.inject(Router);
      });

      it('returns true for plain STUDENT user', () => {
        authService.setTokens(STUDENT_TOKEN, REFRESH_TOKEN);
        const result = TestBed.runInInjectionContext(() =>
          studentGuard({} as any, {} as any)
        );
        expect(result).toBe(true);
      });

      it('returns true for headman STUDENT user (HEAD-WEB-01)', () => {
        authService.setTokens(HEADMAN_TOKEN, REFRESH_TOKEN);
        const result = TestBed.runInInjectionContext(() =>
          studentGuard({} as any, {} as any)
        );
        expect(result).toBe(true);
      });

      it('redirects TEACHER to /teacher/dashboard via resolveDashboardFor', () => {
        authService.setTokens(TEACHER_TOKEN, REFRESH_TOKEN);
        const result = TestBed.runInInjectionContext(() =>
          studentGuard({} as any, {} as any)
        );
        expect(result).not.toBe(true);
        expect(router.createUrlTree).toHaveBeenCalledWith(['/teacher/dashboard']);
      });

      it('redirects ADMIN to /admin/dashboard via resolveDashboardFor', () => {
        authService.setTokens(ADMIN_TOKEN, REFRESH_TOKEN);
        const result = TestBed.runInInjectionContext(() =>
          studentGuard({} as any, {} as any)
        );
        expect(result).not.toBe(true);
        expect(router.createUrlTree).toHaveBeenCalledWith(['/admin/dashboard']);
      });

      it('redirects unauthenticated user to /login', () => {
        const result = TestBed.runInInjectionContext(() =>
          studentGuard({} as any, {} as any)
        );
        expect(result).not.toBe(true);
        expect(router.createUrlTree).toHaveBeenCalledWith(['/login']);
      });
    });
    ```

    Запустить `cd frontends/web-panel && npx vitest run --reporter=dot src/app/core/auth/student.guard.spec.ts`. Все 5 тестов должны упасть с "Cannot find module './student.guard'" — RED.

    **ШАГ 2 — GREEN: создать guard файл.**

    Создать `frontends/web-panel/src/app/core/auth/student.guard.ts`:
    ```typescript
    import { CanActivateFn } from '@angular/router';
    import { inject } from '@angular/core';
    import { Router } from '@angular/router';
    import { AuthService } from './auth.service';

    export const studentGuard: CanActivateFn = () => {
      const auth = inject(AuthService);
      const router = inject(Router);

      const user = auth.currentUser();
      if (!user) return router.createUrlTree(['/login']);
      if (user.role === 'STUDENT') return true;
      return router.createUrlTree([auth.resolveDashboardFor(user)]);
    };
    ```

    **ШАГ 3 — verify GREEN:**
    ```
    cd frontends/web-panel && npx vitest run --reporter=dot src/app/core/auth/student.guard.spec.ts
    ```
    Все 5 тестов green.
  </action>
  <verify>
    <automated>cd frontends/web-panel &amp;&amp; npx vitest run --reporter=dot src/app/core/auth/student.guard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Файл `frontends/web-panel/src/app/core/auth/student.guard.ts` существует
    - `grep -c "export const studentGuard: CanActivateFn" frontends/web-panel/src/app/core/auth/student.guard.ts` → 1
    - `grep -c "user.role === 'STUDENT'" frontends/web-panel/src/app/core/auth/student.guard.ts` → 1
    - `grep -c "auth.resolveDashboardFor(user)" frontends/web-panel/src/app/core/auth/student.guard.ts` → 1
    - `grep -c "createUrlTree\(\['/login'\]\)" frontends/web-panel/src/app/core/auth/student.guard.ts` → 1
    - NO `router.navigate(` в guard файле: `grep -c "router.navigate" frontends/web-panel/src/app/core/auth/student.guard.ts` → 0
    - Файл `frontends/web-panel/src/app/core/auth/student.guard.spec.ts` существует
    - `grep -c "^  it(" frontends/web-panel/src/app/core/auth/student.guard.spec.ts` → 5
    - `cd frontends/web-panel && npx vitest run --reporter=dot src/app/core/auth/student.guard.spec.ts` → exit code 0, 5 tests passed
  </acceptance_criteria>
  <done>
    studentGuard реализован как именованная CanActivateFn, следует паттерну authGuard, все 5 test cases зелёные. AUTH-v9-05 покрыт unit-тестами.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Создать headmanGuard + spec (AUTH-v9-04)</name>
  <files>frontends/web-panel/src/app/core/auth/headman.guard.ts, frontends/web-panel/src/app/core/auth/headman.guard.spec.ts</files>
  <read_first>
    - frontends/web-panel/src/app/core/auth/student.guard.ts (уже созданный в Task 1 — образец)
    - frontends/web-panel/src/app/core/auth/student.guard.spec.ts (образец test boilerplate)
    - .planning/phases/50-basehref-migration-unified-login/50-RESEARCH.md §Code Examples `headmanGuard` block
  </read_first>
  <behavior>
    - `headmanGuard()` с unauthenticated → `UrlTree['/login']`
    - `headmanGuard()` с plain STUDENT (isHeadman=false) → редирект на `/student/dashboard` (через resolveDashboardFor)
    - `headmanGuard()` с headman STUDENT (isHeadman=true) → возвращает `true`
    - `headmanGuard()` с TEACHER → редирект на `/teacher/dashboard`
    - `headmanGuard()` с ADMIN → редирект на `/admin/dashboard`
  </behavior>
  <action>
    **ШАГ 1 — RED: spec сначала.**

    Создать `frontends/web-panel/src/app/core/auth/headman.guard.spec.ts`. Скопировать boilerplate (imports, makeJwt, токены, beforeEach) из `student.guard.spec.ts`, но заменить `studentGuard` на `headmanGuard` в imports. Блок `describe` и тесты:
    ```typescript
    describe('headmanGuard', () => {
      let authService: AuthService;
      let router: Router;

      beforeEach(() => {
        TestBed.configureTestingModule({
          providers: [
            AuthService,
            {
              provide: Router,
              useValue: {
                createUrlTree: vi.fn((commands: string[]) => ({ commands }) as unknown as UrlTree),
                navigate: vi.fn(),
              },
            },
          ],
        });
        authService = TestBed.inject(AuthService);
        router = TestBed.inject(Router);
      });

      it('returns true for headman STUDENT (role=STUDENT && isHeadman=true)', () => {
        authService.setTokens(HEADMAN_TOKEN, REFRESH_TOKEN);
        const result = TestBed.runInInjectionContext(() =>
          headmanGuard({} as any, {} as any)
        );
        expect(result).toBe(true);
      });

      it('redirects plain STUDENT (isHeadman=false) to /student/dashboard', () => {
        authService.setTokens(STUDENT_TOKEN, REFRESH_TOKEN);
        const result = TestBed.runInInjectionContext(() =>
          headmanGuard({} as any, {} as any)
        );
        expect(result).not.toBe(true);
        expect(router.createUrlTree).toHaveBeenCalledWith(['/student/dashboard']);
      });

      it('redirects TEACHER to /teacher/dashboard', () => {
        authService.setTokens(TEACHER_TOKEN, REFRESH_TOKEN);
        const result = TestBed.runInInjectionContext(() =>
          headmanGuard({} as any, {} as any)
        );
        expect(result).not.toBe(true);
        expect(router.createUrlTree).toHaveBeenCalledWith(['/teacher/dashboard']);
      });

      it('redirects ADMIN to /admin/dashboard', () => {
        authService.setTokens(ADMIN_TOKEN, REFRESH_TOKEN);
        const result = TestBed.runInInjectionContext(() =>
          headmanGuard({} as any, {} as any)
        );
        expect(result).not.toBe(true);
        expect(router.createUrlTree).toHaveBeenCalledWith(['/admin/dashboard']);
      });

      it('redirects unauthenticated user to /login', () => {
        const result = TestBed.runInInjectionContext(() =>
          headmanGuard({} as any, {} as any)
        );
        expect(result).not.toBe(true);
        expect(router.createUrlTree).toHaveBeenCalledWith(['/login']);
      });
    });
    ```

    Import header: `import { headmanGuard } from './headman.guard';` + все остальные импорты как в Task 1.

    Запустить: `cd frontends/web-panel && npx vitest run --reporter=dot src/app/core/auth/headman.guard.spec.ts` — ожидается RED.

    **ШАГ 2 — GREEN: создать guard.**

    Создать `frontends/web-panel/src/app/core/auth/headman.guard.ts`:
    ```typescript
    import { CanActivateFn } from '@angular/router';
    import { inject } from '@angular/core';
    import { Router } from '@angular/router';
    import { AuthService } from './auth.service';

    export const headmanGuard: CanActivateFn = () => {
      const auth = inject(AuthService);
      const router = inject(Router);

      const user = auth.currentUser();
      if (!user) return router.createUrlTree(['/login']);
      if (user.role === 'STUDENT' && user.isHeadman) return true;
      return router.createUrlTree([auth.resolveDashboardFor(user)]);
    };
    ```

    **ШАГ 3 — verify GREEN:** `cd frontends/web-panel && npx vitest run --reporter=dot src/app/core/auth/headman.guard.spec.ts`
  </action>
  <verify>
    <automated>cd frontends/web-panel &amp;&amp; npx vitest run --reporter=dot src/app/core/auth/headman.guard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Файл `frontends/web-panel/src/app/core/auth/headman.guard.ts` существует
    - `grep -c "export const headmanGuard: CanActivateFn" frontends/web-panel/src/app/core/auth/headman.guard.ts` → 1
    - `grep -cE "user\.role === 'STUDENT' && user\.isHeadman" frontends/web-panel/src/app/core/auth/headman.guard.ts` → 1
    - `grep -c "auth.resolveDashboardFor(user)" frontends/web-panel/src/app/core/auth/headman.guard.ts` → 1
    - NO `router.navigate` в файле: `grep -c "router.navigate" frontends/web-panel/src/app/core/auth/headman.guard.ts` → 0
    - `grep -c "^  it(" frontends/web-panel/src/app/core/auth/headman.guard.spec.ts` → 5
    - `grep -c "createUrlTree.*'/student/dashboard'" frontends/web-panel/src/app/core/auth/headman.guard.spec.ts` → 1 (тест plain STUDENT → student dashboard)
    - `cd frontends/web-panel && npx vitest run --reporter=dot src/app/core/auth/headman.guard.spec.ts` → 5 tests passed
  </acceptance_criteria>
  <done>
    headmanGuard реализован, 5 test cases зелёные, AUTH-v9-04 покрыт. Plain STUDENT корректно редиректится на `/student/dashboard` (не на `/login`), что критично для UX и для того, чтобы ROADMAP success criterion #3 был verifiable.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Создать guestGuard + spec (D-08) + полный full-suite regression</name>
  <files>frontends/web-panel/src/app/core/auth/guest.guard.ts, frontends/web-panel/src/app/core/auth/guest.guard.spec.ts</files>
  <read_first>
    - frontends/web-panel/src/app/core/auth/student.guard.ts (образец)
    - frontends/web-panel/src/app/core/auth/auth.service.ts (isAuthenticated signal)
    - .planning/phases/50-basehref-migration-unified-login/50-RESEARCH.md §Code Examples `guestGuard` block
    - .planning/phases/50-basehref-migration-unified-login/50-CONTEXT.md §D-08
  </read_first>
  <behavior>
    - `guestGuard()` с unauthenticated → возвращает `true` (форма логина доступна)
    - `guestGuard()` с authenticated ADMIN → `UrlTree['/admin/dashboard']`
    - `guestGuard()` с authenticated TEACHER → `UrlTree['/teacher/dashboard']`
    - `guestGuard()` с authenticated plain STUDENT → `UrlTree['/student/dashboard']`
    - `guestGuard()` с authenticated headman → `UrlTree['/headman/dashboard']`
  </behavior>
  <action>
    **ШАГ 1 — RED: spec.**

    Создать `frontends/web-panel/src/app/core/auth/guest.guard.spec.ts` с boilerplate как в Task 1/2 (makeJwt, 4 токена, TestBed setup). describe block:
    ```typescript
    describe('guestGuard', () => {
      let authService: AuthService;
      let router: Router;

      beforeEach(() => {
        TestBed.configureTestingModule({
          providers: [
            AuthService,
            {
              provide: Router,
              useValue: {
                createUrlTree: vi.fn((commands: string[]) => ({ commands }) as unknown as UrlTree),
                navigate: vi.fn(),
              },
            },
          ],
        });
        authService = TestBed.inject(AuthService);
        router = TestBed.inject(Router);
      });

      it('returns true for unauthenticated user (allows /login form)', () => {
        const result = TestBed.runInInjectionContext(() =>
          guestGuard({} as any, {} as any)
        );
        expect(result).toBe(true);
      });

      it('redirects authenticated ADMIN to /admin/dashboard', () => {
        authService.setTokens(ADMIN_TOKEN, REFRESH_TOKEN);
        const result = TestBed.runInInjectionContext(() =>
          guestGuard({} as any, {} as any)
        );
        expect(result).not.toBe(true);
        expect(router.createUrlTree).toHaveBeenCalledWith(['/admin/dashboard']);
      });

      it('redirects authenticated TEACHER to /teacher/dashboard', () => {
        authService.setTokens(TEACHER_TOKEN, REFRESH_TOKEN);
        const result = TestBed.runInInjectionContext(() =>
          guestGuard({} as any, {} as any)
        );
        expect(result).not.toBe(true);
        expect(router.createUrlTree).toHaveBeenCalledWith(['/teacher/dashboard']);
      });

      it('redirects authenticated plain STUDENT to /student/dashboard', () => {
        authService.setTokens(STUDENT_TOKEN, REFRESH_TOKEN);
        const result = TestBed.runInInjectionContext(() =>
          guestGuard({} as any, {} as any)
        );
        expect(result).not.toBe(true);
        expect(router.createUrlTree).toHaveBeenCalledWith(['/student/dashboard']);
      });

      it('redirects authenticated headman to /headman/dashboard', () => {
        authService.setTokens(HEADMAN_TOKEN, REFRESH_TOKEN);
        const result = TestBed.runInInjectionContext(() =>
          guestGuard({} as any, {} as any)
        );
        expect(result).not.toBe(true);
        expect(router.createUrlTree).toHaveBeenCalledWith(['/headman/dashboard']);
      });
    });
    ```

    Import header: `import { guestGuard } from './guest.guard';` + все остальные.

    **ШАГ 2 — GREEN.**

    Создать `frontends/web-panel/src/app/core/auth/guest.guard.ts`:
    ```typescript
    import { CanActivateFn } from '@angular/router';
    import { inject } from '@angular/core';
    import { Router } from '@angular/router';
    import { AuthService } from './auth.service';

    export const guestGuard: CanActivateFn = () => {
      const auth = inject(AuthService);
      const router = inject(Router);

      if (!auth.isAuthenticated()) return true;
      return router.createUrlTree([auth.resolveDashboardFor(auth.currentUser())]);
    };
    ```

    **ШАГ 3 — Full suite regression (Plan 02 gate).**
    ```
    cd frontends/web-panel && npx vitest run --reporter=dot
    ```
    Ожидается: baseline тесты из Plan 01 (131 + новые) остаются green + 15 новых тестов из этого Plan (5+5+5) green → общий счёт ≥ 146 тестов. Exit code 0.
  </action>
  <verify>
    <automated>cd frontends/web-panel &amp;&amp; npx vitest run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - Файл `frontends/web-panel/src/app/core/auth/guest.guard.ts` существует
    - `grep -c "export const guestGuard: CanActivateFn" frontends/web-panel/src/app/core/auth/guest.guard.ts` → 1
    - `grep -c "!auth.isAuthenticated()" frontends/web-panel/src/app/core/auth/guest.guard.ts` → 1
    - `grep -c "auth.resolveDashboardFor(auth.currentUser())" frontends/web-panel/src/app/core/auth/guest.guard.ts` → 1
    - `grep -c "router.navigate" frontends/web-panel/src/app/core/auth/guest.guard.ts` → 0
    - `grep -c "^  it(" frontends/web-panel/src/app/core/auth/guest.guard.spec.ts` → 5
    - Full suite: `cd frontends/web-panel && npx vitest run --reporter=dot` → exit code 0
    - Total tests ≥ 146 (baseline ~131 + Plan 01 additions + 15 new guard tests)
  </acceptance_criteria>
  <done>
    guestGuard готов, D-08 реализован. Полный test suite green. Plan 02 готов к merge, Plan 03 и Plan 04 могут стартовать в Wave 3.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Angular Router ↔ protected routes | Guards интерсептят navigation events до рендеринга компонентов |
| Browser ↔ server | Guards — client-side only; server-side авторизация через API Gateway JWT filter (не меняется в Phase 50) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-05 | Elevation of Privilege | `/headman/*` доступен plain STUDENT | mitigate | headmanGuard строго проверяет `user.role === 'STUDENT' && user.isHeadman`. Тест "redirects plain STUDENT (isHeadman=false)" явно валидирует блокировку. Backend API Gateway + WPAN-13 (Phase 54) — вторая линия защиты на уровне данных. |
| T-50-06 | Elevation of Privilege | `/student/*` доступен TEACHER/ADMIN | mitigate | studentGuard проверяет `user.role === 'STUDENT'` (который истинен и для headman). Тесты "redirects TEACHER/ADMIN" подтверждают блокировку. |
| T-50-07 | Spoofing | Сессия-fixation через /login форму для уже аутентифицированного юзера | mitigate | guestGuard на маршруте `/login` (применяется в Plan 03) перенаправляет аутентифицированных на их dashboard. Защищает от случайного или намеренного повторного логина. |
| T-50-08 | Tampering | Guard bypass через прямую manipulation UrlTree | accept | Angular guards — защита UX-layer. Клиент может обойти через прямое API-обращение, но API Gateway JWT filter проверит токен. Guards — не security boundary, а UX gate. |
| T-50-09 | Denial of Service | Guard бросает исключение → app-wide navigation freeze | mitigate | Guards имеют только `inject() + sync checks + return`. Никаких async операций, никаких try/catch нужно. Paттерн проверен в `authGuard` тестах. |
</threat_model>

<verification>
Plan gate:
- `cd frontends/web-panel && npx vitest run` → exit 0
- 3 новых guard файла существуют и экспортируют правильные имена
- 3 новых spec файла с 5 it-блоками каждый (15 новых тестов)
- Никаких изменений в существующих файлах (guard только создаются)
</verification>

<success_criteria>
- `studentGuard`, `headmanGuard`, `guestGuard` реализованы как именованные CanActivateFn
- Все guards следуют паттерну `inject() + sync check + return (true | UrlTree)` — никакого `router.navigate`
- 15 новых unit тестов зелёные
- Полный vitest suite green (baseline + Plan 01 + Plan 02 = ≥ 146 it-блоков)
</success_criteria>

<output>
После завершения создать `.planning/phases/50-basehref-migration-unified-login/50-02-SUMMARY.md`
</output>
