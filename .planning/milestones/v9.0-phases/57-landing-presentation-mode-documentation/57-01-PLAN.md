---
phase: 57-landing-presentation-mode-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontends/landing/dist/index.html
autonomous: true
requirements:
  - LAND-v9-02
  - LAND-v9-05
tags:
  - landing
  - gsap
  - animation
  - ui
must_haves:
  truths:
    - "Посетитель /presentation/ видит секцию 'Как работает система' с 6-шаговой цепочкой (git push → CI → backend → RabbitMQ → WebSocket/Push → device)"
    - "На desktop (≥1024px) секция анимируется scroll-driven (pin + scrub), на mobile (<1024px) — batch reveal"
    - "При prefers-reduced-motion: reduce секция отображается статически без анимаций"
    - "В секции #roles все 4 ролевые карточки (Студент/Староста/Преподаватель/Администратор) присутствуют, буллеты у 'Староста' отражают v9.0 web-кабинет (/headman/*) и PWA Группа tab"
    - "Секция рендерится корректно и в dark, и в light теме — только через токены --bg-*/--text-*/--border-*/--accent-*/--gradient-*"
  artifacts:
    - path: frontends/landing/dist/index.html
      provides: "HTML секции architecture-flow + CSS + GSAP init"
      contains: 'id="architecture-flow"'
    - path: frontends/landing/dist/index.html
      provides: "Обновлённая карточка 'Староста' с v9.0 буллетами"
      contains: 'data-role="headman"'
  key_links:
    - from: "<section id='architecture-flow'>"
      to: "gsap.timeline({ scrollTrigger: { trigger: '#architecture-flow' ... } })"
      via: "inline <script> block внутри существующего window.load handler (строка 1391)"
      pattern: "trigger:\\s*'#architecture-flow'"
    - from: ".arch-step / .arch-arrow CSS"
      to: "CSS custom properties --bg-elevated / --border-subtle / --gradient-brand"
      via: "var(--token)"
      pattern: "var\\(--(bg|text|border|accent|gradient)"
---

<objective>
Добавить в `frontends/landing/dist/index.html` новую секцию `#architecture-flow` «Как работает система» с GSAP ScrollTrigger-анимацией 6-шагового сквозного потока (git push → CI/CD → backend микросервисы → RabbitMQ → WebSocket/Push → устройство пользователя) и обновить буллеты карточки 'Староста' под v9.0 web-кабинет. Всё — правки одного файла, без build pipeline.

Purpose: Закрывает LAND-v9-02 (scroll-driven "Как работает система") и завершает LAND-v9-05 (HEADMAN capabilities в ролях отражают v9.0 state).
Output: Одна новая секция + обновлённая карточка 'Староста' + расширенный GSAP matchMedia блок в существующем load-handler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-landing-presentation-mode-documentation/57-RESEARCH.md
@frontends/landing/dist/index.html
@docs/design-decisions.md
@CLAUDE.md

<interfaces>
<!-- Существующие точки расширения в index.html (грепнуто 2026-04-13) -->

Секции и anchors:
- строка 1214:  <section id="how-it-works" class="section how-it-works">  ← секция-предшественник
- строка 1241:  <section id="roles" class="section">                      ← секция после новой
- строка 1247-1295: 4 role cards (student, headman, teacher, admin)       ← LAND-v9-05
- строка 1299:  <section class="cta">                                     ← CTA

CSS токены (строки 52-100, доступны и в dark, и в light):
  --bg-primary, --bg-secondary, --bg-elevated
  --text-primary, --text-secondary, --text-muted
  --border-subtle, --border-strong
  --accent-primary, --accent-secondary
  --gradient-brand (использован в .arch-arrow)
  --space-2..--space-12, --radius-md, --radius-lg

GSAP bootstrap (НЕ дублировать):
- строка 1345-1346: GSAP 3.12.5 + ScrollTrigger CDN, `defer`
- строка 1391:      window.addEventListener('load', function () { ... })
- строка 1392:      if (typeof gsap === 'undefined') return;
- строка 1393:      if (typeof ScrollTrigger !== 'undefined') gsap.registerPlugin(ScrollTrigger);
- строка 1395:      var mm = gsap.matchMedia();
- строка 1397:      mm.add('(prefers-reduced-motion: no-preference)', function () { ... });   ← РАСШИРИТЬ внутри
- строка 1481:      mm.add('(prefers-reduced-motion: reduce)', function () { ... });         ← ДОБАВИТЬ gsap.set для .arch-* внутри

Существующий reveal паттерн (reuse для mobile fallback):
- строка ~1443: ScrollTrigger.batch('.reveal', { ... onEnter: gsap.to(els, { opacity:1, y:0, stagger:... }) })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить HTML секции #architecture-flow + CSS токены</name>
  <files>frontends/landing/dist/index.html</files>
  <read_first>
    - frontends/landing/dist/index.html (строки 750-1000 — existing component CSS pattern; 1210-1300 — section HTML pattern; 52-100 — design tokens)
    - .planning/phases/57-landing-presentation-mode-documentation/57-RESEARCH.md (Example 1 HTML body, Example 2 CSS token-based)
    - .claude/skills/gsap-scrolltrigger/SKILL.md (pin + scrub basics)
  </read_first>
  <action>
    Две правки одного файла `frontends/landing/dist/index.html`:

    **Правка 1 (CSS).** Вставить блок CSS ПЕРЕД существующим комментарием `/* ============ ANIMATION ============ */` (или перед блоком стилей для `.how-it-works` — около строки 750, но после определения токенов). Использовать уникальный анкор-комментарий:

    ```css
    /* ============ ARCHITECTURE FLOW (Phase 57 / LAND-v9-02) ============ */
    .arch-flow {
      background: var(--bg-secondary);
      border-top: 1px solid var(--border-subtle);
      border-bottom: 1px solid var(--border-subtle);
      overflow: hidden;
    }
    .arch-flow__stage {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      padding: var(--space-12) 0;
    }
    .arch-flow__track {
      display: grid;
      grid-template-columns: 1fr;
      gap: var(--space-6);
      margin-top: var(--space-8);
      list-style: none;
      padding: 0;
    }
    .arch-step {
      background: var(--bg-elevated);
      border: 1px solid var(--border-subtle);
      border-radius: 16px;
      padding: var(--space-6) var(--space-4);
      text-align: center;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: var(--space-2);
    }
    .arch-step__icon {
      width: 56px;
      height: 56px;
      border-radius: 14px;
      display: flex;
      align-items: center;
      justify-content: center;
      background: var(--bg-primary);
      border: 1px solid var(--border-subtle);
      color: var(--accent-primary);
      font-size: 28px;
    }
    .arch-step h3 {
      font-size: 1.05rem;
      line-height: 1.3;
      margin: 0;
      color: var(--text-primary);
    }
    .arch-step p {
      font-size: 0.9rem;
      line-height: 1.45;
      margin: 0;
      color: var(--text-secondary);
    }
    .arch-arrow {
      display: block;
      height: 2px;
      background: var(--gradient-brand, linear-gradient(90deg, var(--accent-primary), var(--accent-secondary)));
      transform-origin: left center;
      transform: scaleX(1);
      border-radius: 2px;
    }
    @media (min-width: 1024px) {
      .arch-flow__track {
        grid-template-columns: repeat(11, 1fr);
        gap: var(--space-3);
        align-items: stretch;
      }
      .arch-step { grid-column: span 1; min-height: 220px; }
      .arch-arrow { align-self: center; height: 2px; width: 100%; }
    }
    ```

    Если точных токенов `--space-3`, `--gradient-brand` в :root нет — использовать ближайшие уже существующие (grep `:root` строки 52-100 и подставить) и задокументировать в done-criteria. НЕ вводить новых токенов. НЕ использовать хард-кодед hex-цвета.

    **Правка 2 (HTML).** Вставить новую секцию ПОСЛЕ `</section>` строки 1239 (окончание `#how-it-works`) и ПЕРЕД `<section id="roles"` (строка 1241). Использовать анкор-комментарий:

    ```html
    <!-- ============ ARCHITECTURE FLOW (Phase 57 / LAND-v9-02) ============ -->
    <section id="architecture-flow" class="section arch-flow" aria-labelledby="arch-flow-title">
      <div class="container arch-flow__stage">
        <p class="section__eyebrow">Как работает система</p>
        <h2 id="arch-flow-title" class="section__title">От коммита до вашего&nbsp;устройства</h2>
        <p class="section__subtitle">Каждая отметка — это путь через контейнеры, очереди и&nbsp;сокеты. Вот как он выглядит.</p>
        <ol class="arch-flow__track" role="list">
          <li class="arch-step" data-step="1">
            <div class="arch-step__icon"><i class="ph-duotone ph-git-branch" aria-hidden="true"></i></div>
            <h3>Коммит в&nbsp;репозиторий</h3>
            <p>Разработчик пушит изменения на&nbsp;GitHub.</p>
          </li>
          <span class="arch-arrow" data-to="2" aria-hidden="true"></span>
          <li class="arch-step" data-step="2">
            <div class="arch-step__icon"><i class="ph-duotone ph-gear" aria-hidden="true"></i></div>
            <h3>CI/CD пайплайн</h3>
            <p>GitHub Actions собирает Docker-образы и&nbsp;раскатывает их.</p>
          </li>
          <span class="arch-arrow" data-to="3" aria-hidden="true"></span>
          <li class="arch-step" data-step="3">
            <div class="arch-step__icon"><i class="ph-duotone ph-cube" aria-hidden="true"></i></div>
            <h3>Backend микросервисы</h3>
            <p>Auth, Academic, Schedule, Attendance, Notification.</p>
          </li>
          <span class="arch-arrow" data-to="4" aria-hidden="true"></span>
          <li class="arch-step" data-step="4">
            <div class="arch-step__icon"><i class="ph-duotone ph-paper-plane-tilt" aria-hidden="true"></i></div>
            <h3>Очередь событий</h3>
            <p>RabbitMQ fanout-exchange рассылает события всем подписчикам.</p>
          </li>
          <span class="arch-arrow" data-to="5" aria-hidden="true"></span>
          <li class="arch-step" data-step="5">
            <div class="arch-step__icon"><i class="ph-duotone ph-wifi-high" aria-hidden="true"></i></div>
            <h3>WebSocket / Web Push</h3>
            <p>STOMP-сокет или push-уведомление — в&nbsp;зависимости от&nbsp;канала.</p>
          </li>
          <span class="arch-arrow" data-to="6" aria-hidden="true"></span>
          <li class="arch-step" data-step="6">
            <div class="arch-step__icon"><i class="ph-duotone ph-device-mobile" aria-hidden="true"></i></div>
            <h3>Ваше устройство</h3>
            <p>PWA или Telegram получают обновление в&nbsp;реальном времени.</p>
          </li>
        </ol>
      </div>
    </section>
    ```

    НЕ трогать существующий `#how-it-works` и `#roles`. НЕ менять `<head>`. Используемые иконки Phosphor (`ph-git-branch`, `ph-gear`, `ph-cube`, `ph-paper-plane-tilt`, `ph-wifi-high`, `ph-device-mobile`) есть в Phosphor 2.1.1 — если какая-то не рендерится при визуальной проверке в Task 3, заменить на ближайший duotone-аналог в том же iconset (зафиксировать в commit message).
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack &amp;&amp; grep -c 'id="architecture-flow"' frontends/landing/dist/index.html | grep -q "^1$" &amp;&amp; grep -c 'data-step="6"' frontends/landing/dist/index.html | grep -q "^1$" &amp;&amp; grep -c '/\* ============ ARCHITECTURE FLOW' frontends/landing/dist/index.html | grep -q "^1$" &amp;&amp; ! grep -E '#[0-9a-fA-F]{3,6}' <(sed -n '/ARCHITECTURE FLOW/,/============/p' frontends/landing/dist/index.html) | grep -v 'var(--'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'id="architecture-flow"' frontends/landing/dist/index.html` → 1
    - `grep -c 'data-step="1"' ... `data-step="6"'` — каждый ровно 1 раз
    - `grep -c 'data-to="2"' ... 'data-to="6"'` — каждый ровно 1 раз (5 стрелок)
    - `grep -c 'class="arch-step__icon"' frontends/landing/dist/index.html` → 6
    - Все 6 ожидаемых иконок присутствуют: `ph-git-branch`, `ph-gear`, `ph-cube`, `ph-paper-plane-tilt`, `ph-wifi-high`, `ph-device-mobile`
    - Секция `<section id="architecture-flow"` идёт ПОСЛЕ закрывающего `</section>` от `#how-it-works` и ПЕРЕД `<section id="roles"` (проверяется `grep -n` — номер строки architecture-flow > номер how-it-works и < номер roles)
    - В новом CSS-блоке все цвета/градиенты — через `var(--...)`; прямых hex (`#xxxxxx`) нет
    - Файл остаётся валидным HTML (нет «висячих» открытых тегов) — проверка: `grep -c '<section' == grep -c '</section>'` не ломается
  </acceptance_criteria>
  <done>
    Новая секция `#architecture-flow` отрисована в `index.html` между `#how-it-works` и `#roles`. CSS использует только дизайн-токены. HTML содержит 6 шагов и 5 arrow-разделителей. GSAP ещё не подключён — это Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Расширить GSAP matchMedia блок — pin+scrub desktop / batch mobile / reduced-motion fallback</name>
  <files>frontends/landing/dist/index.html</files>
  <read_first>
    - frontends/landing/dist/index.html (строки 1391-1485 — существующий `window.load` handler с `mm.add`)
    - .planning/phases/57-landing-presentation-mode-documentation/57-RESEARCH.md (Example 3 GSAP timeline + Pitfall 2 mobile + Pitfall 4 reduced-motion)
    - .claude/skills/gsap-scrolltrigger/SKILL.md (pin semantics, matchMedia, scrub)
  </read_first>
  <action>
    Одна правка `frontends/landing/dist/index.html`. Внутри уже существующего блока:

    ```javascript
    mm.add('(prefers-reduced-motion: no-preference)', function () {
      // ... существующий код hero/reveal/feature-card ...
    });
    ```

    Вставить новый суб-блок ПЕРЕД закрывающей `});` строки 1478 (конец no-preference блока), сразу после существующего `document.querySelectorAll('.feature-card')` forEach. Использовать анкор-комментарий `// ---- ARCHITECTURE FLOW (Phase 57) ----`:

    ```javascript
    // ---- ARCHITECTURE FLOW (Phase 57 / LAND-v9-02) ----
    var isArchDesktop = window.matchMedia('(min-width: 1024px)').matches;
    if (isArchDesktop) {
      gsap.set('.arch-step', { opacity: 0, y: 40 });
      gsap.set('.arch-arrow', { scaleX: 0 });
      var archTL = gsap.timeline({
        scrollTrigger: {
          trigger: '#architecture-flow',
          start: 'top top',
          end: '+=500%',
          scrub: 0.6,
          pin: true,
          pinSpacing: true,
          anticipatePin: 1
        }
      });
      for (var archI = 1; archI <= 6; archI++) {
        archTL.to('.arch-step[data-step="' + archI + '"]', { opacity: 1, y: 0, duration: 1, ease: 'power2.out' });
        if (archI < 6) {
          archTL.to('.arch-arrow[data-to="' + (archI + 1) + '"]', { scaleX: 1, duration: 0.6, ease: 'power1.inOut' }, '-=0.3');
        }
      }
    } else {
      gsap.set('.arch-step', { opacity: 0, y: 24 });
      gsap.set('.arch-arrow', { scaleX: 1 });
      ScrollTrigger.batch('.arch-step', {
        start: 'top 85%',
        once: true,
        onEnter: function (els) {
          gsap.to(els, { opacity: 1, y: 0, stagger: 0.1, duration: 0.6, ease: 'power3.out' });
        }
      });
    }
    ```

    И расширить существующий reduced-motion блок (строка 1481-1484): добавить ВНУТРИ него ОДНУ строку:

    ```javascript
    gsap.set('.arch-step, .arch-arrow', { opacity: 1, y: 0, scaleX: 1 });
    ```

    НЕ вводить новый `window.addEventListener('load', ...)` — расширять существующий. НЕ регистрировать ScrollTrigger второй раз (уже сделано в строке 1393). НЕ оставлять `markers: true`.
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack &amp;&amp; grep -c "ARCHITECTURE FLOW (Phase 57" frontends/landing/dist/index.html | grep -qE "^[23]$" &amp;&amp; grep -c "trigger: '#architecture-flow'" frontends/landing/dist/index.html | grep -q "^1$" &amp;&amp; grep -c "ScrollTrigger.batch('.arch-step'" frontends/landing/dist/index.html | grep -q "^1$" &amp;&amp; ! grep -q "markers: true" frontends/landing/dist/index.html &amp;&amp; [ "$(grep -c 'window.addEventListener.*load' frontends/landing/dist/index.html)" = "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "trigger: '#architecture-flow'"` → 1
    - `grep -c "ScrollTrigger.batch('.arch-step'"` → 1
    - `grep -c "pin: true"` → не уменьшилось (новый pin добавился; если до правки было N — теперь N+1)
    - `grep -c "markers: true"` → 0 (не осталось в production)
    - `grep -c "window.addEventListener.*load"` → 1 (не создан второй load-handler)
    - Внутри `mm.add('(prefers-reduced-motion: reduce)'` присутствует `'.arch-step, .arch-arrow'` (reduced-motion fallback): `grep -A3 "prefers-reduced-motion: reduce" frontends/landing/dist/index.html | grep -q "arch-step"`
    - Loop `for (var archI = 1; archI <= 6; archI++)` генерирует 6 step animations (визуально после browser load, но grep проверяет наличие литералов: `grep -q 'archI <= 6'`)
  </acceptance_criteria>
  <done>
    GSAP таймлайн с pin+scrub работает на desktop (≥1024px), batch-reveal на mobile, статический fallback для reduced-motion. Никаких новых load-handler'ов, все правки — ВНУТРИ существующего `mm.add` блока. При визуальной проверке в Task 3 скролл через секцию показывает последовательное появление шагов с прорисовкой стрелок.
  </done>
</task>

<task type="auto">
  <name>Task 3: Обновить карточку 'Староста' (v9.0 web-cabinet + PWA Группа tab)</name>
  <files>frontends/landing/dist/index.html</files>
  <read_first>
    - frontends/landing/dist/index.html (строки 1260-1270 — текущая карточка `data-role="headman"`)
    - .planning/REQUIREMENTS.md (HEAD-WEB-01..08 — capabilities)
    - .planning/STATE.md (v9.0 Architecture Decisions: headman web-cabinet, PWA Группа tab)
  </read_first>
  <action>
    Точечная замена `<ul>` внутри карточки `data-role="headman"` (строки ~1264-1269). Оставить 5 буллетов, отражающих v9.0 state:

    Заменить существующий блок:
    ```html
    <ul>
      <li>Журнал всей группы</li>
      <li>Порог красной зоны</li>
      <li>Рассмотрение excuse-тикетов</li>
      <li>Делегирование помощнику</li>
    </ul>
    ```

    На:
    ```html
    <ul>
      <li>Журнал группы и&nbsp;массовая отметка</li>
      <li>Управление предметами и&nbsp;преподавателями</li>
      <li>Рассмотрение excuse-тикетов и&nbsp;late-check-in</li>
      <li>Порог красной зоны per-subject</li>
      <li>Web-кабинет + вкладка «Группа» в&nbsp;PWA</li>
    </ul>
    ```

    Три остальные карточки (student / teacher / admin) — НЕ трогать. Структура `<article class="role-card">`, иконка `ph-crown`, заголовок «Староста» — остаются.
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack &amp;&amp; grep -c 'data-role="headman"' frontends/landing/dist/index.html | grep -q "^1$" &amp;&amp; grep -c 'data-role="student"' frontends/landing/dist/index.html | grep -q "^1$" &amp;&amp; grep -c 'data-role="teacher"' frontends/landing/dist/index.html | grep -q "^1$" &amp;&amp; grep -c 'data-role="admin"' frontends/landing/dist/index.html | grep -q "^1$" &amp;&amp; grep -q "Вкладка «Группа» в" frontends/landing/dist/index.html || grep -q 'вкладка «Группа» в' frontends/landing/dist/index.html &amp;&amp; grep -q "Web-кабинет" frontends/landing/dist/index.html &amp;&amp; grep -q "late-check-in" frontends/landing/dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - Все 4 `data-role="..."` карточки по-прежнему присутствуют (ровно по 1 каждая: student, headman, teacher, admin) — LAND-v9-05
    - Карточка headman содержит строки: `Журнал группы`, `Управление предметами`, `late-check-in`, `Порог красной зоны per-subject`, `Web-кабинет` + упоминание `«Группа» в`
    - Карточки student / teacher / admin — байт-в-байт идентичны предыдущей версии (diff touch only в пределах `<ul>` карточки headman): `git diff --shortstat frontends/landing/dist/index.html` — не должен показывать правок вне диапазона строк ±5 от `data-role="headman"` ПЛЮС правки Task 1/2
    - Иконка карточки headman остаётся `ph-crown` (не менялась)
  </acceptance_criteria>
  <done>
    Карточка 'Староста' описывает 5 v9.0 capabilities (HEAD-WEB-05 журнал, HEAD-WEB-04 предметы, HEAD-WEB-06+07 excuse/late-check-in, HEAD-WEB-08 порог, PWA-HEAD-01 Группа tab). Остальные 3 карточки не тронуты.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ CDN (jsdelivr, unpkg) | GSAP + Phosphor + fonts загружаются с публичных CDN |
| user browser ↔ static nginx | landing — read-only static HTML, без API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | GSAP/ScrollTrigger CDN script | accept | CDN уже подключён без SRI с Phase 35; добавление SRI — вне scope LAND-v9-02. Риск тот же, что и был до Phase 57 — новых attack surface не вводится. |
| T-57-02 | Tampering | Phosphor Icons CDN | accept | Аналогично T-57-01: no new surface, риск существует с Phase 35 |
| T-57-03 | Information Disclosure | Новая секция `#architecture-flow` раскрывает архитектуру (git → CI → RabbitMQ → WS) | accept | Публичный лендинг намеренно описывает архитектуру; никаких внутренних endpoint'ов, версий, учётных данных не раскрывается. Тексты — высокоуровневые. |
| T-57-04 | Denial of Service | Pinned ScrollTrigger на мобильном как perceived DoS ("страница зависла") | mitigate | Pin + scrub применяется только при `min-width: 1024px`; mobile использует обычный batch-reveal. Reduced-motion — полностью статично. |
| T-57-05 | Tampering | XSS через inline script | accept | Нет user input в лендинге; все литералы контролируются разработчиком. Low risk. |
</threat_model>

<verification>
1. `grep -c 'id="architecture-flow"' frontends/landing/dist/index.html` = 1
2. `grep -c "trigger: '#architecture-flow'"` = 1
3. Все 4 `data-role="..."` карточки присутствуют ровно по 1 разу
4. Нет `markers: true`, нет нового `window.addEventListener.*load`, нет хард-кодед hex внутри `.arch-*` CSS
5. (Manual, phase 57-03) Open https://ruttrack.site/presentation/ (или local `file:// .../dist/index.html`): scroll через секцию показывает последовательное появление 6 шагов с прорисовкой стрелок; dark/light toggle работает; prefers-reduced-motion показывает статический layout
</verification>

<success_criteria>
- [ ] Новая секция `#architecture-flow` с 6 шагами и 5 стрелками в HTML
- [ ] CSS секции использует только design tokens (`var(--...)`)
- [ ] GSAP timeline с `pin: true` + `scrub: 0.6` для `>= 1024px`
- [ ] Batch-reveal fallback для `< 1024px`
- [ ] Reduced-motion fallback устанавливает статические значения для `.arch-step, .arch-arrow`
- [ ] Карточка 'Староста' обновлена 5 буллетами под v9.0
- [ ] Остальные 3 role cards не тронуты
- [ ] Файл `index.html` остаётся валидным HTML (secton open/close balance)
- [ ] Все 3 grep-based acceptance criteria из task'ов проходят
</success_criteria>

<output>
After completion, create `.planning/phases/57-landing-presentation-mode-documentation/57-01-SUMMARY.md`
</output>
