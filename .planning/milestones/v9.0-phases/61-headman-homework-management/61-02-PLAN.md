---
phase: 61-headman-homework-management-ui-homeworkapi-controller-homewo
plan: 02
type: execute
wave: 2
depends_on: [61-01]
files_modified:
  - services/academic-service/academic-app/build.gradle.kts
  - services/academic-service/academic-app/src/main/resources/application.yml
  - services/academic-service/academic-app/src/main/resources/db/migration/V13__homework_lesson_binding.sql
  - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/entity/Homework.java
  - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/grpc/ScheduleGrpcClient.java
  - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/config/ClockConfig.java
  - services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/dto/homework/CreateHomeworkRequest.java
  - services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/dto/homework/HomeworkResponse.java
  - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/homework/HomeworkAssembler.java
  - services/academic-service/academic-app/src/test/java/ru/rutcampustrack/academic/homework/HomeworkMigrationIT.java
autonomous: true
requirements: [HW-LINK]

must_haves:
  truths:
    - "Таблица `homeworks` имеет NOT NULL колонки `lesson_date DATE` и `lesson_number INT`"
    - "Колонка `lesson_id` и индекс `idx_hw_lesson` удалены в V13"
    - "Индекс `idx_homeworks_group_date` на (group_id, lesson_date) создан"
    - "Entity Homework содержит `LocalDate lessonDate` и `Integer lessonNumber`"
    - "`CreateHomeworkRequest` требует `@NotNull @FutureOrPresent LocalDate lessonDate` + `@NotNull @Min(1) @Max(8) Integer lessonNumber`"
    - "`HomeworkResponse` отдаёт lessonDate + lessonNumber"
    - "`UpdateHomeworkRequest` НЕ содержит lessonDate/lessonNumber (привязка фиксируется при create — D-04/D-05)"
    - "`ScheduleGrpcClient` инжектится в academic-app, вызывает schedule-service.ResolveLesson с deadline 3s"
    - "Clock bean инжектится в HomeworkService — для тестируемого `LocalDate.now(clock)`"
  artifacts:
    - path: "services/academic-service/academic-app/src/main/resources/db/migration/V13__homework_lesson_binding.sql"
      provides: "Миграция Flyway V13: TRUNCATE + ADD колонки + DROP lesson_id + CREATE INDEX"
      contains: "ADD COLUMN lesson_date"
    - path: "services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/grpc/ScheduleGrpcClient.java"
      provides: "gRPC клиент для вызова ResolveLesson"
      contains: "@GrpcClient"
    - path: "services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/config/ClockConfig.java"
      provides: "@Bean Clock — Europe/Moscow"
      contains: "Clock clock"
    - path: "services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/dto/homework/CreateHomeworkRequest.java"
      provides: "DTO + validation annotations на lesson-fields"
      contains: "lessonDate"
  key_links:
    - from: "build.gradle.kts: grpc-client-spring-boot-starter"
      to: "@GrpcClient(\"schedule-service\") в ScheduleGrpcClient"
      via: "Spring auto-config после перемещения из testImplementation в implementation"
      pattern: "grpc-client-spring-boot-starter"
    - from: "application.yml grpc.client.schedule-service.address"
      to: "ScheduleGrpcServiceBlockingStub"
      via: "grpc-client-starter resolves named client"
      pattern: "grpc:\\s*client:\\s*schedule-service"
    - from: "Homework entity lesson_date/lesson_number columns"
      to: "HomeworkResponse JSON response"
      via: "HomeworkAssembler.toModel маппинг"
      pattern: "setLessonDate"
---

<objective>
Подготовить academic-service к реализации валидаций из PLAN-03: миграция БД (D-02, D-15), расширение entity/DTO (D-01), настройка gRPC-клиента для schedule-service, Clock bean для тестируемости D-03.

Purpose: Реализует D-01, D-02, D-15. Предоставляет инфраструктуру (gRPC client + Clock + DTO поля + миграция), которую PLAN-03 использует для валидаций.

Output: Flyway миграция V13, расширенные entity+DTO, ScheduleGrpcClient, ClockConfig, ассемблер, integration-тест миграции.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v9.0-phases/61-headman-homework-management/61-CONTEXT.md
@.planning/milestones/v9.0-phases/61-headman-homework-management/61-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Паттерн gRPC-клиента для копирования -->
From services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/grpc/ScheduleGrpcClient.java:
```java
@Component
public class ScheduleGrpcClient {
    @GrpcClient("schedule-service")
    private ScheduleGrpcServiceGrpc.ScheduleGrpcServiceBlockingStub stub;

    public Optional<LessonResponse> getActiveLesson(Long groupId, Instant timestamp) {
        try {
            return Optional.of(stub.withDeadlineAfter(3, TimeUnit.SECONDS).getActiveLesson(...));
        } catch (StatusRuntimeException e) {
            if (e.getStatus().getCode() == Status.Code.NOT_FOUND) return Optional.empty();
            throw new ScheduleServiceUnavailableException(...);
        }
    }
}
```

From services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/dto/homework/CreateHomeworkRequest.java:
```java
// Сейчас record БЕЗ lessonDate/lessonNumber — расширяем.
// ВАЖНО: contract module БЕЗ Lombok, БЕЗ зависимостей на Spring.
public record CreateHomeworkRequest(
    @NotBlank @Size(max=255) String title,
    @Size(max=4000) String description,
    @Size(max=2048) String link,
    @NotNull Long subjectId,
    @NotNull Long groupId,
    @NotNull Long semesterId
) {}
```

From services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/entity/Homework.java:
```java
// JPA entity с Lombok. Сейчас есть поле lessonId (unused, nullable).
// После V13: lessonId удалено, добавлены lessonDate + lessonNumber.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flyway V13 миграция + entity Homework + DTO расширение + Assembler</name>
  <read_first>
    - services/academic-service/academic-app/src/main/resources/db/migration/V1__baseline.sql (смотреть строки про homeworks, понять существующие колонки и индекс idx_hw_lesson)
    - services/academic-service/academic-app/src/main/resources/db/migration/ (ls — убедиться что V12 последняя)
    - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/entity/Homework.java
    - services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/dto/homework/CreateHomeworkRequest.java
    - services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/dto/homework/UpdateHomeworkRequest.java (НЕ трогаем)
    - services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/dto/homework/HomeworkResponse.java
    - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/homework/HomeworkAssembler.java
  </read_first>
  <files>
    services/academic-service/academic-app/src/main/resources/db/migration/V13__homework_lesson_binding.sql,
    services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/entity/Homework.java,
    services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/dto/homework/CreateHomeworkRequest.java,
    services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/dto/homework/HomeworkResponse.java,
    services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/homework/HomeworkAssembler.java,
    services/academic-service/academic-app/src/test/java/ru/rutcampustrack/academic/homework/HomeworkMigrationIT.java
  </files>
  <action>
    1. **V13__homework_lesson_binding.sql** (D-02, D-15):
    ```sql
    -- Phase 61 D-02, D-15: homework binding к паре через natural key
    TRUNCATE homeworks, homework_completions RESTART IDENTITY CASCADE;

    -- D-01: lesson_id не использовался, FK нет; удаляем (и индекс idx_hw_lesson из V1)
    DROP INDEX IF EXISTS idx_hw_lesson;
    ALTER TABLE homeworks DROP COLUMN IF EXISTS lesson_id;

    -- D-01: natural key
    ALTER TABLE homeworks
        ADD COLUMN lesson_date   DATE NOT NULL,
        ADD COLUMN lesson_number INT  NOT NULL;

    -- D-15: индекс для недельных выборок
    CREATE INDEX idx_homeworks_group_date ON homeworks(group_id, lesson_date);
    ```

    2. **Homework.java** — добавить поля (per D-01):
    ```java
    @Column(name = "lesson_date", nullable = false)
    private LocalDate lessonDate;

    @Column(name = "lesson_number", nullable = false)
    private Integer lessonNumber;
    ```
    Удалить поле `lessonId` (колонка больше не существует, ddl-auto: validate упадёт иначе).
    Расширить конструктор `public Homework(...)` на lessonDate, lessonNumber.

    3. **CreateHomeworkRequest.java** (contract module — БЕЗ Lombok, БЕЗ Spring):
    ```java
    public record CreateHomeworkRequest(
        @NotBlank @Size(max=255) String title,
        @Size(max=4000) String description,
        @Size(max=2048) String link,
        @NotNull Long subjectId,
        @NotNull Long groupId,
        @NotNull Long semesterId,
        @NotNull @FutureOrPresent(message="Дата пары должна быть не в прошлом")
        LocalDate lessonDate,
        @NotNull @Min(1) @Max(8) Integer lessonNumber
    ) {}
    ```

    4. **UpdateHomeworkRequest** — НЕ трогать. По D-04/D-05 привязка к паре фиксируется при create. Комментарий в PR: "lessonDate/lessonNumber не редактируются — см. 61-CONTEXT D-04 + 61-RESEARCH backend-валидации".

    5. **HomeworkResponse.java** — добавить поля + сеттеры (class, не record):
    ```java
    private LocalDate lessonDate;
    private Integer lessonNumber;
    // + геттеры/сеттеры (паттерн остального класса)
    ```

    6. **HomeworkAssembler.java** — в `toModel(Homework)` проставить:
    ```java
    model.setLessonDate(homework.getLessonDate());
    model.setLessonNumber(homework.getLessonNumber());
    ```

    7. **HomeworkMigrationIT.java** — Testcontainers Postgres, применяется Flyway, проверяется:
    - колонки `lesson_date` NOT NULL, `lesson_number` NOT NULL существуют
    - колонка `lesson_id` НЕ существует
    - индекс `idx_homeworks_group_date` существует (`pg_indexes`)
    - existing тесты EntityMappingIntegrationTest (если есть) должны пройти после регенерации схемы
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && ./gradlew.bat :services:academic-service:academic-app:test --tests '*HomeworkMigrationIT*'</automated>
  </verify>
  <done>
    - V13 миграция применяется чисто (Testcontainers log)
    - Hibernate ddl-auto: validate проходит (ни одного "column not found")
    - HomeworkMigrationIT зелёный
  </done>
</task>

<task type="auto">
  <name>Task 2: gRPC клиент academic→schedule + Clock bean + application.yml</name>
  <read_first>
    - services/academic-service/academic-app/build.gradle.kts (найти testImplementation "net.devh:grpc-client-spring-boot-starter")
    - services/attendance-service/attendance-app/build.gradle.kts (образец implementation строки)
    - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/grpc/ScheduleGrpcClient.java (паттерн)
    - services/attendance-service/attendance-app/src/main/resources/application.yml (секция grpc.client.schedule-service)
    - services/academic-service/academic-app/src/main/resources/application.yml
    - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/exception/ (найти существующий ScheduleServiceUnavailableException или создать)
  </read_first>
  <files>
    services/academic-service/academic-app/build.gradle.kts,
    services/academic-service/academic-app/src/main/resources/application.yml,
    services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/grpc/ScheduleGrpcClient.java,
    services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/config/ClockConfig.java
  </files>
  <action>
    1. **build.gradle.kts** — переместить зависимость из `testImplementation` в `implementation`:
    ```kotlin
    implementation("net.devh:grpc-client-spring-boot-starter:3.1.0.RELEASE")
    ```
    (убрать старую testImplementation-строку — иначе конфликт).

    2. **application.yml** — добавить секцию grpc client:
    ```yaml
    grpc:
      client:
        schedule-service:
          address: static://schedule-service:9092
          negotiation-type: plaintext
    ```
    Порт 9092 — per CLAUDE.md (Schedule Service). Адрес host = docker-compose service name.

    3. **ScheduleGrpcClient.java** — новый `@Component` (паттерн из attendance):
    ```java
    @Component
    public class ScheduleGrpcClient {
        @GrpcClient("schedule-service")
        private ScheduleGrpcServiceGrpc.ScheduleGrpcServiceBlockingStub stub;

        public Optional<LessonResponse> resolveLesson(Long groupId, LocalDate date, int lessonNumber) {
            try {
                LessonResponse resp = stub.withDeadlineAfter(3, TimeUnit.SECONDS)
                    .resolveLesson(ResolveLessonRequest.newBuilder()
                        .setGroupId(groupId)
                        .setDate(date.toString())
                        .setLessonNumber(lessonNumber)
                        .build());
                return Optional.of(resp);
            } catch (StatusRuntimeException e) {
                if (e.getStatus().getCode() == Status.Code.NOT_FOUND) return Optional.empty();
                throw new ScheduleServiceUnavailableException(
                    "Не удалось связаться с schedule-service: " + e.getStatus(), e);
            }
        }
    }
    ```
    Если `ScheduleServiceUnavailableException` не существует — создать в `exception/` (наследует RuntimeException, маппится в 503 через @ControllerAdvice).

    4. **ClockConfig.java** — новый `@Configuration`:
    ```java
    @Configuration
    public class ClockConfig {
        @Bean
        public Clock clock() {
            return Clock.system(ZoneId.of("Europe/Moscow")); // D-03 + Pitfall 2
        }
    }
    ```

    **НЕ делать:** не вызывать ScheduleGrpcClient из HomeworkService в этом плане — это задача PLAN-03.
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && ./gradlew.bat :services:academic-service:academic-app:compileJava :services:academic-service:academic-app:test --tests '*AcademicApplicationTest*'</automated>
  </verify>
  <done>
    - academic-app компилируется (gRPC stub классы резолвятся после PLAN-01)
    - Spring context стартует (ApplicationContext-тест зелёный)
    - ScheduleGrpcClient инжектится без ошибок
    - Clock bean доступен
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| academic-service ↔ schedule-service (gRPC client→server) | untrusted network (docker bridge) |
| Client JSON → CreateHomeworkRequest | untrusted input crosses API boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-04 | Tampering | CreateHomeworkRequest.lessonNumber | mitigate | `@Min(1) @Max(8)` на DTO + gRPC валидация в PLAN-01 |
| T-61-05 | DoS | gRPC client без deadline | mitigate | `withDeadlineAfter(3, TimeUnit.SECONDS)` — паттерн проекта |
| T-61-06 | Info disclosure | Stack trace в ScheduleServiceUnavailableException | mitigate | @ControllerAdvice не возвращает stack клиенту (RFC 7807 ErrorResponse) |
</threat_model>

<verification>
- V13 в последовательности Flyway применяется после V12 без ошибок
- `ddl-auto: validate` не жалуется на неизвестные/пропавшие колонки
- gRPC client bean доступен в контексте Spring
</verification>

<success_criteria>
1. Таблица `homeworks` после миграции имеет NOT NULL `lesson_date`, `lesson_number`; нет `lesson_id`; есть `idx_homeworks_group_date`
2. Entity `Homework` совпадает со схемой (validate проходит)
3. `CreateHomeworkRequest` требует lessonDate + lessonNumber с правильной валидацией
4. `HomeworkResponse` содержит эти поля
5. `ScheduleGrpcClient` и `Clock` bean инжектятся
6. academic-app test suite проходит (regression)
</success_criteria>

<output>
После завершения создать `.planning/milestones/v9.0-phases/61-headman-homework-management/61-02-SUMMARY.md`.
</output>
