---
phase: 55
plan: 04
type: execute
wave: 2
depends_on:
  - 55-02
files_modified:
  - frontends/web-panel/src/app/features/headman/excuses/headman-excuses.component.ts
  - frontends/web-panel/src/app/features/headman/excuses/headman-excuses.component.spec.ts
  - frontends/web-panel/src/app/features/headman/late-checkin/headman-late-checkin.component.ts
  - frontends/web-panel/src/app/features/headman/late-checkin/headman-late-checkin.component.spec.ts
autonomous: true
requirements:
  - HEAD-WEB-06
  - HEAD-WEB-07

must_haves:
  truths:
    - "Visiting /headman/excuses renders a page with an empty-state containing the text from D-07"
    - "Visiting /headman/late-checkin renders a page with an empty-state containing the text from D-07"
    - "No console errors are thrown when the backend returns 404 for the pending excuses/late-checkins endpoints"
    - "Both pages show the .page-empty block regardless of whether the API returns null or a 404"
  artifacts:
    - path: "frontends/web-panel/src/app/features/headman/excuses/headman-excuses.component.ts"
      provides: "Graceful degradation shell for excuses"
      contains: "HeadmanExcusesComponent"
    - path: "frontends/web-panel/src/app/features/headman/late-checkin/headman-late-checkin.component.ts"
      provides: "Graceful degradation shell for late check-in"
      contains: "HeadmanLateCheckinComponent"
  key_links:
    - from: "HeadmanExcusesComponent.ngOnInit"
      to: "catchError(() => of(null))"
      via: "getPendingExcuses() pipe"
      pattern: "catchError"
    - from: "template"
      to: ".page-empty text"
      via: "@if (data() === null)"
      pattern: "Функция находится в разработке"
---

<objective>
Build the graceful-degradation shell components for `/headman/excuses` and `/headman/late-checkin`. Both pages call their respective deferred backend endpoints, handle 404/error via `catchError(() => of(null))`, and display the standard empty-state message.

Purpose: These are thin shells — no approval/rejection actions in this phase (deferred, per CONTEXT.md Deferred section). The graceful degradation pattern is already established in `HeadmanDashboardComponent`.

Output: `HeadmanExcusesComponent` and `HeadmanLateCheckinComponent` with specs verifying empty-state on 404.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-headman-web-cabinet-attendance-management-stats/55-CONTEXT.md
@.planning/phases/55-headman-web-cabinet-attendance-management-stats/55-UI-SPEC.md
@.planning/phases/55-headman-web-cabinet-attendance-management-stats/55-02-SUMMARY.md
</context>

<interfaces>
<!-- HeadmanDashboardComponent graceful degradation pattern (VERIFIED existing code): -->
```typescript
// On init:
this.headmanApi.getPendingExcuses()
  .pipe(catchError(() => of(null)))
  .subscribe(data => {
    this.excuses.set(data);  // null on 404/error
  });

// Template:
@if (excuses() === null) {
  <div class="page-empty" role="status" aria-live="polite">
    <i class="ph ph-clock"></i>
    Функция находится в разработке...
  </div>
}
```

<!-- HeadmanApiService deferred endpoints: -->
```typescript
getPendingExcuses(): Observable<any>     // → 404 (deferred backend)
getPendingLateCheckins(): Observable<any> // → 404 (deferred backend)
```

<!-- UI-SPEC empty-state structure (locked by D-07): -->
```html
.page-card
  └── .page-empty [role="status"] [aria-live="polite"]
        ├── .page-empty__icon  (ph-file-text for excuses / ph-clock-countdown for late-checkin)
        ├── .page-empty__title  "Нет активных заявок" / "Нет активных запросов"
        └── .page-empty__text   "Функция находится в разработке. Заявки появятся здесь автоматически."
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HeadmanExcusesComponent + HeadmanLateCheckinComponent shells (D-06, D-07, D-08)</name>
  <files>
    frontends/web-panel/src/app/features/headman/excuses/headman-excuses.component.ts
    frontends/web-panel/src/app/features/headman/excuses/headman-excuses.component.spec.ts
    frontends/web-panel/src/app/features/headman/late-checkin/headman-late-checkin.component.ts
    frontends/web-panel/src/app/features/headman/late-checkin/headman-late-checkin.component.spec.ts
  </files>
  <read_first>
    - frontends/web-panel/src/app/features/headman/dashboard/headman-dashboard.component.ts
    - frontends/web-panel/src/app/features/headman/shared/headman-api.service.ts
  </read_first>
  <behavior>
    - HeadmanExcusesComponent: on init calls getPendingExcuses().pipe(catchError(() => of(null))), stores result in excusesData signal
    - HeadmanLateCheckinComponent: on init calls getPendingLateCheckins().pipe(catchError(() => of(null))), stores result in lateCheckinData signal
    - When data signal is null (API returned error/404): show .page-empty with the exact text from D-07
    - When data is a non-null array: if empty array, also show .page-empty (no items); if non-empty, show count (this is a future-proof placeholder — current backend always returns 404)
    - No dialog or action buttons rendered in either component (D-08: no CRUD needed)
    - routeFade animation on host element
    - Spec tests: mock service returns throwError({ status: 404 }) → component shows .page-empty element with the D-07 text
  </behavior>
  <action>
**Create `headman-excuses.component.ts`:**

```typescript
import { ChangeDetectionStrategy, Component, OnInit, inject, signal } from '@angular/core';
import { trigger, transition, style, animate } from '@angular/animations';
import { catchError, of } from 'rxjs';
import { HeadmanApiService } from '../shared/headman-api.service';

@Component({
  selector: 'app-headman-excuses',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [],
  animations: [
    trigger('routeFade', [
      transition(':enter', [
        style({ opacity: 0, transform: 'translateY(8px)' }),
        animate('200ms cubic-bezier(0.16, 1, 0.3, 1)', style({ opacity: 1, transform: 'translateY(0)' })),
      ]),
    ]),
  ],
  host: { '[@routeFade]': '' },
  template: `
    <div class="page-header">
      <h1>Пропуски</h1>
      <span class="page-header__eyebrow">Староста</span>
    </div>
    <div class="page-card">
      <div class="page-empty" role="status" aria-live="polite">
        <div class="page-empty__icon"><i class="ph ph-file-text"></i></div>
        <p class="page-empty__title">Нет активных заявок</p>
        <p class="page-empty__text">Функция находится в разработке. Заявки появятся здесь автоматически.</p>
      </div>
    </div>
  `,
})
export class HeadmanExcusesComponent implements OnInit {
  private readonly headmanApi = inject(HeadmanApiService);
  excusesData = signal<any>(null);

  ngOnInit(): void {
    this.headmanApi.getPendingExcuses()
      .pipe(catchError(() => of(null)))
      .subscribe(data => this.excusesData.set(data));
  }
}
```

**Create `headman-late-checkin.component.ts`** — same pattern, different copy:
- selector: `app-headman-late-checkin`
- class: `HeadmanLateCheckinComponent`
- lateCheckinData signal
- on init: `getPendingLateCheckins().pipe(catchError(() => of(null)))`
- Template: title "Запросы отметки", icon `ph-clock-countdown`, empty title "Нет активных запросов", empty text "Функция находится в разработке. Заявки появятся здесь автоматически."

**Create spec files** for both. Each spec:
1. Provides mock `HeadmanApiService` where the relevant method returns `throwError(() => new Error('404'))`
2. Calls `fixture.detectChanges()`
3. Asserts that an element with class `.page-empty` is present in the DOM
4. Asserts that the element contains text "Функция находится в разработке. Заявки появятся здесь автоматически."

Pattern for spec:
```typescript
import { render, screen } from '@testing-library/angular';
import { signal } from '@angular/core';
import { throwError } from 'rxjs';
import { HeadmanExcusesComponent } from './headman-excuses.component';
import { HeadmanApiService } from '../shared/headman-api.service';

describe('HeadmanExcusesComponent', () => {
  it('shows degradation empty state when API returns error', async () => {
    const mockService = { getPendingExcuses: () => throwError(() => new Error('404')) };
    await render(HeadmanExcusesComponent, {
      providers: [{ provide: HeadmanApiService, useValue: mockService }],
    });
    expect(screen.getByText(/Функция находится в разработке/)).toBeTruthy();
  });
});
```
  </action>
  <verify>
    <automated>cd frontends/web-panel && npx vitest run src/app/features/headman/excuses/ src/app/features/headman/late-checkin/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - HeadmanExcusesComponent exists with catchError degradation and .page-empty template
    - HeadmanLateCheckinComponent exists with catchError degradation and .page-empty template
    - Both spec files exist and pass
    - Exact D-07 text is present: "Функция находится в разработке. Заявки появятся здесь автоматически."
  </done>
  <acceptance_criteria>
    - grep "HeadmanExcusesComponent" frontends/web-panel/src/app/features/headman/excuses/headman-excuses.component.ts returns a match
    - grep "HeadmanLateCheckinComponent" frontends/web-panel/src/app/features/headman/late-checkin/headman-late-checkin.component.ts returns a match
    - grep "Функция находится в разработке" frontends/web-panel/src/app/features/headman/excuses/headman-excuses.component.ts returns a match
    - grep "Функция находится в разработке" frontends/web-panel/src/app/features/headman/late-checkin/headman-late-checkin.component.ts returns a match
    - grep "catchError" frontends/web-panel/src/app/features/headman/excuses/headman-excuses.component.ts returns a match
    - grep "catchError" frontends/web-panel/src/app/features/headman/late-checkin/headman-late-checkin.component.ts returns a match
    - npx vitest run src/app/features/headman/excuses/ src/app/features/headman/late-checkin/ exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API error → component | 404 must not propagate as uncaught exception |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-07 | D (Denial of Service) | getPendingExcuses 404 error propagation | mitigate | catchError(() => of(null)) prevents unhandled rejection from crashing the component; both components handle null gracefully |
| T-55-08 | E (Elevation of Privilege) | /headman/excuses route | accept | Route protected by headmanGuard (registered in Plan 02); plain students cannot reach this component |
</threat_model>

<verification>
1. `cd frontends/web-panel && npx vitest run src/app/features/headman/excuses/ src/app/features/headman/late-checkin/` exits 0
2. Both components contain the exact locked text from D-07: "Функция находится в разработке. Заявки появятся здесь автоматически."
3. Both components contain `catchError(() => of(null))` — no uncaught errors on 404
4. `cd frontends/web-panel && npx vitest run` (full suite) exits 0 — no regressions
</verification>

<success_criteria>
- HeadmanExcusesComponent renders .page-empty with D-07 copy on any API failure
- HeadmanLateCheckinComponent renders .page-empty with D-07 copy on any API failure
- No action buttons rendered (no CRUD in these shells)
- Both spec files pass with mocked throwError service
</success_criteria>

<output>
After completion, create `.planning/phases/55-headman-web-cabinet-attendance-management-stats/55-04-SUMMARY.md`
</output>
