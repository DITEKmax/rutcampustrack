---
phase: 53
plan: 03
type: execute
wave: 2
depends_on:
  - 53-01
files_modified:
  - frontends/web-panel/src/app/features/student/late-checkin/student-late-checkin.component.ts
  - frontends/web-panel/src/app/features/student/late-checkin/student-late-checkin.component.html
  - frontends/web-panel/src/app/features/student/late-checkin/student-late-checkin.component.css
  - frontends/web-panel/src/app/features/student/late-checkin/student-late-checkin.component.spec.ts
autonomous: true
requirements:
  - STU-WEB-08

must_haves:
  truths:
    - "Страница /student/late-checkin показывает только занятия со статусом 'absent'"
    - "Каждая строка содержит кнопку 'Запросить отметку'"
    - "После нажатия кнопки она заменяется pill 'Запрос отправлен' без перезагрузки страницы"
    - "При HTTP 404 от requestLateCheckin — тот же success state (graceful degradation)"
    - "При HTTP 5xx — кнопка восстанавливается + inline error 'Ошибка. Попробуйте ещё раз.'"
    - "Empty state при отсутствии absent записей: 'Нет пропущенных занятий'"
    - "npm test проходит"
  artifacts:
    - path: "frontends/web-panel/src/app/features/student/late-checkin/student-late-checkin.component.ts"
      provides: "Страница Запрос отметки"
      exports: ["StudentLateCheckinComponent"]
  key_links:
    - from: "StudentLateCheckinComponent"
      to: "StudentApiService.requestLateCheckin"
      via: "inject(StudentApiService).requestLateCheckin(lessonId)"
      pattern: "requestLateCheckin"
    - from: "StudentLateCheckinComponent"
      to: "StudentApiService.getStudentRecords"
      via: "filter(r => r.status === 'absent')"
      pattern: "getStudentRecords"
---

<objective>
Реализовать страницу `/student/late-checkin` — список absent занятий с per-row action "Запросить отметку" и оптимистичным in-place success state.

Purpose: Доставить STU-WEB-08 — студент может запросить позднюю отметку из браузера.
Output: StudentLateCheckinComponent с полной логикой per-row request и graceful degradation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53/53-RESEARCH.md
@.planning/phases/53/53-UI-SPEC.md

<interfaces>
Из student-api.service.ts (добавлено в Plan 01):
```typescript
getStudentRecords(subjectId?: number): Observable<AttendanceRecord[]>
requestLateCheckin(lessonId: number): Observable<void>
```

Из student-schedule.types.ts:
```typescript
export interface AttendanceRecord {
  lessonId: number;
  subjectId: number;
  lessonDate: string;  // YYYY-MM-DD
  lessonNumber: number;
  status: string;      // 'present' | 'absent' | 'excused' | 'free_attendance' | 'cancelled'
  symbol: string;
  source: string;
}
```

Паттерн per-row state из student-homework.component.ts:
```typescript
readonly pendingItems = signal<Record<number, boolean>>({});
readonly itemErrors = signal<Record<number, string>>({});
// Обновить: pendingItems.update(p => ({ ...p, [id]: true }))
```

CSS-классы (не переопределять):
- `.page-stack`, `.page-header`, `.page-card--flush`
- `.page-empty` (ph-icon + heading + body)
- `.page-error` (inline error block)
- `.status-chip` (статусный чип)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StudentLateCheckinComponent — страница запроса поздней отметки</name>

  <read_first>
    - frontends/web-panel/src/app/features/student/late-checkin/student-late-checkin.component.spec.ts
    - frontends/web-panel/src/app/features/student/homework/student-homework.component.ts
    - .planning/phases/53/53-UI-SPEC.md (раздел "2. /student/late-checkin — StudentLateCheckinComponent")
    - .planning/phases/53/53-RESEARCH.md (Pattern 3)
  </read_first>

  <files>
    frontends/web-panel/src/app/features/student/late-checkin/student-late-checkin.component.ts,
    frontends/web-panel/src/app/features/student/late-checkin/student-late-checkin.component.html,
    frontends/web-panel/src/app/features/student/late-checkin/student-late-checkin.component.css,
    frontends/web-panel/src/app/features/student/late-checkin/student-late-checkin.component.spec.ts
  </files>

  <behavior>
    - getStudentRecords() возвращает записи с разными статусами → показываются ТОЛЬКО absent
    - Нет absent записей → empty state 'Нет пропущенных занятий'
    - Клик 'Запросить отметку' → spinner в кнопке + disabled
    - requestLateCheckin() → success (или graceful 404): кнопка заменяется pill с иконкой + 'Запрос отправлен'
    - requestLateCheckin() → HTTP 5xx: кнопка восстанавливается + под ней 'Ошибка. Попробуйте ещё раз.'
    - sentRows.has(lessonId) = true → pill, не кнопка (реактивно через signal)
  </behavior>

  <action>
**student-late-checkin.component.ts:**

```typescript
import {
  ChangeDetectionStrategy, Component, OnInit, inject, signal, computed,
} from '@angular/core';
import { CommonModule } from '@angular/common';
import { trigger, transition, style, animate } from '@angular/animations';
import { StudentApiService } from '../shared/student-api.service';
import type { AttendanceRecord } from '../shared/student-schedule.types';

@Component({
  selector: 'app-student-late-checkin',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CommonModule],
  animations: [
    trigger('routeFade', [
      transition(':enter', [
        style({ opacity: 0, transform: 'translateY(8px)' }),
        animate('200ms var(--ease-out, ease-out)', style({ opacity: 1, transform: 'translateY(0)' })),
      ]),
    ]),
  ],
  templateUrl: './student-late-checkin.component.html',
  styleUrl: './student-late-checkin.component.css',
})
export class StudentLateCheckinComponent implements OnInit {
  private readonly apiService = inject(StudentApiService);

  readonly loading = signal(false);
  readonly error = signal<string | null>(null);
  readonly allRecords = signal<AttendanceRecord[]>([]);

  /** IDs of rows where request has been sent successfully (or graceful 404) */
  readonly sentRows = signal<Set<number>>(new Set());
  /** IDs of rows currently being submitted */
  readonly pendingRows = signal<Set<number>>(new Set());
  /** Per-row error messages */
  readonly rowErrors = signal<Record<number, string>>({});

  readonly absentRecords = computed(() =>
    this.allRecords().filter(r => r.status === 'absent'),
  );

  ngOnInit(): void {
    this.loading.set(true);
    this.apiService.getStudentRecords().subscribe({
      next: (records) => {
        this.allRecords.set(records);
        this.loading.set(false);
      },
      error: () => {
        this.error.set('Не удалось загрузить список занятий. Попробуйте обновить страницу.');
        this.loading.set(false);
      },
    });
  }

  requestLateCheckin(lessonId: number): void {
    if (this.sentRows().has(lessonId) || this.pendingRows().has(lessonId)) return;

    // Clear previous row error
    this.rowErrors.update(e => {
      const next = { ...e };
      delete next[lessonId];
      return next;
    });

    // Set pending
    this.pendingRows.update(set => {
      const next = new Set(set);
      next.add(lessonId);
      return next;
    });

    this.apiService.requestLateCheckin(lessonId).subscribe({
      next: () => {
        // Success or graceful 404 — show success state in-place
        this.pendingRows.update(set => {
          const next = new Set(set);
          next.delete(lessonId);
          return next;
        });
        this.sentRows.update(set => {
          const next = new Set(set);
          next.add(lessonId);
          return next;
        });
      },
      error: () => {
        this.pendingRows.update(set => {
          const next = new Set(set);
          next.delete(lessonId);
          return next;
        });
        this.rowErrors.update(e => ({
          ...e,
          [lessonId]: 'Ошибка. Попробуйте ещё раз.',
        }));
      },
    });
  }

  isSent(lessonId: number): boolean {
    return this.sentRows().has(lessonId);
  }

  isPending(lessonId: number): boolean {
    return this.pendingRows().has(lessonId);
  }

  getRowError(lessonId: number): string | null {
    return this.rowErrors()[lessonId] ?? null;
  }
}
```

**student-late-checkin.component.html:**

```html
<div class="page-stack" @routeFade>
  <div class="page-header">
    <div>
      <p class="page-header__eyebrow">Студент</p>
      <h1 class="page-header__title">Запрос отметки</h1>
    </div>
  </div>

  @if (loading()) {
    <div class="page-card">
      <div class="skeleton-row" *ngFor="let i of [1,2,3]"></div>
    </div>
  } @else if (error()) {
    <div class="page-error" role="alert">
      <i class="ph ph-warning-circle"></i>
      {{ error() }}
    </div>
  } @else if (absentRecords().length === 0) {
    <div class="page-empty">
      <i class="ph-duotone ph-check-circle page-empty__icon"></i>
      <h2 class="page-empty__heading">Нет пропущенных занятий</h2>
      <p class="page-empty__body">Все ваши занятия отмечены — запрашивать позднюю отметку не нужно.</p>
    </div>
  } @else {
    <div class="page-card page-card--flush">
      <div
        class="checkin-table"
        role="table"
        aria-label="Список пропущенных занятий"
        aria-live="polite"
      >
        <div class="checkin-table__header" role="row">
          <span role="columnheader">Дата</span>
          <span role="columnheader">Занятие</span>
          <span role="columnheader">Статус</span>
          <span role="columnheader">Действие</span>
        </div>

        @for (record of absentRecords(); track record.lessonId) {
          <div class="checkin-table__row" role="row">
            <span class="cell-date" role="cell">{{ record.lessonDate }}</span>
            <span class="cell-lesson" role="cell">Занятие {{ record.lessonNumber }}</span>
            <span
              class="status-chip status-chip--absent"
              [attr.aria-label]="'Статус: пропущено'"
              role="cell"
            >н</span>
            <div class="cell-action" role="cell">
              @if (isSent(record.lessonId)) {
                <span class="sent-pill" role="status" aria-label="Запрос отправлен">
                  <i class="ph-fill ph-check-circle sent-pill__icon"></i>
                  Запрос отправлен
                </span>
              } @else {
                <button
                  class="btn-request"
                  type="button"
                  (click)="requestLateCheckin(record.lessonId)"
                  [disabled]="isPending(record.lessonId)"
                  [attr.aria-busy]="isPending(record.lessonId)"
                >
                  @if (isPending(record.lessonId)) {
                    <span class="btn-spinner-sm"></span>
                  } @else {
                    <i class="ph ph-clock-countdown btn-request__icon"></i>
                  }
                  Запросить отметку
                </button>
                @if (getRowError(record.lessonId)) {
                  <p class="row-error" role="alert">{{ getRowError(record.lessonId) }}</p>
                }
              }
            </div>
          </div>
        }
      </div>
    </div>
  }
</div>
```

**student-late-checkin.component.css:**

```css
.checkin-table {
  width: 100%;
}

.checkin-table__header {
  display: grid;
  grid-template-columns: 100px 1fr 60px 180px;
  padding: var(--space-2) var(--space-4);
  font-size: var(--text-xs);
  font-weight: 600;
  color: var(--text-muted);
  border-bottom: 1px solid var(--border-subtle);
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.checkin-table__row {
  display: grid;
  grid-template-columns: 100px 1fr 60px 180px;
  padding: var(--space-4);
  min-height: 52px;
  align-items: center;
  gap: var(--space-4);
  border-bottom: 1px solid var(--border-subtle);
}

.checkin-table__row:last-child {
  border-bottom: none;
}

.cell-date {
  font-size: var(--text-xs);
  font-family: var(--font-mono);
  color: var(--text-muted);
  font-variant-numeric: tabular-nums;
}

.cell-lesson {
  font-size: var(--text-base);
  font-family: var(--font-heading);
  font-weight: 600;
  color: var(--text-primary);
}

.cell-action {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: var(--space-1);
}

.btn-request {
  display: inline-flex;
  align-items: center;
  gap: var(--space-1);
  min-height: 36px;
  padding: 0 var(--space-4);
  background: transparent;
  border: 1px solid var(--accent-primary);
  border-radius: var(--radius-pill, 9999px);
  color: var(--accent-primary);
  font-size: var(--text-sm);
  cursor: pointer;
  transition: background 150ms ease;
  white-space: nowrap;
}

.btn-request:hover:not(:disabled) {
  background: color-mix(in srgb, var(--accent-primary) 8%, transparent);
}

.btn-request:disabled {
  opacity: 0.55;
  cursor: not-allowed;
}

.btn-request__icon {
  font-size: 16px;
}

.sent-pill {
  display: inline-flex;
  align-items: center;
  gap: var(--space-1);
  font-size: var(--text-sm);
  color: var(--accent-primary);
}

.sent-pill__icon {
  font-size: 16px;
  color: var(--accent-primary);
}

.row-error {
  font-size: var(--text-xs);
  color: var(--accent-danger);
  margin: 0;
}

.skeleton-row {
  height: 52px;
  border-radius: var(--radius-md);
  background: var(--bg-elevated);
  margin-bottom: var(--space-2);
  animation: dashboard-shimmer 1.5s infinite;
}

.btn-spinner-sm {
  display: inline-block;
  width: 12px;
  height: 12px;
  border: 2px solid color-mix(in srgb, var(--accent-primary) 30%, transparent);
  border-top-color: var(--accent-primary);
  border-radius: 50%;
  animation: spin 0.6s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

@media (max-width: 600px) {
  .checkin-table__header,
  .checkin-table__row {
    grid-template-columns: 1fr 60px auto;
  }
  .cell-date {
    display: none;
  }
}
```

**student-late-checkin.component.spec.ts** — заменить заглушку Wave 0:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen } from '@testing-library/angular';
import { userEvent } from '@testing-library/user-event';
import { StudentLateCheckinComponent } from './student-late-checkin.component';
import { StudentApiService } from '../shared/student-api.service';
import { provideAnimations } from '@angular/platform-browser/animations';
import { of, throwError } from 'rxjs';
import { HttpErrorResponse } from '@angular/common/http';
import type { AttendanceRecord } from '../shared/student-schedule.types';

const mockRecord = (lessonId: number, status: string): AttendanceRecord => ({
  lessonId,
  subjectId: 1,
  lessonDate: '2026-04-01',
  lessonNumber: 1,
  status,
  symbol: status === 'absent' ? 'н' : 'б',
  source: 'auto',
});

const mockApiService = {
  getStudentRecords: vi.fn(),
  requestLateCheckin: vi.fn(),
};

describe('StudentLateCheckinComponent', () => {
  beforeEach(() => vi.clearAllMocks());

  it('показывает только absent записи (фильтрует present/excused)', async () => {
    mockApiService.getStudentRecords.mockReturnValue(
      of([
        mockRecord(1, 'absent'),
        mockRecord(2, 'present'),
        mockRecord(3, 'excused'),
      ]),
    );
    await render(StudentLateCheckinComponent, {
      providers: [
        provideAnimations(),
        { provide: StudentApiService, useValue: mockApiService },
      ],
    });
    // Only lesson 1 is absent — should appear as a row with "Запросить отметку"
    const buttons = screen.getAllByText('Запросить отметку');
    expect(buttons).toHaveLength(1);
  });

  it('empty state при отсутствии absent записей', async () => {
    mockApiService.getStudentRecords.mockReturnValue(
      of([mockRecord(1, 'present')]),
    );
    await render(StudentLateCheckinComponent, {
      providers: [
        provideAnimations(),
        { provide: StudentApiService, useValue: mockApiService },
      ],
    });
    expect(screen.getByText('Нет пропущенных занятий')).toBeTruthy();
  });

  it('клик по кнопке → success state при HTTP 404 (graceful degradation)', async () => {
    mockApiService.getStudentRecords.mockReturnValue(of([mockRecord(1, 'absent')]));
    mockApiService.requestLateCheckin.mockReturnValue(of(undefined));
    const user = userEvent.setup();
    await render(StudentLateCheckinComponent, {
      providers: [
        provideAnimations(),
        { provide: StudentApiService, useValue: mockApiService },
      ],
    });
    const btn = screen.getByText('Запросить отметку');
    await user.click(btn);
    expect(screen.getByText('Запрос отправлен')).toBeTruthy();
  });

  it('ошибка API → кнопка восстанавливается + inline error', async () => {
    mockApiService.getStudentRecords.mockReturnValue(of([mockRecord(1, 'absent')]));
    mockApiService.requestLateCheckin.mockReturnValue(
      throwError(() => new HttpErrorResponse({ status: 500 })),
    );
    const user = userEvent.setup();
    await render(StudentLateCheckinComponent, {
      providers: [
        provideAnimations(),
        { provide: StudentApiService, useValue: mockApiService },
      ],
    });
    const btn = screen.getByText('Запросить отметку');
    await user.click(btn);
    expect(screen.getByText('Ошибка. Попробуйте ещё раз.')).toBeTruthy();
    // Button should still be present (not replaced by pill)
    expect(screen.getByText('Запросить отметку')).toBeTruthy();
  });
});
```
  </action>

  <acceptance_criteria>
    - grep "StudentLateCheckinComponent" frontends/web-panel/src/app/features/student/late-checkin/student-late-checkin.component.ts → найдено
    - grep "absentRecords\|status === 'absent'" frontends/web-panel/src/app/features/student/late-checkin/student-late-checkin.component.ts → найдено
    - grep "sentRows" frontends/web-panel/src/app/features/student/late-checkin/student-late-checkin.component.ts → найдено
    - grep "Запрос отправлен" frontends/web-panel/src/app/features/student/late-checkin/student-late-checkin.component.html → найдено
    - grep "Нет пропущенных занятий" frontends/web-panel/src/app/features/student/late-checkin/student-late-checkin.component.html → найдено
    - grep "Ошибка. Попробуйте ещё раз" frontends/web-panel/src/app/features/student/late-checkin/student-late-checkin.component.ts → найдено
    - cd frontends/web-panel && npm test — все тесты зелёные
  </acceptance_criteria>

  <verify>
    <automated>cd frontends/web-panel && npm test 2>&1 | tail -20</automated>
  </verify>

  <done>
    StudentLateCheckinComponent реализован: фильтрует absent, per-row кнопка, in-place success state, graceful degradation, inline error recovery. Все тесты зелёные.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API Gateway | POST /api/attendance/late-checkin/{lessonId} |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-03-01 | Spoofing | requestLateCheckin с произвольным lessonId | accept | Backend должен валидировать lessonId принадлежит студенту (scope по JWT); backend deferred — TODO комментарий. Фронтенд показывает только записи из собственного getStudentRecords() |
| T-53-03-02 | Spoofing | Graceful 404 snackbar | accept | Преднамеренное UX-решение из ROADMAP Phase 53 Notes |
| T-53-03-03 | Elevation of Privilege | studentGuard | accept | Маршрут /student/late-checkin уже защищён studentGuard в app.routes.ts (Plan 01) |
</threat_model>

<verification>
1. `cd frontends/web-panel && npm test` — все тесты зелёные
2. `grep "status === 'absent'" frontends/web-panel/src/app/features/student/late-checkin/student-late-checkin.component.ts` → фильтр найден
3. `grep "sentRows" frontends/web-panel/src/app/features/student/late-checkin/student-late-checkin.component.ts` → сигнал найден
4. `grep "Запрос отправлен\|sent-pill" frontends/web-panel/src/app/features/student/late-checkin/student-late-checkin.component.html` → success state найдено
</verification>

<success_criteria>
- /student/late-checkin показывает только записи со status='absent'
- Empty state "Нет пропущенных занятий" когда нет absent записей
- Кнопка "Запросить отметку" → spinner → success pill "Запрос отправлен" (in-place, no reload)
- HTTP 404 (graceful) → success state
- HTTP 5xx → кнопка восстановлена + "Ошибка. Попробуйте ещё раз."
- npm test проходит
</success_criteria>

<output>
После завершения создать `.planning/phases/53/53-03-SUMMARY.md`
</output>
