---
phase: 29-pwa-scaffold-auth
plan: 02
subsystem: ui
tags: [react, vite, pwa, tailwindcss, shadcn-ui, service-worker, workbox, tanstack-query, axios, vitest]

requires:
  - phase: 28-api-gateway-cors-nginx
    provides: PWA nginx container and placeholder dist/ files
provides:
  - Buildable React + Vite PWA project at frontends/pwa/
  - Service Worker with injectManifest precaching (sw.ts)
  - PWA manifest with RutCampusTrack branding
  - shadcn/ui slate theme with 6 base components
  - Axios instance with silent 401 refresh interceptor
  - TanStack Query client configured
  - useNetworkStatus hook and OfflineBanner component
  - vitest test infrastructure with jsdom
affects: [29-03, 29-04, 30-pwa-schedule, 31-web-push]

tech-stack:
  added: [react@19, vite@7, vite-plugin-pwa@1.2, tailwindcss@4, shadcn-ui@4.1, tanstack-react-query@5, axios@1.14, motion@12, phosphor-icons-react@2.1, react-router@7, workbox-precaching@7.4, vitest@3]
  patterns: [injectManifest-sw, axios-silent-refresh-interceptor, useSyncExternalStore-network-hook, path-alias-@]

key-files:
  created:
    - frontends/pwa/package.json
    - frontends/pwa/vite.config.ts
    - frontends/pwa/src/sw.ts
    - frontends/pwa/src/shared/lib/axios.ts
    - frontends/pwa/src/shared/lib/queryClient.ts
    - frontends/pwa/src/shared/hooks/useNetworkStatus.ts
    - frontends/pwa/src/shared/components/OfflineBanner.tsx
    - frontends/pwa/src/shared/components/LoadingSpinner.tsx
    - frontends/pwa/components.json
    - frontends/pwa/vitest.config.ts
  modified: []

key-decisions:
  - "Vite 7.x pinned (not 8) — vite-plugin-pwa 1.2.0 peer dep requires ^7.0.0"
  - "shadcn/ui default component path @/components/ui kept (not moved to shared/) — follows shadcn conventions"
  - "Placeholder PNG icons generated via Node.js zlib — no external image tool dependency"
  - "ESM-style import.meta.url for path alias in vite.config.ts — avoids __dirname in ESM modules"

patterns-established:
  - "Path alias: @/ maps to src/ in both tsconfig and vite.config.ts"
  - "Axios interceptor: isRefreshing + pendingQueue for concurrent 401 race prevention"
  - "Network status: useSyncExternalStore with online/offline window events"
  - "vitest config: separate from vite.config.ts, jsdom environment, @testing-library/jest-dom setup"

requirements-completed: [PWA-04, PWA-05]

duration: 7min
completed: 2026-04-06
---

# Phase 29 Plan 02: PWA Scaffold Summary

**React 19 + Vite 7 PWA project with injectManifest Service Worker, Tailwind 4 + shadcn/ui slate theme, Axios silent-refresh interceptor, and TanStack Query**

## Performance

- **Duration:** 7 min
- **Started:** 2026-04-06T07:19:15Z
- **Completed:** 2026-04-06T07:26:06Z
- **Tasks:** 2
- **Files modified:** 32

## Accomplishments
- Complete React + TypeScript + Vite PWA project builds successfully from scratch
- Service Worker with workbox precacheAndRoute via injectManifest strategy — 6 precache entries
- PWA manifest generated by vite-plugin-pwa with RutCampusTrack/RutTrack branding, standalone display, slate-900 theme
- shadcn/ui initialized with slate theme and 6 components (button, input, label, card, alert, separator)
- Axios instance with full silent refresh interceptor: isRefreshing flag + pendingQueue for T-29-08 race protection
- vitest test infrastructure ready with jsdom environment

## Task Commits

Each task was committed atomically:

1. **Task 1: Create React + Vite project with Tailwind + shadcn/ui** - `a6d9c1b` (feat)
2. **Task 2: Create shared infrastructure** - `3eb5db7` (feat)

## Files Created/Modified
- `frontends/pwa/package.json` - React PWA project with all dependencies (Vite 7, React 19, TanStack Query, Axios, etc.)
- `frontends/pwa/vite.config.ts` - Vite config with PWA plugin (injectManifest), Tailwind, path alias, dev proxy
- `frontends/pwa/src/sw.ts` - Custom Service Worker with workbox precacheAndRoute
- `frontends/pwa/components.json` - shadcn/ui configuration (base-nova style, neutral base color)
- `frontends/pwa/index.html` - HTML entry point with viewport-fit=cover and theme-color meta
- `frontends/pwa/src/main.tsx` - App entry with QueryClientProvider
- `frontends/pwa/src/App.tsx` - Placeholder app component
- `frontends/pwa/src/index.css` - Tailwind + shadcn CSS with slate theme variables
- `frontends/pwa/src/shared/lib/axios.ts` - Axios instance with Bearer token and silent 401 refresh interceptor
- `frontends/pwa/src/shared/lib/queryClient.ts` - TanStack QueryClient with 5min staleTime, 10min gcTime
- `frontends/pwa/src/shared/hooks/useNetworkStatus.ts` - Network status hook using useSyncExternalStore
- `frontends/pwa/src/shared/components/OfflineBanner.tsx` - Animated offline banner with Motion
- `frontends/pwa/src/shared/components/LoadingSpinner.tsx` - CSS spin loading indicator
- `frontends/pwa/src/shared/types/pwa.d.ts` - BeforeInstallPromptEvent type declaration
- `frontends/pwa/src/components/ui/*.tsx` - 6 shadcn components (button, input, label, card, alert, separator)
- `frontends/pwa/src/lib/utils.ts` - shadcn utility (cn function)
- `frontends/pwa/vitest.config.ts` - vitest config with jsdom, path alias, react plugin
- `frontends/pwa/src/test/setup.ts` - Test setup importing @testing-library/jest-dom
- `frontends/pwa/public/icons/icon-192.png` - 192x192 solid slate-900 placeholder PNG
- `frontends/pwa/public/icons/icon-512.png` - 512x512 solid slate-900 placeholder PNG
- `frontends/pwa/tsconfig.json` - TypeScript project references config
- `frontends/pwa/tsconfig.app.json` - App TypeScript config with path alias
- `frontends/pwa/tsconfig.node.json` - Node TypeScript config for vite/vitest configs
- `frontends/pwa/.gitignore` - Node/Vite gitignore

## Decisions Made
- Pinned Vite 7.x instead of 8.x due to vite-plugin-pwa 1.2.0 peer dependency constraint
- Kept shadcn default component path (`src/components/ui/`) rather than moving to `src/shared/components/ui/` — avoids fighting shadcn's built-in path management while custom shared components live in `src/shared/components/`
- Used `fileURLToPath(import.meta.url)` for ESM-compatible path resolution in vite.config.ts instead of `__dirname`
- Generated placeholder PNG icons via Node.js zlib (no external image tool needed)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added @types/node devDependency**
- **Found during:** Task 1 (build verification)
- **Issue:** `tsc -b` failed with "Cannot find module 'path'" — @types/node not installed
- **Fix:** `npm install -D @types/node` and switched to ESM `import.meta.url` pattern
- **Files modified:** package.json, vite.config.ts
- **Verification:** `npm run build` exits 0
- **Committed in:** a6d9c1b (Task 1 commit)

**2. [Rule 3 - Blocking] Used manual project scaffold instead of `npm create vite@latest`**
- **Found during:** Task 1 (project initialization)
- **Issue:** `npm create vite@latest .` cancelled due to existing files (dist/, nginx.conf)
- **Fix:** Created all project files manually with correct Vite template structure
- **Files modified:** All frontends/pwa/ files
- **Verification:** `npm run build` exits 0
- **Committed in:** a6d9c1b (Task 1 commit)

---

**Total deviations:** 2 auto-fixed (2 blocking)
**Impact on plan:** Both fixes necessary to unblock build. No scope creep.

## Issues Encountered
None beyond the auto-fixed deviations above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- PWA project scaffold complete, ready for Plan 03 to add auth flow (AuthProvider, login screen, protected routes)
- All shared infrastructure (Axios with interceptor, QueryClient, network status hook) ready for auth feature consumption
- shadcn components (button, input, label, card, alert) ready for login form UI

## Self-Check: PASSED

All 15 key files verified present on disk. Both task commits (a6d9c1b, 3eb5db7) verified in git log.

---
*Phase: 29-pwa-scaffold-auth*
*Completed: 2026-04-06*
