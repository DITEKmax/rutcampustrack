---
phase: 59-excuses-backend
plan: 03
type: execute
wave: 2
depends_on:
  - 59-01
files_modified:
  - proto/schedule.proto
  - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/grpc/ScheduleGrpcServiceImpl.java
  - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/grpc/ScheduleGrpcClient.java
  - services/schedule-service/schedule-app/src/test/java/ru/rutcampustrack/schedule/grpc/LessonsByIdsGrpcIT.java
autonomous: true
requirements:
  - FR-1
  - AC-11

must_haves:
  truths:
    - "proto/schedule.proto содержит LessonsByIdsRequest и LessonsByIdsResponse и rpc GetLessonsByIds"
    - "ScheduleGrpcServiceImpl реализует GetLessonsByIds: возвращает LessonInfo для каждого lessonId (groupId, subjectId, startsAt)"
    - "ScheduleGrpcClient.getLessonsByIds(List<Long>) добавлен в attendance-service"
    - "Существующие gRPC методы (GetActiveLesson, GetLessonById, GetLessonsByGroup) продолжают работать"
    - "Интеграционный тест LessonsByIdsGrpcIT зелёный"
  artifacts:
    - path: "proto/schedule.proto"
      provides: "GetLessonsByIds rpc + сообщения LessonsByIdsRequest/Response/LessonInfo"
      contains: "rpc GetLessonsByIds"
    - path: "services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/grpc/ScheduleGrpcServiceImpl.java"
      provides: "Реализация GetLessonsByIds"
    - path: "services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/grpc/ScheduleGrpcClient.java"
      provides: "getLessonsByIds клиентский метод"
  key_links:
    - from: "ExcuseService.createExcuse (план 02)"
      to: "ScheduleGrpcClient.getLessonsByIds"
      via: "вызов для валидации lessonIds принадлежности группе D-25"
      pattern: "getLessonsByIds"
    - from: "ScheduleGrpcClient.getLessonsByIds"
      to: "ScheduleGrpcServiceImpl.getLessonsByIds"
      via: "gRPC stub call"
      pattern: "getLessonsByIds"
---

<objective>
Расширить gRPC контракт schedule-service методом GetLessonsByIds и реализовать его (Wave 2, параллельно с планом 02).

Purpose: Требуется для валидации в ExcuseService — проверить что все lessonIds принадлежат группе студента (D-25). Это независимый от ExcuseService план — выполняется параллельно в Wave 2.

Output:
- `proto/schedule.proto` — новый rpc GetLessonsByIds + сообщения LessonsByIdsRequest, LessonsByIdsResponse, LessonInfo
- `ScheduleGrpcServiceImpl.java` — имплементация нового rpc
- `ScheduleGrpcClient.java` (attendance-service) — новый метод getLessonsByIds
- `LessonsByIdsGrpcIT.java` — интеграционный тест
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-excuses-backend/59-CONTEXT.md
@.planning/phases/59-excuses-backend/59-RESEARCH.md

<interfaces>
<!-- Существующий schedule.proto -->

```proto
syntax = "proto3";
package rutcampustrack.schedule;
option java_multiple_files = true;
option java_package = "ru.rutcampustrack.schedule.grpc";

service ScheduleGrpcService {
  rpc GetActiveLesson (ActiveLessonRequest) returns (LessonResponse);
  rpc GetLessonById (LessonByIdRequest) returns (LessonResponse);
  rpc GetLessonsByGroup (LessonsByGroupRequest) returns (LessonsResponse);
  // ДОБАВИТЬ: rpc GetLessonsByIds (LessonsByIdsRequest) returns (LessonsByIdsResponse);
}
```

Добавить (из RESEARCH.md D-25):
```proto
message LessonsByIdsRequest {
    repeated int64 lesson_ids = 1;
}
message LessonsByIdsResponse {
    repeated LessonInfo lessons = 1;
}
message LessonInfo {
    int64 lesson_id = 1;
    int64 group_id = 2;
    int64 subject_id = 3;
    string starts_at = 4;  // ISO datetime строка
}
```

ScheduleGrpcServiceImpl (образец существующего метода getLessonsByGroup):
```java
@Override
public void getLessonsByGroup(LessonsByGroupRequest request, StreamObserver<LessonsResponse> responseObserver) {
    // ... queries lessonRepository + scheduleItemRepository
    responseObserver.onNext(LessonsResponse.newBuilder().addAllLessons(responses).build());
    responseObserver.onCompleted();
}

// Новый метод будет:
@Override
public void getLessonsByIds(LessonsByIdsRequest request, StreamObserver<LessonsByIdsResponse> responseObserver) {
    List<Long> ids = request.getLessonIdsList();
    List<Lesson> lessons = lessonRepository.findAllById(ids);
    // Для каждого lesson найти ScheduleItem по lesson.getScheduleItemId()
    // Собрать LessonInfo с lesson.getId(), item.getGroupId(), item.getSubjectId(), lesson.getDate()+"T"+item.getStartTime()
}
```

ScheduleGrpcClient (attendance-service — образец):
```java
public LessonResponse getLessonById(Long lessonId) {
    return stub.withDeadlineAfter(3, TimeUnit.SECONDS)
            .getLessonById(LessonByIdRequest.newBuilder().setLessonId(lessonId).build());
}
// Новый метод:
public List<LessonInfo> getLessonsByIds(List<Long> lessonIds) {
    return stub.withDeadlineAfter(3, TimeUnit.SECONDS)
            .getLessonsByIds(LessonsByIdsRequest.newBuilder().addAllLessonIds(lessonIds).build())
            .getLessonsList();
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Расширить proto + реализовать GetLessonsByIds в schedule-service</name>
  <files>
    proto/schedule.proto
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/grpc/ScheduleGrpcServiceImpl.java
  </files>
  <read_first>
    - proto/schedule.proto (РЕДАКТИРОВАТЬ — добавить rpc и messages)
    - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/grpc/ScheduleGrpcServiceImpl.java (РЕДАКТИРОВАТЬ — добавить override метод)
    - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/entity/Lesson.java (поля entity)
    - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/item/entity/ScheduleItem.java (поля entity)
    - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/repository/LessonRepository.java (доступные методы)
    - .planning/phases/59-excuses-backend/59-CONTEXT.md (D-25)
  </read_first>
  <action>
    1. Открыть proto/schedule.proto. В сервисе ScheduleGrpcService добавить строку:
       `rpc GetLessonsByIds (LessonsByIdsRequest) returns (LessonsByIdsResponse);`
       После всех message-блоков добавить:
       ```proto
       message LessonsByIdsRequest {
           repeated int64 lesson_ids = 1;
       }
       message LessonsByIdsResponse {
           repeated LessonInfo lessons = 1;
       }
       message LessonInfo {
           int64 lesson_id = 1;
           int64 group_id = 2;
           int64 subject_id = 3;
           string starts_at = 4;
       }
       ```
    2. Открыть ScheduleGrpcServiceImpl.java. Добавить @Override метод getLessonsByIds:
       - Получить ids из request.getLessonIdsList()
       - Загрузить все Lesson через lessonRepository.findAllById(ids)
       - Для каждого Lesson найти ScheduleItem через scheduleItemRepository.findById(lesson.getScheduleItemId()) — если не найден, пропустить
       - Собрать LessonInfo: lessonId=lesson.getId(), groupId=item.getGroupId(), subjectId=item.getSubjectId(), startsAt=lesson.getDate().toString()+"T"+item.getStartTime().toString()
       - Вернуть LessonsByIdsResponse.newBuilder().addAllLessons(list).build()
       - responseObserver.onCompleted()
    3. Запустить ./gradlew.bat :services:schedule-service:schedule-app:compileJava — должно скомпилироваться.
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && ./gradlew.bat :services:schedule-service:schedule-app:compileJava 2>&1 | tail -20</automated>
  </verify>
  <done>Компиляция schedule-service зелёная; grep "getLessonsByIds" services/schedule-service/schedule-app/ выдаёт ScheduleGrpcServiceImpl.java</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Клиент getLessonsByIds в attendance-service + интеграционный тест</name>
  <files>
    services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/grpc/ScheduleGrpcClient.java
    services/schedule-service/schedule-app/src/test/java/ru/rutcampustrack/schedule/grpc/LessonsByIdsGrpcIT.java
  </files>
  <read_first>
    - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/grpc/ScheduleGrpcClient.java (РЕДАКТИРОВАТЬ — добавить getLessonsByIds)
    - services/schedule-service/schedule-app/src/test/java/ru/rutcampustrack/schedule/ (найти существующий интеграционный тест для образца)
    - .planning/phases/59-excuses-backend/59-RESEARCH.md (§ AC-11, LessonsByIdsGrpcIT)
  </read_first>
  <behavior>
    - Test 1 (AC-11): создать 3 урока в БД, вызвать getLessonsByIds([id1, id2, id3]) → вернуть 3 LessonInfo с корректными groupId/subjectId
    - Test 2: getLessonsByIds([несуществующий_id]) → вернуть пустой список (не бросать исключение)
    - Test 3: getLessonsByIds([]) → вернуть пустой список
  </behavior>
  <action>
    1. Открыть ScheduleGrpcClient.java в attendance-service. Добавить метод:
       ```java
       public List<LessonInfo> getLessonsByIds(List<Long> lessonIds) {
           if (lessonIds == null || lessonIds.isEmpty()) return List.of();
           try {
               return stub.withDeadlineAfter(3, TimeUnit.SECONDS)
                       .getLessonsByIds(LessonsByIdsRequest.newBuilder()
                               .addAllLessonIds(lessonIds)
                               .build())
                       .getLessonsList();
           } catch (StatusRuntimeException e) {
               throw new ScheduleServiceUnavailableException("Schedule Service unavailable: " + e.getStatus());
           }
       }
       ```
       LessonInfo генерируется protoc — импортировать из ru.rutcampustrack.schedule.grpc.LessonInfo.
    2. Создать LessonsByIdsGrpcIT.java в schedule-service тестах:
       - @SpringBootTest, использовать @EmbeddedPostgres или Testcontainers PostgreSQL (по образцу существующих тестов schedule-service)
       - setUp(): создать тестовые данные (ScheduleItem + Lesson) в репозитории
       - test_getLessonsByIds_returnsCorrectGroupId() — вызвать gRPC сервис напрямую (не через HTTP), проверить результат
       - Если в schedule-service нет testcontainers-postgres зависимости, добавить её в build.gradle.kts
    3. Запустить тест: ./gradlew.bat :services:schedule-service:schedule-app:test --tests "*LessonsByIdsGrpcIT"
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && ./gradlew.bat :services:attendance-service:attendance-app:compileJava :services:schedule-service:schedule-app:test --tests "*LessonsByIdsGrpcIT" 2>&1 | tail -30</automated>
  </verify>
  <done>attendance-service компилируется; LessonsByIdsGrpcIT зелёный (3 теста); grep "getLessonsByIds" services/attendance-service/attendance-app/ выдаёт ScheduleGrpcClient.java</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| attendance-service → schedule-service (gRPC) | lesson_ids приходят от клиента через attendance-service — потенциально произвольные значения |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-03-01 | Spoofing | getLessonsByIds: произвольные lessonIds | mitigate | ExcuseService проверяет что все возвращённые lessons имеют groupId == ctx.groupId |
| T-59-03-02 | Denial of Service | getLessonsByIds: огромный список ids | mitigate | Deadline 3 секунды; разумное ограничение lessonIds.size() в ExcuseService (≤ 50) |
</threat_model>

<verification>
- `./gradlew.bat :services:schedule-service:schedule-app:compileJava` → BUILD SUCCESSFUL
- `./gradlew.bat :services:attendance-service:attendance-app:compileJava` → BUILD SUCCESSFUL
- `grep "rpc GetLessonsByIds" proto/schedule.proto` → находит строку
- `./gradlew.bat :services:schedule-service:schedule-app:test --tests "*LessonsByIdsGrpcIT"` → зелёный
- `./gradlew.bat :services:schedule-service:schedule-app:test` → все существующие тесты проходят (регрессия)
</verification>

<success_criteria>
- proto/schedule.proto содержит GetLessonsByIds rpc + 3 message типа
- ScheduleGrpcServiceImpl.java реализует getLessonsByIds(): загружает Lesson по ids, собирает LessonInfo
- ScheduleGrpcClient.java в attendance-service имеет метод getLessonsByIds(List&lt;Long&gt;)
- Существующие gRPC тесты schedule-service проходят без регрессий
- LessonsByIdsGrpcIT зелёный
</success_criteria>

<output>
После завершения создать `.planning/phases/59-excuses-backend/59-03-SUMMARY.md`
</output>
