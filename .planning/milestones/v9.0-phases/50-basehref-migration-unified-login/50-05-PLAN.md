---
phase: 50
plan: 05
type: execute
wave: 4
depends_on: ["50-01", "50-02", "50-03", "50-04"]
files_modified:
  - frontends/web-panel/angular.json
  - nginx/conf.d/default.conf
  - frontends/landing/dist/index.html
autonomous: true
requirements:
  - INFRA-v9-04
must_haves:
  truths:
    - "angular.json обе baseHref entry изменены с '/admin/' на '/' (architect.build.options и architect.build.configurations.production)"
    - "nginx/conf.d/default.conf — блок location /admin/ удалён, добавлен catch-all location / в конец HTTPS server блока"
    - "nginx/conf.d/default.conf — location = / { return 301 /login; } сохранён (Phase 49)"
    - "frontends/landing/dist/index.html:1330 — https://ruttrack.site/admin/ заменено на /login"
    - "Nginx конфиг синтаксически валиден (prefix-match правила соблюдены: все специфичные location выше catch-all)"
  artifacts:
    - path: "frontends/web-panel/angular.json"
      provides: "baseHref = '/' в build options и production configuration"
      contains: "\"baseHref\": \"/\""
    - path: "nginx/conf.d/default.conf"
      provides: "catch-all location / для web-panel + сохранённый /login 301"
    - path: "frontends/landing/dist/index.html"
      provides: "Футерный линк ведёт на /login, не на внешнюю /admin/ ссылку"
      contains: "href=\"/login\""
  key_links:
    - from: "nginx location / fallback"
      to: "rct-web-panel-nginx:80/"
      via: "proxy_pass"
      pattern: "proxy_pass http://rct-web-panel-nginx"
    - from: "angular.json baseHref"
      to: "Angular build <base href> tag в index.html"
      via: "Angular CLI @angular/build:application"
      pattern: "\"baseHref\": \"/\""
---

<objective>
Финальный infra flip: изменить `baseHref` в `angular.json` с `/admin/` на `/`, перестроить prod nginx (`nginx/conf.d/default.conf`) в соответствии с D-01/D-02 (убрать `/admin/` блок, добавить catch-all `/` в конце, сохранить exact-match `= /` для 301), заменить последнюю внешнюю ссылку `https://ruttrack.site/admin/` в footer'е landing page на `/login`.

Purpose: Это единственный план в Wave 4 — он собирает всё вместе, потому что angular.json и nginx MUST сменяться вместе (иначе образ с `<base href="/">` получит 404 на старом nginx с `location /admin/`). Landing fix — маленькая независимая задача, но тематически связана (external `/admin/` references) и удобно делать одним атомарным коммитом.
Output:
- `angular.json` оба вхождения `"baseHref": "/admin/"` → `"baseHref": "/"`
- `nginx/conf.d/default.conf` — блок `location /admin/ { ... }` удалён, блок `location / { proxy_pass http://rct-web-panel-nginx:80/; proxy_set_header Host $host; }` добавлен в конец HTTPS server блока (ПОСЛЕ `location = /`, но ПЕРЕД закрывающей `}`)
- `frontends/landing/dist/index.html:1330` — `<a href="https://ruttrack.site/admin/">Панель администратора</a>` → `<a href="/login">Войти</a>`
- Полный vitest suite остаётся green
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-basehref-migration-unified-login/50-CONTEXT.md
@.planning/phases/50-basehref-migration-unified-login/50-RESEARCH.md
@frontends/web-panel/angular.json
@nginx/conf.d/default.conf
@frontends/landing/dist/index.html

<interfaces>
<!-- Current angular.json baseHref locations (lines 44 and 62) -->
```json
"architect": {
  "build": {
    "options": {
      "outputPath": "dist",
      "index": "src/index.html",
      "browser": "src/main.ts",
      "baseHref": "/admin/",        // ← line 44, change to "/"
      ...
    },
    "configurations": {
      "production": {
        "baseHref": "/admin/",       // ← line 62, change to "/"
        ...
      }
    }
  }
}
```

<!-- Current nginx/conf.d/default.conf — lines 77-81 — block to DELETE -->
```nginx
    # --- Admin Web Panel (NET-02) ---
    location /admin/ {
        proxy_pass http://rct-web-panel-nginx:80/;
        proxy_set_header Host $host;
    }
```

<!-- nginx/conf.d/default.conf — lines 130-135 — block to PRESERVE (Phase 49) -->
```nginx
    # --- Root redirects to unified /login entry point (INFRA-v9-01) ---
    location = / {
        return 301 /login;
    }
```

<!-- nginx/conf.d/default.conf — block to ADD at end of HTTPS server block (AFTER line 135, BEFORE the closing }) -->
```nginx

    # --- Web Panel SPA — catch-all (INFRA-v9-04, Phase 50) ---
    # Must be last prefix block; more specific prefixes above win by nginx prefix-match precedence
    location / {
        proxy_pass http://rct-web-panel-nginx:80/;
        proxy_set_header Host $host;
    }
```

<!-- frontends/landing/dist/index.html:1330 — current -->
```html
        <a href="https://ruttrack.site/admin/">Панель администратора</a>
```

<!-- Replacement -->
```html
        <a href="/login">Войти</a>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Изменить angular.json baseHref и landing footer link + полный vitest + ng build sanity check</name>
  <files>frontends/web-panel/angular.json, frontends/landing/dist/index.html</files>
  <read_first>
    - frontends/web-panel/angular.json (полный файл — 107 строк, найти оба вхождения baseHref)
    - frontends/landing/dist/index.html строки 1320-1335 (footer блок)
    - frontends/web-panel/package.json (scripts.build = "ng build" — никакого --base-href override, значит angular.json единственный источник)
    - frontends/web-panel/proxy.conf.json (только /api proxy, никакого /admin/)
    - frontends/web-panel/src/app/core/auth/auth.interceptor.ts (только /api/auth/ endpoints — не трогать)
  </read_first>
  <action>
    **ШАГ 1 — angular.json изменения.**

    Открыть `frontends/web-panel/angular.json`.

    Строка 44 (внутри `architect.build.options`):
    ```json
          "baseHref": "/admin/",
    ```
    Заменить на:
    ```json
          "baseHref": "/",
    ```

    Строка 62 (внутри `architect.build.configurations.production`):
    ```json
            "baseHref": "/admin/",
    ```
    Заменить на:
    ```json
            "baseHref": "/",
    ```

    Оба вхождения должны быть заменены. НЕ добавлять baseHref в `serve` или `development` configuration — они наследуют из `build`.

    **ШАГ 2 — landing footer link.**

    Открыть `frontends/landing/dist/index.html`. Найти строку 1330:
    ```html
            <a href="https://ruttrack.site/admin/">Панель администратора</a>
    ```
    Заменить на:
    ```html
            <a href="/login">Войти</a>
    ```

    Обратить внимание: меняется и href, и text content. "Панель администратора" → "Войти" потому что /login — универсальная точка входа для всех 4 ролей, а не только ADMIN panel. По D-03/INFRA-v9-05 (Phase 49 аналогично заменял .t.me/ ссылки на /login).

    **ШАГ 3 — проверки что ничего не забыто.**

    Запустить проверку что никаких других внешних `/admin/` ссылок не осталось в frontends/landing и docs:
    ```bash
    grep -rn "ruttrack.site/admin" frontends/landing/ docs/ 2>/dev/null
    ```
    Ожидается: пустой вывод (ни одной совпадающей строки). Если найдутся новые — заменить аналогично.

    Внутренние Angular `routerLink="/admin/..."` в `frontends/web-panel/src/app/` оставить нетронутыми — они работают через SPA Router, не через внешние URL (per D-03).

    **ШАГ 4 — vitest full suite (должен оставаться green).**
    ```
    cd frontends/web-panel && npx vitest run --reporter=dot
    ```
    angular.json baseHref изменение не влияет на vitest (тесты не зависят от base href), поэтому suite должен остаться green. Exit code 0.

    **ШАГ 5 — ng build sanity check.**
    ```
    cd frontends/web-panel && npx ng build --configuration development
    ```
    Ожидается: build успешен. Проверить что в выходном `dist/browser/index.html` содержится `<base href="/">` а НЕ `<base href="/admin/">`:
    ```bash
    grep -c 'base href="/"' frontends/web-panel/dist/browser/index.html
    ```
    Должно вернуть ≥ 1. И:
    ```bash
    grep -c 'base href="/admin/"' frontends/web-panel/dist/browser/index.html
    ```
    Должно вернуть 0.
  </action>
  <verify>
    <automated>cd frontends/web-panel &amp;&amp; npx vitest run --reporter=dot &amp;&amp; npx ng build --configuration development &amp;&amp; grep -c 'base href="/"' dist/browser/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"baseHref": "/"' frontends/web-panel/angular.json` → 2 (build options + production configuration)
    - `grep -c '"baseHref": "/admin/"' frontends/web-panel/angular.json` → 0
    - `grep -c 'href="https://ruttrack.site/admin/"' frontends/landing/dist/index.html` → 0
    - `grep -c 'href="/login"' frontends/landing/dist/index.html` → ≥ 4 (3 из Phase 49 + 1 новый футерный линк)
    - Пустой результат от: `grep -rn "ruttrack.site/admin" frontends/landing/ docs/` (проверять только эти директории, не .planning/)
    - `cd frontends/web-panel && npx vitest run --reporter=dot` → exit 0, full suite green
    - `cd frontends/web-panel && npx ng build --configuration development` → exit 0 (build успешен)
    - `grep -c 'base href="/"' frontends/web-panel/dist/browser/index.html` → ≥ 1 (после build)
    - `grep -c 'base href="/admin/"' frontends/web-panel/dist/browser/index.html` → 0
  </acceptance_criteria>
  <done>
    angular.json baseHref мигрирован. landing footer link обновлён. ng build корректно генерирует `<base href="/">`. Никаких регрессов в тестах. Осталась одна задача — prod nginx.
  </done>
</task>

<task type="auto">
  <name>Task 2: Обновить prod nginx/conf.d/default.conf — удалить /admin/ блок, добавить catch-all /</name>
  <files>nginx/conf.d/default.conf</files>
  <read_first>
    - nginx/conf.d/default.conf (полный файл — 136 строк)
    - .planning/phases/50-basehref-migration-unified-login/50-CONTEXT.md §D-01 и §D-02
    - .planning/phases/50-basehref-migration-unified-login/50-RESEARCH.md §Q4 "Prod nginx" и §Pitfall 6 "catch-all должен быть последним"
    - .planning/phases/49-nginx-routing-landing-dead-link-fix/49-01-PLAN.md (контекст добавления location = / → 301)
  </read_first>
  <action>
    Открыть `nginx/conf.d/default.conf`.

    **ШАГ 1 — УДАЛИТЬ блок `location /admin/`.**

    Текущие строки 77-81:
    ```nginx
        # --- Admin Web Panel (NET-02) ---
        location /admin/ {
            proxy_pass http://rct-web-panel-nginx:80/;
            proxy_set_header Host $host;
        }
    ```

    Удалить эти 5 строк целиком (включая комментарий).

    **ШАГ 2 — ПРОВЕРИТЬ что `location = /` (строки 130-135) НЕ изменён.** Он должен остаться:
    ```nginx
        # --- Root redirects to unified /login entry point (INFRA-v9-01) ---
        # Note: /login itself will be resolved by the web-panel Angular SPA
        # after Phase 50 migrates baseHref to /. This plan only installs the redirect.
        location = / {
            return 301 /login;
        }
    ```

    **Обновить** комментарий (убрать "after Phase 50 migrates" формулировку, которая теперь неактуальна):
    ```nginx
        # --- Root redirects to unified /login entry point (INFRA-v9-01) ---
        # /login is resolved by the web-panel Angular SPA (baseHref: /) via the
        # catch-all location / block below. Exact match = / wins over prefix /.
        location = / {
            return 301 /login;
        }
    ```

    **ШАГ 3 — ДОБАВИТЬ catch-all `location /` в конец HTTPS server блока.**

    Найти закрывающую `}` HTTPS server блока (после блока `location = /`, примерно строка 136). Перед этой закрывающей `}` вставить:
    ```nginx

        # --- Web Panel SPA — catch-all (INFRA-v9-04, Phase 50) ---
        # MUST be the last prefix block; more specific prefixes (/api/, /app/,
        # /presentation/, /mini-app/, /swagger-ui*, /v3/api-docs, /openapi/)
        # above win by nginx prefix-match precedence. Exact-match `= /` above
        # still 301-redirects root to /login. This block serves the web-panel
        # SPA for /login, /admin/*, /teacher/*, /student/*, /headman/*.
        location / {
            proxy_pass http://rct-web-panel-nginx:80/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    ```

    Итоговый порядок location блоков в HTTPS server (сверху вниз):
    1. `/api/ws/` (WebSocket)
    2. `/api/` (API Gateway)
    3. `/presentation/` (landing)
    4. `/app/` (PWA)
    5. `/mini-app/` (Telegram Mini App)
    6. `/swagger-ui.html`, `/swagger-ui/`, `/v3/api-docs`, `/openapi/` (Swagger with basic auth)
    7. `= /` exact match → 301 /login
    8. `/` catch-all → web-panel (NEW)

    **НЕТ** `/admin/` блока — его удалили в Шаге 1.

    **ШАГ 4 — синтаксическая проверка.**

    Если доступен локально nginx или docker, проверить синтаксис:
    ```bash
    docker run --rm -v "$(pwd)/nginx/conf.d:/etc/nginx/conf.d:ro" nginx:alpine nginx -t 2>&1
    ```

    Ожидается: `configuration file /etc/nginx/nginx.conf test is successful` (или хотя бы не должно быть syntax error на `default.conf`). Если docker недоступен — пропустить этот шаг; финальная валидация произойдёт при deploy.

    Альтернатива без docker: проверить balanced braces:
    ```bash
    awk '/^server {/{s++} /^}/{s--} END{print s}' nginx/conf.d/default.conf
    ```
    Должно вернуть 0 (все блоки сбалансированы).

    **ШАГ 5 — vitest не должен регрессировать.**
    ```
    cd frontends/web-panel && npx vitest run --reporter=dot
    ```
    nginx изменения не влияют на vitest, но прогнать для safety.
  </action>
  <verify>
    <automated>cd frontends/web-panel &amp;&amp; npx vitest run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "location /admin/" nginx/conf.d/default.conf` → 0 (блок удалён)
    - `grep -c "Admin Web Panel" nginx/conf.d/default.conf` → 0 (комментарий тоже удалён)
    - `grep -c "location = /" nginx/conf.d/default.conf` → 1 (Phase 49 блок сохранён)
    - `grep -c "return 301 /login" nginx/conf.d/default.conf` → 1 (Phase 49 redirect сохранён)
    - `grep -c "Web Panel SPA — catch-all" nginx/conf.d/default.conf` → 1 (новый комментарий добавлен)
    - `grep -c "location / {" nginx/conf.d/default.conf` → 2 (1 в HTTP server для ACME + HTTPS redirect, 1 новый catch-all в HTTPS server)
    - `grep -c "proxy_pass http://rct-web-panel-nginx:80/" nginx/conf.d/default.conf` → 1 (был 1 в /admin/ блоке, теперь 1 в catch-all)
    - В файле порядок соблюдён: все специфичные `location /api/`, `/app/`, `/presentation/`, `/mini-app/`, `/swagger-*`, `/v3/api-docs`, `/openapi/` находятся ВЫШЕ нового `location /` catch-all (проверить через grep с -n и отсортировать line numbers)
    - `location = /` block (exact match) находится ВЫШЕ нового `location /` catch-all
    - `awk '/server \{/{s++} /^\}/{s--} END{print s}' nginx/conf.d/default.conf` → 0 (balanced braces)
    - `cd frontends/web-panel && npx vitest run --reporter=dot` → exit 0 (vitest не регрессирует)
  </acceptance_criteria>
  <done>
    Prod nginx обновлён согласно D-01/D-02: `/admin/` блок удалён, catch-all `/` добавлен в конец, exact-match `= /` → 301 сохранён. Порядок prefix-match соблюдён. Локальный vitest остаётся green. Физический prod rebuild/restart (docker compose pull + up -d) — MANUAL deployment step out of scope Plan, выполняется оператором после merge Phase 50.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External HTTPS → nginx reverse proxy | Первый hop — nginx решает, какой контейнер обслуживает запрос |
| nginx → backend services | Внутренний Docker network trust |
| landing page footer link | External-facing HTML, может быть scraped / bookmarked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-19 | Information Disclosure | nginx catch-all случайно отдаёт `/api/` на web-panel вместо gateway | mitigate | nginx prefix-match precedence: `location /api/` длиннее `location /`, побеждает. Acceptance criterion "все специфичные location выше catch-all" явно валидирует порядок. Pitfall 6 в Research задокументирован и учтён. |
| T-50-20 | Spoofing | Старые bookmark на `/admin/dashboard` → nginx отдаёт 404 | mitigate | SPA fallback (D-04): catch-all `location /` отдаёт web-panel на `/admin/*`, Angular router внутри матчит `AdminDashboardComponent`, authGuard → /login если unauth, иначе dashboard. Graceful degradation без 301 required. |
| T-50-21 | Information Disclosure | Landing footer показывает `/admin/` ссылку, раскрывая внутреннее routing | mitigate | Task 1 заменяет footer ссылку на `/login` + text "Войти". Grep acceptance criterion `ruttrack.site/admin` = 0 в `frontends/landing/` и `docs/`. |
| T-50-22 | Denial of Service | Malformed nginx config ломает весь reverse proxy на deploy | mitigate | Task 2 Step 4 запускает `nginx -t` через docker контейнер перед commit. `awk` balanced braces check как backup. Фиксированные строки изменений (без regex-replace). |
| T-50-23 | Tampering | Assets c старым base href `/admin/` кешированы в CDN | accept | После deploy пользователям нужен `Ctrl+F5`. На этапе развёртывания `docker compose up -d --force-recreate rct-web-panel-nginx` получит свежие assets с `<base href="/">`. Noted в Pitfall 1 — deployment runbook, не planning concern. |
</threat_model>

<verification>
Plan gate:
- `cd frontends/web-panel && npx vitest run` → exit 0
- `cd frontends/web-panel && npx ng build --configuration development` → exit 0
- Built `dist/browser/index.html` содержит `<base href="/">` (не `<base href="/admin/">`)
- `grep -c "location /admin/" nginx/conf.d/default.conf` → 0
- `grep -c "proxy_pass http://rct-web-panel-nginx:80/" nginx/conf.d/default.conf` → 1
- `grep -c "ruttrack.site/admin" frontends/landing/ docs/` recursive → 0
- Ручное (не-automated) тестирование в Plan 06 UAT:
  - После deploy: `curl -I https://ruttrack.site/` → 301 /login
  - `curl -I https://ruttrack.site/login` → 200 HTML
  - `curl -I https://ruttrack.site/api/health` → проходит на gateway (не web-panel)
  - `curl -I https://ruttrack.site/admin/dashboard` → 200 HTML (SPA fallback)
</verification>

<success_criteria>
- angular.json: оба baseHref = "/"
- landing/dist/index.html: никаких `/admin/` external links
- nginx/conf.d/default.conf: /admin/ блок удалён, catch-all / добавлен в конец HTTPS server, /login 301 сохранён
- ng build успешно генерирует `<base href="/">` в index.html
- Full vitest suite green
- Порядок nginx prefix-match соблюдён — catch-all последний
</success_criteria>

<output>
После завершения создать `.planning/phases/50-basehref-migration-unified-login/50-05-SUMMARY.md`
</output>
