---
phase: 33-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/url-layout.md
  - frontends/mini-app/nginx.conf
  - frontends/mini-app/dist/index.html
  - frontends/web-panel/nginx.conf
  - frontends/web-panel/dist/index.html
  - frontends/landing/nginx.conf
  - frontends/landing/dist/index.html
  - docker-compose.yml
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03]

must_haves:
  truths:
    - "URL layout document exists capturing port assignments for all four frontends with no routing conflicts"
    - "Three nginx configs exist following the established PWA pattern"
    - "docker compose up starts three new nginx containers and each serves its placeholder page"
  artifacts:
    - path: "docs/url-layout.md"
      provides: "URL layout decision document"
      contains: "Mini App"
    - path: "frontends/mini-app/nginx.conf"
      provides: "Mini App nginx config"
      contains: "try_files"
    - path: "frontends/web-panel/nginx.conf"
      provides: "Web Panel nginx config"
      contains: "try_files"
    - path: "frontends/landing/nginx.conf"
      provides: "Landing nginx config"
      contains: "try_files"
    - path: "frontends/mini-app/dist/index.html"
      provides: "Mini App placeholder"
      contains: "Mini App"
    - path: "frontends/web-panel/dist/index.html"
      provides: "Web Panel placeholder"
      contains: "Web Panel"
    - path: "frontends/landing/dist/index.html"
      provides: "Landing placeholder"
      contains: "Landing"
    - path: "docker-compose.yml"
      provides: "Three new nginx service blocks"
      contains: "mini-app-nginx"
  key_links:
    - from: "docker-compose.yml"
      to: "frontends/mini-app/nginx.conf"
      via: "volume mount"
      pattern: "./frontends/mini-app/nginx.conf:/etc/nginx/conf.d/default.conf"
    - from: "docker-compose.yml"
      to: "frontends/web-panel/nginx.conf"
      via: "volume mount"
      pattern: "./frontends/web-panel/nginx.conf:/etc/nginx/conf.d/default.conf"
    - from: "docker-compose.yml"
      to: "frontends/landing/nginx.conf"
      via: "volume mount"
      pattern: "./frontends/landing/nginx.conf:/etc/nginx/conf.d/default.conf"
---

<objective>
Create the URL layout decision document, scaffold three nginx configs (Mini App, Web Panel, Landing), add placeholder HTML files, and wire them into docker-compose.

Purpose: Phases 34-40 depend on knowing port assignments and having running nginx containers to deploy into.
Output: docs/url-layout.md, 3 nginx.conf files, 3 placeholder index.html files, updated docker-compose.yml with 3 new services.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@docker-compose.yml
@frontends/pwa/nginx.conf

<interfaces>
<!-- Existing PWA nginx pattern to replicate (from frontends/pwa/nginx.conf): -->
```nginx
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location ~* ^/(sw\.js|index\.html)$ {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
        expires 0;
    }

    location ~* \.(js|css|png|jpg|jpeg|svg|ico|woff|woff2)$ {
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

<!-- Existing pwa-nginx docker-compose block to replicate: -->
```yaml
pwa-nginx:
  image: nginx:1.27-alpine
  container_name: rct-pwa-nginx
  ports:
    - "80:80"
  volumes:
    - ./frontends/pwa/dist:/usr/share/nginx/html:ro
    - ./frontends/pwa/nginx.conf:/etc/nginx/conf.d/default.conf:ro
  networks:
    - private_net
  restart: unless-stopped
```

<!-- Port assignment table (decided per INFRA-01): -->
| Frontend    | Container port | Host port | Dev Vite port |
|-------------|---------------|-----------|---------------|
| PWA         | 80            | 80        | 5173          |
| Mini App    | 80            | 3000      | 5174          |
| Web Panel   | 80            | 4200      | 4200          |
| Landing     | 80            | 8081      | n/a (static)  |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create URL layout decision document and placeholder directories</name>
  <files>docs/url-layout.md, frontends/mini-app/dist/index.html, frontends/web-panel/dist/index.html, frontends/landing/dist/index.html</files>
  <read_first>
    - docker-compose.yml (see existing pwa-nginx port mapping on line 181)
    - frontends/pwa/nginx.conf (reference pattern)
    - docs/design-decisions.md (if exists, for document style consistency)
  </read_first>
  <action>
1. Create `docs/url-layout.md` with the following content:

```markdown
# URL Layout — RutCampusTrack Frontends

Decided: Phase 33 (v7.0 Infrastructure)

## Port Assignments

| Frontend    | Container port | Host port | Dev port | Technology    |
|-------------|---------------|-----------|----------|---------------|
| PWA         | 80            | 80        | 5173     | React + Vite  |
| Mini App    | 80            | 3000      | 5174     | React + Vite  |
| Web Panel   | 80            | 4200      | 4200     | Angular CLI   |
| Landing     | 80            | 8081      | n/a      | Static HTML   |

## Container Names

| Frontend    | Container name          | nginx config path                    |
|-------------|------------------------|--------------------------------------|
| PWA         | rct-pwa-nginx          | frontends/pwa/nginx.conf             |
| Mini App    | rct-mini-app-nginx     | frontends/mini-app/nginx.conf        |
| Web Panel   | rct-web-panel-nginx    | frontends/web-panel/nginx.conf       |
| Landing     | rct-landing-nginx      | frontends/landing/nginx.conf         |

## Gateway CORS Origins (dev)

- `http://localhost:5173` -- PWA dev (Vite)
- `http://localhost:80` -- PWA prod (pwa-nginx)
- `http://localhost:5174` -- Mini App dev (Vite)
- `http://localhost:3000` -- Mini App prod (mini-app-nginx)
- `http://localhost:4200` -- Web Panel dev/prod (Angular CLI / web-panel-nginx)

Landing does not make API calls -- no CORS origin needed.

## Notes

- All containers listen on port 80 internally; host port differs per container.
- Production domains TBD -- only localhost origins configured in Phase 33.
- Mini App Vite port (5174) is one above PWA (5173) to avoid conflict.
- Angular CLI defaults to 4200; web-panel-nginx uses the same port for consistency.
```

2. Create three placeholder index.html files:

`frontends/mini-app/dist/index.html`:
```html
<!DOCTYPE html>
<html lang="ru">
<head><meta charset="UTF-8"><title>Mini App — placeholder</title></head>
<body><h1>Mini App placeholder</h1><p>Phase 36 will replace this with the real build.</p></body>
</html>
```

`frontends/web-panel/dist/index.html`:
```html
<!DOCTYPE html>
<html lang="ru">
<head><meta charset="UTF-8"><title>Web Panel — placeholder</title></head>
<body><h1>Web Panel placeholder</h1><p>Phase 38 will replace this with the real build.</p></body>
</html>
```

`frontends/landing/dist/index.html`:
```html
<!DOCTYPE html>
<html lang="ru">
<head><meta charset="UTF-8"><title>Landing — placeholder</title></head>
<body><h1>Landing placeholder</h1><p>Phase 35 will replace this with the real build.</p></body>
</html>
```
  </action>
  <verify>
    <automated>test -f docs/url-layout.md && test -f frontends/mini-app/dist/index.html && test -f frontends/web-panel/dist/index.html && test -f frontends/landing/dist/index.html && echo "ALL_FILES_EXIST"</automated>
  </verify>
  <acceptance_criteria>
    - docs/url-layout.md contains "Mini App" and "3000" and "5174" and "4200" and "8081"
    - docs/url-layout.md contains "rct-mini-app-nginx" and "rct-web-panel-nginx" and "rct-landing-nginx"
    - frontends/mini-app/dist/index.html contains "Mini App placeholder"
    - frontends/web-panel/dist/index.html contains "Web Panel placeholder"
    - frontends/landing/dist/index.html contains "Landing placeholder"
  </acceptance_criteria>
  <done>URL layout document exists with port assignments for all 4 frontends; three placeholder HTML files exist in their dist/ directories</done>
</task>

<task type="auto">
  <name>Task 2: Create nginx configs and add three containers to docker-compose</name>
  <files>frontends/mini-app/nginx.conf, frontends/web-panel/nginx.conf, frontends/landing/nginx.conf, docker-compose.yml</files>
  <read_first>
    - frontends/pwa/nginx.conf (lines 1-22 -- the template to replicate)
    - docker-compose.yml (lines 177-189 -- existing pwa-nginx block to replicate)
  </read_first>
  <action>
1. Create `frontends/mini-app/nginx.conf` (React SPA -- same as PWA but without sw.js special caching since Mini App has no Service Worker):

```nginx
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location ~* \.(js|css|png|jpg|jpeg|svg|ico|woff|woff2)$ {
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

2. Create `frontends/web-panel/nginx.conf` (Angular SPA -- same pattern, adds eot/ttf for Angular font assets):

```nginx
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location ~* \.(js|css|png|jpg|jpeg|svg|ico|woff|woff2|eot|ttf)$ {
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

3. Create `frontends/landing/nginx.conf` (static HTML -- no SPA fallback, returns 404 for missing files):

```nginx
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location ~* \.(js|css|png|jpg|jpeg|svg|ico|woff|woff2)$ {
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
```

4. Edit `docker-compose.yml` -- add three new service blocks AFTER the existing `pwa-nginx` block (before the `networks:` section at line 190). Add a section comment before the new blocks:

```yaml
  # ============================================================
  # MINI APP
  # ============================================================

  mini-app-nginx:
    image: nginx:1.27-alpine
    container_name: rct-mini-app-nginx
    ports:
      - "3000:80"
    volumes:
      - ./frontends/mini-app/dist:/usr/share/nginx/html:ro
      - ./frontends/mini-app/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - private_net
    restart: unless-stopped

  # ============================================================
  # WEB PANEL
  # ============================================================

  web-panel-nginx:
    image: nginx:1.27-alpine
    container_name: rct-web-panel-nginx
    ports:
      - "4200:80"
    volumes:
      - ./frontends/web-panel/dist:/usr/share/nginx/html:ro
      - ./frontends/web-panel/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - private_net
    restart: unless-stopped

  # ============================================================
  # LANDING
  # ============================================================

  landing-nginx:
    image: nginx:1.27-alpine
    container_name: rct-landing-nginx
    ports:
      - "8081:80"
    volumes:
      - ./frontends/landing/dist:/usr/share/nginx/html:ro
      - ./frontends/landing/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - private_net
    restart: unless-stopped
```

IMPORTANT: Do NOT change the notification-bot HEALTH_PORT which also uses 8081 -- that is an internal container port (not exposed to host). The landing-nginx uses host port 8081 mapped to container port 80. There is no conflict because notification-bot's 8081 is only on `expose` (internal network), not `ports` (host mapping).
  </action>
  <verify>
    <automated>grep -q "mini-app-nginx" docker-compose.yml && grep -q "web-panel-nginx" docker-compose.yml && grep -q "landing-nginx" docker-compose.yml && grep -q "3000:80" docker-compose.yml && grep -q "4200:80" docker-compose.yml && grep -q "8081:80" docker-compose.yml && test -f frontends/mini-app/nginx.conf && test -f frontends/web-panel/nginx.conf && test -f frontends/landing/nginx.conf && echo "ALL_CONFIGS_OK"</automated>
  </verify>
  <acceptance_criteria>
    - frontends/mini-app/nginx.conf contains "try_files $uri $uri/ /index.html"
    - frontends/web-panel/nginx.conf contains "try_files $uri $uri/ /index.html"
    - frontends/landing/nginx.conf contains "try_files $uri $uri/ =404"
    - docker-compose.yml contains "mini-app-nginx:" with "container_name: rct-mini-app-nginx"
    - docker-compose.yml contains "web-panel-nginx:" with "container_name: rct-web-panel-nginx"
    - docker-compose.yml contains "landing-nginx:" with "container_name: rct-landing-nginx"
    - docker-compose.yml contains "3000:80" and "4200:80" and "8081:80"
    - docker-compose.yml contains "nginx:1.27-alpine" for all three new services
    - docker-compose.yml contains "restart: unless-stopped" for all three new services
    - docker-compose.yml contains "private_net" in networks for all three new services
    - All three volume mounts reference the correct nginx.conf paths
  </acceptance_criteria>
  <done>Three nginx configs created (SPA fallback for Mini App and Web Panel, static 404 for Landing). docker-compose.yml has three new container definitions with correct port mappings, volume mounts, network, and restart policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> nginx | Static file serving; no dynamic content, no user input processing |
| nginx -> filesystem | Read-only volume mounts; nginx serves files from dist/ only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | nginx configs | accept | nginx serves read-only mounted files; no write paths; configs are version-controlled |
| T-33-02 | Information Disclosure | placeholder HTML | accept | Placeholder files contain no sensitive data; replaced by real builds in later phases |
| T-33-03 | Denial of Service | port conflicts | accept | Low risk in dev; ports 3000/4200/8081 are conventional and unlikely to conflict |
</threat_model>

<verification>
1. `test -f docs/url-layout.md` -- URL layout document exists
2. `grep -c "nginx:1.27-alpine" docker-compose.yml` returns 4 (1 existing + 3 new)
3. `docker compose config --services | sort` includes mini-app-nginx, web-panel-nginx, landing-nginx
4. After `docker compose up -d mini-app-nginx web-panel-nginx landing-nginx`:
   - `curl -s http://localhost:3000/` contains "Mini App placeholder"
   - `curl -s http://localhost:4200/` contains "Web Panel placeholder"
   - `curl -s http://localhost:8081/` contains "Landing placeholder"
</verification>

<success_criteria>
- URL layout document captures all 4 frontend port assignments with no conflicts
- Three nginx configs exist matching the established PWA pattern
- docker-compose.yml defines three new services that start successfully
- Each placeholder page is accessible on its assigned host port
</success_criteria>

<output>
After completion, create `.planning/phases/33-infrastructure/33-01-SUMMARY.md`
</output>
