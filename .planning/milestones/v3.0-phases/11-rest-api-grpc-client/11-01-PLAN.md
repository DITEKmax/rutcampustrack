---
phase: 11-rest-api-grpc-client
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/schedule-service/schedule-app/build.gradle.kts
  - services/schedule-service/schedule-app/src/main/resources/application.yml
  - services/schedule-service/schedule-app/src/test/resources/application-test.yml
  - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/grpc/AcademicGrpcClient.java
  - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/exception/AcademicServiceUnavailableException.java
  - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/exception/InvalidLessonStateException.java
  - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/exception/ResourceNotFoundException.java
  - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/exception/GlobalExceptionHandler.java
  - services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/api/ScheduleItemApi.java
  - services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/api/LessonApi.java
  - services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/item/CreateScheduleItemRequest.java
  - services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/item/UpdateScheduleItemRequest.java
  - services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/item/ScheduleItemResponse.java
  - services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/lesson/CancelLessonRequest.java
  - services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/lesson/MassCancelRequest.java
  - services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/lesson/MassCancelResponse.java
  - services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/lesson/GeoBlockRequest.java
  - services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/lesson/LessonResponse.java
  - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/item/repository/ScheduleItemRepository.java
  - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/repository/LessonRepository.java
autonomous: true
requirements:
  - TMPL-05
must_haves:
  truths:
    - "Proto compilation generates AcademicGrpcServiceGrpc stubs from academic.proto"
    - "AcademicGrpcClient wraps GetGroup, GetActiveSemester, IsHeadman with 3s deadline"
    - "gRPC failure maps to HTTP 503 via GlobalExceptionHandler"
    - "All contract DTOs and API interfaces compile and are available for downstream plans"
  artifacts:
    - path: "services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/grpc/AcademicGrpcClient.java"
      provides: "gRPC client wrapper for Academic Service"
      contains: "withDeadlineAfter"
    - path: "services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/api/ScheduleItemApi.java"
      provides: "Template CRUD contract interface"
      contains: "interface ScheduleItemApi"
    - path: "services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/api/LessonApi.java"
      provides: "Lesson operations + view contract interface"
      contains: "interface LessonApi"
  key_links:
    - from: "build.gradle.kts"
      to: "proto/academic.proto"
      via: "protobuf plugin sourceSets.main.proto"
      pattern: "srcDir.*proto"
    - from: "AcademicGrpcClient.java"
      to: "AcademicGrpcServiceGrpc"
      via: "@GrpcClient annotation"
      pattern: "@GrpcClient"
    - from: "GlobalExceptionHandler.java"
      to: "AcademicServiceUnavailableException"
      via: "@ExceptionHandler mapping to 503"
      pattern: "AcademicServiceUnavailableException"
---

<objective>
Wire gRPC client infrastructure, define all contract interfaces and DTOs, add exception handlers, and extend repositories for Phase 11.

Purpose: Establishes the foundation layer that Plan 02 (ScheduleItem CRUD) and Plan 03 (Lesson ops + View) build on. Without compiled proto stubs, the AcademicGrpcClient wrapper, and the contract interfaces, no controller or service can be implemented.

Output: Compilable schedule-service with gRPC client stubs, all DTOs, API interfaces, exception infrastructure, and extended repositories. No controllers or services yet.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-rest-api-grpc-client/11-CONTEXT.md
@.planning/phases/11-rest-api-grpc-client/11-RESEARCH.md

<interfaces>
<!-- Existing contracts and patterns the executor needs -->

From services/schedule-service/schedule-api-contract enums:
```java
// LessonStatus.java
package ru.rutcampustrack.schedule.contract.enums;
public enum LessonStatus { PLANNED, ACTIVE, CLOSED, CANCELLED; }

// WeekType.java
package ru.rutcampustrack.schedule.contract.enums;
public enum WeekType { ALL, ODD, EVEN; }

// UserRole.java
package ru.rutcampustrack.schedule.contract.enums;
public enum UserRole { ADMIN, TEACHER, STUDENT }
```

From services/schedule-service/schedule-app entities:
```java
// ScheduleItem fields: id(Long), groupId(Long), subjectId(Long), teacherId(Long),
//   semesterId(Long), dayOfWeek(Short), lessonNumber(Short), startTime(LocalTime),
//   endTime(LocalTime), weekType(WeekType), room(String), isActive(boolean), createdAt(OffsetDateTime)

// Lesson fields: id(Long), scheduleItemId(Long), date(LocalDate), status(LessonStatus),
//   isGeoBlocked(boolean), cancelReason(String), createdAt(OffsetDateTime), closedAt(OffsetDateTime)
```

From services/schedule-service/schedule-app/exception:
```java
// GlobalExceptionHandler.java — currently handles: AccessDeniedException->403, Exception->500
// PROBLEM_BASE = "https://api.rutcampustrack.ru/problems/"
```

From services/schedule-service/schedule-api-contract/exception:
```java
// ErrorResponse record with: status, type, title, detail, instance, timestamp, fieldErrors
// ErrorResponse.FieldError record with: field, rejectedValue, message
```

From proto/academic.proto (RPCs needed as client per D-05):
```protobuf
service AcademicGrpcService {
  rpc GetGroup (GroupRequest) returns (GroupResponse);
  rpc IsHeadman (HeadmanCheckRequest) returns (HeadmanCheckResponse);
  rpc GetActiveSemester (Empty) returns (SemesterResponse);
}
// GroupRequest { int64 group_id = 1; }
// GroupResponse { int64 id = 1; string name = 2; string code = 3; bool is_active = 4; }
// HeadmanCheckRequest { int64 user_id = 1; int64 group_id = 2; }
// HeadmanCheckResponse { bool is_headman = 1; }
// SemesterResponse { int64 id = 1; string name = 2; string date_from = 3; string date_to = 4; }
```

From academic-app/build.gradle.kts (protobuf plugin pattern to copy):
```kotlin
plugins { id("com.google.protobuf") version "0.9.4" }
// implementation("net.devh:grpc-server-spring-boot-starter:3.1.0.RELEASE")
// compileOnly("javax.annotation:javax.annotation-api:1.3.2")
sourceSets { main { proto { srcDir(rootProject.file("proto")) } } }
protobuf {
    protoc { artifact = "com.google.protobuf:protoc:3.25.3" }
    plugins { create("grpc") { artifact = "io.grpc:protoc-gen-grpc-java:1.63.0" } }
    generateProtoTasks { ofSourceSet("main").forEach { it.plugins { create("grpc") { } } } }
}
```

From services/academic-service/academic-api-contract SubjectApi pattern:
```java
@Tag(name = "Subjects", description = "...")
@RequestMapping("/academic/subjects")
public interface SubjectApi {
    @PostMapping ResponseEntity<EntityModel<SubjectResponse>> createSubject(@Valid @RequestBody ...);
    @GetMapping("/{id}") ResponseEntity<EntityModel<SubjectResponse>> getSubject(@PathVariable Long id);
    @GetMapping ResponseEntity<PagedModel<EntityModel<SubjectResponse>>> listSubjects(Pageable, PagedResourcesAssembler<SubjectResponse>);
    @PutMapping("/{id}") ResponseEntity<EntityModel<SubjectResponse>> updateSubject(@PathVariable Long id, @Valid @RequestBody ...);
    @DeleteMapping("/{id}") ResponseEntity<Void> deleteSubject(@PathVariable Long id);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gradle protobuf plugin + gRPC client dependencies + application.yml config</name>
  <files>
    services/schedule-service/schedule-app/build.gradle.kts,
    services/schedule-service/schedule-app/src/main/resources/application.yml,
    services/schedule-service/schedule-app/src/test/resources/application-test.yml
  </files>
  <read_first>
    services/schedule-service/schedule-app/build.gradle.kts,
    services/academic-service/academic-app/build.gradle.kts,
    services/schedule-service/schedule-app/src/main/resources/application.yml,
    services/schedule-service/schedule-app/src/test/resources/application-test.yml,
    proto/academic.proto
  </read_first>
  <action>
**build.gradle.kts** — Add protobuf plugin and gRPC client dependencies (per D-01):

1. Add plugin `id("com.google.protobuf") version "0.9.4"` to plugins block (after existing plugins).

2. Add dependencies (after existing implementation lines):
   ```
   implementation("net.devh:grpc-client-spring-boot-starter:3.1.0.RELEASE")
   compileOnly("javax.annotation:javax.annotation-api:1.3.2")
   ```

3. Add sourceSets block (after dependencies block):
   ```kotlin
   sourceSets {
       main {
           proto {
               srcDir(rootProject.file("proto"))
           }
       }
   }
   ```

4. Add protobuf block (after sourceSets, copy verbatim from academic-app):
   ```kotlin
   protobuf {
       protoc {
           artifact = "com.google.protobuf:protoc:3.25.3"
       }
       plugins {
           create("grpc") {
               artifact = "io.grpc:protoc-gen-grpc-java:1.63.0"
           }
       }
       generateProtoTasks {
           ofSourceSet("main").forEach {
               it.plugins {
                   create("grpc") { }
               }
           }
       }
   }
   ```

**application.yml** — Add gRPC client config block (per D-02, after existing `grpc.server.port: 19092`):
```yaml
grpc:
  server:
    port: 19092
  client:
    academic-service:
      address: static://academic-service:19091
      negotiation-type: plaintext
```
Note: Replace the existing `grpc:` section entirely to add the `client` sub-block alongside the existing `server` block.

Also add pagination defaults:
```yaml
spring:
  data:
    web:
      pageable:
        default-page-size: 50
        max-page-size: 200
```

**application-test.yml** — Add gRPC client config to prevent context load failure (Pitfall 2 from research):
```yaml
grpc:
  server:
    port: -1
  client:
    academic-service:
      address: static://localhost:19091
      negotiation-type: plaintext
```
Replace existing `grpc:` section with the full block above.
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && cmd.exe /c "set JAVA_HOME=C:\Users\maksd\.jdks\ms-21.0.9&& .\gradlew.bat :services:schedule-service:schedule-app:compileJava --no-daemon -x javadoc 2>&1 | tail -5"</automated>
  </verify>
  <acceptance_criteria>
    - build.gradle.kts contains `id("com.google.protobuf") version "0.9.4"`
    - build.gradle.kts contains `implementation("net.devh:grpc-client-spring-boot-starter:3.1.0.RELEASE")`
    - build.gradle.kts contains `compileOnly("javax.annotation:javax.annotation-api:1.3.2")`
    - build.gradle.kts contains `srcDir(rootProject.file("proto"))`
    - build.gradle.kts contains `artifact = "com.google.protobuf:protoc:3.25.3"`
    - build.gradle.kts contains `artifact = "io.grpc:protoc-gen-grpc-java:1.63.0"`
    - application.yml contains `address: static://academic-service:19091`
    - application.yml contains `negotiation-type: plaintext`
    - application.yml contains `max-page-size: 200`
    - application-test.yml contains `address: static://localhost:19091`
    - compileJava task succeeds (proto stubs generated from academic.proto)
  </acceptance_criteria>
  <done>Protobuf plugin compiles academic.proto into Java stubs. gRPC client starter and javax.annotation on classpath. application.yml has grpc.client.academic-service config. application-test.yml has matching test config. Gradle compileJava passes.</done>
</task>

<task type="auto">
  <name>Task 2: AcademicGrpcClient + exceptions + GlobalExceptionHandler + contract DTOs/APIs + repository extensions</name>
  <files>
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/grpc/AcademicGrpcClient.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/exception/AcademicServiceUnavailableException.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/exception/InvalidLessonStateException.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/exception/ResourceNotFoundException.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/exception/GlobalExceptionHandler.java,
    services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/api/ScheduleItemApi.java,
    services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/api/LessonApi.java,
    services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/item/CreateScheduleItemRequest.java,
    services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/item/UpdateScheduleItemRequest.java,
    services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/item/ScheduleItemResponse.java,
    services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/lesson/CancelLessonRequest.java,
    services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/lesson/MassCancelRequest.java,
    services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/lesson/MassCancelResponse.java,
    services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/lesson/GeoBlockRequest.java,
    services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/lesson/LessonResponse.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/item/repository/ScheduleItemRepository.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/repository/LessonRepository.java
  </files>
  <read_first>
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/exception/GlobalExceptionHandler.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/exception/AccessDeniedException.java,
    services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/exception/ErrorResponse.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/item/repository/ScheduleItemRepository.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/repository/LessonRepository.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/item/entity/ScheduleItem.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/entity/Lesson.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/security/RequestContext.java,
    services/academic-service/academic-api-contract/src/main/java/ru/rutcampustrack/academic/contract/api/SubjectApi.java,
    services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/enums/LessonStatus.java,
    services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/enums/WeekType.java
  </read_first>
  <action>
**Part A: AcademicGrpcClient** (per D-02)

Create `services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/grpc/AcademicGrpcClient.java`:
- Package: `ru.rutcampustrack.schedule.grpc`
- Annotate `@Component`
- Field: `@GrpcClient("academic-service") private AcademicGrpcServiceGrpc.AcademicGrpcServiceBlockingStub stub;`
- Import generated stubs from `ru.rutcampustrack.academic.grpc.*`
- Three methods:
  1. `public GroupResponse validateGroup(Long groupId)` — calls `stub.withDeadlineAfter(3, TimeUnit.SECONDS).getGroup(GroupRequest.newBuilder().setGroupId(groupId).build())`. If `response.getIsActive() == false`, throw `ResourceNotFoundException("Group", "id", groupId)`. On `StatusRuntimeException`, throw `AcademicServiceUnavailableException("Academic Service unavailable: " + e.getStatus())`. Per D-03 and D-06.
  2. `public SemesterResponse getActiveSemester()` — calls `stub.withDeadlineAfter(3, TimeUnit.SECONDS).getActiveSemester(Empty.getDefaultInstance())`. On `StatusRuntimeException` with `NOT_FOUND` status, throw `ResourceNotFoundException("Semester", "status", "active")`. On other `StatusRuntimeException`, throw `AcademicServiceUnavailableException(...)`.
  3. `public boolean isHeadman(Long userId, Long groupId)` — calls `stub.withDeadlineAfter(3, TimeUnit.SECONDS).isHeadman(HeadmanCheckRequest.newBuilder().setUserId(userId).setGroupId(groupId).build())`. Returns `response.getIsHeadman()`. On `StatusRuntimeException`, throw `AcademicServiceUnavailableException(...)`.

**Part B: Exception classes** (in `ru.rutcampustrack.schedule.exception` package)

1. `AcademicServiceUnavailableException extends RuntimeException` — constructor `(String message)` calling `super(message)`. Per D-03.
2. `InvalidLessonStateException extends RuntimeException` — constructor `(String message)` calling `super(message)`. For Pitfall 7.
3. `ResourceNotFoundException extends RuntimeException` — constructor `(String entityName, String fieldName, Object fieldValue)` calling `super(entityName + " not found by " + fieldName + ": " + fieldValue)`. Also store fields for error response construction. Add getters for `entityName`, `fieldName`, `fieldValue`.

**Part C: GlobalExceptionHandler updates**

Add these `@ExceptionHandler` methods to existing `GlobalExceptionHandler` (keep existing `AccessDeniedException` and `Exception` handlers):

1. `@ExceptionHandler(ResourceNotFoundException.class)` -> HTTP 404, type `PROBLEM_BASE + "not-found"`, title "Resource not found"
2. `@ExceptionHandler(AcademicServiceUnavailableException.class)` -> HTTP 503, type `PROBLEM_BASE + "service-unavailable"`, title "Academic Service unavailable". Per D-03.
3. `@ExceptionHandler(InvalidLessonStateException.class)` -> HTTP 422, type `PROBLEM_BASE + "invalid-lesson-state"`, title "Invalid lesson state transition". Per Pitfall 7.
4. `@ExceptionHandler(DataIntegrityViolationException.class)` (import `org.springframework.dao.DataIntegrityViolationException`) -> HTTP 409, type `PROBLEM_BASE + "conflict"`, title "Data conflict". Per Pitfall 6.
5. `@ExceptionHandler(MethodArgumentNotValidException.class)` (import `org.springframework.web.bind.MethodArgumentNotValidException`) -> HTTP 400, type `PROBLEM_BASE + "validation-error"`, title "Validation failed". Extract field errors from `ex.getBindingResult().getFieldErrors()` and map to `ErrorResponse.FieldError(fe.getField(), fe.getRejectedValue(), fe.getDefaultMessage())`.

IMPORTANT: Place the specific handlers BEFORE the catch-all `@ExceptionHandler(Exception.class)`.

**Part D: Contract DTOs** (in `schedule-api-contract`, NO Lombok per CLAUDE.md)

All in package `ru.rutcampustrack.schedule.contract.dto.item`:

1. `CreateScheduleItemRequest` — Java `record`:
   ```java
   public record CreateScheduleItemRequest(
       @NotNull Long groupId,
       @NotNull Long subjectId,
       @NotNull Long teacherId,
       @NotNull Long semesterId,
       @NotNull @Min(1) @Max(7) Short dayOfWeek,
       @NotNull @Min(1) @Max(8) Short lessonNumber,
       @NotNull LocalTime startTime,
       @NotNull LocalTime endTime,
       @NotNull WeekType weekType,
       @Size(max = 64) String room
   ) {}
   ```

2. `UpdateScheduleItemRequest` — Java `record` (per D-09, NO groupId/semesterId):
   ```java
   public record UpdateScheduleItemRequest(
       @NotNull Long subjectId,
       @NotNull Long teacherId,
       @NotNull @Min(1) @Max(7) Short dayOfWeek,
       @NotNull @Min(1) @Max(8) Short lessonNumber,
       @NotNull LocalTime startTime,
       @NotNull LocalTime endTime,
       @NotNull WeekType weekType,
       @Size(max = 64) String room
   ) {}
   ```

3. `ScheduleItemResponse` — plain class extending `RepresentationModel<ScheduleItemResponse>`:
   Fields: `Long id`, `Long groupId`, `Long subjectId`, `Long teacherId`, `Long semesterId`, `Short dayOfWeek`, `Short lessonNumber`, `LocalTime startTime`, `LocalTime endTime`, `WeekType weekType`, `String room`, `boolean active`, `OffsetDateTime createdAt`.
   All-args constructor + getters. NO Lombok.

All in package `ru.rutcampustrack.schedule.contract.dto.lesson`:

4. `CancelLessonRequest` — `public record CancelLessonRequest(@NotBlank @Size(max = 512) String reason) {}` Per D-13.
5. `MassCancelRequest` — `public record MassCancelRequest(@NotNull Long groupId, @NotNull LocalDate dateFrom, @NotNull LocalDate dateTo, @NotBlank @Size(max = 512) String reason) {}` Per D-12, D-13.
6. `MassCancelResponse` — `public record MassCancelResponse(int cancelledCount) {}`
7. `GeoBlockRequest` — `public record GeoBlockRequest(@NotNull Boolean blocked) {}` Per D-15.
8. `LessonResponse` — plain class extending `RepresentationModel<LessonResponse>`:
   Fields (from BOTH Lesson + ScheduleItem per VIEW-02, D-16): `Long id`, `Long scheduleItemId`, `Long groupId`, `Long subjectId`, `Long teacherId`, `LocalDate date`, `LessonStatus status`, `Short dayOfWeek`, `Short lessonNumber`, `LocalTime startTime`, `LocalTime endTime`, `WeekType weekType`, `String room`, `boolean geoBlocked`, `String cancelReason`, `OffsetDateTime createdAt`.
   All-args constructor + getters. NO Lombok.

**Part E: Contract API interfaces** (in `schedule-api-contract`, HTTP mappings ONLY here per CLAUDE.md contract-first rule)

1. `ScheduleItemApi` (package `ru.rutcampustrack.schedule.contract.api`):
   - `@Tag(name = "Schedule Items", description = "Управление шаблонами расписания")`
   - `@RequestMapping("/schedule/items")`
   - Methods with full `@Operation` + `@ApiResponses`:
     - `@PostMapping` `createScheduleItem(@Valid @RequestBody CreateScheduleItemRequest)` -> `ResponseEntity<EntityModel<ScheduleItemResponse>>` (201, 400, 403, 503)
     - `@GetMapping("/{id}")` `getScheduleItem(@PathVariable Long id)` -> `ResponseEntity<EntityModel<ScheduleItemResponse>>` (200, 404)
     - `@GetMapping` `listScheduleItems(@RequestParam Long groupId, @RequestParam Long semesterId, Pageable pageable, PagedResourcesAssembler<ScheduleItemResponse> assembler)` -> `ResponseEntity<PagedModel<EntityModel<ScheduleItemResponse>>>` (200)
     - `@PutMapping("/{id}")` `updateScheduleItem(@PathVariable Long id, @Valid @RequestBody UpdateScheduleItemRequest)` -> `ResponseEntity<EntityModel<ScheduleItemResponse>>` (200, 400, 403, 404, 409, 503)
     - `@DeleteMapping("/{id}")` `deleteScheduleItem(@PathVariable Long id)` -> `ResponseEntity<Void>` (204, 403, 404)

2. `LessonApi` (package `ru.rutcampustrack.schedule.contract.api`):
   - `@Tag(name = "Lessons", description = "Управление уроками и расписание")`
   - `@RequestMapping("/schedule")`
   - Methods:
     - `@PatchMapping("/lessons/{id}/cancel")` `cancelLesson(@PathVariable Long id, @Valid @RequestBody CancelLessonRequest)` -> `ResponseEntity<EntityModel<LessonResponse>>` (200, 400, 403, 404, 422)
     - `@PatchMapping("/lessons/{id}/restore")` `restoreLesson(@PathVariable Long id)` -> `ResponseEntity<EntityModel<LessonResponse>>` (200, 403, 404, 422)
     - `@PostMapping("/lessons/mass-cancel")` `massCancelLessons(@Valid @RequestBody MassCancelRequest)` -> `ResponseEntity<MassCancelResponse>` (200, 400, 403, 503)
     - `@PatchMapping("/lessons/{id}/geo-block")` `toggleGeoBlock(@PathVariable Long id, @Valid @RequestBody GeoBlockRequest)` -> `ResponseEntity<EntityModel<LessonResponse>>` (200, 403, 404)
     - `@GetMapping("/groups/{groupId}/lessons")` `getLessons(@PathVariable Long groupId, @RequestParam @DateTimeFormat(iso = DateTimeFormat.ISO.DATE) LocalDate dateFrom, @RequestParam @DateTimeFormat(iso = DateTimeFormat.ISO.DATE) LocalDate dateTo, @RequestParam(required = false) List<LessonStatus> status, Pageable pageable, PagedResourcesAssembler<LessonResponse> assembler)` -> `ResponseEntity<PagedModel<EntityModel<LessonResponse>>>` (200) Per D-17, D-18.

**Part F: Repository extensions**

1. `ScheduleItemRepository` — add method: `List<ScheduleItem> findByGroupId(Long groupId);` (for VIEW endpoint, includes inactive templates per Pitfall 3).

2. `LessonRepository` — add method: `List<Lesson> findByScheduleItemIdInAndDateBetweenAndStatusIn(List<Long> scheduleItemIds, LocalDate from, LocalDate to, List<LessonStatus> statuses);` (for VIEW and mass-cancel queries).
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && cmd.exe /c "set JAVA_HOME=C:\Users\maksd\.jdks\ms-21.0.9&& .\gradlew.bat :services:schedule-service:schedule-api-contract:compileJava :services:schedule-service:schedule-app:compileJava --no-daemon -x javadoc 2>&1 | tail -5"</automated>
  </verify>
  <acceptance_criteria>
    - AcademicGrpcClient.java exists and contains `@GrpcClient("academic-service")`
    - AcademicGrpcClient.java contains `withDeadlineAfter(3, TimeUnit.SECONDS)` in all three methods
    - AcademicServiceUnavailableException.java exists in exception package
    - InvalidLessonStateException.java exists in exception package
    - ResourceNotFoundException.java exists in exception package with entityName/fieldName/fieldValue fields
    - GlobalExceptionHandler.java contains `@ExceptionHandler(AcademicServiceUnavailableException.class)` mapping to 503
    - GlobalExceptionHandler.java contains `@ExceptionHandler(ResourceNotFoundException.class)` mapping to 404
    - GlobalExceptionHandler.java contains `@ExceptionHandler(InvalidLessonStateException.class)` mapping to 422
    - GlobalExceptionHandler.java contains `@ExceptionHandler(DataIntegrityViolationException.class)` mapping to 409
    - GlobalExceptionHandler.java contains `@ExceptionHandler(MethodArgumentNotValidException.class)` mapping to 400
    - ScheduleItemApi.java contains `interface ScheduleItemApi` with `@RequestMapping("/schedule/items")`
    - ScheduleItemApi.java contains `@PostMapping`, `@GetMapping("/{id}")`, `@GetMapping`, `@PutMapping("/{id}")`, `@DeleteMapping("/{id}")`
    - LessonApi.java contains `interface LessonApi` with `@RequestMapping("/schedule")`
    - LessonApi.java contains `@PatchMapping("/lessons/{id}/cancel")`, `@PatchMapping("/lessons/{id}/restore")`, `@PostMapping("/lessons/mass-cancel")`, `@PatchMapping("/lessons/{id}/geo-block")`, `@GetMapping("/groups/{groupId}/lessons")`
    - CreateScheduleItemRequest.java is a record with @NotNull on groupId, subjectId, teacherId, semesterId
    - UpdateScheduleItemRequest.java is a record WITHOUT groupId and semesterId fields (per D-09)
    - LessonResponse.java extends RepresentationModel and has fields from BOTH Lesson and ScheduleItem (groupId, subjectId, teacherId, room, lessonNumber, startTime, endTime)
    - CancelLessonRequest.java has @NotBlank on reason field
    - MassCancelRequest.java has groupId, dateFrom, dateTo, reason fields
    - ScheduleItemRepository.java contains `findByGroupId(Long groupId)`
    - LessonRepository.java contains `findByScheduleItemIdInAndDateBetweenAndStatusIn`
    - Both contract and app modules compile successfully
  </acceptance_criteria>
  <done>All contract interfaces, DTOs, AcademicGrpcClient, exceptions, GlobalExceptionHandler extensions, and repository methods compile. Proto stubs are generated. Plan 02 and Plan 03 can implement controllers and services against these contracts.</done>
</task>

</tasks>

<verification>
- `gradlew :services:schedule-service:schedule-api-contract:compileJava` passes
- `gradlew :services:schedule-service:schedule-app:compileJava` passes (proto stubs generated)
- Generated stubs directory exists: `schedule-app/build/generated/source/proto/main/`
- All new files follow package convention `ru.rutcampustrack.schedule.*`
- No Lombok imports in any `schedule-api-contract` file
</verification>

<success_criteria>
- Proto compilation generates `AcademicGrpcServiceGrpc` from `academic.proto`
- `AcademicGrpcClient` has 3 methods with `.withDeadlineAfter(3, SECONDS)` on every call
- 5 new exception handler methods in `GlobalExceptionHandler` (404, 422, 409, 400, 503)
- `ScheduleItemApi` interface has 5 endpoints (POST, GET/{id}, GET, PUT/{id}, DELETE/{id})
- `LessonApi` interface has 5 endpoints (cancel, restore, mass-cancel, geo-block, getLessons)
- All request DTOs use `@Valid` annotations; response DTOs extend `RepresentationModel`
- Both `schedule-api-contract` and `schedule-app` compile with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-rest-api-grpc-client/11-01-SUMMARY.md`
</output>
