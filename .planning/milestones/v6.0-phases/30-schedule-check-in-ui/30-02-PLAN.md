---
phase: 30-schedule-check-in-ui
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - frontends/pwa/src/features/checkin/CheckInButton.tsx
  - frontends/pwa/src/features/checkin/CheckInToast.tsx
  - frontends/pwa/src/features/checkin/useStompCheckin.ts
  - frontends/pwa/src/features/checkin/StompProvider.tsx
  - frontends/pwa/src/features/checkin/CheckInScreen.tsx
  - frontends/pwa/src/features/schedule/LessonCard.tsx
  - frontends/pwa/src/features/schedule/SchedulePage.tsx
  - frontends/pwa/src/main.tsx
  - frontends/pwa/src/features/checkin/__tests__/CheckInButton.test.tsx
  - frontends/pwa/src/features/checkin/__tests__/useStompCheckin.test.ts
autonomous: true
requirements:
  - CHKIN-01
  - CHKIN-02
  - CHKIN-03
must_haves:
  truths:
    - "Student taps 'Отметиться' on active lesson card, GPS captured and submitted, success toast appears"
    - "When check-in fails, student sees specific failure reason (not generic error)"
    - "When another student checks in, the attendance count on the lesson card updates in real-time"
    - "The /checkin tab shows the current active lesson with check-in button or empty state"
    - "Check-in button is disabled when offline"
    - "Single shared STOMP WebSocket connection serves both SchedulePage and CheckInScreen (per D-11)"
  artifacts:
    - path: "frontends/pwa/src/features/checkin/CheckInButton.tsx"
      provides: "GPS capture + submit button with loading/success/error states"
      exports: ["CheckInButton"]
    - path: "frontends/pwa/src/features/checkin/CheckInToast.tsx"
      provides: "Slide-up success/failure toast"
      exports: ["CheckInToast"]
    - path: "frontends/pwa/src/features/checkin/useStompCheckin.ts"
      provides: "STOMP WebSocket hook for attendance.marked events"
      exports: ["useStompCheckin"]
    - path: "frontends/pwa/src/features/checkin/StompProvider.tsx"
      provides: "Shared STOMP context provider at AppShell level per D-11"
      exports: ["StompProvider", "useStompEvents"]
    - path: "frontends/pwa/src/features/checkin/CheckInScreen.tsx"
      provides: "Dedicated /checkin tab screen"
      exports: ["CheckInScreen"]
  key_links:
    - from: "frontends/pwa/src/features/checkin/CheckInButton.tsx"
      to: "/api/attendance/checkin"
      via: "useCheckin mutation"
      pattern: "useCheckin"
    - from: "frontends/pwa/src/features/checkin/StompProvider.tsx"
      to: "/api/ws (STOMP over SockJS)"
      via: "@stomp/stompjs Client"
      pattern: "new Client"
    - from: "frontends/pwa/src/features/schedule/LessonCard.tsx"
      to: "frontends/pwa/src/features/checkin/CheckInButton.tsx"
      via: "import and render when lesson is ACTIVE"
      pattern: "CheckInButton"
    - from: "frontends/pwa/src/main.tsx"
      to: "frontends/pwa/src/features/checkin/CheckInScreen.tsx"
      via: "lazy import for /checkin route"
      pattern: "CheckInScreen"
    - from: "frontends/pwa/src/main.tsx"
      to: "frontends/pwa/src/features/checkin/StompProvider.tsx"
      via: "wraps ProtectedRoute children at AppShell level"
      pattern: "StompProvider"
---

<objective>
Build the check-in flow: CheckInButton with GPS capture, CheckInToast for feedback, shared StompProvider for real-time attendance updates, and CheckInScreen for the dedicated /checkin tab. Integrate check-in into LessonCard and SchedulePage.

Purpose: Students can check in to active lessons from the schedule or dedicated check-in tab, see immediate feedback, and watch attendance counts update in real-time via a single shared STOMP connection.
Output: Working geo check-in flow with shared STOMP real-time updates across all screens.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-schedule-check-in-ui/30-CONTEXT.md
@.planning/phases/30-schedule-check-in-ui/30-RESEARCH.md
@.planning/phases/30-schedule-check-in-ui/30-UI-SPEC.md
@.planning/phases/30-schedule-check-in-ui/30-VALIDATION.md
@.planning/phases/30-schedule-check-in-ui/30-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From frontends/pwa/src/features/schedule/types.ts:
```typescript
export interface LessonResponse {
  id: number; scheduleItemId: number; groupId: number; subjectId: number;
  teacherId: number; date: string; status: 'PLANNED' | 'ACTIVE' | 'CLOSED' | 'CANCELLED';
  dayOfWeek: number; lessonNumber: number; startTime: string; endTime: string;
  weekType: 'NUMERATOR' | 'DENOMINATOR' | 'BOTH'; room: string;
  geoBlocked: boolean; cancelReason: string | null; createdAt: string;
}
export type LessonStatus = LessonResponse['status']
export type AttendanceStatus = 'present' | 'absent' | 'excused' | 'free_attendance'
```

From frontends/pwa/src/features/checkin/types.ts:
```typescript
export interface CheckinRequest { lat: number; lng: number }
export interface AttendanceMarkedPayload {
  lesson_id: number; user_id: number; group_id: number;
  status: string; marked_by: string;
}
```

From frontends/pwa/src/features/checkin/api.ts:
```typescript
export function useCheckin(): UseMutationResult  // POST /attendance/checkin {lat,lng}
export function mapCheckinError(status: number): string
```

From frontends/pwa/src/features/schedule/api.ts:
```typescript
export function useWeekSchedule(groupId: number, weekStart: string, weekEnd: string): UseQueryResult
```

<!-- Backend STOMP config verified -->
STOMP endpoint: /ws (SockJS) via gateway at /api/ws
JWT auth: ?token= query parameter (NOT Authorization header)
Topic: /topic/group/{groupId}
Event envelope: { type: "attendance.marked", payload: AttendanceMarkedPayload }
WebSocketConfig.java: .addEndpoint("/ws").addInterceptors(jwtHandshakeInterceptor).withSockJS()
JwtHandshakeInterceptor: reads ?token= from query string

<!-- Auth context -->
useAuth() returns { user: { id, role, groupId }, accessToken, isAuthenticated }
accessToken changes on silent refresh — STOMP must use factory function (Pitfall 7 in RESEARCH.md)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CheckInButton with GPS capture and CheckInToast feedback</name>
  <files>
    frontends/pwa/src/features/checkin/CheckInButton.tsx,
    frontends/pwa/src/features/checkin/CheckInToast.tsx,
    frontends/pwa/src/features/checkin/__tests__/CheckInButton.test.tsx
  </files>
  <read_first>
    frontends/pwa/src/features/checkin/types.ts,
    frontends/pwa/src/features/checkin/api.ts,
    frontends/pwa/src/shared/hooks/useNetworkStatus.ts,
    frontends/pwa/src/features/checkin/__tests__/CheckInButton.test.tsx,
    frontends/pwa/src/test/setup.ts,
    .planning/phases/30-schedule-check-in-ui/30-UI-SPEC.md,
    .planning/phases/30-schedule-check-in-ui/30-CONTEXT.md
  </read_first>
  <behavior>
    - Test: CheckInButton calls navigator.geolocation.getCurrentPosition on click
    - Test: CheckInButton shows spinner during GPS/submit loading (aria-busy="true")
    - Test: CheckInButton is disabled when offline (useNetworkStatus returns false)
    - Test: On successful check-in (mutation resolves), onSuccess callback is called
    - Test: On GPS error, shows failure toast with "Нет доступа к GPS. Разрешите доступ в настройках браузера" (per D-09 exact copy)
    - Test: On API 409 error, shows failure toast with "Вы уже отмечены на этом занятии"
    - Test: On API 422 error, shows failure toast with "Вы находитесь вне зоны отметки"
    - Test: On API 404 error, shows failure toast with "Активное занятие не найдено"
    - Test: CheckInToast auto-dismisses success after 3000ms
    - Test: CheckInToast auto-dismisses failure after 5000ms
  </behavior>
  <action>
    1. Update `CheckInButton.test.tsx` (already a stub from Plan 01 Wave 0):
       - Fill in real test implementations. Mock `navigator.geolocation.getCurrentPosition` to simulate success with coords `{latitude: 55.7558, longitude: 37.6173}` and error cases.
       - Mock `useCheckin` from `@/features/checkin/api` to return a controllable mutation.
       - Mock `useNetworkStatus` from `@/shared/hooks/useNetworkStatus`.
       - Test the GPS->submit pipeline and error mapping exhaustively.
       - CRITICAL for D-09: The GPS denial error test MUST assert the exact string "Нет доступа к GPS. Разрешите доступ в настройках браузера" — NOT "Не удалось получить геолокацию...".

    2. Create `CheckInButton.tsx`:
       - Props: `onSuccess: () => void`, `onError?: (message: string) => void`, `disabled?: boolean`
       - Uses `useCheckin()` mutation from `@/features/checkin/api`
       - Uses `useNetworkStatus()` — if `!isOnline`, button is disabled
       - State: `isCapturing: boolean` (covers GPS + submit time)
       - On click:
         a. Set `isCapturing = true`
         b. Call `navigator.geolocation.getCurrentPosition` with options `{ timeout: 10000, maximumAge: 30000 }` (per UI-SPEC interaction contract step 3)
         c. On GPS success: call `checkinMutation.mutate({ lat: position.coords.latitude, lng: position.coords.longitude })`
         d. On GPS error (permission denied, position unavailable, timeout): call `onError('Нет доступа к GPS. Разрешите доступ в настройках браузера')` per D-09 exact copy, set `isCapturing = false`
         e. On mutation success: call `onSuccess()`, set `isCapturing = false`
         f. On mutation error: extract HTTP status from `error.response?.status`, call `onError(mapCheckinError(status))`, set `isCapturing = false`
       - Button rendering:
         - Normal: `bg-primary text-primary-foreground rounded-lg px-4 min-h-[44px] font-medium text-sm` with text "Отметиться"
         - Loading: replace text with spinner `<span class="h-4 w-4 animate-spin rounded-full border-2 border-primary-foreground border-t-transparent"/>` (from LoginPage pattern), add `aria-busy="true"`
         - Disabled: standard disabled styles + when offline

    3. Create `CheckInToast.tsx`:
       - Props: `type: 'success' | 'error'`, `message: string`, `onDismiss: () => void`
       - Success: `bg-card border border-green-200 text-green-800`, auto-dismiss after 3000ms (`useEffect` with `setTimeout`)
       - Error: `bg-card border border-destructive/30 text-destructive`, auto-dismiss after 5000ms
       - Position: `fixed bottom-16 left-4 right-4 z-40` (above bottom nav per UI-SPEC)
       - Animation: `motion.div` with `initial={{ y: 64, opacity: 0 }}`, `animate={{ y: 0, opacity: 1 }}`, `exit={{ y: 64, opacity: 0 }}`, transition 0.2s easeOut for enter, 0.15s easeIn for exit
       - `role="status"` for success, `role="alert"` for error (per UI-SPEC Accessibility)
       - Success heading: "Отметка принята" (no body)
       - Error: display the `message` prop directly (already localized from `mapCheckinError`)
       - Wrap in `AnimatePresence` in the parent component, not inside this component
  </action>
  <verify>
    <automated>cd frontends/pwa && npm run test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - frontends/pwa/src/features/checkin/CheckInButton.tsx contains "getCurrentPosition" and "timeout: 10000" and "maximumAge: 30000"
    - frontends/pwa/src/features/checkin/CheckInButton.tsx contains "Отметиться" button label
    - frontends/pwa/src/features/checkin/CheckInButton.tsx contains "useNetworkStatus" import for offline check
    - frontends/pwa/src/features/checkin/CheckInButton.tsx contains "useCheckin" import
    - frontends/pwa/src/features/checkin/CheckInButton.tsx contains 'aria-busy="true"' during loading
    - frontends/pwa/src/features/checkin/CheckInButton.tsx contains "mapCheckinError" for error handling
    - frontends/pwa/src/features/checkin/CheckInButton.tsx contains "Нет доступа к GPS. Разрешите доступ в настройках браузера" for GPS denial per D-09 (NOT "Не удалось получить геолокацию")
    - frontends/pwa/src/features/checkin/CheckInToast.tsx contains "Отметка принята" for success
    - frontends/pwa/src/features/checkin/CheckInToast.tsx contains 'role="status"' and 'role="alert"'
    - frontends/pwa/src/features/checkin/CheckInToast.tsx contains "3000" and "5000" for auto-dismiss timers
    - frontends/pwa/src/features/checkin/CheckInToast.tsx contains "bottom-16" for position above bottom nav
    - frontends/pwa/src/features/checkin/__tests__/CheckInButton.test.tsx contains tests for GPS error, 409, 422, 404 status codes
    - frontends/pwa/src/features/checkin/__tests__/CheckInButton.test.tsx contains "Нет доступа к GPS" in GPS denial test assertion
    - `npm run test --prefix frontends/pwa` exits with code 0
  </acceptance_criteria>
  <done>CheckInButton captures GPS and submits to /attendance/checkin, shows spinner during loading, disabled when offline, GPS denial shows "Нет доступа к GPS. Разрешите доступ в настройках браузера" per D-09 exact copy. CheckInToast shows localized success/error messages with auto-dismiss. All error codes mapped. Tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StompProvider shared context, useStompCheckin hook, and attendance count integration in LessonCard</name>
  <files>
    frontends/pwa/src/features/checkin/useStompCheckin.ts,
    frontends/pwa/src/features/checkin/StompProvider.tsx,
    frontends/pwa/src/features/schedule/LessonCard.tsx,
    frontends/pwa/src/features/schedule/SchedulePage.tsx,
    frontends/pwa/src/main.tsx,
    frontends/pwa/src/features/checkin/__tests__/useStompCheckin.test.ts
  </files>
  <read_first>
    frontends/pwa/src/features/checkin/types.ts,
    frontends/pwa/src/features/checkin/useStompCheckin.ts,
    frontends/pwa/src/features/checkin/__tests__/useStompCheckin.test.ts,
    frontends/pwa/src/features/schedule/LessonCard.tsx,
    frontends/pwa/src/features/schedule/SchedulePage.tsx,
    frontends/pwa/src/features/auth/AuthProvider.tsx,
    frontends/pwa/src/shared/lib/axios.ts,
    frontends/pwa/src/main.tsx,
    .planning/phases/30-schedule-check-in-ui/30-RESEARCH.md,
    .planning/phases/30-schedule-check-in-ui/30-CONTEXT.md
  </read_first>
  <behavior>
    - Test: useStompCheckin creates Client with webSocketFactory using SockJS and token
    - Test: useStompCheckin subscribes to /topic/group/{groupId} on connect
    - Test: useStompCheckin calls onMarked when attendance.marked event received
    - Test: useStompCheckin ignores events with different type (not attendance.marked)
    - Test: useStompCheckin deactivates client on unmount
    - Test: LessonCard renders attendance count when provided
    - Test: LessonCard renders CheckInButton when lesson status is ACTIVE and no personalStatus
  </behavior>
  <action>
    1. Update `useStompCheckin.test.ts` (fill in stub from Plan 01):
       - Mock `@stomp/stompjs` Client class: `vi.mock('@stomp/stompjs', () => ({ Client: vi.fn().mockImplementation(() => ({ activate: vi.fn(), deactivate: vi.fn(), subscribe: vi.fn() })) }))`
       - Mock `sockjs-client`: `vi.mock('sockjs-client', () => ({ default: vi.fn() }))`
       - Test that Client is constructed with a webSocketFactory function
       - Test that on simulated connect, subscribe is called with `/topic/group/5`
       - Test that received message with `{ type: 'attendance.marked', payload: {...} }` calls onMarked callback
       - Test that events with type !== 'attendance.marked' are ignored
       - Test that deactivate is called on cleanup

    2. Create `useStompCheckin.ts`:
       ```typescript
       import { useEffect, useRef } from 'react'
       import { Client } from '@stomp/stompjs'
       import SockJS from 'sockjs-client'
       import type { AttendanceMarkedPayload } from './types'
       ```
       - Props: `groupId: number`, `getAccessToken: () => string | null`, `onMarked: (payload: AttendanceMarkedPayload) => void`
       - CRITICAL: Use `getAccessToken` as a factory function, NOT the token value directly. This avoids Pitfall 7 from RESEARCH.md — on STOMP reconnect after silent token refresh, the factory reads the current token, not the stale one.
       - Create `Client` with:
         - `webSocketFactory: () => new SockJS('/api/ws?token=' + (getAccessToken() ?? ''))` — token read at factory call time per D-13 reconnect pattern
         - `reconnectDelay: 1000` — @stomp/stompjs handles exponential backoff internally per D-13
         - `onConnect`: subscribe to `/topic/group/${groupId}`, parse `JSON.parse(message.body)`, check `envelope.type === 'attendance.marked'` then call `onMarked(envelope.payload)`
         - `onStompError`: log error silently (no UI notification unless >30s, which is handled by the 30s disconnect indicator — out of scope for this hook, can be added later)
       - `client.activate()` in useEffect, `client.deactivate()` on cleanup
       - Dependencies: `[groupId]` — NOT `getAccessToken` (it's a ref/callback, stable identity)
       - Use `useRef` for the `onMarked` callback to avoid re-subscribing when callback changes

    3. Create `StompProvider.tsx` (per D-11 — shared STOMP connection):
       - This is a React context provider that creates ONE STOMP connection and shares events to all consumers.
       - Creates a context: `StompContext` with value `{ attendanceCounts: Record<number, number>, personalStatuses: Record<number, AttendanceStatus | null> }`
       - Props: `children: ReactNode`
       - Uses `useAuth()` to get `user.groupId`, `user.id`, and `accessToken`
       - Maintains `accessTokenRef` pattern: `const accessTokenRef = useRef(accessToken); useEffect(() => { accessTokenRef.current = accessToken }, [accessToken])`
       - Maintains `attendanceCounts: Record<number, number>` (lessonId -> count) state
       - Maintains `personalStatuses: Record<number, AttendanceStatus | null>` (lessonId -> status if current user checked in) state
       - Calls `useStompCheckin(groupId, () => accessTokenRef.current, (payload) => { setAttendanceCounts(prev => ({ ...prev, [payload.lesson_id]: (prev[payload.lesson_id] ?? 0) + 1 })); if (payload.user_id === user.id) { setPersonalStatuses(prev => ({ ...prev, [payload.lesson_id]: payload.status as AttendanceStatus })) } })`
       - Provides `attendanceCounts`, `personalStatuses`, and a `markPersonalStatus(lessonId, status)` setter via context
       - Export `useStompEvents()` hook that calls `useContext(StompContext)` — consumers use this to read attendance state

    4. Wire `StompProvider` into the app at AppShell level in `main.tsx`:
       - Import `StompProvider` from `@/features/checkin/StompProvider`
       - Wrap the `<AppShell>` children (or the ProtectedRoute's element) with `<StompProvider>` so that both SchedulePage and CheckInScreen share one connection per D-11
       - The StompProvider should be INSIDE `<AuthProvider>` (needs auth context) but wrapping `<AppShell>` or at the ProtectedRoute level

    5. Update `LessonCard.tsx` to integrate CheckInButton and attendance count:
       - Add new props: `onCheckin?: () => void`, `onCheckinError?: (msg: string) => void`, `isCheckinLoading?: boolean`, `attendanceCount?: number`, `personalStatus?: AttendanceStatus | null`
       - When lesson.status === 'ACTIVE' AND personalStatus is null (not yet checked in):
         - Render bottom row with `<CheckInButton onSuccess={onCheckin!} onError={onCheckinError} />` on the left
         - Render attendance count on the right: `{attendanceCount ?? 0} чел` with `text-xs text-muted-foreground`
         - Animate the count with `motion.span` and `key={attendanceCount}` for number flip: `initial={{ y: -8, opacity: 0 }}`, `animate={{ y: 0, opacity: 1 }}`, `transition={{ type: 'spring', stiffness: 300, damping: 30 }}` per UI-SPEC Animation Contracts
       - When personalStatus is set (already checked in per D-08):
         - Replace CheckInButton with a checkmark icon: `<Check size={20} weight="bold" className="text-green-600" />` from `@phosphor-icons/react`
         - Show StatusBadge with personalStatus (e.g., "б" green badge)
         - Status badge changes from "Идёт" to the personal attendance status badge per D-08

    6. Update `SchedulePage.tsx` to consume from StompProvider instead of managing STOMP directly:
       - Import `useStompEvents` from `@/features/checkin/StompProvider`
       - Call `const { attendanceCounts, personalStatuses, markPersonalStatus } = useStompEvents()`
       - Remove any direct `useStompCheckin` call from SchedulePage — the provider handles it
       - Pass `attendanceCount={attendanceCounts[lesson.id]}` and `personalStatus={personalStatuses[lesson.id] ?? null}` to each LessonCard
       - Toast state: `toast: { type: 'success' | 'error', message: string } | null`. Render `<CheckInToast>` wrapped in `<AnimatePresence>` when toast is set.
       - On check-in success from LessonCard: set toast success "Отметка принята", call `markPersonalStatus(lessonId, 'present')`
       - On check-in error from LessonCard: set toast with error message

    Note on Open Question #1 (initial attendance count): Per RESEARCH.md, LessonResponse has no attendance count field and no endpoint was found. The count starts at 0 and only reflects STOMP events received during the current session. This is the pragmatic approach — the counter shows live check-in activity, not historical totals. The display shows just "{N} чел" without a denominator total (since group size is also not in the API response).
  </action>
  <verify>
    <automated>cd frontends/pwa && npm run test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - frontends/pwa/src/features/checkin/useStompCheckin.ts contains "new Client" and "new SockJS" and "/api/ws?token="
    - frontends/pwa/src/features/checkin/useStompCheckin.ts contains "/topic/group/" subscription
    - frontends/pwa/src/features/checkin/useStompCheckin.ts contains "attendance.marked" event type check
    - frontends/pwa/src/features/checkin/useStompCheckin.ts contains "getAccessToken" factory function pattern (NOT direct token string)
    - frontends/pwa/src/features/checkin/useStompCheckin.ts contains "client.deactivate()" cleanup
    - frontends/pwa/src/features/checkin/StompProvider.tsx contains "createContext" and "useStompCheckin" and "useAuth"
    - frontends/pwa/src/features/checkin/StompProvider.tsx exports "StompProvider" and "useStompEvents"
    - frontends/pwa/src/features/checkin/StompProvider.tsx contains "attendanceCounts" and "personalStatuses" state
    - frontends/pwa/src/main.tsx contains "StompProvider" wrapping AppShell or ProtectedRoute children
    - frontends/pwa/src/features/schedule/LessonCard.tsx contains "CheckInButton" import
    - frontends/pwa/src/features/schedule/LessonCard.tsx contains "motion.span" with "stiffness: 300" for count animation
    - frontends/pwa/src/features/schedule/SchedulePage.tsx contains "useStompEvents" import (NOT "useStompCheckin" directly)
    - frontends/pwa/src/features/schedule/SchedulePage.tsx contains "attendanceCounts" from context
    - frontends/pwa/src/features/schedule/SchedulePage.tsx contains "CheckInToast" render
    - `npm run test --prefix frontends/pwa` exits with code 0
  </acceptance_criteria>
  <done>Shared StompProvider created at AppShell level per D-11 — single STOMP WebSocket connection serves both SchedulePage and CheckInScreen. useStompCheckin hook connects with SockJS, subscribes to group topic, increments attendance counts on events. LessonCard renders CheckInButton for active lessons and shows personal status after check-in. SchedulePage consumes from StompProvider context. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: CheckInScreen dedicated tab consuming shared StompProvider</name>
  <files>
    frontends/pwa/src/features/checkin/CheckInScreen.tsx,
    frontends/pwa/src/main.tsx
  </files>
  <read_first>
    frontends/pwa/src/features/schedule/types.ts,
    frontends/pwa/src/features/schedule/api.ts,
    frontends/pwa/src/features/checkin/CheckInButton.tsx,
    frontends/pwa/src/features/checkin/CheckInToast.tsx,
    frontends/pwa/src/features/checkin/StompProvider.tsx,
    frontends/pwa/src/features/checkin/useStompCheckin.ts,
    frontends/pwa/src/features/checkin/api.ts,
    frontends/pwa/src/features/auth/AuthProvider.tsx,
    frontends/pwa/src/shared/hooks/useNetworkStatus.ts,
    frontends/pwa/src/main.tsx,
    .planning/phases/30-schedule-check-in-ui/30-UI-SPEC.md,
    .planning/phases/30-schedule-check-in-ui/30-CONTEXT.md
  </read_first>
  <action>
    1. Create `CheckInScreen.tsx` (per D-10):
       - Uses `useAuth()` to get `user.groupId`
       - Uses `useWeekSchedule(groupId, todayStr, todayStr)` to fetch today's lessons only (dateFrom=dateTo=today)
       - Finds the lesson with `status === 'ACTIVE'` from today's lessons (there should be at most one active lesson at any time)
       - Uses `useSubjectName(activeLesson?.subjectId)` to get subject name
       - Uses `useStompEvents()` from StompProvider to read `attendanceCounts` and `personalStatuses` — per D-11 shared STOMP connection. NO separate useStompCheckin call here; the StompProvider at AppShell level already manages the single connection.

       When active lesson exists:
       - Render a single large card (not LessonCard — custom layout for this screen):
         - Subject name: `text-2xl font-semibold` centered
         - Time range: `text-base text-muted-foreground` centered, "09:00 - 10:35"
         - Room: `text-sm text-muted-foreground` centered, "Ауд. 305"
         - StatusBadge: "Идёт" (or personal status if already checked in)
         - Large `<CheckInButton />` centered, full width `w-full`
         - Attendance count below button: `{count} чел` centered
       - Toast rendered in this screen too (same pattern as SchedulePage)

       When NO active lesson (per D-10 empty state):
       - Find the next upcoming lesson today (status === 'PLANNED', sorted by startTime)
       - Show: heading "Сейчас нет активных пар" (`text-xl font-semibold text-center text-muted-foreground`)
       - If next lesson exists: "Следующая: {subjectName} в {startTime}" (`text-sm text-muted-foreground text-center`)
       - If no more lessons today: "На сегодня пар больше нет" (`text-sm text-muted-foreground text-center`)
       - No CheckInButton in empty state

       Offline handling (per D-17):
       - When offline: CheckInButton inside this screen is disabled (handled by CheckInButton itself)
       - OfflineStaleNotice rendered at top (pass `dataUpdatedAt` from useWeekSchedule query per D-15)

       Container: `flex flex-col items-center justify-center px-4 pt-8 gap-6 min-h-[60vh]`

    2. Update `frontends/pwa/src/main.tsx`:
       - Add lazy import:
         ```typescript
         const CheckInScreen = lazy(() => import('./features/checkin/CheckInScreen').then(m => ({ default: m.CheckInScreen })))
         ```
       - Replace the `/checkin` route element (currently `<HomePlaceholder />`) with:
         ```typescript
         <Suspense fallback={<LoadingSpinner />}><CheckInScreen /></Suspense>
         ```
       - Ensure HomePlaceholder is no longer imported at all if not used by any route. If `/profile` still uses it, keep the import. Otherwise remove.

    Run final verification: `cd frontends/pwa && npx tsc --noEmit && npm run test && npm run build`
  </action>
  <verify>
    <automated>cd frontends/pwa && npx tsc --noEmit 2>&1 | tail -10 && npm run test 2>&1 | tail -20 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - frontends/pwa/src/features/checkin/CheckInScreen.tsx contains "Сейчас нет активных пар" empty state text
    - frontends/pwa/src/features/checkin/CheckInScreen.tsx contains "Следующая:" next lesson hint
    - frontends/pwa/src/features/checkin/CheckInScreen.tsx contains "useWeekSchedule" import for fetching today's lessons
    - frontends/pwa/src/features/checkin/CheckInScreen.tsx contains "useSubjectName" import
    - frontends/pwa/src/features/checkin/CheckInScreen.tsx contains "CheckInButton" import
    - frontends/pwa/src/features/checkin/CheckInScreen.tsx contains "useStompEvents" import (NOT "useStompCheckin" — consumes from shared provider per D-11)
    - frontends/pwa/src/features/checkin/CheckInScreen.tsx does NOT contain "new Client" or "useStompCheckin(" (no separate STOMP connection)
    - frontends/pwa/src/features/checkin/CheckInScreen.tsx contains "OfflineStaleNotice" import
    - frontends/pwa/src/main.tsx contains "CheckInScreen" import
    - frontends/pwa/src/main.tsx does NOT contain "HomePlaceholder" for /checkin route
    - `npx tsc --noEmit` in frontends/pwa exits with code 0
    - `npm run test --prefix frontends/pwa` exits with code 0
    - `npm run build --prefix frontends/pwa` exits with code 0
  </acceptance_criteria>
  <done>CheckInScreen at /checkin shows active lesson with check-in button or empty state with next lesson info. Consumes attendance state from shared StompProvider (per D-11) — no separate STOMP connection. Router updated with all real feature components. Full build succeeds. All tests pass. Phase 30 feature-complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PWA -> GPS API | Browser geolocation API returns user coordinates |
| PWA -> Gateway -> attendance-service | Check-in request with GPS coords crosses network |
| Gateway -> notification-service STOMP | WebSocket connection with JWT in query param |
| STOMP topic subscription | Client subscribes to group topic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | Tampering | GPS spoofing for check-in bypass | accept | Server-side geofence validation in CheckinService.geofenceService — PWA cannot prevent client-side spoofing; this is an inherent limitation of browser geolocation |
| T-30-05 | Information Disclosure | JWT token in WebSocket URL query string | accept | Unavoidable for browser WebSocket API; mitigated by short JWT expiry (15min from Phase 29) and HTTPS in production; SockJS URL is not logged in browser history |
| T-30-06 | Elevation of Privilege | STOMP subscription to another group's topic | mitigate | Backend JwtHandshakeInterceptor validates JWT at handshake; subscription authorization should be enforced server-side (verify notification-service enforces group_id match on subscribe). PWA only subscribes to user's own groupId from JWT. |
| T-30-07 | Spoofing | Stale token used on STOMP reconnect | mitigate | useStompCheckin uses getAccessToken factory function (not direct token value); factory reads current token from ref at reconnect time, ensuring fresh token after silent refresh |
</threat_model>

<verification>
1. `cd frontends/pwa && npx tsc --noEmit` — zero type errors
2. `cd frontends/pwa && npm run test` — all tests pass
3. `cd frontends/pwa && npm run build` — production build succeeds
4. Manual: open http://localhost:5173/checkin — CheckInScreen renders
5. Manual: tap "Отметиться" on active lesson — GPS prompt appears, check-in submits
</verification>

<success_criteria>
- Student taps "Отметиться" on active lesson card, GPS captured, check-in submitted, success toast appears
- GPS denial shows "Нет доступа к GPS. Разрешите доступ в настройках браузера" per D-09 exact copy
- Check-in errors show specific Russian failure messages per HTTP status code
- Single shared STOMP WebSocket (via StompProvider at AppShell level per D-11) serves both SchedulePage and /checkin screen
- STOMP subscribes to group topic, attendance count animates on events
- /checkin tab shows active lesson or empty state with next lesson info
- Check-in disabled when offline
- All tests pass, TypeScript compiles, production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/30-schedule-check-in-ui/30-02-SUMMARY.md`
</output>
