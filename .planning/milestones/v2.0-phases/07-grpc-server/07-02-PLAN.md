---
phase: 07-grpc-server
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - services/academic-service/academic-app/src/test/resources/application-test.yml
  - services/academic-service/academic-app/src/test/java/ru/rutcampustrack/academic/integration/AcademicGrpcIntegrationTest.java
autonomous: true
requirements: [GRPC-01, GRPC-02, GRPC-03, GRPC-04, GRPC-05, GRPC-06, GRPC-07]

must_haves:
  truths:
    - "GetGroup with valid ID returns group name, code, and active flag"
    - "GetGroup with invalid ID returns gRPC NOT_FOUND status"
    - "GetGroupMembers returns only active (non-archived) students"
    - "GetTeacherSubjects returns subject+group info for a teacher in a semester"
    - "IsHeadman returns true for the headman of a group, false otherwise"
    - "GetActiveSemester returns the active semester; NOT_FOUND when none active"
    - "GetCampusGeofence returns lat, lng, radius from campus_settings"
    - "GetUserById returns user info including archived users"
  artifacts:
    - path: "services/academic-service/academic-app/src/test/resources/application-test.yml"
      provides: "In-process gRPC server and client config for tests"
      contains: "in-process-name: test"
    - path: "services/academic-service/academic-app/src/test/java/ru/rutcampustrack/academic/integration/AcademicGrpcIntegrationTest.java"
      provides: "Integration tests for all 7 gRPC RPCs"
      min_lines: 150
  key_links:
    - from: "AcademicGrpcIntegrationTest"
      to: "AcademicGrpcServiceImpl"
      via: "@GrpcClient blocking stub"
      pattern: "AcademicGrpcServiceGrpc.AcademicGrpcServiceBlockingStub"
    - from: "AcademicGrpcIntegrationTest"
      to: "Testcontainers PostgreSQL"
      via: "extends AbstractAcademicIntegrationTest"
      pattern: "extends AbstractAcademicIntegrationTest"
---

<objective>
Create Testcontainers-based integration tests for all 7 gRPC RPCs using grpc-spring-boot-starter in-process server mode. Tests verify correct data mapping, NOT_FOUND error handling, and soft-delete filtering against a real PostgreSQL instance.

Purpose: Prove all 7 RPCs work end-to-end (GRPC-01 through GRPC-07) before moving to Phase 8 Redis caching.
Output: Green test suite covering all 7 RPCs with positive and negative cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-grpc-server/07-CONTEXT.md
@.planning/phases/07-grpc-server/07-RESEARCH.md
@.planning/phases/07-grpc-server/07-01-SUMMARY.md

@proto/academic.proto
@services/academic-service/academic-app/src/test/java/ru/rutcampustrack/academic/integration/AbstractAcademicIntegrationTest.java
@services/academic-service/academic-app/src/test/resources/application-test.yml
@services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/grpc/AcademicGrpcServiceImpl.java

<interfaces>
<!-- Key types for the test executor -->

From proto/academic.proto (generated blocking stub):
```java
AcademicGrpcServiceGrpc.AcademicGrpcServiceBlockingStub
// Methods: getGroup(GroupRequest), getGroupMembers(GroupMembersRequest),
// getTeacherSubjects(TeacherSubjectsRequest), isHeadman(HeadmanCheckRequest),
// getActiveSemester(Empty), getCampusGeofence(Empty), getUserById(UserRequest)
```

From AbstractAcademicIntegrationTest:
```java
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
@ActiveProfiles("test")
// Provides: Testcontainers PostgreSQL, excludes RabbitMQ + Redis autoconfigs
// Flyway runs V1 (schema) + V2 (seed data) + V3-V5 (migrations)
```

From V2 seed data (test fixtures already in DB via Flyway):
- Groups: at least one group exists
- Users: at least one student and one teacher exist
- Semesters: at least one semester exists
- campus_settings: single row with lat/lng/radius_m
- Subjects: at least one subject exists
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure in-process gRPC server for tests</name>
  <files>
    services/academic-service/academic-app/src/test/resources/application-test.yml
  </files>
  <read_first>
    services/academic-service/academic-app/src/test/resources/application-test.yml,
    services/academic-service/academic-app/src/main/resources/application.yml,
    services/academic-service/academic-app/build.gradle.kts
  </read_first>
  <action>
    Update `application-test.yml` to add gRPC in-process server configuration. This prevents port binding (avoids BindException in parallel test runs — per RESEARCH.md Pitfall 5) and uses in-memory transport.

    Add the following YAML block to the existing test config:

    ```yaml
    grpc:
      server:
        in-process-name: test
        port: -1
      client:
        inProcess:
          address: in-process:test
          negotiationType: plaintext
    ```

    The existing content (jpa, flyway, logging config) MUST be preserved. Append the `grpc:` block after the existing content.

    The final file should contain:
    ```yaml
    spring:
      jpa:
        hibernate:
          ddl-auto: validate
        open-in-view: false
        properties:
          hibernate:
            dialect: org.hibernate.dialect.PostgreSQLDialect
      flyway:
        enabled: true
        locations: classpath:db/migration
    logging:
      level:
        ru.rutcampustrack: DEBUG
        org.hibernate.SQL: DEBUG
    grpc:
      server:
        in-process-name: test
        port: -1
      client:
        inProcess:
          address: in-process:test
          negotiationType: plaintext
    ```
  </action>
  <verify>
    <automated>grep -c "in-process-name: test" C:/Users/maksd/IntelliJIDEA/rutcampustrack/services/academic-service/academic-app/src/test/resources/application-test.yml</automated>
  </verify>
  <acceptance_criteria>
    - application-test.yml contains `in-process-name: test`
    - application-test.yml contains `port: -1`
    - application-test.yml contains `address: in-process:test`
    - application-test.yml contains `negotiationType: plaintext`
    - application-test.yml still contains `ddl-auto: validate`
    - application-test.yml still contains `flyway:` with `enabled: true`
  </acceptance_criteria>
  <done>Test YAML configures in-process gRPC server — no real port needed during tests</done>
</task>

<task type="auto">
  <name>Task 2: Implement AcademicGrpcIntegrationTest with all 7 RPCs</name>
  <files>
    services/academic-service/academic-app/src/test/java/ru/rutcampustrack/academic/integration/AcademicGrpcIntegrationTest.java
  </files>
  <read_first>
    services/academic-service/academic-app/src/test/java/ru/rutcampustrack/academic/integration/AbstractAcademicIntegrationTest.java,
    services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/grpc/AcademicGrpcServiceImpl.java,
    services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/grpc/GrpcExceptionAdvice.java,
    services/academic-service/academic-app/src/main/resources/db/migration/V1__baseline.sql,
    services/academic-service/academic-app/src/main/resources/db/migration/V2__seed_test_data.sql,
    proto/academic.proto,
    services/academic-service/academic-app/src/test/resources/application-test.yml,
    services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/entity/User.java,
    services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/entity/Group.java,
    services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/entity/CampusSetting.java
  </read_first>
  <action>
    Create `AcademicGrpcIntegrationTest.java` in package `ru.rutcampustrack.academic.integration`.

    **Class structure:**
    - Extend `AbstractAcademicIntegrationTest` (provides Testcontainers PostgreSQL, excludes Redis/RabbitMQ)
    - Override `@SpringBootTest` with additional properties: `"grpc.server.in-process-name=test"`, `"grpc.server.port=-1"` (supplement the test YAML, ensure in-process mode)
    - Inject `@GrpcClient("inProcess") AcademicGrpcServiceGrpc.AcademicGrpcServiceBlockingStub stub`
    - Inject `GroupRepository`, `UserRepository`, `SemesterRepository`, `SubjectRepository`, `TeacherSubjectGroupRepository`, `CampusSettingRepository` for test data setup
    - Use `@Autowired` for repositories and `@GrpcClient("inProcess")` for the stub

    **Test data setup** (`@BeforeEach` or `@Sql` — read V2 seed data first to understand what's already seeded):
    - Read V2__seed_test_data.sql carefully to understand what groups, users, semesters, subjects, campus_settings are already in the DB
    - If V2 seed data is sufficient, use it directly — do NOT create duplicate data
    - If V2 seed data is insufficient (e.g., no archived user for GetUserById test), insert additional test data via repository `.save()` in a `@BeforeEach` method
    - For the archived user test: create a user with `status=ARCHIVED` directly via SQL (since `@SQLRestriction` blocks JPA save for archived users) — use `@Sql` annotation or `JdbcTemplate`

    **Required test methods (at minimum):**

    GRPC-01:
    - `getGroup_validId_returnsGroupInfo()` — call `stub.getGroup(GroupRequest)` with a known group ID from seed data. Assert response has correct `name`, `code`, `isActive`.
    - `getGroup_invalidId_throwsNotFound()` — call with ID 99999. Assert `StatusRuntimeException` with `Status.Code.NOT_FOUND`.

    GRPC-02:
    - `getGroupMembers_returnsActiveStudentsOnly()` — call with a group ID that has students. Assert returned list contains expected students. If an archived user exists in that group, assert they are NOT in the response.

    GRPC-03:
    - `getTeacherSubjects_returnsSubjectsWithGroups()` — call with a teacher ID and semester ID that have assignments. Assert response contains `TeacherSubjectInfo` entries with non-empty `subjectName` and `groupName`.
    - `getTeacherSubjects_noAssignments_returnsEmptyList()` — call with a teacher ID that has no assignments. Assert empty list.

    GRPC-04:
    - `isHeadman_headmanOfGroup_returnsTrue()` — call with a user who is headman of the specified group. Assert `isHeadman=true`.
    - `isHeadman_notHeadman_returnsFalse()` — call with a regular student. Assert `isHeadman=false`.
    - `isHeadman_userNotFound_returnsFalse()` — call with ID 99999. Assert `isHeadman=false`.

    GRPC-05:
    - `getActiveSemester_activeSemesterExists_returnsSemester()` — ensure one semester is active (from seed data or setup). Assert response has `name`, `dateFrom`, `dateTo` as non-empty strings.
    - `getActiveSemester_noActiveSemester_throwsNotFound()` — deactivate all semesters, then call. Assert `StatusRuntimeException` with `NOT_FOUND`. Restore active semester in cleanup.

    GRPC-06:
    - `getCampusGeofence_returnsCampusSettings()` — call. Assert `lat`, `lng` are non-zero, `radiusM > 0`.

    GRPC-07:
    - `getUserById_validId_returnsUserInfo()` — call with known user ID. Assert `login`, `displayName`, `role` are non-empty.
    - `getUserById_archivedUser_stillReturnsUser()` — call with an archived user's ID. Assert the response is returned (NOT a NOT_FOUND error). This validates that `findByIdIncludingArchived` is used.
    - `getUserById_invalidId_throwsNotFound()` — call with ID 99999. Assert `StatusRuntimeException` with `NOT_FOUND`.

    **Imports needed:**
    ```java
    import net.devh.boot.grpc.client.inject.GrpcClient;
    import io.grpc.StatusRuntimeException;
    import io.grpc.Status;
    import ru.rutcampustrack.academic.grpc.*;
    import static org.assertj.core.api.Assertions.*;
    import static org.junit.jupiter.api.Assertions.*;
    ```

    **Transaction management:** Use `@Transactional` on test class or `@DirtiesContext` per class to ensure test isolation. Since Flyway seeds data, prefer `@Transactional` for rollback after each test. BUT note: `@GrpcClient` calls go through a channel — they won't participate in the test transaction. Therefore use `@DirtiesContext(classMode = ClassMode.AFTER_CLASS)` and manage test data carefully (V2 seed data + additional inserts that don't conflict).

    **IMPORTANT:** The `@SpringBootTest` annotation on this class MUST NOT specify `webEnvironment = RANDOM_PORT` if the parent already does. Check AbstractAcademicIntegrationTest — it uses `RANDOM_PORT`. This is fine; the gRPC in-process server is separate from the web server.
  </action>
  <verify>
    <automated>cd C:/Users/maksd/IntelliJIDEA/rutcampustrack && cmd.exe /c "set JAVA_HOME=C:\Users\maksd\.jdks\ms-21.0.9&& .\gradlew.bat :services:academic-service:academic-app:test --tests *GrpcIntegrationTest* --no-daemon 2>&1 | tail -20"</automated>
  </verify>
  <acceptance_criteria>
    - AcademicGrpcIntegrationTest.java extends AbstractAcademicIntegrationTest
    - AcademicGrpcIntegrationTest.java contains `@GrpcClient("inProcess")`
    - AcademicGrpcIntegrationTest.java contains `AcademicGrpcServiceBlockingStub`
    - AcademicGrpcIntegrationTest.java has test methods for all 7 RPCs: getGroup (2 tests), getGroupMembers (1+), getTeacherSubjects (2), isHeadman (2+), getActiveSemester (2), getCampusGeofence (1), getUserById (3)
    - AcademicGrpcIntegrationTest.java `getUserById_archivedUser` asserts successful response (not NOT_FOUND)
    - AcademicGrpcIntegrationTest.java `getGroup_invalidId` asserts `Status.Code.NOT_FOUND`
    - All gRPC integration tests pass: `.\gradlew :services:academic-service:academic-app:test --tests *GrpcIntegrationTest*` exits 0
    - Full test suite still passes: `.\gradlew :services:academic-service:academic-app:test` exits 0
  </acceptance_criteria>
  <done>All 7 gRPC RPCs covered by integration tests with positive and negative cases; all tests green against Testcontainers PostgreSQL</done>
</task>

</tasks>

<verification>
- `.\gradlew.bat :services:academic-service:academic-app:test --tests "*GrpcIntegrationTest*"` passes — all 7 RPCs tested
- `.\gradlew.bat :services:academic-service:academic-app:test` passes — existing tests not broken
- Test count >= 13 (at minimum 2+1+2+2+2+1+3 = 13 tests across 7 RPCs)
</verification>

<success_criteria>
- All 7 gRPC RPC integration tests pass against real PostgreSQL via Testcontainers
- GetGroup returns group info and NOT_FOUND on invalid ID
- GetGroupMembers returns only active students (no archived)
- GetTeacherSubjects returns subject+group enriched data
- IsHeadman correctly identifies headman vs non-headman
- GetActiveSemester returns active semester and NOT_FOUND when none
- GetCampusGeofence returns campus coordinates
- GetUserById returns user info including for archived users; NOT_FOUND on invalid ID
</success_criteria>

<output>
After completion, create `.planning/phases/07-grpc-server/07-02-SUMMARY.md`
</output>
