---
phase: 54-headman-web-cabinet-group-management-subjects
plan: 04
type: execute
wave: 2
depends_on:
  - 54-02
files_modified:
  - frontends/web-panel/src/app/features/headman/group/headman-group.component.ts
  - frontends/web-panel/src/app/features/headman/group/assign-assistant-dialog.component.ts
  - frontends/web-panel/src/app/features/headman/group/delete-assistant-dialog.component.ts
autonomous: true
requirements:
  - HEAD-WEB-03

must_haves:
  truths:
    - "Student list table loads and shows group members with name + login + role chip"
    - "Assistants section shows existing assistants with permission chips and delete buttons"
    - "Clicking 'Назначить помощника' opens the assign dialog; form validates student selection and at least one permission"
    - "On successful assistant assignment, dialog closes and assistant list refreshes"
    - "Clicking the trash icon opens the confirmation dialog; confirming calls revokeAssistant and reloads"
    - "Empty state, loading skeleton, and error state all render correctly"
  artifacts:
    - path: "frontends/web-panel/src/app/features/headman/group/headman-group.component.ts"
      provides: "Group page with student table + assistants section"
      contains: "listAssistants"
      min_lines: 100
    - path: "frontends/web-panel/src/app/features/headman/group/assign-assistant-dialog.component.ts"
      provides: "MatDialog for assigning assistant with student select + permission checkboxes"
      contains: "MatCheckbox"
    - path: "frontends/web-panel/src/app/features/headman/group/delete-assistant-dialog.component.ts"
      provides: "MatDialog confirmation for deleting an assistant"
  key_links:
    - from: "HeadmanGroupComponent"
      to: "AssignAssistantDialogComponent"
      via: "MatDialog.open()"
      pattern: "MatDialog"
    - from: "AssignAssistantDialogComponent.submit()"
      to: "HeadmanApiService.assignAssistant()"
      via: "subscribe to POST /api/academic/assistants"
      pattern: "assignAssistant"
---

<objective>
Build the `/headman/group` page: student list (read-only), assistant CRUD (assign + delete), and the two dialog components.

Purpose: This page closes WPAN-13 from the UI side — the headman can create/delete assistants without 403 errors now that Plan 1 fixed the AOP aspect. The student list is read-only; the assistant management is the primary action.

Output:
- `headman-group.component.ts` — page component
- `assign-assistant-dialog.component.ts` — MatDialog with form
- `delete-assistant-dialog.component.ts` — MatDialog confirmation
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54/54-CONTEXT.md
@.planning/phases/54/54-UI-SPEC.md

@frontends/web-panel/src/app/features/headman/shared/headman-api.service.ts
@frontends/web-panel/src/app/features/admin/groups/groups-page.component.ts
</context>

<interfaces>
<!-- HeadmanApiService methods used in this plan -->
```typescript
getGroupMembers(page?: number, size?: number): Observable<any>   // all students
listAssistants(groupId: number): Observable<any>                  // existing assistants
assignAssistant(body: { studentId: number; permissions: string[] }): Observable<any>
revokeAssistant(id: number): Observable<any>
```

<!-- MatDialog pattern (from admin groups page) -->
```typescript
// Open dialog:
const ref = this.dialog.open(DialogComponent, {
  width: '520px',
  maxWidth: '95vw',
  data: { ... }
});
// After close:
ref.afterClosed().subscribe(result => {
  if (result) { this.reload(); }
});
```

<!-- MatSnackBar pattern -->
```typescript
this.snackBar.open('Помощник назначен.', undefined, { duration: 4000 });
```

<!-- HATEOAS unwrap defensive pattern -->
```typescript
const list = Object.values(resp?._embedded ?? {})[0] as any[] ?? [];
```

<!-- Permission constants -->
```typescript
// Permissions per UI-SPEC §2:
const PERMISSIONS = [
  { value: 'excuse_approve',       label: 'Одобрять тикеты о пропусках' },
  { value: 'late_checkin_approve', label: 'Одобрять запросы поздней отметки' },
  { value: 'subject_manage',       label: 'Управлять предметами группы' },
  { value: 'attendance_mark',      label: 'Проставлять посещаемость' },
];
// Permission chip labels per UI-SPEC §2:
const PERMISSION_CHIP_LABELS: Record<string, string> = {
  excuse_approve: 'Одобрение пропусков',
  late_checkin_approve: 'Поздние отметки',
  subject_manage: 'Управление предметами',
  attendance_mark: 'Проставление посещаемости',
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: HeadmanGroupComponent — student list + assistants section + delete dialog</name>
  <files>
    frontends/web-panel/src/app/features/headman/group/headman-group.component.ts
    frontends/web-panel/src/app/features/headman/group/delete-assistant-dialog.component.ts
  </files>
  <action>
    Create directory `frontends/web-panel/src/app/features/headman/group/`.

    FILE 1 — `delete-assistant-dialog.component.ts`:
    Simple standalone MatDialog confirmation component.
    Inject `MAT_DIALOG_DATA` to receive `{ fullName: string }`.
    Template:
    ```html
    <h2 mat-dialog-title>Удалить помощника?</h2>
    <mat-dialog-content>
      Студент {{ data.fullName }} потеряет все права помощника старосты.
    </mat-dialog-content>
    <mat-dialog-actions align="end">
      <button mat-stroked-button [mat-dialog-close]="false">Отмена</button>
      <button mat-flat-button class="btn-danger" [mat-dialog-close]="true">Удалить</button>
    </mat-dialog-actions>
    ```
    Style `.btn-danger` with `background: var(--accent-danger); color: white`.
    Imports: `MatDialogModule`, `MatButtonModule`.

    FILE 2 — `headman-group.component.ts`:
    Standalone component. Imports: `CommonModule`, `MatTableModule`, `MatButtonModule`, `MatIconModule`, `MatSnackBar`, `MatDialog`, `MatProgressSpinnerModule`, `RouterModule`, `AssignAssistantDialogComponent`, `DeleteAssistantDialogComponent`.

    SIGNALS:
    - `loading = signal(false)`
    - `error = signal<string | null>(null)`
    - `students = signal<any[]>([])`
    - `assistants = signal<any[]>([])`

    COMPUTED:
    - `assistantIds = computed(() => new Set(this.assistants().map(a => a.studentId)))`

    Inject: `HeadmanApiService`, `MatDialog`, `MatSnackBar`, `AuthService`.

    DATA LOADING (loadData method, called in ngOnInit):
    ```typescript
    loadData() {
      this.loading.set(true);
      this.error.set(null);
      const groupId = this.auth.currentUser()?.groupId;
      if (!groupId) {
        this.error.set('Не удалось загрузить список студентов. Попробуйте обновить страницу.');
        this.loading.set(false);
        return;
      }
      forkJoin([
        this.headmanApi.getGroupMembers(),
        this.headmanApi.listAssistants(groupId),
      ]).subscribe({
        next: ([membersResp, assistantsResp]) => {
          this.students.set(Object.values(membersResp?._embedded ?? {})[0] as any[] ?? []);
          this.assistants.set(Object.values(assistantsResp?._embedded ?? {})[0] as any[] ?? []);
          this.loading.set(false);
        },
        error: () => {
          this.error.set('Не удалось загрузить список студентов. Попробуйте обновить страницу.');
          this.loading.set(false);
        }
      });
    }
    ```

    TEMPLATE (per UI-SPEC §2):
    ```html
    <div class="page-stack" [@routeFade]>
      <!-- Page header -->
      <div class="page-header">
        <div>
          <span class="page-eyebrow">Старостат</span>
          <h1 class="page-title">Моя группа</h1>
        </div>
        <div class="page-header__actions">
          <button class="btn-brand" (click)="openAssignDialog()">
            <i class="ph ph-plus"></i> Назначить помощника
          </button>
        </div>
      </div>

      <!-- Section 1: Student list -->
      <div class="page-card page-card--flush">
        <div class="page-card__header">
          <h2>Студенты</h2>
        </div>

        @if (loading()) {
          <!-- 5 skeleton rows -->
          <div class="skeleton-row" *ngFor="let i of [1,2,3,4,5]" aria-hidden="true"></div>
          <span aria-live="polite" class="sr-only">Загрузка...</span>
        }
        @else if (error()) {
          <div class="page-error"><i class="ph ph-warning-circle"></i>{{ error() }}</div>
        }
        @else if (students().length === 0) {
          <div class="page-empty">
            <i class="ph-duotone ph-users"></i>
            <h3>Группа пуста</h3>
            <p>В вашей группе пока нет студентов.</p>
          </div>
        }
        @else {
          <table mat-table [dataSource]="students()">
            <ng-container matColumnDef="student">
              <th mat-header-cell *matHeaderCellDef>Студент</th>
              <td mat-cell *matCellDef="let s">
                <span class="student-name">{{ s.fullName }}</span>
                <span class="student-login">{{ s.login }}</span>
              </td>
            </ng-container>
            <ng-container matColumnDef="status">
              <th mat-header-cell *matHeaderCellDef>Статус</th>
              <td mat-cell *matCellDef="let s">
                @if (assistantIds().has(s.id)) {
                  <span class="role-chip role-chip--headman-assistant">Помощник старосты</span>
                } @else {
                  <span class="role-chip role-chip--student">Студент</span>
                }
              </td>
            </ng-container>
            <ng-container matColumnDef="action">
              <th mat-header-cell *matHeaderCellDef></th>
              <td mat-cell *matCellDef="let s"></td>
            </ng-container>
            <tr mat-header-row *matHeaderRowDef="['student','status','action']"></tr>
            <tr mat-row *matRowDef="let row; columns: ['student','status','action']"></tr>
          </table>
        }
      </div>

      <!-- Section 2: Assistant list -->
      <div class="page-card">
        <div class="page-card__header">
          <h2>Помощники старосты</h2>
        </div>
        @if (assistants().length === 0 && !loading()) {
          <p class="assistant-empty">Помощники не назначены</p>
        }
        @for (a of assistants(); track a.id) {
          <div class="assistant-item">
            <i class="ph ph-user-circle"></i>
            <div class="assistant-info">
              <span class="assistant-name">{{ a.fullName }}</span>
              <span class="assistant-login">{{ a.login }}</span>
              <div class="permission-chips">
                @for (p of a.permissions; track p) {
                  <span class="permission-chip">{{ permissionLabel(p) }}</span>
                }
              </div>
            </div>
            <button class="icon-btn icon-btn--danger"
                    [attr.aria-label]="'Удалить помощника ' + a.fullName"
                    (click)="openDeleteDialog(a)">
              <i class="ph ph-trash"></i>
            </button>
          </div>
        }
      </div>
    </div>
    ```

    METHOD `openAssignDialog()`:
    ```typescript
    openAssignDialog() {
      const ref = this.dialog.open(AssignAssistantDialogComponent, {
        width: '520px',
        maxWidth: '95vw',
        ariaLabel: 'Назначить помощника старосты',
        data: { students: this.students(), assistantIds: this.assistantIds() }
      });
      ref.afterClosed().subscribe(result => {
        if (result) { this.loadData(); }
      });
    }
    ```

    METHOD `openDeleteDialog(assistant)`:
    ```typescript
    openDeleteDialog(assistant: any) {
      const ref = this.dialog.open(DeleteAssistantDialogComponent, {
        data: { fullName: assistant.fullName },
        ariaLabel: 'Подтверждение удаления'
      });
      ref.afterClosed().subscribe(confirmed => {
        if (!confirmed) return;
        this.headmanApi.revokeAssistant(assistant.id).subscribe({
          next: () => {
            this.snackBar.open('Помощник удалён.', undefined, { duration: 4000 });
            this.loadData();
          },
          error: () => this.snackBar.open('Не удалось удалить помощника.', undefined, { duration: 6000 })
        });
      });
    }
    ```

    METHOD `permissionLabel(p: string): string`:
    Return the chip label from the PERMISSION_CHIP_LABELS constant.

    STYLES: `.student-login` in `--font-mono --text-xs --text-muted`. `.assistant-login` same. `.permission-chip` with `--bg-elevated border-radius: var(--radius-full) --text-xs`. `.icon-btn--danger:hover { color: var(--accent-danger) }`. Skeleton rows height 52px with `dashboard-shimmer` animation.

    Add `role-chip--headman-assistant` to component styles: `background: var(--accent-info-subtle, rgba(139,92,246,0.12)); color: var(--accent-info); font-size: var(--text-xs); font-weight: 600; padding: 2px 8px; border-radius: var(--radius-full)`.
  </action>
  <verify>
    <automated>cd C:/Users/maksd/IntelliJIDEA/rutcampustrack/frontends/web-panel && npx tsc --noEmit 2>&1 | grep -i error | head -20</automated>
  </verify>
  <done>
    TypeScript compiles without errors. `headman-group.component.ts` and `delete-assistant-dialog.component.ts` exist. Student list table renders with name/login/role chip columns. Assistants section has permission chips and delete icon. `openDeleteDialog` calls `revokeAssistant` after confirmation.
  </done>
</task>

<task type="auto">
  <name>Task 2: AssignAssistantDialogComponent — student select + permission checkboxes</name>
  <files>
    frontends/web-panel/src/app/features/headman/group/assign-assistant-dialog.component.ts
  </files>
  <action>
    Create `assign-assistant-dialog.component.ts` as a standalone MatDialog component.

    DIALOG DATA: `{ students: any[]; assistantIds: Set<number> }` via `MAT_DIALOG_DATA`.
    The student select shows only students who are NOT already assistants and NOT the headman themselves (filter: `students.filter(s => !assistantIds.has(s.id) && s.id !== currentUser.id)`).

    FORM (ReactiveFormsModule or template-driven):
    Use `FormGroup` with:
    - `studentId: FormControl<number | null>` — required
    - `permissions: FormGroup` with one `FormControl<boolean>` per permission key

    Template:
    ```html
    <h2 mat-dialog-title>Назначить помощника старосты</h2>
    <mat-dialog-content>
      <form [formGroup]="form" (ngSubmit)="onSubmit()">
        <!-- Student select -->
        <mat-form-field appearance="outline" style="width:100%">
          <mat-label>Выберите студента</mat-label>
          <mat-select formControlName="studentId" aria-label="Выберите студента" required>
            @for (s of eligibleStudents; track s.id) {
              <mat-option [value]="s.id">{{ s.fullName }} ({{ s.login }})</mat-option>
            }
          </mat-select>
          <mat-error *ngIf="form.get('studentId')?.invalid">Выберите студента</mat-error>
        </mat-form-field>

        <!-- Permission checkboxes -->
        <p class="permissions-label">Права помощника</p>
        <div formGroupName="permissions">
          @for (p of permissionList; track p.value) {
            <div class="permission-row" style="min-height:44px">
              <mat-checkbox [formControlName]="p.value">{{ p.label }}</mat-checkbox>
            </div>
          }
        </div>
        <mat-error *ngIf="showPermissionError" class="permission-error">
          Выберите хотя бы одно право
        </mat-error>
        <p class="permissions-hint">
          Можно выбрать несколько прав. Права можно изменить позже через переназначение.
        </p>

        @if (apiError) {
          <div class="page-error">{{ apiError }}</div>
        }
      </form>
    </mat-dialog-content>
    <mat-dialog-actions align="end">
      <button mat-stroked-button type="button" [mat-dialog-close]="false">Отмена</button>
      <button class="btn-brand" type="button" [disabled]="submitting" (click)="onSubmit()">
        @if (submitting) { <mat-spinner diameter="16"></mat-spinner> }
        Назначить помощника
      </button>
    </mat-dialog-actions>
    ```

    SIGNALS/FIELDS:
    - `submitting = false`
    - `showPermissionError = false`
    - `apiError: string | null = null`
    - `eligibleStudents` — filtered in constructor from `data.students`

    `onSubmit()`:
    ```typescript
    onSubmit() {
      this.showPermissionError = false;
      if (this.form.get('studentId')?.invalid) {
        this.form.markAllAsTouched();
        return;
      }
      const selectedPermissions = Object.entries(this.form.value.permissions ?? {})
        .filter(([_, v]) => v)
        .map(([k]) => k);
      if (selectedPermissions.length === 0) {
        this.showPermissionError = true;
        return;
      }
      this.submitting = true;
      this.apiError = null;
      this.headmanApi.assignAssistant({
        studentId: this.form.value.studentId!,
        permissions: selectedPermissions
      }).subscribe({
        next: () => {
          this.submitting = false;
          this.snackBar.open('Помощник назначен.', undefined, { duration: 4000 });
          this.dialogRef.close(true);
        },
        error: () => {
          this.submitting = false;
          this.apiError = 'Не удалось назначить помощника. Попробуйте ещё раз.';
        }
      });
    }
    ```

    IMPORTS: `MatDialogModule`, `MatButtonModule`, `MatSelectModule`, `MatCheckboxModule`, `MatFormFieldModule`, `MatProgressSpinnerModule`, `ReactiveFormsModule`, `MatSnackBar`, `CommonModule`.
    Inject: `HeadmanApiService`, `MatSnackBar`, `MatDialogRef`, `MAT_DIALOG_DATA`, `AuthService`.

    PERMISSION_LIST constant inside this component:
    ```typescript
    readonly permissionList = [
      { value: 'excuse_approve',       label: 'Одобрять тикеты о пропусках' },
      { value: 'late_checkin_approve', label: 'Одобрять запросы поздней отметки' },
      { value: 'subject_manage',       label: 'Управлять предметами группы' },
      { value: 'attendance_mark',      label: 'Проставлять посещаемость' },
    ];
    ```
  </action>
  <verify>
    <automated>cd C:/Users/maksd/IntelliJIDEA/rutcampustrack/frontends/web-panel && npx tsc --noEmit 2>&1 | grep -i error | head -20</automated>
  </verify>
  <done>
    TypeScript compiles without errors. `assign-assistant-dialog.component.ts` exists with ReactiveFormsModule form, student MatSelect filtered to non-assistants, 4 permission MatCheckboxes (44px rows), submit calls `assignAssistant()`, API error inline, permission validation error "Выберите хотя бы одно право". All UI-SPEC §2 copywriting matches exactly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dialog data → API | studentId and permissions array come from dialog form; validated before submission |
| Assistant deletion | No undo — confirmation dialog required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-09 | Elevation of Privilege | `assignAssistant` — only headman should call | accept | Backend `AssistantService.requireHeadman()` is the enforcement layer. Angular guard only controls route access. |
| T-54-10 | Tampering | Permission array in POST body | accept | Backend validates `permissions` enum values. Angular UI presents fixed checkboxes; user cannot inject arbitrary permission values through the form UI. |
| T-54-11 | Information Disclosure | Student list in assign dialog | accept | Dialog only shows students from the headman's own group (loaded via `GET /api/academic/groups/my/members` which is scoped to the headman's group by gateway JWT). |
</threat_model>

<verification>
TypeScript compilation:
```bash
cd frontends/web-panel && npx tsc --noEmit
```
Visual: navigate to `/headman/group` as headman user — student table loads, "Назначить помощника" opens dialog with student select and checkboxes.
</verification>

<success_criteria>
1. `headman-group.component.ts` renders student list table with columns: Студент (name + login), Статус (chip), Действие (empty)
2. Assistants section shows list with permission chips; delete icon calls `revokeAssistant` after confirmation dialog
3. `assign-assistant-dialog.component.ts` has `MatSelect` for student selection (filtered to non-assistants), 4 `MatCheckbox` rows with 44px min-height
4. Permission validation: "Выберите хотя бы одно право" shown if no checkbox selected on submit
5. Snackbar "Помощник назначен." on success; inline error on API failure
6. `delete-assistant-dialog.component.ts` uses `[mat-dialog-close]="true/false"` pattern
7. All UI-SPEC §2 copywriting is exact (no paraphrasing)
8. TypeScript compiles without errors; existing vitest tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/54-headman-web-cabinet-group-management-subjects/54-04-SUMMARY.md`
</output>
