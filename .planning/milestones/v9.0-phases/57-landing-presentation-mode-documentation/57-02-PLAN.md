---
phase: 57-landing-presentation-mode-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - docs/url-layout.md
  - docs/job-stories.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - DOCS-v9-01
  - DOCS-v9-02
  - DOCS-v9-03
  - DOCS-v9-04
tags:
  - documentation
  - sync
must_haves:
  truths:
    - "CLAUDE.md раздел Текущий статус отражает v1.0-v8.0 ЗАВЕРШЕНА и v9.0 В РАБОТЕ (фазы 49-57); устаревшая строка v6.0 В РАБОТЕ отсутствует"
    - "docs/url-layout.md содержит раздел Production Path Routing v9.0 с полной таблицей маршрутов /, /login, /app/, /presentation/, /admin/*, /teacher/*, /student/*, /headman/*"
    - "docs/job-stories.md содержит все истории JS-STUDENT-WEB-01..10 и JS-HEADMAN-WEB-01..08"
    - ".planning/PROJECT.md отражает v9.0 ship-ready после phase 57 и расширяет Recently Validated v9.0"
  artifacts:
    - path: CLAUDE.md
      provides: "Актуальный project status block + URL Layout section"
      contains: "v9.0"
    - path: docs/url-layout.md
      provides: "Production routing table v9.0"
      contains: "Production Path Routing"
    - path: docs/job-stories.md
      provides: "Web cabinet user stories"
      contains: "JS-STUDENT-WEB-01"
    - path: .planning/PROJECT.md
      provides: "v9.0 milestone closing entry"
      contains: "ship-ready after Phase 57"
  key_links:
    - from: CLAUDE.md status block
      to: ROADMAP.md фазы 49-57
      via: явное упоминание v9.0 и номеров фаз
      pattern: "v9\\.0.*49-57|фазы 49-57"
    - from: docs/url-layout.md Production Path Routing
      to: nginx config + INFRA-v9-01..04 decisions
      via: таблица путей с ссылками на requirement IDs
      pattern: "/headman/\\*.*STUDENT|INFRA-v9-01"
---

<objective>
Синхронизировать project documentation с v9.0 state: фикс stale status в CLAUDE.md, добавление production routing таблицы в url-layout.md, расширение job-stories.md web-cabinet историями, обновление PROJECT.md. Все правки — чистый Markdown, без кода.

Purpose: Закрывает DOCS-v9-01..04. Без этих правок любой новый контрибьютор / Claude session стартует с устаревшим контекстом ("v6.0 В РАБОТЕ").
Output: 4 обновлённых Markdown файла.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/57-landing-presentation-mode-documentation/57-RESEARCH.md
@CLAUDE.md
@docs/url-layout.md
@docs/job-stories.md

<interfaces>
Актуальные строки для правок (grep 2026-04-13):

CLAUDE.md:
  строка 9-13: v1.0-v5.0 ЗАВЕРШЕНА (актуально, не трогать тексты)
  строка 14:  - **v6.0**: В РАБОТЕ (PWA + Web Push) — STALE, v6.0 завершён 2026-04-06

docs/url-layout.md (39 строк): только dev-порты и контейнеры. Production routing отсутствует.

docs/job-stories.md (208 строк):
  строка 80: ## 3. Староста (JS-HEADMAN-01..19 — Telegram/PWA flows)
  Секций JS-STUDENT-WEB-* и JS-HEADMAN-WEB-* НЕТ.

.planning/PROJECT.md:
  строка 12:  ## Current Milestone: v9.0 ...
  строка 55:  ## Shipped Milestones
  строка 200: ### Recently Validated (v9.0) — частично заполнено phase 49-51
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Обновить CLAUDE.md project status block + URL Layout (DOCS-v9-01)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (строки 1-30 — статус-блок)
    - .planning/ROADMAP.md (Milestones overview с датами)
  </read_first>
  <action>
    Заменить точечно блок строк 7-15 файла CLAUDE.md (раздел `## Текущий статус` целиком вместе с 6 milestone строками) на новый блок:

    ```markdown
    ## Текущий статус

    - **v1.0**: ЗАВЕРШЕНА (Auth Service + API Gateway) — фазы 1.1-1.4, 26 тестов
    - **v2.0**: ЗАВЕРШЕНА (Academic Service) — фазы 5-9, 50 тестов
    - **v3.0**: ЗАВЕРШЕНА (Schedule Service) — фазы 10-14, 55 тестов
    - **v4.0**: ЗАВЕРШЕНА (Attendance Service) — фазы 15-19, ~95 тестов
    - **v5.0**: ЗАВЕРШЕНА (Notification Service — Web + Bot) — фазы 20-26, ~128 тестов (20 Java + 108 Python)
    - **v6.0**: ЗАВЕРШЕНА (PWA + Web Push) — фазы 27-32
    - **v7.0**: ЗАВЕРШЕНА (Frontends — Mini App, Web Panel, Landing) — фазы 33-40
    - **v8.0**: ЗАВЕРШЕНА (CI/CD, Deployment & Documentation) — фазы 41-48
    - **v9.0**: В РАБОТЕ (Frontend Unification — Single Login & Role-Based Web Clients) — фазы 49-57, завершается фазой 57

    ### URL Layout (v9.0)

    Production reverse-proxy nginx на `https://ruttrack.site`:

    | Путь | Обслуживает | Назначение |
    |------|-------------|------------|
    | `/` | 301 → `/login` | INFRA-v9-01 — единая точка входа |
    | `/login` | web-panel (Angular SPA, baseHref `/`) | AUTH-v9-01 |
    | `/admin/*` | web-panel lazy feature | роль ADMIN |
    | `/teacher/*` | web-panel lazy feature | роль TEACHER |
    | `/student/*` | web-panel lazy feature | роль STUDENT (включая headman) |
    | `/headman/*` | web-panel lazy feature | STUDENT + `is_headman=true` |
    | `/app/` | PWA (React + Vite) | INFRA-v9-03 — мобильный клиент |
    | `/presentation/` | Landing (static HTML) | INFRA-v9-02 — описание проекта |
    | `/api/*` | API Gateway (proxy pass) | backend REST + STOMP WebSocket |

    Полная таблица: `docs/url-layout.md`.
    ```

    НЕ трогать остальные разделы CLAUDE.md (Архитектура, Правила кодирования, Структура репозитория, Роли, Статусы, Бизнес-правила, Запуск, Документация, Инструкция Claude Code).
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && bash -c '! grep -q "v6.0\*\*: В РАБОТЕ" CLAUDE.md' && bash -c '[ "$(grep -c "v9.0" CLAUDE.md)" -ge 2 ]' && grep -q "## URL Layout" CLAUDE.md && grep -q "/presentation/" CLAUDE.md && grep -q "/headman/\*" CLAUDE.md && bash -c '[ "$(grep -cE "^- \*\*v[0-9]\.0\*\*: ЗАВЕРШЕНА" CLAUDE.md)" -ge 8 ]'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "v6.0\*\*: В РАБОТЕ" CLAUDE.md` → 0
    - `grep -c "v9.0" CLAUDE.md` ≥ 2
    - `grep -q "## URL Layout" CLAUDE.md` → true
    - `grep -q "/presentation/" CLAUDE.md` → true
    - `grep -q "/headman/\*" CLAUDE.md` → true
    - `grep -q "/admin/\*" CLAUDE.md` → true
    - `grep -q "/app/" CLAUDE.md` → true
    - Минимум 8 строк формата `- **vX.0**: ЗАВЕРШЕНА` (v1.0-v8.0)
    - Существующие разделы Архитектура / Правила кодирования / Структура репозитория сохранены
  </acceptance_criteria>
  <done>
    CLAUDE.md раздел Текущий статус отражает все 9 milestone (v1.0-v8.0 ЗАВЕРШЕНА, v9.0 В РАБОТЕ) + раздел URL Layout с production routes. Stale строка `v6.0 В РАБОТЕ` удалена.
  </done>
</task>

<task type="auto">
  <name>Task 2: Дополнить docs/url-layout.md разделом Production Path Routing (DOCS-v9-02)</name>
  <files>docs/url-layout.md</files>
  <read_first>
    - docs/url-layout.md (текущие 39 строк)
    - .planning/phases/57-landing-presentation-mode-documentation/57-RESEARCH.md (Example 4 — таблица)
    - .planning/REQUIREMENTS.md (INFRA-v9-01..04)
  </read_first>
  <action>
    ДОБАВИТЬ в конец `docs/url-layout.md` (после существующих строк, не удаляя их) следующие разделы:

    ```markdown

    ---

    ## Production Path Routing (v9.0)

    Decided: Phase 49-50 (v9.0). Nginx reverse proxy at `https://ruttrack.site`.

    | Path | Served By | Requirement | Notes |
    |------|-----------|-------------|-------|
    | `/` | 301 redirect → `/login` | INFRA-v9-01 | Единая точка входа. Корень больше не отдаёт PWA. |
    | `/login` | web-panel SPA (Angular, baseHref `/`) | AUTH-v9-01, INFRA-v9-04 | Login form для всех ролей. |
    | `/admin/*` | web-panel SPA (lazy feature) | — | Роль ADMIN после login. |
    | `/teacher/*` | web-panel SPA (lazy feature) | — | Роль TEACHER. |
    | `/student/*` | web-panel SPA (lazy feature) | AUTH-v9-05 | Роль STUDENT (headman тоже проходит studentGuard). |
    | `/headman/*` | web-panel SPA (lazy feature) | AUTH-v9-04 | STUDENT + is_headman=true (headmanGuard). |
    | `/app/` | PWA (React + Vite, rct-pwa-nginx) | INFRA-v9-03 | Мобильный клиент RutTrack. |
    | `/presentation/` | Landing (static HTML, rct-landing-nginx) | INFRA-v9-02, LAND-v9-01 | Описание проекта; доступ только по прямой ссылке. |
    | `/api/*` | API Gateway (localhost:8080 внутри сети) | — | REST + gRPC (обратный прокси из backend). |
    | `/api/ws` | Notification Web (notification-web:9094) | — | STOMP WebSocket endpoint. |

    ### Пост-login routing

    | Роль | Landing dashboard | Guard |
    |------|-------------------|-------|
    | ADMIN | `/admin/dashboard` | roleGuard(['ADMIN']) |
    | TEACHER | `/teacher/dashboard` | roleGuard(['TEACHER']) |
    | STUDENT (is_headman=false) | `/student/dashboard` | studentGuard |
    | STUDENT (is_headman=true) | `/headman/dashboard` | headmanGuard (+ passes studentGuard) |

    Реализация: `AuthService.resolveDashboardFor()` — single source of truth (Phase 50).

    ### Deprecated paths

    - `/landing/` — удалён в Phase 49 (INFRA-v9-02), перенесён в `/presentation/`.
    - Корень `/` больше не отдаёт PWA — PWA теперь на `/app/` (INFRA-v9-01, INFRA-v9-03).
    ```

    НЕ перемещать и НЕ модифицировать существующие dev-port секции (Port Assignments, Container Names, Gateway CORS Origins, Notes).
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && grep -q "## Production Path Routing" docs/url-layout.md && grep -q "/login" docs/url-layout.md && grep -q "/admin/\*" docs/url-layout.md && grep -q "/teacher/\*" docs/url-layout.md && grep -q "/student/\*" docs/url-layout.md && grep -q "/headman/\*" docs/url-layout.md && grep -q "/app/" docs/url-layout.md && grep -q "/presentation/" docs/url-layout.md && grep -q "INFRA-v9-01" docs/url-layout.md && grep -q "## Port Assignments" docs/url-layout.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "## Production Path Routing" docs/url-layout.md` → true
    - Все 8 production paths присутствуют: `/login`, `/admin/*`, `/teacher/*`, `/student/*`, `/headman/*`, `/app/`, `/presentation/`, `/api/*`
    - Хотя бы один INFRA-v9-XX requirement ID процитирован для traceability
    - Существующая секция `## Port Assignments` сохранена
    - Существующая секция `## Container Names` сохранена
    - Новая таблица содержит маппинг роль → dashboard
  </acceptance_criteria>
  <done>
    `docs/url-layout.md` содержит новый раздел Production Path Routing v9.0 с полной таблицей nginx routes, маппингом ролей на dashboards и списком deprecated путей. Существующие dev-port секции не тронуты.
  </done>
</task>

<task type="auto">
  <name>Task 3: Расширить docs/job-stories.md JS-STUDENT-WEB и JS-HEADMAN-WEB (DOCS-v9-03)</name>
  <files>docs/job-stories.md</files>
  <read_first>
    - docs/job-stories.md (формат существующих историй: `**JS-XXX-NN**: Когда..., я хочу..., чтобы...`)
    - .planning/REQUIREMENTS.md (STU-WEB-01..10 и HEAD-WEB-01..08 — тексты требований)
    - .planning/ROADMAP.md (Phase 51-55 Goals)
  </read_first>
  <action>
    ДОБАВИТЬ в конец `docs/job-stories.md` два новых раздела (после строки 208). Не модифицировать существующие истории.

    ```markdown

    ---

    ## 4. Web-кабинет студента (STUDENT, v9.0)

    Истории описывают функциональность Angular web-panel `/student/*` (фазы 51-53). Дополняют существующие PWA/Telegram потоки студента.

    **JS-STUDENT-WEB-01**: Когда я открываю `/student/dashboard`, я хочу видеть расписание на сегодня, ближайшую активную пару и предупреждения по предметам в красной зоне, чтобы быстро понять, что мне нужно сделать. (STU-WEB-01)

    **JS-STUDENT-WEB-02**: Когда я нахожусь на `/student/schedule`, я хочу переключаться по неделям и дням и видеть детали пар (предмет, время, аудитория, статус), чтобы планировать своё время с тем же UX, что и в PWA. (STU-WEB-02)

    **JS-STUDENT-WEB-03**: Когда идёт активная пара и я открываю `/student/checkin`, я хочу нажать «Отметиться», чтобы геопозиция была отправлена и без перезагрузки появилось подтверждение через STOMP-сокет. (STU-WEB-03)

    **JS-STUDENT-WEB-04**: Когда я открываю `/student/homework`, я хочу видеть все задания группы и одним кликом отмечать их как выполненные с оптимистичным UI и откатом при ошибке сервера. (STU-WEB-04)

    **JS-STUDENT-WEB-05**: Когда я смотрю `/student/stats`, я хочу видеть процент посещаемости по каждому предмету с подсветкой красной зоны и хотя бы одной диаграммой, чтобы оценить риски до экзамена. (STU-WEB-05)

    **JS-STUDENT-WEB-06**: Когда я открываю `/student/notifications`, я хочу видеть лог прошедших уведомлений (lesson.started, lesson.cancelled, homework.published, attendance.marked) с типом и временем, чтобы не потерять важное событие. (STU-WEB-06)

    **JS-STUDENT-WEB-07**: Когда мне нужно подать excuse-тикет, я хочу на `/student/excuses` нажать «Подать тикет», выбрать пары, прикрепить файл и отправить, чтобы файлы ушли через Telegram-бот старосте без хранения у нас. (STU-WEB-07)

    **JS-STUDENT-WEB-08**: Когда я забыл отметиться на пару и она получила статус `absent`, я хочу на `/student/late-checkin` нажать «Запросить» — староста увидит запрос и сможет одобрить. (STU-WEB-08)

    **JS-STUDENT-WEB-09**: Когда я хочу сменить пароль, я хочу на `/student/profile` ввести старый и новый пароль, чтобы система проверила старый и сохранила новый без перезагрузки страницы. (STU-WEB-09)

    **JS-STUDENT-WEB-10**: Когда я первый раз залогинился в web-панели как STUDENT, я хочу увидеть ненавязчивый баннер «Установите RutTrack на главный экран» с кнопкой на `/app/`, который можно закрыть один раз навсегда — без принудительного редиректа в PWA. (STU-WEB-10)

    ---

    ## 5. Web-кабинет старосты (STUDENT + is_headman=true, v9.0)

    Истории описывают функциональность Angular web-panel `/headman/*` (фазы 54-55). Дополняют JS-HEADMAN-01..19 (Telegram/PWA flows).

    **JS-HEADMAN-WEB-01**: Когда я зашёл в систему как староста, я хочу иметь доступ ко всем `/student/*` страницам в дополнение к `/headman/*`, чтобы пользоваться своим студенческим интерфейсом без повторного логина. (HEAD-WEB-01)

    **JS-HEADMAN-WEB-02**: Когда я открываю `/headman/dashboard`, я хочу видеть число студентов группы, текущую активную пару и счётчики ожидающих excuse-тикетов и late-check-in запросов, чтобы сразу понять объём работы на день. (HEAD-WEB-02)

    **JS-HEADMAN-WEB-03**: Когда мне нужно делегировать работу, я хочу на `/headman/group` видеть весь список группы, выбрать студента и назначить его помощником с конкретными правами (mark_attendance / manage_excuses / manage_homework / cancel_lessons / view_stats), чтобы делить нагрузку. (HEAD-WEB-03)

    **JS-HEADMAN-WEB-04**: Когда формируется семестр, я хочу на `/headman/subjects` создать предметы своей группы и назначить им преподавателей из общего пула, чтобы расписание начало работать. (HEAD-WEB-04)

    **JS-HEADMAN-WEB-05**: Когда я открываю `/headman/journal`, я хочу выбрать предмет, увидеть матрицу студенты × пары и кликом по ячейке циклически менять статус (present → absent → excused → free_attendance → cancelled), чтобы массово отметить пропуски за прошедший период. (HEAD-WEB-05)

    **JS-HEADMAN-WEB-06**: Когда студент подал excuse-тикет, я хочу на `/headman/excuses` одним кликом одобрить или отклонить его с оптимистичным обновлением списка, чтобы быстро разгрести очередь. (HEAD-WEB-06)

    **JS-HEADMAN-WEB-07**: Когда студент забыл отметиться, я хочу на `/headman/late-checkin` одним кликом одобрить запрос — статус автоматически меняется с `absent` на `present` без перезагрузки. (HEAD-WEB-07)

    **JS-HEADMAN-WEB-08**: Когда я смотрю `/headman/stats`, я хочу видеть посещаемость группы по каждому предмету с цветовой индикацией красной зоны и редактировать порог per-subject inline без полной перезагрузки. (HEAD-WEB-08)
    ```

    Не менять существующие 208 строк.
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && bash -c '[ "$(grep -cE "JS-STUDENT-WEB-" docs/job-stories.md)" -ge 10 ]' && bash -c '[ "$(grep -cE "JS-HEADMAN-WEB-" docs/job-stories.md)" -ge 8 ]' && grep -q "## 4. Web-кабинет студента" docs/job-stories.md && grep -q "## 5. Web-кабинет старосты" docs/job-stories.md && grep -q "JS-STUDENT-WEB-01" docs/job-stories.md && grep -q "JS-STUDENT-WEB-10" docs/job-stories.md && grep -q "JS-HEADMAN-WEB-01" docs/job-stories.md && grep -q "JS-HEADMAN-WEB-08" docs/job-stories.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "JS-STUDENT-WEB-" docs/job-stories.md` ≥ 10
    - `grep -cE "JS-HEADMAN-WEB-" docs/job-stories.md` ≥ 8
    - Все ID JS-STUDENT-WEB-01..10 присутствуют каждый ровно 1 раз
    - Все ID JS-HEADMAN-WEB-01..08 присутствуют каждый ровно 1 раз
    - Существующие истории JS-ADMIN-*, JS-TEACHER-*, JS-HEADMAN-01..19 не модифицированы (только append в конец)
    - Каждая web-история содержит ссылку на родительский requirement: `(STU-WEB-NN)` или `(HEAD-WEB-NN)`
  </acceptance_criteria>
  <done>
    `docs/job-stories.md` содержит 18 новых web-cabinet историй (10 STUDENT + 8 HEADMAN), каждая в формате job-story с ссылкой на родительский requirement. Существующие 208 строк не тронуты.
  </done>
</task>

<task type="auto">
  <name>Task 4: Обновить .planning/PROJECT.md — v9.0 ship-ready запись (DOCS-v9-04)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (строки 12-50 Current Milestone, строки 200-208 Recently Validated v9.0)
    - .planning/ROADMAP.md (v9.0 Phase 49-57 итоговое состояние)
  </read_first>
  <action>
    Точечная правка `.planning/PROJECT.md`. Цель — отметить v9.0 как ship-ready после Phase 57. Не делать big-bang переписывания.

    Две правки:

    Правка A. В разделе `### Recently Validated (v9.0)` (около строки 200) ДОБАВИТЬ в конец списка две новые строки (после последней `✓ AUTH-v9-07: vitest regression suite ...`):

    ```markdown
    - ✓ INFRA-v9-04, AUTH-v9-01..07, STU-WEB-01..10, HEAD-WEB-01..08, PWA-HEAD-01..04: завершены в фазах 50-56 (см. ROADMAP)
    - ✓ LAND-v9-02, LAND-v9-04, LAND-v9-05, DOCS-v9-01..04: завершены в Phase 57 — landing GSAP-секция «Как работает система», 4 роли описаны включая HEADMAN, документация синхронизирована
    ```

    Правка B. В разделе `## Current Milestone: v9.0 ...` (строка 12) ДОБАВИТЬ в конец этого блока (перед `## Shipped Milestones`) одну новую строку:

    ```markdown

    **Status:** v9.0 ship-ready after Phase 57. Будет перемещён в Shipped Milestones при следующем `/gsd-close-milestone v9.0`.
    ```

    НЕ перемещать v9.0 в `## Shipped Milestones` секцию — это работа `/gsd-close-milestone`, не текущей фазы.
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && grep -q "ship-ready after Phase 57" .planning/PROJECT.md && grep -q "LAND-v9-02, LAND-v9-04, LAND-v9-05, DOCS-v9-01..04" .planning/PROJECT.md && grep -q "Recently Validated (v9.0)" .planning/PROJECT.md && grep -q "## Shipped Milestones" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "ship-ready after Phase 57" .planning/PROJECT.md` → true
    - `grep -q "LAND-v9-02, LAND-v9-04, LAND-v9-05, DOCS-v9-01..04" .planning/PROJECT.md` → true
    - Существующая секция `### Recently Validated (v9.0)` сохранена и расширена
    - Существующая секция `## Shipped Milestones` НЕ получила записи v9.0 (это для `/gsd-close-milestone`)
    - Заголовок `## Current Milestone: v9.0 ...` остался на месте
  </acceptance_criteria>
  <done>
    `.planning/PROJECT.md` отражает завершение Phase 57 и готовность v9.0 к переводу в Shipped через `/gsd-close-milestone`. Recently Validated v9.0 список дополнен LAND и DOCS требованиями.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo author ↔ repo readers | Документация — public read-only; нет integrity controls (это git, не CMS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-06 | Information Disclosure | url-layout.md публикует production routing | accept | Production routing уже виден из nginx config + landing CTA. Никаких внутренних API endpoint'ов или ключей не раскрывается. |
| T-57-07 | Tampering | Stale CLAUDE.md status вводит в заблуждение Claude session / контрибьютора | mitigate | Task 1 удаляет stale `v6.0 В РАБОТЕ` и синхронизирует статус с реальностью |
| T-57-08 | Repudiation | Web stories в job-stories.md без traceability к requirements | mitigate | Каждая JS-*-WEB-NN story содержит ссылку `(STU-WEB-NN)` или `(HEAD-WEB-NN)` для двусторонней traceability |
</threat_model>

<verification>
1. `! grep -q "v6.0\*\*: В РАБОТЕ" CLAUDE.md` (stale удалён)
2. `grep -q "## URL Layout" CLAUDE.md` (новый раздел)
3. `grep -q "## Production Path Routing" docs/url-layout.md` (DOCS-v9-02)
4. `grep -cE "JS-(STUDENT|HEADMAN)-WEB-" docs/job-stories.md` ≥ 18 (DOCS-v9-03)
5. `grep -q "ship-ready after Phase 57" .planning/PROJECT.md` (DOCS-v9-04)
6. `git status --porcelain` показывает только 4 файла из frontmatter
</verification>

<success_criteria>
- [ ] CLAUDE.md статус-блок отражает v1.0-v8.0 ЗАВЕРШЕНА и v9.0 В РАБОТЕ
- [ ] CLAUDE.md содержит раздел `## URL Layout (v9.0)` с таблицей путей
- [ ] docs/url-layout.md содержит раздел `## Production Path Routing (v9.0)` с 8+ путями
- [ ] docs/job-stories.md содержит JS-STUDENT-WEB-01..10 и JS-HEADMAN-WEB-01..08 (18 историй)
- [ ] .planning/PROJECT.md помечает v9.0 ship-ready и расширяет Recently Validated v9.0
- [ ] Все существующие разделы каждого файла сохранены
</success_criteria>

<output>
After completion, create `.planning/phases/57-landing-presentation-mode-documentation/57-02-SUMMARY.md`
</output>
