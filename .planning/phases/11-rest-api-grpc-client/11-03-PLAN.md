---
phase: 11-rest-api-grpc-client
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/LessonService.java
  - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/LessonController.java
  - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/LessonAssembler.java
  - services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/LessonWithItem.java
  - services/schedule-service/schedule-app/src/test/java/ru/rutcampustrack/schedule/integration/LessonApiTest.java
  - services/schedule-service/schedule-app/src/test/java/ru/rutcampustrack/schedule/integration/ScheduleViewTest.java
autonomous: true
requirements:
  - LSSN-04
  - LSSN-05
  - LSSN-06
  - LSSN-07
  - VIEW-01
  - VIEW-02
must_haves:
  truths:
    - "Headman can cancel a planned lesson with required reason"
    - "Headman can restore a cancelled lesson back to planned"
    - "Headman can mass-cancel all planned lessons in a date range for their group"
    - "Headman can toggle geo-block on a specific lesson"
    - "Any authenticated user can view group schedule for a date range"
    - "Schedule view response includes fields from both Lesson and ScheduleItem entities"
    - "Restoring a non-cancelled lesson returns 422"
    - "Cancelling a non-planned lesson returns 422"
  artifacts:
    - path: "services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/LessonService.java"
      provides: "Lesson operations + schedule view business logic"
      contains: "requireHeadmanForGroup"
    - path: "services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/LessonController.java"
      provides: "REST controller implementing LessonApi"
      contains: "implements LessonApi"
    - path: "services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/LessonAssembler.java"
      provides: "HATEOAS assembler for LessonResponse"
      contains: "EntityModel<LessonResponse>"
    - path: "services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/LessonWithItem.java"
      provides: "App-internal record pairing Lesson + ScheduleItem"
      contains: "record LessonWithItem"
    - path: "services/schedule-service/schedule-app/src/test/java/ru/rutcampustrack/schedule/integration/LessonApiTest.java"
      provides: "Integration tests for LSSN-04..07"
      contains: "class LessonApiTest"
    - path: "services/schedule-service/schedule-app/src/test/java/ru/rutcampustrack/schedule/integration/ScheduleViewTest.java"
      provides: "Integration tests for VIEW-01..02"
      contains: "class ScheduleViewTest"
  key_links:
    - from: "LessonController.java"
      to: "LessonApi.java"
      via: "implements LessonApi"
      pattern: "implements LessonApi"
    - from: "LessonService.java"
      to: "ScheduleItemRepository"
      via: "group ownership lookup for headman authorization"
      pattern: "scheduleItemRepository\\.findById"
    - from: "LessonService.java"
      to: "LessonRepository"
      via: "findByScheduleItemIdInAndDateBetweenAndStatusIn for view + mass-cancel"
      pattern: "findByScheduleItemIdInAndDateBetweenAndStatusIn"
---

<objective>
Implement Lesson operations (cancel, restore, mass-cancel, geo-block toggle) and Schedule View endpoint with integration tests.

Purpose: Delivers lesson management (LSSN-04..07) and the read-only schedule view (VIEW-01..02) — the last REST endpoints for Phase 11.

Output: Working lesson operation endpoints + schedule view endpoint with 8+ integration tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-rest-api-grpc-client/11-CONTEXT.md
@.planning/phases/11-rest-api-grpc-client/11-RESEARCH.md
@.planning/phases/11-rest-api-grpc-client/11-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

LessonApi contract (from schedule-api-contract):
- PATCH /schedule/lessons/{id}/cancel -> ResponseEntity<EntityModel<LessonResponse>> cancelLesson(@PathVariable Long id, @Valid @RequestBody CancelLessonRequest)
- PATCH /schedule/lessons/{id}/restore -> ResponseEntity<EntityModel<LessonResponse>> restoreLesson(@PathVariable Long id)
- POST /schedule/lessons/mass-cancel -> ResponseEntity<MassCancelResponse> massCancelLessons(@Valid @RequestBody MassCancelRequest)
- PATCH /schedule/lessons/{id}/geo-block -> ResponseEntity<EntityModel<LessonResponse>> toggleGeoBlock(@PathVariable Long id, @Valid @RequestBody GeoBlockRequest)
- GET /schedule/groups/{groupId}/lessons?dateFrom&dateTo&status -> ResponseEntity<PagedModel<EntityModel<LessonResponse>>> getLessons(...)

CancelLessonRequest: record { @NotBlank @Size(max=512) String reason }
MassCancelRequest: record { @NotNull Long groupId, @NotNull LocalDate dateFrom, @NotNull LocalDate dateTo, @NotBlank @Size(max=512) String reason }
MassCancelResponse: record { int cancelledCount }
GeoBlockRequest: record { @NotNull Boolean blocked }
LessonResponse extends RepresentationModel: id, scheduleItemId, groupId, subjectId, teacherId, date, status, dayOfWeek, lessonNumber, startTime, endTime, weekType, room, geoBlocked, cancelReason, createdAt

Entities:
- Lesson: id, scheduleItemId(Long), date(LocalDate), status(LessonStatus), isGeoBlocked(boolean), cancelReason(String), createdAt, closedAt
- ScheduleItem: id, groupId, subjectId, teacherId, semesterId, dayOfWeek(Short), lessonNumber(Short), startTime, endTime, weekType, room, isActive, createdAt

LessonRepository methods:
- findById(Long id) — JpaRepository
- findByScheduleItemIdInAndDateBetweenAndStatusIn(List<Long> ids, LocalDate from, LocalDate to, List<LessonStatus> statuses)
- findByScheduleItemIdAndDateBetween(Long itemId, LocalDate from, LocalDate to)

ScheduleItemRepository methods:
- findById(Long id), findByGroupId(Long groupId), findByGroupIdAndSemesterIdAndIsActiveTrue(...)

AcademicGrpcClient:
- boolean isHeadman(Long userId, Long groupId)

RequestContext: getUserId(), getRole(), getGroupId(), isHeadman()

LessonStatus: PLANNED, ACTIVE, CLOSED, CANCELLED
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LessonWithItem + LessonService + LessonController + LessonAssembler</name>
  <files>
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/LessonWithItem.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/LessonService.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/LessonController.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/LessonAssembler.java
  </files>
  <read_first>
    services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/api/LessonApi.java,
    services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/lesson/CancelLessonRequest.java,
    services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/lesson/MassCancelRequest.java,
    services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/lesson/MassCancelResponse.java,
    services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/lesson/GeoBlockRequest.java,
    services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/lesson/LessonResponse.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/entity/Lesson.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/repository/LessonRepository.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/item/entity/ScheduleItem.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/item/repository/ScheduleItemRepository.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/grpc/AcademicGrpcClient.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/security/RequestContext.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/security/RequireRole.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/exception/InvalidLessonStateException.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/exception/ResourceNotFoundException.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/exception/AccessDeniedException.java
  </read_first>
  <action>
**LessonWithItem.java** — app-internal record (NOT in contract):
```java
package ru.rutcampustrack.schedule.lesson;

import ru.rutcampustrack.schedule.item.entity.ScheduleItem;
import ru.rutcampustrack.schedule.lesson.entity.Lesson;

public record LessonWithItem(Lesson lesson, ScheduleItem scheduleItem) {}
```

**LessonService.java** — `ru.rutcampustrack.schedule.lesson` package, `@Service`, `@Transactional`:

Constructor-inject: `LessonRepository`, `ScheduleItemRepository`, `AcademicGrpcClient`, `RequestContext`.

Private helper (identical pattern to ScheduleItemService):
```java
private void requireHeadmanForGroup(Long targetGroupId) {
    UserRole role = requestContext.getRole();
    if (role == UserRole.ADMIN) return;
    if (!requestContext.isHeadman()) {
        throw new AccessDeniedException("Only headman or admin can perform this action");
    }
    boolean confirmed = academicGrpcClient.isHeadman(requestContext.getUserId(), targetGroupId);
    if (!confirmed) {
        throw new AccessDeniedException("You are not headman of group " + targetGroupId);
    }
}
```

Private helper to resolve lesson + validate group ownership:
```java
private LessonWithItem findLessonAndValidateGroup(Long lessonId) {
    Lesson lesson = lessonRepository.findById(lessonId)
            .orElseThrow(() -> new ResourceNotFoundException("Lesson", "id", lessonId));
    ScheduleItem item = scheduleItemRepository.findById(lesson.getScheduleItemId())
            .orElseThrow(() -> new ResourceNotFoundException("ScheduleItem", "id", lesson.getScheduleItemId()));
    requireHeadmanForGroup(item.getGroupId());
    return new LessonWithItem(lesson, item);
}
```

Methods:

1. `cancelLesson(Long lessonId, CancelLessonRequest request)` -> `LessonWithItem`:
   - Call `findLessonAndValidateGroup(lessonId)`
   - If `lesson.getStatus() != LessonStatus.PLANNED`, throw `new InvalidLessonStateException("Only planned lessons can be cancelled, current status: " + lesson.getStatus())` per D-14 (only planned can be cancelled directly by headman)
   - Set `lesson.setStatus(LessonStatus.CANCELLED)`, `lesson.setCancelReason(request.reason())` per D-13
   - Save and return `new LessonWithItem(lessonRepository.save(lesson), item)`

2. `restoreLesson(Long lessonId)` -> `LessonWithItem`:
   - Call `findLessonAndValidateGroup(lessonId)`
   - If `lesson.getStatus() != LessonStatus.CANCELLED`, throw `new InvalidLessonStateException("Only cancelled lessons can be restored, current status: " + lesson.getStatus())` per D-14
   - Set `lesson.setStatus(LessonStatus.PLANNED)`, `lesson.setCancelReason(null)` per D-14 (clears cancel_reason)
   - Save and return

3. `massCancelLessons(MassCancelRequest request)` -> `int`:
   - Call `requireHeadmanForGroup(request.groupId())` FIRST per Pitfall 4
   - `List<ScheduleItem> items = scheduleItemRepository.findByGroupId(request.groupId())`
   - `List<Long> itemIds = items.stream().map(ScheduleItem::getId).toList()`
   - If `itemIds.isEmpty()` return 0
   - `List<Lesson> toCancel = lessonRepository.findByScheduleItemIdInAndDateBetweenAndStatusIn(itemIds, request.dateFrom(), request.dateTo(), List.of(LessonStatus.PLANNED))`
   - For each: `l.setStatus(LessonStatus.CANCELLED); l.setCancelReason(request.reason());`
   - `lessonRepository.saveAll(toCancel)`
   - Return `toCancel.size()` per D-12

4. `toggleGeoBlock(Long lessonId, GeoBlockRequest request)` -> `LessonWithItem`:
   - Call `findLessonAndValidateGroup(lessonId)`
   - Set `lesson.setGeoBlocked(request.blocked())` per D-15
   - Save and return

5. `getLessonsForGroup(Long groupId, LocalDate from, LocalDate to, List<LessonStatus> statuses, Pageable pageable)` -> `Page<LessonWithItem>`:
   - Default statuses if null/empty: `List.of(LessonStatus.PLANNED, LessonStatus.ACTIVE, LessonStatus.CLOSED)` per D-18 (exclude cancelled by default)
   - `List<ScheduleItem> items = scheduleItemRepository.findByGroupId(groupId)` — NO isActive filter per Pitfall 3
   - `List<Long> itemIds = items.stream().map(ScheduleItem::getId).toList()`
   - If `itemIds.isEmpty()` return `Page.empty(pageable)`
   - `List<Lesson> lessons = lessonRepository.findByScheduleItemIdInAndDateBetweenAndStatusIn(itemIds, from, to, effectiveStatuses)`
   - Build `Map<Long, ScheduleItem> itemMap = items.stream().collect(Collectors.toMap(ScheduleItem::getId, si -> si))`
   - Convert to `List<LessonWithItem>`: `lessons.stream().map(l -> new LessonWithItem(l, itemMap.get(l.getScheduleItemId()))).toList()`
   - Apply pagination manually: calculate sublist from `pageable.getOffset()` to `min(offset + pageSize, list.size())`, return `new PageImpl<>(sublist, pageable, total)`
   - Mark method `@Transactional(readOnly = true)`

**LessonAssembler.java** — `ru.rutcampustrack.schedule.lesson` package, `@Component`:

Method `toModel(LessonWithItem lwi)` returns `EntityModel<LessonResponse>`:
- Extract `Lesson l = lwi.lesson()`, `ScheduleItem si = lwi.scheduleItem()`
- Create `LessonResponse` with ALL fields from both entities per VIEW-02 / D-16:
  - From Lesson: `l.getId()`, `l.getScheduleItemId()`, `l.getDate()`, `l.getStatus()`, `l.isGeoBlocked()`, `l.getCancelReason()`, `l.getCreatedAt()`
  - From ScheduleItem: `si.getGroupId()`, `si.getSubjectId()`, `si.getTeacherId()`, `si.getDayOfWeek()`, `si.getLessonNumber()`, `si.getStartTime()`, `si.getEndTime()`, `si.getWeekType()`, `si.getRoom()`
- Add HATEOAS links:
  - Self link: build manually since there's no single getLesson endpoint — use `Link.of("/schedule/lessons/" + l.getId()).withSelfRel()` or use URI builder
  - Cancel action: `Link.of("/schedule/lessons/" + l.getId() + "/cancel").withRel("cancel")` (only if status == PLANNED)
  - Restore action: `Link.of("/schedule/lessons/" + l.getId() + "/restore").withRel("restore")` (only if status == CANCELLED)
  - Geo-block action: `Link.of("/schedule/lessons/" + l.getId() + "/geo-block").withRel("geo-block")`
- Return `EntityModel.of(response, links)`

**LessonController.java** — `ru.rutcampustrack.schedule.lesson` package, `@RestController`:
- `implements LessonApi`
- Constructor-inject: `LessonService`, `LessonAssembler`
- NO `@RequestMapping` on class (in contract interface)

Methods:
1. `cancelLesson` — `@RequireRole({UserRole.ADMIN, UserRole.STUDENT})`, call service, return `ResponseEntity.ok(assembler.toModel(result))`
2. `restoreLesson` — `@RequireRole({UserRole.ADMIN, UserRole.STUDENT})`, call service, return `ResponseEntity.ok(assembler.toModel(result))`
3. `massCancelLessons` — `@RequireRole({UserRole.ADMIN, UserRole.STUDENT})`, call service, return `ResponseEntity.ok(new MassCancelResponse(count))`
4. `toggleGeoBlock` — `@RequireRole({UserRole.ADMIN, UserRole.STUDENT})`, call service, return `ResponseEntity.ok(assembler.toModel(result))`
5. `getLessons` — no `@RequireRole` (any authenticated user per D-19/VIEW-01), call service with pageable, use Spring's `PagedResourcesAssembler` to convert `Page<LessonWithItem>` to `PagedModel`. Since the `PagedResourcesAssembler<LessonResponse>` from the method signature works with `LessonResponse` not `LessonWithItem`, you need to first convert the page:
   ```java
   Page<LessonResponse> responsePage = lessonWithItemPage.map(lwi -> {
       // extract response from assembler without wrapping in EntityModel
       return assembler.toResponse(lwi);  // add a toResponse helper in LessonAssembler
   });
   PagedModel<EntityModel<LessonResponse>> pagedModel = pagedResourcesAssembler.toModel(responsePage,
       resp -> EntityModel.of(resp));  // or use assembler::toEntityModel
   ```
   Alternatively, keep it simpler: have `LessonAssembler` provide both `toResponse(LessonWithItem)` and `toModel(LessonWithItem)` methods. Controller uses `toResponse` for the page mapping, then wraps in `EntityModel`.
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && cmd.exe /c "set JAVA_HOME=C:\Users\maksd\.jdks\ms-21.0.9&& .\gradlew.bat :services:schedule-service:schedule-app:compileJava --no-daemon -x javadoc 2>&1 | tail -5"</automated>
  </verify>
  <acceptance_criteria>
    - LessonWithItem.java is a record with Lesson and ScheduleItem fields
    - LessonService.java contains `requireHeadmanForGroup` private method
    - LessonService.cancelLesson checks `status != PLANNED` throws InvalidLessonStateException
    - LessonService.restoreLesson checks `status != CANCELLED` throws InvalidLessonStateException
    - LessonService.restoreLesson sets cancelReason to null
    - LessonService.massCancelLessons calls `requireHeadmanForGroup(request.groupId())` before any repository access
    - LessonService.getLessonsForGroup uses `findByGroupId` (no isActive filter) per Pitfall 3
    - LessonService.getLessonsForGroup defaults to exclude CANCELLED when status param is null
    - LessonController.java contains `implements LessonApi`
    - LessonController has `@RequireRole` on cancelLesson, restoreLesson, massCancelLessons, toggleGeoBlock
    - LessonController.getLessons has NO @RequireRole (any authenticated user per VIEW-01)
    - LessonAssembler builds LessonResponse with fields from both Lesson and ScheduleItem
    - compileJava passes
  </acceptance_criteria>
  <done>All 5 LessonApi endpoints compile. Cancel/restore enforce state machine (planned->cancelled, cancelled->planned only). Mass-cancel validates group ownership first. View endpoint defaults to excluding cancelled lessons. LessonResponse includes ScheduleItem fields (groupId, subjectId, teacherId, room, etc.).</done>
</task>

<task type="auto">
  <name>Task 2: LessonApiTest + ScheduleViewTest integration tests</name>
  <files>
    services/schedule-service/schedule-app/src/test/java/ru/rutcampustrack/schedule/integration/LessonApiTest.java,
    services/schedule-service/schedule-app/src/test/java/ru/rutcampustrack/schedule/integration/ScheduleViewTest.java
  </files>
  <read_first>
    services/schedule-service/schedule-app/src/test/java/ru/rutcampustrack/schedule/integration/AbstractScheduleIntegrationTest.java,
    services/schedule-service/schedule-app/src/test/java/ru/rutcampustrack/schedule/integration/ScheduleItemApiTest.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/LessonService.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/LessonController.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/lesson/entity/Lesson.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/item/entity/ScheduleItem.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/security/UserContextFilter.java,
    services/schedule-service/schedule-app/src/main/java/ru/rutcampustrack/schedule/grpc/AcademicGrpcClient.java,
    services/schedule-service/schedule-api-contract/src/main/java/ru/rutcampustrack/schedule/contract/dto/lesson/LessonResponse.java
  </read_first>
  <action>
**LessonApiTest.java** — extends `AbstractScheduleIntegrationTest`, `@AutoConfigureMockMvc`:

Setup:
- `@MockitoBean AcademicGrpcClient academicGrpcClient`
- `@Autowired MockMvc mockMvc`, `@Autowired ObjectMapper objectMapper`
- `@Autowired LessonRepository lessonRepository`, `@Autowired ScheduleItemRepository scheduleItemRepository`

Constants: `GROUP_ID = 1L`, `USER_ID = 42L`

`@BeforeEach`:
- `lessonRepository.deleteAll(); scheduleItemRepository.deleteAll();`
- Mock `academicGrpcClient.isHeadman(USER_ID, GROUP_ID)` returns `true`

Helper to create a ScheduleItem in DB (use repository.save directly):
```java
private ScheduleItem createScheduleItem() {
    ScheduleItem item = new ScheduleItem();
    item.setGroupId(GROUP_ID);
    item.setSubjectId(100L);
    item.setTeacherId(200L);
    item.setSemesterId(10L);
    item.setDayOfWeek((short) 1);
    item.setLessonNumber((short) 1);
    item.setStartTime(LocalTime.of(8, 30));
    item.setEndTime(LocalTime.of(10, 0));
    item.setWeekType(WeekType.ALL);
    item.setRoom("A-101");
    item.setActive(true);
    item.setCreatedAt(OffsetDateTime.now());
    return scheduleItemRepository.save(item);
}
```

Helper to create a Lesson in DB:
```java
private Lesson createLesson(Long scheduleItemId, LessonStatus status, LocalDate date) {
    Lesson lesson = new Lesson();
    lesson.setScheduleItemId(scheduleItemId);
    lesson.setDate(date);
    lesson.setStatus(status);
    lesson.setGeoBlocked(false);
    lesson.setCreatedAt(OffsetDateTime.now());
    return lessonRepository.save(lesson);
}
```

Header helpers (same pattern as ScheduleItemApiTest):
- `withHeadmanHeaders`: X-User-Id=42, X-User-Role=STUDENT, X-User-Group-Id=1, X-User-Is-Headman=true
- `withAdminHeaders`: X-User-Id=999, X-User-Role=ADMIN

**Tests (LSSN-04):**

1. `cancelLesson_planned_success` — Create schedule item + lesson (PLANNED). PATCH `/schedule/lessons/{id}/cancel` with `{"reason":"Snow day"}` + headman headers. Assert: 200, response `$.status == "CANCELLED"`, `$.cancelReason == "Snow day"`. DB verify: lesson status is CANCELLED.

2. `cancelLesson_activeLesson_returns422` — Create lesson with status ACTIVE. PATCH cancel. Assert: 422.

3. `cancelLesson_missingReason_returns400` — PATCH cancel with `{"reason":""}`. Assert: 400 (validation error on @NotBlank).

**Tests (LSSN-05):**

4. `restoreLesson_cancelled_success` — Create lesson (CANCELLED, cancelReason="old reason"). PATCH `/schedule/lessons/{id}/restore`. Assert: 200, `$.status == "PLANNED"`, `$.cancelReason` is null. DB verify: cancel_reason is null.

5. `restoreLesson_planned_returns422` — Create lesson (PLANNED). PATCH restore. Assert: 422.

**Tests (LSSN-06):**

6. `massCancelLessons_success` — Create schedule item + 3 lessons (2 PLANNED on dates 2026-04-01 and 2026-04-02, 1 ACTIVE on 2026-04-03). POST `/schedule/lessons/mass-cancel` with `{"groupId":1,"dateFrom":"2026-04-01","dateTo":"2026-04-03","reason":"Holiday"}`. Assert: 200, `$.cancelledCount == 2` (only PLANNED lessons cancelled, ACTIVE untouched). DB verify: 2 lessons are CANCELLED, 1 remains ACTIVE.

**Tests (LSSN-07):**

7. `toggleGeoBlock_success` — Create lesson (PLANNED). PATCH `/schedule/lessons/{id}/geo-block` with `{"blocked":true}`. Assert: 200, `$.geoBlocked == true`. DB verify: is_geo_blocked = true. Then toggle off: PATCH with `{"blocked":false}`, assert `$.geoBlocked == false`.

---

**ScheduleViewTest.java** — extends `AbstractScheduleIntegrationTest`, `@AutoConfigureMockMvc`:

Setup:
- `@MockitoBean AcademicGrpcClient academicGrpcClient` (needed for context load even though view doesn't use it)
- `@Autowired MockMvc mockMvc`
- `@Autowired LessonRepository lessonRepository`, `@Autowired ScheduleItemRepository scheduleItemRepository`

`@BeforeEach`: delete all, create test data:
- 1 active schedule item (groupId=1, subjectId=100, teacherId=200, room="A-101")
- 3 lessons: PLANNED (2026-04-07), ACTIVE (2026-04-08), CANCELLED (2026-04-09)

**Tests (VIEW-01):**

8. `anyRoleCanView_student` — GET `/schedule/groups/1/lessons?dateFrom=2026-04-01&dateTo=2026-04-30` with STUDENT headers (not headman). Assert: 200, response contains lessons. Proves any authenticated user can read.

9. `anyRoleCanView_teacher` — Same GET with TEACHER headers. Assert: 200.

**Tests (VIEW-02):**

10. `responseContainsAllFields` — GET schedule. Assert response JSON contains ALL LessonResponse fields:
    - `$.content[0].id` is not null
    - `$.content[0].groupId == 1`
    - `$.content[0].subjectId == 100`
    - `$.content[0].teacherId == 200`
    - `$.content[0].room == "A-101"`
    - `$.content[0].status` exists
    - `$.content[0].dayOfWeek == 1`
    - `$.content[0].lessonNumber == 1`
    - `$.content[0].startTime` exists
    - `$.content[0].endTime` exists
    NOTE: Use `$._embedded.lessonResponseList[0]` or `$.content[0]` depending on how PagedModel serializes — check the actual JSON path structure. With HATEOAS, it's typically `$._embedded.lessonResponseList`.

11. `defaultStatusFilter_excludesCancelled` — GET without `status` param. Assert: response contains 2 lessons (PLANNED + ACTIVE), CANCELLED is excluded per D-18.

12. `explicitStatusFilter_includesCancelled` — GET with `?status=PLANNED,ACTIVE,CLOSED,CANCELLED`. Assert: response contains all 3 lessons.

For all header helpers, verify exact header names from `UserContextFilter` source before hardcoding.
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && cmd.exe /c "set JAVA_HOME=C:\Users\maksd\.jdks\ms-21.0.9&& .\gradlew.bat :services:schedule-service:schedule-app:test --tests \"*.integration.LessonApiTest\" --tests \"*.integration.ScheduleViewTest\" --no-daemon -x javadoc 2>&1 | tail -15"</automated>
  </verify>
  <acceptance_criteria>
    - LessonApiTest.java extends AbstractScheduleIntegrationTest
    - LessonApiTest has @MockitoBean AcademicGrpcClient
    - Test cancelLesson_planned_success exists and asserts status 200 with CANCELLED status
    - Test cancelLesson_activeLesson_returns422 exists and asserts 422
    - Test restoreLesson_cancelled_success exists and asserts PLANNED status + null cancelReason
    - Test restoreLesson_planned_returns422 exists and asserts 422
    - Test massCancelLessons_success asserts cancelledCount == 2 (only PLANNED affected)
    - Test toggleGeoBlock_success asserts geoBlocked toggling
    - ScheduleViewTest.java extends AbstractScheduleIntegrationTest
    - Test anyRoleCanView_student asserts 200 for STUDENT role
    - Test responseContainsAllFields asserts groupId, subjectId, teacherId, room fields present in response
    - Test defaultStatusFilter_excludesCancelled verifies cancelled lessons excluded by default
    - All tests pass (exit code 0)
  </acceptance_criteria>
  <done>12+ integration tests pass covering LSSN-04 (cancel with state check), LSSN-05 (restore only from cancelled), LSSN-06 (mass-cancel date range), LSSN-07 (geo-block toggle), VIEW-01 (any role access), VIEW-02 (full response fields + status filter defaults).</done>
</task>

</tasks>

<verification>
- All LessonApiTest tests pass: `gradlew test --tests "*.integration.LessonApiTest"`
- All ScheduleViewTest tests pass: `gradlew test --tests "*.integration.ScheduleViewTest"`
- Cancel only works on PLANNED lessons (422 for ACTIVE/CLOSED/CANCELLED)
- Restore only works on CANCELLED lessons (422 for others)
- Mass-cancel only affects PLANNED lessons in date range
- View endpoint accessible by all authenticated roles (STUDENT, TEACHER, ADMIN)
- View response includes fields from both Lesson and ScheduleItem tables
- Default status filter excludes CANCELLED
- Full test suite green: `gradlew :services:schedule-service:schedule-app:test`
</verification>

<success_criteria>
- LessonService has 5 methods with headman auth on all write operations
- State machine: cancel (planned->cancelled), restore (cancelled->planned) enforced with 422 on violation
- Mass-cancel respects date range and only affects PLANNED lessons
- Schedule view joins Lesson + ScheduleItem data into LessonResponse
- 12+ integration tests pass across LessonApiTest and ScheduleViewTest
- Full schedule-app test suite passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/11-rest-api-grpc-client/11-03-SUMMARY.md`
</output>
