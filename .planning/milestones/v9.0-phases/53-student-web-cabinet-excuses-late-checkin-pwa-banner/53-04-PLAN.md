---
phase: 53
plan: 04
type: execute
wave: 2
depends_on:
  - 53-01
files_modified:
  - frontends/web-panel/src/app/features/student/shared/student-banner.service.ts
  - frontends/web-panel/src/app/layout/shell/student-pwa-banner/student-pwa-banner.component.ts
  - frontends/web-panel/src/app/layout/shell/student-pwa-banner/student-pwa-banner.component.html
  - frontends/web-panel/src/app/layout/shell/student-pwa-banner/student-pwa-banner.component.css
  - frontends/web-panel/src/app/layout/shell/student-pwa-banner/student-pwa-banner.component.spec.ts
  - frontends/web-panel/src/app/layout/shell/shell.component.ts
  - frontends/web-panel/src/app/layout/shell/shell.component.html
autonomous: true
requirements:
  - STU-WEB-10

must_haves:
  truths:
    - "Баннер не показывается если localStorage['pwa-banner-dismissed'] === 'true'"
    - "Баннер не показывается для TEACHER или ADMIN"
    - "Баннер показывается STUDENT при первом посещении (dismissed != 'true')"
    - "Клик × устанавливает localStorage['pwa-banner-dismissed']='true' и скрывает баннер"
    - "Баннер имеет кнопку 'Установить приложение' (Chrome/Edge) или 'Открыть в Safari' (iOS)"
    - "ShellComponent импортирует StudentPwaBannerComponent"
    - "npm test проходит"
  artifacts:
    - path: "frontends/web-panel/src/app/features/student/shared/student-banner.service.ts"
      provides: "Сервис управления состоянием PWA banner через localStorage"
      exports: ["StudentBannerService"]
    - path: "frontends/web-panel/src/app/layout/shell/student-pwa-banner/student-pwa-banner.component.ts"
      provides: "PWA install banner с beforeinstallprompt и iOS-fallback"
      exports: ["StudentPwaBannerComponent"]
  key_links:
    - from: "shell.component.html"
      to: "StudentPwaBannerComponent"
      via: "<app-student-pwa-banner />"
      pattern: "app-student-pwa-banner"
    - from: "StudentPwaBannerComponent"
      to: "StudentBannerService.shouldShow"
      via: "@if(bannerService.shouldShow())"
      pattern: "shouldShow"
    - from: "StudentBannerService.dismiss"
      to: "localStorage['pwa-banner-dismissed']"
      via: "localStorage.setItem('pwa-banner-dismissed', 'true')"
      pattern: "pwa-banner-dismissed"
---

<objective>
Реализовать PWA Install Banner: сервис управления состоянием через localStorage, компонент с beforeinstallprompt / iOS-fallback / анимацией, интеграция в ShellComponent.

Purpose: Доставить STU-WEB-10 — первая навигация студента показывает ненавязчивый баннер "Установите RutTrack на главный экран", после dismiss — больше не показывается.
Output: StudentBannerService, StudentPwaBannerComponent, обновлённый ShellComponent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53/53-RESEARCH.md
@.planning/phases/53/53-UI-SPEC.md

<interfaces>
Из shell.component.ts (текущее):
```typescript
@Component({
  selector: 'app-shell',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [RouterOutlet, SidebarComponent, HeaderComponent],
  templateUrl: './shell.component.html',
  styleUrl: './shell.component.css',
})
export class ShellComponent {}
```

Из shell.component.html (текущее):
```html
<div class="shell">
  <app-sidebar class="shell__sidebar" />
  <div class="shell__column">
    <app-header />
    <main class="shell__main" tabindex="-1">
      <div class="shell__content">
        <router-outlet />
      </div>
    </main>
  </div>
</div>
```

Из auth.service.ts — AuthUser interface:
```typescript
export interface AuthUser {
  id: number;
  username: string;
  role: 'ADMIN' | 'TEACHER' | 'STUDENT';
  isHeadman: boolean;
  groupId?: number;
}
currentUser: Signal<AuthUser | null>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StudentBannerService + StudentPwaBannerComponent</name>

  <read_first>
    - frontends/web-panel/src/app/layout/shell/student-pwa-banner/student-pwa-banner.component.spec.ts
    - frontends/web-panel/src/app/core/auth/auth.service.ts
    - .planning/phases/53/53-UI-SPEC.md (раздел "3. PWA Install Banner — StudentPwaBannerComponent")
    - .planning/phases/53/53-RESEARCH.md (Pattern 4 StudentBannerService, Pattern 5 beforeinstallprompt)
  </read_first>

  <files>
    frontends/web-panel/src/app/features/student/shared/student-banner.service.ts,
    frontends/web-panel/src/app/layout/shell/student-pwa-banner/student-pwa-banner.component.ts,
    frontends/web-panel/src/app/layout/shell/student-pwa-banner/student-pwa-banner.component.html,
    frontends/web-panel/src/app/layout/shell/student-pwa-banner/student-pwa-banner.component.css,
    frontends/web-panel/src/app/layout/shell/student-pwa-banner/student-pwa-banner.component.spec.ts
  </files>

  <behavior>
    - StudentBannerService.shouldShow() = false если localStorage['pwa-banner-dismissed'] === 'true'
    - StudentBannerService.shouldShow() = false если user.role !== 'STUDENT'
    - StudentBannerService.shouldShow() = true если STUDENT и 'pwa-banner-dismissed' отсутствует
    - dismiss() → localStorage['pwa-banner-dismissed'] = 'true' + shouldShow = false
    - Banner не рендерит DOM при shouldShow() = false (через @if внутри компонента)
    - Dismiss × устанавливает localStorage flag
    - iOS: isIos = true → кнопка "Открыть в Safari" с href=/app/
    - Chrome/Edge: hasPrompt = true → кнопка "Установить приложение" вызывает deferredPrompt.prompt()
  </behavior>

  <action>
**student-banner.service.ts** (создать в `features/student/shared/`):

```typescript
import { Injectable, inject, signal } from '@angular/core';
import { AuthService } from '../../../core/auth/auth.service';

const DISMISSED_KEY = 'pwa-banner-dismissed';
const SHOWN_KEY = 'pwa-banner-shown';

/**
 * Manages the PWA install banner state using localStorage flags.
 *
 * shouldShow logic:
 * 1. If 'pwa-banner-dismissed' === 'true' → never show
 * 2. If user.role !== 'STUDENT' → never show (banner is student-only)
 * 3. Otherwise → show (even on repeat visits until explicitly dismissed)
 *
 * STU-WEB-10: no forced redirect, no auto-dismiss on navigation.
 */
@Injectable({ providedIn: 'root' })
export class StudentBannerService {
  private readonly authService = inject(AuthService);

  readonly shouldShow = signal(false);

  /**
   * Call once after auth resolves (e.g. from StudentPwaBannerComponent.ngOnInit).
   * Safe to call multiple times — idempotent.
   */
  init(): void {
    const user = this.authService.currentUser();
    if (user?.role !== 'STUDENT') {
      this.shouldShow.set(false);
      return;
    }
    if (localStorage.getItem(DISMISSED_KEY) === 'true') {
      this.shouldShow.set(false);
      return;
    }
    localStorage.setItem(SHOWN_KEY, 'true');
    this.shouldShow.set(true);
  }

  /** Permanently suppress banner for this browser. */
  dismiss(): void {
    localStorage.setItem(DISMISSED_KEY, 'true');
    this.shouldShow.set(false);
  }
}
```

**student-pwa-banner.component.ts:**

```typescript
import {
  ChangeDetectionStrategy, Component, OnDestroy, OnInit, inject, signal,
} from '@angular/core';
import { CommonModule } from '@angular/common';
import {
  trigger, transition, style, animate,
} from '@angular/animations';
import { StudentBannerService } from '../../features/student/shared/student-banner.service';

/** Chrome/Edge beforeinstallprompt — not in @types/web for all targets */
interface BeforeInstallPromptEvent extends Event {
  prompt(): Promise<void>;
  userChoice: Promise<{ outcome: 'accepted' | 'dismissed' }>;
}

/**
 * PWA install banner — STU-WEB-10.
 *
 * Renders only for STUDENT role when pwa-banner-dismissed is not set.
 * Supports three install paths:
 * 1. Chrome/Edge: native beforeinstallprompt → calls deferredPrompt.prompt()
 * 2. iOS Safari: shows instruction text + link to /app/
 * 3. Other browsers: shows generic link to /app/
 *
 * Dismiss (× button) persists pwa-banner-dismissed=true via StudentBannerService.
 */
@Component({
  selector: 'app-student-pwa-banner',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CommonModule],
  animations: [
    trigger('bannerSlide', [
      transition(':enter', [
        style({ transform: 'translateY(-100%)', opacity: 0 }),
        animate('200ms ease-out', style({ transform: 'translateY(0)', opacity: 1 })),
      ]),
      transition(':leave', [
        animate('150ms ease-in', style({ opacity: 0, transform: 'translateY(-8px)' })),
      ]),
    ]),
  ],
  templateUrl: './student-pwa-banner.component.html',
  styleUrl: './student-pwa-banner.component.css',
})
export class StudentPwaBannerComponent implements OnInit, OnDestroy {
  readonly bannerService = inject(StudentBannerService);

  readonly isIos = signal(false);
  readonly hasPrompt = signal(false);

  private deferredPrompt: BeforeInstallPromptEvent | null = null;

  private readonly promptHandler = (e: Event): void => {
    e.preventDefault();
    this.deferredPrompt = e as BeforeInstallPromptEvent;
    this.hasPrompt.set(true);
  };

  ngOnInit(): void {
    this.bannerService.init();

    // Detect iOS (no beforeinstallprompt support)
    const ua = navigator.userAgent.toLowerCase();
    const isIosBrowser =
      /iphone|ipad|ipod/.test(ua) && !(window as unknown as Record<string, unknown>)['MSStream'];
    this.isIos.set(isIosBrowser);

    // Listen for Chrome/Edge install prompt
    if (!isIosBrowser) {
      window.addEventListener('beforeinstallprompt', this.promptHandler);
    }
  }

  ngOnDestroy(): void {
    window.removeEventListener('beforeinstallprompt', this.promptHandler);
  }

  async install(): Promise<void> {
    if (this.deferredPrompt) {
      this.deferredPrompt.prompt();
      const { outcome } = await this.deferredPrompt.userChoice;
      if (outcome === 'accepted') {
        this.bannerService.dismiss();
      }
      // Clear prompt regardless of outcome (browser won't re-fire)
      this.deferredPrompt = null;
      this.hasPrompt.set(false);
      // Keep banner visible if dismissed — user can still use the × button
    }
    // iOS / other: button is an <a> link to /app/ — no JS action needed
  }

  dismiss(): void {
    this.bannerService.dismiss();
  }

  /** Used to skip animation when user prefers-reduced-motion. */
  prefersReducedMotion(): boolean {
    return window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  }
}
```

**student-pwa-banner.component.html:**

```html
@if (bannerService.shouldShow()) {
  <div
    class="pwa-banner"
    [@bannerSlide]
    role="banner"
    aria-label="Установите приложение RutTrack"
  >
    <i class="ph-duotone ph-device-mobile-camera pwa-banner__icon" aria-hidden="true"></i>

    <div class="pwa-banner__text">
      @if (isIos()) {
        <span class="pwa-banner__title">Установите RutTrack: откройте в Safari → Поделиться → На экран «Домой»</span>
      } @else {
        <span class="pwa-banner__title">Установите RutTrack на главный экран</span>
      }
    </div>

    @if (isIos()) {
      <a href="/app/" class="btn-brand btn-brand--sm" target="_blank" rel="noopener">
        Открыть в Safari
      </a>
    } @else if (hasPrompt()) {
      <button class="btn-brand btn-brand--sm" type="button" (click)="install()">
        Установить приложение
      </button>
    } @else {
      <a href="/app/" class="btn-brand btn-brand--sm">
        Открыть приложение
      </a>
    }

    <button
      class="pwa-banner__dismiss"
      type="button"
      aria-label="Закрыть баннер"
      (click)="dismiss()"
    >
      <i class="ph ph-x" aria-hidden="true"></i>
    </button>
  </div>
}
```

**student-pwa-banner.component.css:**

```css
.pwa-banner {
  background: var(--bg-secondary);
  border-bottom: 1px solid var(--border-subtle);
  border-left: 4px solid var(--accent-primary);
  padding: var(--space-2) var(--space-5);
  display: flex;
  align-items: center;
  gap: var(--space-4);
  flex-wrap: wrap;
}

.pwa-banner__icon {
  font-size: 24px;
  color: var(--accent-primary);
  flex-shrink: 0;
}

.pwa-banner__text {
  flex: 1;
  min-width: 0;
}

.pwa-banner__title {
  font-size: var(--text-sm);
  font-family: var(--font-heading);
  font-weight: 600;
  color: var(--text-primary);
  display: block;
}

/* Compact variant for banner CTA */
.btn-brand--sm {
  padding: 8px 16px;
  min-height: 36px;
  font-size: var(--text-sm);
  text-decoration: none;
  display: inline-flex;
  align-items: center;
  white-space: nowrap;
}

.pwa-banner__dismiss {
  width: 32px;
  height: 32px;
  border: none;
  background: transparent;
  color: var(--text-muted);
  border-radius: var(--radius-sm);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: color 150ms ease;
  flex-shrink: 0;
}

.pwa-banner__dismiss:hover {
  color: var(--text-secondary);
}

.pwa-banner__dismiss:active {
  transform: scale(0.92);
}

.pwa-banner__dismiss:focus-visible {
  outline: 2px solid var(--accent-primary);
  outline-offset: 2px;
}
```

**student-pwa-banner.component.spec.ts** — заменить заглушку:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen } from '@testing-library/angular';
import { userEvent } from '@testing-library/user-event';
import { StudentPwaBannerComponent } from './student-pwa-banner.component';
import { StudentBannerService } from '../../../features/student/shared/student-banner.service';
import { provideAnimations } from '@angular/platform-browser/animations';
import { signal } from '@angular/core';

const makeMockBannerService = (show: boolean) => ({
  shouldShow: signal(show),
  init: vi.fn(),
  dismiss: vi.fn(),
});

describe('StudentPwaBannerComponent', () => {
  beforeEach(() => {
    localStorage.removeItem('pwa-banner-dismissed');
  });

  it('не рендерит баннер когда shouldShow() = false', async () => {
    const mockService = makeMockBannerService(false);
    await render(StudentPwaBannerComponent, {
      providers: [
        provideAnimations(),
        { provide: StudentBannerService, useValue: mockService },
      ],
    });
    expect(screen.queryByRole('banner')).toBeNull();
  });

  it('рендерит баннер когда shouldShow() = true', async () => {
    const mockService = makeMockBannerService(true);
    await render(StudentPwaBannerComponent, {
      providers: [
        provideAnimations(),
        { provide: StudentBannerService, useValue: mockService },
      ],
    });
    expect(screen.getByRole('banner')).toBeTruthy();
    expect(screen.getByText('Установите RutTrack на главный экран')).toBeTruthy();
  });

  it('клик × вызывает bannerService.dismiss()', async () => {
    const mockService = makeMockBannerService(true);
    const user = userEvent.setup();
    await render(StudentPwaBannerComponent, {
      providers: [
        provideAnimations(),
        { provide: StudentBannerService, useValue: mockService },
      ],
    });
    await user.click(screen.getByLabelText('Закрыть баннер'));
    expect(mockService.dismiss).toHaveBeenCalledOnce();
  });
});
```

Также создать unit test для StudentBannerService в том же файле или отдельно:

```typescript
// Добавить в student-pwa-banner.component.spec.ts как отдельный describe:
import { StudentBannerService } from '../../../features/student/shared/student-banner.service';
import { AuthService } from '../../../core/auth/auth.service';
import { TestBed } from '@angular/core/testing';

describe('StudentBannerService', () => {
  beforeEach(() => {
    localStorage.removeItem('pwa-banner-dismissed');
  });

  it('shouldShow() = false если dismissed = true', () => {
    localStorage.setItem('pwa-banner-dismissed', 'true');
    TestBed.configureTestingModule({
      providers: [
        StudentBannerService,
        {
          provide: AuthService,
          useValue: { currentUser: signal({ role: 'STUDENT', isHeadman: false, id: 1, username: 'student', groupId: 1 }) },
        },
      ],
    });
    const service = TestBed.inject(StudentBannerService);
    service.init();
    expect(service.shouldShow()).toBe(false);
  });

  it('shouldShow() = false для TEACHER', () => {
    TestBed.configureTestingModule({
      providers: [
        StudentBannerService,
        {
          provide: AuthService,
          useValue: { currentUser: signal({ role: 'TEACHER', isHeadman: false, id: 2, username: 'teacher', groupId: null }) },
        },
      ],
    });
    const service = TestBed.inject(StudentBannerService);
    service.init();
    expect(service.shouldShow()).toBe(false);
  });

  it('shouldShow() = true для STUDENT без dismissed flag', () => {
    TestBed.configureTestingModule({
      providers: [
        StudentBannerService,
        {
          provide: AuthService,
          useValue: { currentUser: signal({ role: 'STUDENT', isHeadman: false, id: 1, username: 'student', groupId: 1 }) },
        },
      ],
    });
    const service = TestBed.inject(StudentBannerService);
    service.init();
    expect(service.shouldShow()).toBe(true);
  });

  it('dismiss() устанавливает localStorage flag и shouldShow = false', () => {
    TestBed.configureTestingModule({
      providers: [
        StudentBannerService,
        {
          provide: AuthService,
          useValue: { currentUser: signal({ role: 'STUDENT', isHeadman: false, id: 1, username: 'student', groupId: 1 }) },
        },
      ],
    });
    const service = TestBed.inject(StudentBannerService);
    service.init();
    service.dismiss();
    expect(localStorage.getItem('pwa-banner-dismissed')).toBe('true');
    expect(service.shouldShow()).toBe(false);
  });
});
```
  </action>

  <acceptance_criteria>
    - grep "StudentBannerService" frontends/web-panel/src/app/features/student/shared/student-banner.service.ts → найдено
    - grep "pwa-banner-dismissed" frontends/web-panel/src/app/features/student/shared/student-banner.service.ts → найдено
    - grep "StudentPwaBannerComponent" frontends/web-panel/src/app/layout/shell/student-pwa-banner/student-pwa-banner.component.ts → найдено
    - grep "beforeinstallprompt\|promptHandler" frontends/web-panel/src/app/layout/shell/student-pwa-banner/student-pwa-banner.component.ts → найдено
    - grep "Закрыть баннер" frontends/web-panel/src/app/layout/shell/student-pwa-banner/student-pwa-banner.component.html → найдено
    - grep "bannerSlide\|@bannerSlide" frontends/web-panel/src/app/layout/shell/student-pwa-banner/student-pwa-banner.component.ts → найдено
    - cd frontends/web-panel && npm test — все тесты зелёные
  </acceptance_criteria>

  <verify>
    <automated>cd frontends/web-panel && npm test 2>&1 | tail -20</automated>
  </verify>

  <done>
    StudentBannerService и StudentPwaBannerComponent реализованы и протестированы. Тесты zелёные.
  </done>
</task>

<task type="auto">
  <name>Task 2: Интеграция баннера в ShellComponent</name>

  <read_first>
    - frontends/web-panel/src/app/layout/shell/shell.component.ts
    - frontends/web-panel/src/app/layout/shell/shell.component.html
    - frontends/web-panel/src/app/layout/shell/student-pwa-banner/student-pwa-banner.component.ts
  </read_first>

  <files>
    frontends/web-panel/src/app/layout/shell/shell.component.ts,
    frontends/web-panel/src/app/layout/shell/shell.component.html
  </files>

  <action>
**shell.component.ts** — добавить импорт StudentPwaBannerComponent:

```typescript
import { ChangeDetectionStrategy, Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';
import { SidebarComponent } from '../sidebar/sidebar.component';
import { HeaderComponent } from '../header/header.component';
import { StudentPwaBannerComponent } from './student-pwa-banner/student-pwa-banner.component';

@Component({
  selector: 'app-shell',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [RouterOutlet, SidebarComponent, HeaderComponent, StudentPwaBannerComponent],
  templateUrl: './shell.component.html',
  styleUrl: './shell.component.css',
})
export class ShellComponent {}
```

**shell.component.html** — добавить `<app-student-pwa-banner />` МЕЖДУ `<app-header />` и `<main ...>`:

```html
<div class="shell">
  <app-sidebar class="shell__sidebar" />

  <div class="shell__column">
    <app-header />
    <app-student-pwa-banner />
    <main class="shell__main" tabindex="-1">
      <div class="shell__content">
        <router-outlet />
      </div>
    </main>
  </div>
</div>
```

Важно: баннер само управляет своей видимостью через `@if (bannerService.shouldShow())` внутри компонента. ShellComponent не добавляет никаких дополнительных условий — это намеренно (StudentBannerService.init() уже проверяет role).
  </action>

  <acceptance_criteria>
    - grep "StudentPwaBannerComponent" frontends/web-panel/src/app/layout/shell/shell.component.ts → найдено в imports[]
    - grep "app-student-pwa-banner" frontends/web-panel/src/app/layout/shell/shell.component.html → найдено
    - grep "app-header" frontends/web-panel/src/app/layout/shell/shell.component.html → app-header стоит ДО app-student-pwa-banner (проверить порядок: app-header затем app-student-pwa-banner затем main)
    - cd frontends/web-panel && npm test — все тесты зелёные
  </acceptance_criteria>

  <verify>
    <automated>cd frontends/web-panel && npm test 2>&1 | tail -20</automated>
  </verify>

  <done>
    ShellComponent импортирует StudentPwaBannerComponent и рендерит его между header и main. npm test зелёный. Shell существующие тесты не сломаны.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage ↔ Component | banner dismissed flag читается/записывается StudentBannerService |
| Browser API | beforeinstallprompt — нестандартный Chrome/Edge API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-04-01 | Information Disclosure | localStorage flag pwa-banner-dismissed | accept | Флаг содержит только boolean ('true'/'null'), не токен/ПДн; низкий риск |
| T-53-04-02 | Tampering | beforeinstallprompt deferredPrompt | accept | Нестандартный API доступен только в Chrome/Edge; вызов prompt() не может быть подделан JS (браузер контролирует диалог) |
| T-53-04-03 | Information Disclosure | TEACHER/ADMIN видят баннер | mitigate | StudentBannerService.init() проверяет user.role !== 'STUDENT' → shouldShow.set(false); @if внутри компонента защищает DOM |
| T-53-04-04 | Tampering | XSS через href /app/ | accept | Значение /app/ хардкодировано в шаблоне, не из user input; Angular не применяет innerHTML |
</threat_model>

<verification>
1. `cd frontends/web-panel && npm test` — все тесты зелёные
2. `grep "app-student-pwa-banner" frontends/web-panel/src/app/layout/shell/shell.component.html` → найдено
3. `grep "StudentPwaBannerComponent" frontends/web-panel/src/app/layout/shell/shell.component.ts` → найдено в imports
4. `grep "pwa-banner-dismissed" frontends/web-panel/src/app/features/student/shared/student-banner.service.ts` → найдено
5. `grep "shouldShow.*false\|role.*STUDENT" frontends/web-panel/src/app/features/student/shared/student-banner.service.ts` → role-check найден
</verification>

<success_criteria>
- StudentBannerService: shouldShow=false при dismissed=true или не-STUDENT роли; dismiss() устанавливает localStorage flag
- StudentPwaBannerComponent: рендерит @if(shouldShow()); dismiss × работает; beforeinstallprompt поддерживается; iOS-fallback с ссылкой /app/
- ShellComponent: импортирует и рендерит StudentPwaBannerComponent над main
- aria-label="Закрыть баннер" на dismiss-кнопке
- npm test проходит
</success_criteria>

<output>
После завершения создать `.planning/phases/53/53-04-SUMMARY.md`
</output>
