---
phase: 59-excuses-backend
plan: 08
type: execute
wave: 4
depends_on:
  - 59-01
files_modified:
  - frontends/web-panel/src/app/features/headman/excuses/headman-excuses.component.ts
  - frontends/web-panel/src/app/features/headman/excuses/headman-excuses.component.spec.ts
  - frontends/web-panel/src/app/features/headman/shared/headman-api.service.ts
autonomous: true
requirements:
  - FR-4
  - AC-10
  - D-23
  - D-24

must_haves:
  truths:
    - "HeadmanExcusesComponent загружает тикеты группы через GET /api/attendance/excuses/group/{groupId}"
    - "HeadmanExcusesComponent отображает pending тикеты сверху, остальные ниже"
    - "HeadmanExcusesComponent имеет кнопки 'Одобрить' и 'Отклонить' на pending тикетах"
    - "Кнопка 'Отклонить' требует обязательный decisionComment"
    - "HeadmanApiService.getGroupExcuses(groupId) и approveExcuse(id, comment?) и rejectExcuse(id, comment) добавлены"
    - "headman-excuses.component.spec.ts зелёный (3+ тестов)"
  artifacts:
    - path: "frontends/web-panel/src/app/features/headman/excuses/headman-excuses.component.ts"
      provides: "Полная реализация headman excuses page (D-23, D-24)"
    - path: "frontends/web-panel/src/app/features/headman/shared/headman-api.service.ts"
      provides: "getGroupExcuses, approveExcuse, rejectExcuse методы"
    - path: "frontends/web-panel/src/app/features/headman/excuses/headman-excuses.component.spec.ts"
      provides: "Vitest тесты"
  key_links:
    - from: "HeadmanExcusesComponent.approve(id)"
      to: "HeadmanApiService.approveExcuse(id, comment)"
      via: "PATCH /api/attendance/excuses/{id}/status {status: 'approved'}"
      pattern: "approveExcuse"
    - from: "HeadmanExcusesComponent.reject(id, comment)"
      to: "HeadmanApiService.rejectExcuse(id, comment)"
      via: "PATCH /api/attendance/excuses/{id}/status {status: 'rejected', decisionComment}"
      pattern: "rejectExcuse"
---

<objective>
Заменить graceful-degradation заглушку headman excuses page на полную реализацию с реальными API calls (Wave 4, параллельно с планами 06 и 07).

Purpose: D-23/D-24 — headman должен видеть pending тикеты и иметь возможность одобрить/отклонить с комментарием. Страница уже создана (Фаза 55) как пустая заглушка.

Output:
- `headman-excuses.component.ts` — полная реализация с list/approve/reject
- `headman-api.service.ts` — 3 новых метода для excuses
- `headman-excuses.component.spec.ts` — 3+ тестов (AC-10)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-excuses-backend/59-CONTEXT.md
@.planning/phases/59-excuses-backend/59-01-SUMMARY.md

<interfaces>
<!-- API endpoints из плана 01 -->

GET /api/attendance/excuses/group/{groupId}?status=submitted&page=0&size=20:
- Возвращает PagedModel с embedded excuseTicketList
- Только для headman (is_headman=true) и только своей группы

PATCH /api/attendance/excuses/{id}/status:
```json
{ "status": "approved", "decisionComment": null }
{ "status": "rejected", "decisionComment": "Нет документов" }
```

ExcuseTicket тип (из плана 07 / student-schedule.types.ts):
```typescript
export interface ExcuseTicket {
  id: string;
  studentId: number;
  studentName: string;
  lessonIds: number[];
  excuseType: ExcuseType;
  comment: string | null;
  status: ExcuseTicketStatus;  // 'submitted' | 'approved' | 'rejected' | 'draft'
  decisionBy: number | null;
  decisionComment: string | null;
  createdAt: string;
}
```

ExcuseType русские подписи — использовать тот же EXCUSE_TYPE_LABELS что в плане 07.

HeadmanApiService (текущий getPendingExcuses использует неправильный URL):
```typescript
// Нужно заменить/добавить:
getGroupExcuses(groupId: number, status?: string): Observable<any>
  // GET /api/attendance/excuses/group/{groupId}
approveExcuse(id: string, decisionComment?: string | null): Observable<void>
  // PATCH /api/attendance/excuses/{id}/status {status: 'approved', decisionComment}
rejectExcuse(id: string, decisionComment: string): Observable<void>
  // PATCH /api/attendance/excuses/{id}/status {status: 'rejected', decisionComment}
```

HeadmanExcusesComponent (текущее содержимое — заглушка):
```typescript
@Component({
  selector: 'app-headman-excuses', standalone: true, ...
  template: `<div class="page-empty">...</div>`
})
export class HeadmanExcusesComponent implements OnInit {
  private headmanApi = inject(HeadmanApiService);
  excusesData = signal<any>(null);
  ngOnInit() { this.headmanApi.getPendingExcuses().pipe(catchError(()=>of(null))).subscribe(...) }
}
```

AuthService/JWT доступ (для groupId): использовать inject(AuthService) или inject(JwtService) — проверить как другие headman компоненты получают groupId из токена.

Дизайн UI (Claude's Discretion, D-24): карточки (не таблица). Pending сверху, approved/rejected ниже с фильтром по статусу. Кнопки "Одобрить" и "Отклонить" только на pending-карточках.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HeadmanApiService — новые методы для excuses</name>
  <files>
    frontends/web-panel/src/app/features/headman/shared/headman-api.service.ts
  </files>
  <read_first>
    - frontends/web-panel/src/app/features/headman/shared/headman-api.service.ts (РЕДАКТИРОВАТЬ — полный файл)
    - frontends/web-panel/src/app/features/student/shared/student-api.service.ts (образец HTTP patterns)
    - frontends/web-panel/src/app/features/student/shared/student-schedule.types.ts (ExcuseTicket тип из плана 07)
    - .planning/phases/59-excuses-backend/59-CONTEXT.md (D-23)
  </read_first>
  <action>
    1. Открыть headman-api.service.ts. Добавить импорт ExcuseTicket, PagedResponse из student-schedule.types или создать локальный интерфейс.
    2. Добавить метод getGroupExcuses():
       ```typescript
       getGroupExcuses(groupId: number, status?: string): Observable<ExcuseTicket[]> {
         let params = new HttpParams().set('size', '50');
         if (status) params = params.set('status', status);
         return this.http
           .get<PagedResponse<ExcuseTicket>>(`/api/attendance/excuses/group/${groupId}`, { params })
           .pipe(
             map(resp => resp._embedded?.['excuseTicketList'] ?? []),
             catchError((err: HttpErrorResponse) => {
               if (err.status === 404 || err.status === 403) return of([]);
               return throwError(() => err);
             }),
           );
       }
       ```
    3. Добавить метод approveExcuse():
       ```typescript
       approveExcuse(id: string, decisionComment?: string | null): Observable<void> {
         return this.http.patch<void>(`/api/attendance/excuses/${id}/status`, {
           status: 'approved',
           decisionComment: decisionComment ?? null,
         });
       }
       ```
    4. Добавить метод rejectExcuse():
       ```typescript
       rejectExcuse(id: string, decisionComment: string): Observable<void> {
         return this.http.patch<void>(`/api/attendance/excuses/${id}/status`, {
           status: 'rejected',
           decisionComment,
         });
       }
       ```
    5. Убедиться что импортированы catchError, throwError, of, map, HttpErrorResponse.
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack/frontends/web-panel && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript компилируется; grep "getGroupExcuses\|approveExcuse\|rejectExcuse" headman-api.service.ts находит 3 метода</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HeadmanExcusesComponent полная реализация + тесты</name>
  <files>
    frontends/web-panel/src/app/features/headman/excuses/headman-excuses.component.ts
    frontends/web-panel/src/app/features/headman/excuses/headman-excuses.component.spec.ts
  </files>
  <read_first>
    - frontends/web-panel/src/app/features/headman/excuses/headman-excuses.component.ts (РЕДАКТИРОВАТЬ — полностью заменить заглушку)
    - frontends/web-panel/src/app/features/headman/excuses/headman-excuses.component.spec.ts (РЕДАКТИРОВАТЬ — написать реальные тесты)
    - frontends/web-panel/src/app/features/headman/ (найти как другие headman компоненты получают groupId — AuthService или store)
    - frontends/web-panel/src/app/features/headman/shared/headman-api.service.ts (обновлённый в task 1)
    - .planning/phases/59-excuses-backend/59-CONTEXT.md (D-23, D-24)
  </read_first>
  <behavior>
    - Test 1 (AC-10 загрузка): ngOnInit → HeadmanApiService.getGroupExcuses(groupId) вызывается
    - Test 2 (AC-10 approve): нажать кнопку "Одобрить" на pending тикете → approveExcuse(id) вызывается; список перезагружается
    - Test 3 (AC-10 reject): нажать "Отклонить" без комментария → ошибка "Введите комментарий"; с комментарием → rejectExcuse(id, comment) вызывается
    - Test 4: тикеты с status=submitted отображаются в pending-секции; approved/rejected в resolved-секции
  </behavior>
  <action>
    1. Заменить headman-excuses.component.ts полностью:
       - ChangeDetectionStrategy.OnPush, standalone: true
       - inject HeadmanApiService, inject MatSnackBar, inject(something для groupId)
       - signals: loading, tickets (ExcuseTicket[]), rejectComment (string), rejectingId (string | null)
       - computed/getter: pendingTickets = tickets().filter(t => t.status === 'submitted')
       - computed/getter: resolvedTickets = tickets().filter(t => t.status !== 'submitted')
       - ngOnInit() → loadTickets()
       - loadTickets(): вызвать headmanApi.getGroupExcuses(groupId) → tickets.set()
       - approve(id: string): headmanApi.approveExcuse(id).subscribe({ next: () => this.loadTickets() })
       - reject(id: string, comment: string): если !comment → validationError; headmanApi.rejectExcuse(id, comment).subscribe({ next: () => this.loadTickets() })
       - Template (inline или отдельный файл): секция "На рассмотрении" для pending, секция "Решения" для остальных. Карточки с именем студента, причиной, lessonIds count, кнопки. Для отклонения — инлайн текстовое поле для comment.
       - Добавить EXCUSE_TYPE_LABELS для отображения русских названий причин
       - Импорты: CommonModule, MatButtonModule, MatFormFieldModule, MatInputModule, MatSnackBarModule (или то что используется в headman)
    2. Обновить headman-excuses.component.spec.ts — 4 теста из behavior-блока.
    3. Запустить тесты.
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack/frontends/web-panel && npx vitest run --reporter=verbose 2>&1 | grep -E "headman-excuses|excuse|PASS|FAIL|Tests" | tail -20</automated>
  </verify>
  <done>headman-excuses тесты зелёные (4 штуки); grep "getGroupExcuses" headman-excuses.component.ts находит; grep "approveExcuse\|rejectExcuse" headman-excuses.component.ts находит</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Headman Angular → API | PATCH /excuses/{id}/status; id может быть произвольным (backend проверяет ownership) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-08-01 | Elevation of Privilege | headman пытается approve тикет чужой группы | mitigate | Backend: ExcuseService.updateStatus проверяет ctx.groupId == ticket.groupId (план 02) |
| T-59-08-02 | Elevation of Privilege | plain STUDENT вызывает approve endpoint | mitigate | Backend: @RequireRole(STUDENT) + if(!isHeadman) AccessDeniedException (план 02) |
| T-59-08-03 | Tampering | XSS в decisionComment | mitigate | Angular template binding escapes HTML; Mat input не вставляет innerHTML |
</threat_model>

<verification>
- `cd frontends/web-panel && npx tsc --noEmit` → без ошибок
- `cd frontends/web-panel && npx vitest run --reporter=verbose 2>&1 | grep -E "headman-excuses"` → тесты зелёные
- `grep "getGroupExcuses" frontends/web-panel/src/app/features/headman/excuses/headman-excuses.component.ts` → находит
- `grep "rejectExcuse\|approveExcuse" frontends/web-panel/src/app/features/headman/shared/headman-api.service.ts` → находит
- `cd frontends/web-panel && npm test -- --run` → все тесты (346+) зелёные
</verification>

<success_criteria>
- HeadmanExcusesComponent: не заглушка — реальная реализация с loadTickets/approve/reject
- Pending тикеты вверху, resolved внизу (D-24)
- Reject требует обязательный комментарий (D-24)
- HeadmanApiService: 3 новых метода getGroupExcuses/approveExcuse/rejectExcuse
- 4 Vitest теста зелёные (AC-10 частично)
</success_criteria>

<output>
После завершения создать `.planning/phases/59-excuses-backend/59-08-SUMMARY.md`
</output>
