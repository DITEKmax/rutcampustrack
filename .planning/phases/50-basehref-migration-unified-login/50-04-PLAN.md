---
phase: 50
plan: 04
type: execute
wave: 3
depends_on: ["50-01", "50-02"]
files_modified:
  - frontends/web-panel/src/app/features/login/login.component.ts
  - frontends/web-panel/src/app/features/login/login.component.spec.ts
  - frontends/web-panel/src/app/core/auth/role.guard.ts
  - frontends/web-panel/src/app/core/auth/auth.guard.spec.ts
autonomous: true
requirements:
  - AUTH-v9-01
  - AUTH-v9-02
  - AUTH-v9-03
  - AUTH-v9-06
must_haves:
  truths:
    - "login.component.ts использует resolveDashboardFor вместо hardcoded ADMIN/TEACHER ternary"
    - "После успешного логина ADMIN → /admin/dashboard, TEACHER → /teacher/dashboard, plain STUDENT → /student/dashboard, headman → /headman/dashboard"
    - "role.guard.ts fallback использует resolveDashboardFor (не hardcoded /admin или /teacher) — покрывает STUDENT"
    - "logout clears tokens + calls AuthApi.logout + redirects to /login (AUTH-v9-06 — существующее поведение НЕ регрессирует)"
    - "4 новых it-блока в login.component.spec.ts покрывают все 4 post-login редиректа"
    - "role.guard тесты покрывают STUDENT и headman сценарии"
  artifacts:
    - path: "frontends/web-panel/src/app/features/login/login.component.ts"
      provides: "Refactored onSubmit() with resolveDashboardFor"
      contains: "resolveDashboardFor"
    - path: "frontends/web-panel/src/app/core/auth/role.guard.ts"
      provides: "Fallback через resolveDashboardFor — поддерживает STUDENT"
      contains: "resolveDashboardFor"
  key_links:
    - from: "login.component.ts onSubmit next callback"
      to: "auth.service.ts resolveDashboardFor"
      via: "router.navigateByUrl(this.authService.resolveDashboardFor(this.authService.currentUser()))"
      pattern: "navigateByUrl.*resolveDashboardFor"
    - from: "role.guard.ts fallback"
      to: "auth.service.ts resolveDashboardFor"
      via: "auth.resolveDashboardFor(user)"
      pattern: "resolveDashboardFor"
---

<objective>
Заменить hardcoded role→path ternary в `login.component.ts` и `role.guard.ts` на вызов единого `AuthService.resolveDashboardFor`. Обновить специфицируемые существующие тесты на новый API (`navigateByUrl` вместо `navigate`) и добавить покрытие для STUDENT + headman сценариев.

Purpose: Это последний шаг для AUTH-v9-02 (4-role post-login redirect) и AUTH-v9-03 (currentUser reads from JWT). Также закрывает pitfall Research §Pitfall 2 ("roleGuard fallback hardcode ломает STUDENT"). Логаут (AUTH-v9-06) уже реализован в AuthService.logout — этот plan verify-only через существующий тест.

Plan 04 параллелен Plan 02 в идеальном мире (разные файлы), но вынужденно depends_on Plan 02 тоже — полный vitest suite в ШАГ 3 Task 2 подтягивает `app.routes.ts` (обновляется в Plan 03), который импортирует `guest.guard.ts` (создаётся в Plan 02). Plan 03 → Plan 02 → поэтому тут `depends_on: ["50-01", "50-02"]` (Plan 03 не требуется напрямую для тестов этого plan'а; app.routes.ts импортирует guest.guard независимо от того, поменял ли Plan 03 routes). Plan 01 нужен для `resolveDashboardFor`.

Output:
- `login.component.ts:49-50` → вызов `resolveDashboardFor`
- `role.guard.ts:16` → замена ternary
- `login.component.spec.ts` → 4 it-блока для post-login редиректов + фикс существующих (`navigate` → `navigateByUrl`)
- `auth.guard.spec.ts` (roleGuard describe) → STUDENT + headman тесты
- Полный vitest suite green
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-basehref-migration-unified-login/50-CONTEXT.md
@.planning/phases/50-basehref-migration-unified-login/50-RESEARCH.md
@frontends/web-panel/src/app/features/login/login.component.ts
@frontends/web-panel/src/app/features/login/login.component.spec.ts
@frontends/web-panel/src/app/core/auth/role.guard.ts
@frontends/web-panel/src/app/core/auth/auth.guard.spec.ts
@frontends/web-panel/src/app/core/auth/auth.service.ts

<interfaces>
<!-- Current hardcoded patterns that MUST be replaced -->

<!-- login.component.ts:47-51 — current ternary -->
```typescript
next: (tokens) => {
  this.authService.setTokens(tokens.accessToken, tokens.refreshToken);
  const role = this.authService.currentUser()?.role;
  this.router.navigate([role === 'ADMIN' ? '/admin/dashboard' : '/teacher/dashboard']);
},
```

<!-- role.guard.ts:15-18 — current ternary -->
```typescript
if (!allowedRoles.includes(user.role)) {
  const dashboard = user.role === 'ADMIN' ? '/admin/dashboard' : '/teacher/dashboard';
  return router.createUrlTree([dashboard]);
}
```

<!-- AuthService.resolveDashboardFor (from Plan 01) — the replacement -->
```typescript
resolveDashboardFor(user: AuthUser | null): string {
  if (!user) return '/login';
  if (user.role === 'ADMIN') return '/admin/dashboard';
  if (user.role === 'TEACHER') return '/teacher/dashboard';
  if (user.isHeadman) return '/headman/dashboard';
  return '/student/dashboard';
}
```

<!-- Existing login.component.spec.ts mock shape (must be updated) -->
```typescript
mockRouter = {
  navigate: vi.fn(),
  // ← нужно добавить: navigateByUrl: vi.fn()
};

mockAuthService = {
  setTokens: vi.fn(),
  currentUser: vi.fn().mockReturnValue(null),
  isAuthenticated: vi.fn().mockReturnValue(false) as any,
  // ← нужно добавить: resolveDashboardFor: vi.fn()
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Обновить role.guard.ts + role.guard тесты (STUDENT/headman coverage)</name>
  <files>frontends/web-panel/src/app/core/auth/role.guard.ts, frontends/web-panel/src/app/core/auth/auth.guard.spec.ts</files>
  <read_first>
    - frontends/web-panel/src/app/core/auth/role.guard.ts (полный файл — строки 1-21)
    - frontends/web-panel/src/app/core/auth/auth.guard.spec.ts (roleGuard describe block — строки 57-112)
    - frontends/web-panel/src/app/core/auth/auth.service.ts (resolveDashboardFor signature, обновлённый AuthUser)
    - .planning/phases/50-basehref-migration-unified-login/50-CONTEXT.md §D-09
    - .planning/phases/50-basehref-migration-unified-login/50-RESEARCH.md §Pitfall 2
  </read_first>
  <behavior>
    - `roleGuard(['ADMIN'])` для TEACHER токена → редирект на `/teacher/dashboard` (через resolveDashboardFor, не hardcoded)
    - `roleGuard(['TEACHER'])` для ADMIN токена → редирект на `/admin/dashboard` (через resolveDashboardFor)
    - `roleGuard(['ADMIN'])` для plain STUDENT токена → редирект на `/student/dashboard` (НОВОЕ — раньше улетал на /teacher/dashboard из-за hardcode)
    - `roleGuard(['ADMIN'])` для headman токена → редирект на `/headman/dashboard` (НОВОЕ)
    - `roleGuard(['TEACHER'])` для STUDENT токена → редирект на `/student/dashboard`
    - Существующие 4 it-блока (из auth.guard.spec.ts) остаются green после замены implementation
  </behavior>
  <action>
    **ШАГ 1 — RED: добавить новые тесты в существующий auth.guard.spec.ts.**

    Открыть `frontends/web-panel/src/app/core/auth/auth.guard.spec.ts`.

    В секции constants (после `const ADMIN_TOKEN = ...`) добавить:
    ```typescript
    const STUDENT_TOKEN = makeJwt({ sub: '3', role: 'STUDENT', is_headman: false, group_id: 5, exp: 9999999999 });
    const HEADMAN_TOKEN = makeJwt({ sub: '4', role: 'STUDENT', is_headman: true, group_id: 5, exp: 9999999999 });
    ```

    Также обновить `TEACHER_TOKEN` и `ADMIN_TOKEN` для соответствия новому JWT shape:
    ```typescript
    const TEACHER_TOKEN = makeJwt({ sub: '1', role: 'TEACHER', is_headman: false, group_id: null, exp: 9999999999 });
    const ADMIN_TOKEN = makeJwt({ sub: '2', role: 'ADMIN', is_headman: false, group_id: null, exp: 9999999999 });
    ```

    В `describe('roleGuard', ...)` блок добавить 3 новых it-блока в конец (перед закрывающей `});`):
    ```typescript
    it('roleGuard([ADMIN]) redirects plain STUDENT to /student/dashboard via resolveDashboardFor', () => {
      authService.setTokens(STUDENT_TOKEN, REFRESH_TOKEN);
      const result = TestBed.runInInjectionContext(() =>
        roleGuard(['ADMIN'])({} as any, {} as any)
      );
      expect(result).not.toBe(true);
      expect(router.createUrlTree).toHaveBeenCalledWith(['/student/dashboard']);
    });

    it('roleGuard([ADMIN]) redirects headman to /headman/dashboard via resolveDashboardFor', () => {
      authService.setTokens(HEADMAN_TOKEN, REFRESH_TOKEN);
      const result = TestBed.runInInjectionContext(() =>
        roleGuard(['ADMIN'])({} as any, {} as any)
      );
      expect(result).not.toBe(true);
      expect(router.createUrlTree).toHaveBeenCalledWith(['/headman/dashboard']);
    });

    it('roleGuard([TEACHER]) redirects plain STUDENT to /student/dashboard via resolveDashboardFor', () => {
      authService.setTokens(STUDENT_TOKEN, REFRESH_TOKEN);
      const result = TestBed.runInInjectionContext(() =>
        roleGuard(['TEACHER'])({} as any, {} as any)
      );
      expect(result).not.toBe(true);
      expect(router.createUrlTree).toHaveBeenCalledWith(['/student/dashboard']);
    });
    ```

    Запустить тесты: `cd frontends/web-panel && npx vitest run --reporter=dot src/app/core/auth/auth.guard.spec.ts`

    Ожидается: 3 новых теста падают потому что role.guard.ts всё ещё использует hardcoded ternary, отправляя STUDENT на `/teacher/dashboard` вместо `/student/dashboard`.

    **ШАГ 2 — GREEN: обновить role.guard.ts.**

    Открыть `frontends/web-panel/src/app/core/auth/role.guard.ts`.

    Заменить всю функцию (строки 6-21) на:
    ```typescript
    export const roleGuard =
      (allowedRoles: string[]): CanActivateFn =>
      () => {
        const auth = inject(AuthService);
        const router = inject(Router);

        const user = auth.currentUser();
        if (!user) return router.createUrlTree(['/login']);

        if (!allowedRoles.includes(user.role)) {
          return router.createUrlTree([auth.resolveDashboardFor(user)]);
        }

        return true;
      };
    ```

    Это удаляет hardcoded строку `const dashboard = user.role === 'ADMIN' ? '/admin/dashboard' : '/teacher/dashboard';` и заменяет её на единый вызов `resolveDashboardFor`.

    **ШАГ 3 — verify GREEN.**
    ```
    cd frontends/web-panel && npx vitest run --reporter=dot src/app/core/auth/auth.guard.spec.ts
    ```
    Все roleGuard тесты (4 существующих + 3 новых = 7) + все authGuard тесты (2) должны быть green.
  </action>
  <verify>
    <automated>cd frontends/web-panel &amp;&amp; npx vitest run --reporter=dot src/app/core/auth/auth.guard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "auth.resolveDashboardFor(user)" frontends/web-panel/src/app/core/auth/role.guard.ts` → 1
    - NO hardcoded ternary: `grep -c "role === 'ADMIN' \? '/admin/dashboard'" frontends/web-panel/src/app/core/auth/role.guard.ts` → 0
    - `grep -c "'/teacher/dashboard'" frontends/web-panel/src/app/core/auth/role.guard.ts` → 0
    - `grep -c "'/admin/dashboard'" frontends/web-panel/src/app/core/auth/role.guard.ts` → 0
    - `grep -c "STUDENT_TOKEN" frontends/web-panel/src/app/core/auth/auth.guard.spec.ts` → ≥ 2 (definition + uses)
    - `grep -c "HEADMAN_TOKEN" frontends/web-panel/src/app/core/auth/auth.guard.spec.ts` → ≥ 2
    - `grep -c "'/student/dashboard'" frontends/web-panel/src/app/core/auth/auth.guard.spec.ts` → ≥ 2 (2 новых теста)
    - `grep -c "'/headman/dashboard'" frontends/web-panel/src/app/core/auth/auth.guard.spec.ts` → ≥ 1 (1 новый тест)
    - `grep -c "^  it(" frontends/web-panel/src/app/core/auth/auth.guard.spec.ts` → ≥ 9 (2 auth + 4 existing roleGuard + 3 new)
    - `cd frontends/web-panel && npx vitest run --reporter=dot src/app/core/auth/auth.guard.spec.ts` → exit 0, все тесты passed
  </acceptance_criteria>
  <done>
    role.guard.ts использует единый resolveDashboardFor — Pitfall 2 закрыт. STUDENT и headman корректно редиректятся при попытке зайти на роль, которой у них нет. 3 новых теста подтверждают.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Обновить login.component.ts + login.component.spec.ts (4 post-login редиректа)</name>
  <files>frontends/web-panel/src/app/features/login/login.component.ts, frontends/web-panel/src/app/features/login/login.component.spec.ts</files>
  <read_first>
    - frontends/web-panel/src/app/features/login/login.component.ts (полный файл — строки 1-64)
    - frontends/web-panel/src/app/features/login/login.component.spec.ts (все 10 it-блоков, особенно строки 108-157 — существующие redirect тесты)
    - frontends/web-panel/src/app/core/auth/auth.service.ts (resolveDashboardFor signature)
    - .planning/phases/50-basehref-migration-unified-login/50-CONTEXT.md §D-09 "Used by: login.component.ts"
    - .planning/phases/50-basehref-migration-unified-login/50-RESEARCH.md §Q7 "login.component.ts redirect logic"
    - .planning/phases/50-basehref-migration-unified-login/50-RESEARCH.md §Pitfall 4 "login.component.spec.ts — существующие тесты сломаются"
  </read_first>
  <behavior>
    - На успешный login, компонент вызывает `router.navigateByUrl(this.authService.resolveDashboardFor(this.authService.currentUser()))` (не `router.navigate([...])`)
    - TEACHER логин → `navigateByUrl('/teacher/dashboard')`
    - ADMIN логин → `navigateByUrl('/admin/dashboard')`
    - Plain STUDENT логин → `navigateByUrl('/student/dashboard')` (НОВОЕ)
    - Headman STUDENT логин → `navigateByUrl('/headman/dashboard')` (НОВОЕ)
    - Существующие 8 не-redirect тестов (render форма, disabled кнопка, 401 error, network error, password clear) остаются green
  </behavior>
  <action>
    **ШАГ 1 — RED: обновить spec.**

    Открыть `frontends/web-panel/src/app/features/login/login.component.spec.ts`.

    В начале файла (после `const REFRESH_TOKEN = ...`) добавить 2 новых токена:
    ```typescript
    const STUDENT_TOKEN = makeJwt({ sub: '3', role: 'STUDENT', is_headman: false, group_id: 5, exp: 9999999999 });
    const HEADMAN_TOKEN = makeJwt({ sub: '4', role: 'STUDENT', is_headman: true, group_id: 5, exp: 9999999999 });
    ```

    Также обновить существующие `TEACHER_TOKEN` и `ADMIN_TOKEN` для соответствия новому JWT shape:
    ```typescript
    const TEACHER_TOKEN = makeJwt({ sub: '1', role: 'TEACHER', is_headman: false, group_id: null, exp: 9999999999 });
    const ADMIN_TOKEN = makeJwt({ sub: '2', role: 'ADMIN', is_headman: false, group_id: null, exp: 9999999999 });
    ```

    В `beforeEach` (строки 26-40) обновить mockAuthService и mockRouter:
    ```typescript
    mockAuthService = {
      setTokens: vi.fn(),
      currentUser: vi.fn().mockReturnValue(null),
      isAuthenticated: vi.fn().mockReturnValue(false) as any,
      resolveDashboardFor: vi.fn((user) => {
        if (!user) return '/login';
        if (user.role === 'ADMIN') return '/admin/dashboard';
        if (user.role === 'TEACHER') return '/teacher/dashboard';
        if (user.isHeadman) return '/headman/dashboard';
        return '/student/dashboard';
      }),
    };

    mockRouter = {
      navigate: vi.fn(),
      navigateByUrl: vi.fn(),
    };
    ```

    **Обновить существующий тест "on successful login ... TEACHER role"** (строки 108-132). Заменить:
    ```typescript
    (mockAuthService.currentUser as ReturnType<typeof vi.fn>).mockReturnValue({
      id: 1,
      role: 'TEACHER',
    });
    ```
    на:
    ```typescript
    (mockAuthService.currentUser as ReturnType<typeof vi.fn>).mockReturnValue({
      id: 1,
      role: 'TEACHER',
      isHeadman: false,
      groupId: null,
    });
    ```

    И assertions в конце теста (строка 131) заменить:
    ```typescript
    expect(mockRouter.navigate).toHaveBeenCalledWith(['/teacher/dashboard']);
    ```
    на:
    ```typescript
    expect(mockRouter.navigateByUrl).toHaveBeenCalledWith('/teacher/dashboard');
    ```

    **Обновить существующий тест "on successful login with ADMIN role"** (строки 134-157) аналогично: mock currentUser с полным shape `{id:2, role:'ADMIN', isHeadman:false, groupId:null}` и assertion `expect(mockRouter.navigateByUrl).toHaveBeenCalledWith('/admin/dashboard')`.

    **Добавить 2 новых it-блока** после существующего ADMIN теста (перед тестом "on 401 error"):
    ```typescript
    it('on successful login with plain STUDENT role, navigates to /student/dashboard', async () => {
      (mockAuthApi.login as ReturnType<typeof vi.fn>).mockReturnValue(
        of({ accessToken: STUDENT_TOKEN, refreshToken: REFRESH_TOKEN, expiresIn: 3600 })
      );
      (mockAuthService.currentUser as ReturnType<typeof vi.fn>).mockReturnValue({
        id: 3,
        role: 'STUDENT',
        isHeadman: false,
        groupId: 5,
      });

      await render(LoginComponent, {
        providers: [
          { provide: AuthApi, useValue: mockAuthApi },
          { provide: AuthService, useValue: mockAuthService },
          { provide: Router, useValue: mockRouter },
        ],
      });

      const user = userEvent.setup();
      await user.type(screen.getByLabelText(/логин/i), 'student00001');
      await user.type(screen.getByLabelText(/пароль/i), 'password123');
      await user.click(screen.getByRole('button', { name: /войти/i }));

      expect(mockRouter.navigateByUrl).toHaveBeenCalledWith('/student/dashboard');
    });

    it('on successful login with headman STUDENT role, navigates to /headman/dashboard', async () => {
      (mockAuthApi.login as ReturnType<typeof vi.fn>).mockReturnValue(
        of({ accessToken: HEADMAN_TOKEN, refreshToken: REFRESH_TOKEN, expiresIn: 3600 })
      );
      (mockAuthService.currentUser as ReturnType<typeof vi.fn>).mockReturnValue({
        id: 4,
        role: 'STUDENT',
        isHeadman: true,
        groupId: 5,
      });

      await render(LoginComponent, {
        providers: [
          { provide: AuthApi, useValue: mockAuthApi },
          { provide: AuthService, useValue: mockAuthService },
          { provide: Router, useValue: mockRouter },
        ],
      });

      const user = userEvent.setup();
      await user.type(screen.getByLabelText(/логин/i), 'headman01');
      await user.type(screen.getByLabelText(/пароль/i), 'password123');
      await user.click(screen.getByRole('button', { name: /войти/i }));

      expect(mockRouter.navigateByUrl).toHaveBeenCalledWith('/headman/dashboard');
    });
    ```

    Запустить `cd frontends/web-panel && npx vitest run --reporter=dot src/app/features/login/login.component.spec.ts` — ожидается что 2 существующих (обновлённых) + 2 новых теста падают, потому что login.component.ts всё ещё вызывает `router.navigate` вместо `router.navigateByUrl`.

    **ШАГ 2 — GREEN: обновить login.component.ts.**

    Открыть `frontends/web-panel/src/app/features/login/login.component.ts`.

    Найти блок `next: (tokens) => { ... }` (строки 47-51). Заменить на:
    ```typescript
    next: (tokens) => {
      this.authService.setTokens(tokens.accessToken, tokens.refreshToken);
      const target = this.authService.resolveDashboardFor(this.authService.currentUser());
      this.router.navigateByUrl(target);
    },
    ```

    Обратить внимание: `const role = ...` (строка 49) удаляется полностью. `router.navigate([ternary])` (строка 50) заменяется на `router.navigateByUrl(target)`.

    **ШАГ 3 — verify login spec + full suite.**
    ```
    cd frontends/web-panel && npx vitest run --reporter=dot src/app/features/login/login.component.spec.ts
    ```
    Все 12 тестов (10 существующих + 2 новых) green.

    ```
    cd frontends/web-panel && npx vitest run --reporter=dot
    ```
    Полный suite green.
  </action>
  <verify>
    <automated>cd frontends/web-panel &amp;&amp; npx vitest run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "this.authService.resolveDashboardFor(this.authService.currentUser())" frontends/web-panel/src/app/features/login/login.component.ts` → 1
    - `grep -c "this.router.navigateByUrl" frontends/web-panel/src/app/features/login/login.component.ts` → 1
    - NO hardcoded ternary: `grep -c "role === 'ADMIN' \? '/admin/dashboard'" frontends/web-panel/src/app/features/login/login.component.ts` → 0
    - NO `router.navigate([` в next callback: `grep -cE "this\.router\.navigate\(\[" frontends/web-panel/src/app/features/login/login.component.ts` → 0
    - `grep -c "STUDENT_TOKEN" frontends/web-panel/src/app/features/login/login.component.spec.ts` → ≥ 2
    - `grep -c "HEADMAN_TOKEN" frontends/web-panel/src/app/features/login/login.component.spec.ts` → ≥ 2
    - `grep -c "navigateByUrl" frontends/web-panel/src/app/features/login/login.component.spec.ts` → ≥ 4 (4 redirect тестa используют)
    - `grep -c "'/student/dashboard'" frontends/web-panel/src/app/features/login/login.component.spec.ts` → ≥ 1
    - `grep -c "'/headman/dashboard'" frontends/web-panel/src/app/features/login/login.component.spec.ts` → ≥ 1
    - `grep -c "resolveDashboardFor" frontends/web-panel/src/app/features/login/login.component.spec.ts` → ≥ 1 (mock)
    - `grep -c "^    it(" frontends/web-panel/src/app/features/login/login.component.spec.ts` → ≥ 12 (10 existing + 2 new)
    - `cd frontends/web-panel && npx vitest run --reporter=dot` → exit 0, full suite green
  </acceptance_criteria>
  <done>
    login.component.ts полностью использует resolveDashboardFor. 4 redirect теста (TEACHER/ADMIN обновлены, STUDENT/headman добавлены) green. role.guard.ts обновлён (Task 1). AUTH-v9-01, AUTH-v9-02, AUTH-v9-03 закрыты unit-тестами. AUTH-v9-06 (logout) не регрессирует — существующий AuthService.logout test остаётся green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User credentials ↔ auth.service.login | Passes through existing tested auth.api.ts |
| JWT → routing decision | resolveDashboardFor является the only authorized source для post-auth routing в SPA |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-15 | Tampering | Пользователь подделывает JWT с `role: 'ADMIN'` | accept | Клиент-side routing — не security boundary. API Gateway (неизменен в Phase 50) валидирует signature JWT на каждом запросе. resolveDashboardFor меняет только URL, куда пойдёт браузер; как только user попадает на `/admin/dashboard`, каждый API-запрос оттуда проверяется gateway'ем. |
| T-50-16 | Elevation of Privilege | roleGuard hardcoded fallback отправляет STUDENT на `/teacher/dashboard` (существующий bug) | mitigate | Task 1 заменяет fallback на resolveDashboardFor. Теперь STUDENT→student dashboard, headman→headman dashboard. Test "roleGuard([ADMIN]) redirects plain STUDENT" явно валидирует. |
| T-50-17 | Session Hijack | logout не отзывает refresh token | accept | AuthService.logout уже вызывает `authApi.logout(rt)` который обращается к backend `/api/auth/logout`. Существующий test validates это. Phase 50 не меняет logout flow, AUTH-v9-06 — verify-only. |
| T-50-18 | Information Disclosure | navigateByUrl с неэкранированным path | accept | resolveDashboardFor возвращает только 5 hardcoded string литералов — никакого user input. Path injection невозможен. |
</threat_model>

<verification>
Plan gate:
- `cd frontends/web-panel && npx vitest run` → exit 0, full suite green
- Grep проверки из обоих tasks все passing
- Никаких hardcoded role→path ternary в login.component.ts или role.guard.ts
- `login.component.spec.ts` имеет 12+ it-блоков, покрывает все 4 post-login редиректа через navigateByUrl
</verification>

<success_criteria>
- login.component.ts и role.guard.ts используют AuthService.resolveDashboardFor как единственный источник истины
- 4 post-login redirect сценария протестированы (TEACHER, ADMIN, plain STUDENT, headman)
- roleGuard тесты покрывают STUDENT + headman fallback paths
- 5+ новых тестов добавлено (3 в role.guard + 2 в login.component)
- Полный vitest suite green
- AUTH-v9-01, AUTH-v9-02, AUTH-v9-03 закрыты unit-тестами
- AUTH-v9-06 verify-only (существующий test не регрессирует)
</success_criteria>

<output>
После завершения создать `.planning/phases/50-basehref-migration-unified-login/50-04-SUMMARY.md`
</output>
