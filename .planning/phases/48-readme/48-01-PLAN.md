---
phase: 48-readme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-04

must_haves:
  truths:
    - "A developer can understand the system architecture (services, ports, databases, communication patterns) from the README alone"
    - "A developer can follow the README setup instructions to run the system locally with docker compose"
    - "The README links to the live Swagger UI and summarizes key API endpoints by role"
    - "The README contains a complete deploy guide (VPS setup, GitHub Secrets, first certbot run, compose up)"
  artifacts:
    - path: "README.md"
      provides: "Complete project documentation for first-time developers"
      contains: "## Architecture"
      min_lines: 250
  key_links:
    - from: "README.md"
      to: "docker-compose.yml"
      via: "setup instructions reference"
      pattern: "docker.compose"
    - from: "README.md"
      to: "docker-compose.prod.yml"
      via: "production deploy guide"
      pattern: "docker-compose\\.prod\\.yml"
    - from: "README.md"
      to: "Swagger UI"
      via: "API documentation link"
      pattern: "swagger|openapi"
---

<objective>
Write a complete README.md for the RutCampusTrack repository that enables a developer reading it for the first time to understand the architecture, run the system locally, find API documentation, and deploy to production.

Purpose: The current README is a minimal stub (~90 lines). Phase 48 replaces it with a comprehensive developer-facing document covering all four success criteria from the roadmap.
Output: README.md (complete rewrite, ~300-400 lines)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@docs/architecture.md
@docker-compose.yml
@docker-compose.prod.yml
@nginx/scripts/init-letsencrypt.sh
@.github/workflows/ci.yml
@.github/workflows/deploy.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md with full project documentation</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state to understand existing structure)
    - CLAUDE.md (source of truth for architecture, services, ports, roles, business rules)
    - docs/architecture.md (detailed architecture diagram and service topology)
    - docker-compose.yml (dev infrastructure services and ports)
    - docker-compose.prod.yml (production compose structure, all 17 services)
    - nginx/scripts/init-letsencrypt.sh (SSL bootstrap procedure)
    - .github/workflows/ci.yml (CI pipeline structure)
    - .github/workflows/deploy.yml (deploy pipeline structure, GHCR image names)
  </read_first>
  <action>
Completely rewrite README.md with the following sections. The file should be written in Russian (matching the existing project language in CLAUDE.md) with English for code/commands. Target ~300-400 lines.

**Section structure (exact headings):**

1. **Title block**: `# RutCampusTrack` with a one-line description: "Микросервисная система учёта посещаемости для РУТ (МИИТ)". Add badges for Java 21, Spring Boot 3.4, Python 3.12, Docker, GitHub Actions CI status.

2. **## Стек технологий**: Technology table (already exists, keep and update). Must include all: Java 21 + Spring Boot 3.4, Spring Cloud Gateway, Python 3.12 + Aiogram 3.x, Gradle 8.12, gRPC, RabbitMQ 3.13, PostgreSQL 16, MongoDB 7, Redis 7, Docker + Docker Compose, GitHub Actions, React + Vite (PWA and Mini App), Angular 18 (Web Panel), HTML+CSS (Landing), Nginx + Let's Encrypt (SSL).

3. **## Архитектура**: ASCII architecture diagram from docs/architecture.md (the topology box diagram showing Gateway, services, databases, notification containers, RabbitMQ, Redis). Include the service-to-storage matrix table from architecture.md. Add a brief text paragraph explaining: contract-first approach, database-per-service, single external port (Gateway :8080 in dev, nginx :443 in prod), gRPC for inter-service sync, RabbitMQ for async events.

4. **## Структура проекта**: Directory tree (already exists, update to include PWA frontend which is missing from current README). Must show:
   ```
   rutcampustrack/
   ├── proto/                              gRPC контракты (.proto)
   ├── event-schemas/                      JSON Schema для событий RabbitMQ
   ├── services/
   │   ├── api-gateway/                    Spring Cloud Gateway (:8080)
   │   ├── auth-service/                   JWT, OTP, Redis (:9090)
   │   ├── academic-service/
   │   │   ├── academic-api-contract/      DTO, интерфейсы
   │   │   └── academic-app/              (:9091)
   │   ├── schedule-service/
   │   │   ├── schedule-api-contract/
   │   │   └── schedule-app/              (:9092)
   │   ├── attendance-service/
   │   │   ├── attendance-api-contract/
   │   │   └── attendance-app/            (:9093)
   │   ├── notification-web/              WebSocket + Web Push (:9094)
   │   └── notification-bot/              Python Aiogram
   ├── frontends/
   │   ├── pwa/                            React PWA (мобильный клиент)
   │   ├── mini-app/                       React (Telegram Mini App)
   │   ├── web-panel/                      Angular (админка)
   │   └── landing/                        HTML + CSS
   ├── nginx/                              Reverse proxy + SSL config
   ├── .github/workflows/                  CI + Deploy pipelines
   ├── docker-compose.yml                  Dev инфраструктура
   ├── docker-compose.prod.yml             Production compose (17 сервисов)
   └── build.gradle.kts                    Корневой Gradle
   ```

5. **## Роли**: Table with 4 roles (ADMIN, TEACHER, STUDENT, STUDENT+headman) and their capabilities from CLAUDE.md. Keep brief — 4 rows, 2 columns (role, capabilities).

6. **## Быстрый старт (Development)**: Step-by-step local development setup. Must include:
   - Prerequisites: Java 21, Docker, Node.js 18+
   - `git clone`, `cd rutcampustrack`
   - `docker compose up -d` (starts PostgreSQL x2, MongoDB, Redis, RabbitMQ)
   - `docker compose ps` (verify all infra healthy)
   - `./gradlew build` (builds all Java services)
   - Running individual services (e.g., `./gradlew :services:auth-service:bootRun`)
   - For frontends: `cd frontends/pwa && npm install && npm run dev`
   - Ports table: Gateway 8080, Auth 9090, Academic 9091, Schedule 9092, Attendance 9093, Notification Web 9094, RabbitMQ Management 15672
   - Test credentials: `student` / `teacher` / `admin` (password: mention looking at seed data)

7. **## API документация (Swagger UI)**: Explain that Swagger UI is aggregated at the Gateway. In development: `http://localhost:8080/swagger-ui.html`. In production: `https://{domain}/swagger-ui.html`. List grouped API specs: Auth (`/api/auth/**`), Academic (`/api/academic/**`), Schedule (`/api/schedule/**`), Attendance (`/api/attendance/**`). Note: notification-web has separate endpoints at `/api/notifications/**`. Mention role-based access — which endpoints need JWT and which roles.

8. **## Тестирование**: Commands to run tests:
   - All Java: `./gradlew test`
   - Specific service: `./gradlew :services:academic-service:academic-app:test`
   - Python bot: `cd services/notification-bot && python -m pytest`
   - Frontend: `cd frontends/pwa && npm test`
   - Total test count: ~350+ tests across all services

9. **## CI/CD**: Describe the two GitHub Actions workflows:
   - **CI** (`ci.yml`): Triggered on push/PR. Builds and tests all Java services, lints Python bot (ruff), builds frontends. Uses Gradle cache.
   - **Deploy** (`deploy.yml`): Triggered on push to main. Builds 11 Docker images, pushes to GHCR (`ghcr.io/maksd/rutcampustrack/{service}:latest`), SSHs to VPS and runs `docker compose pull && docker compose up -d`.

10. **## Развёртывание (Production)**: Complete deploy guide subsections:
    - **### Требования к серверу**: Ubuntu 22.04+, Docker + Docker Compose v2, 4GB+ RAM, ports 80/443 open, domain with DNS A record.
    - **### GitHub Secrets**: List all required secrets for the deploy workflow. Read deploy.yml to extract the exact secret names (VPS_HOST, VPS_USER, VPS_SSH_KEY, plus all .env.prod variables).
    - **### Первое развёртывание**: Step by step:
      1. SSH into VPS
      2. Clone repo
      3. Create `.env.prod` from template (list key variables: POSTGRES passwords, MONGO password, Redis password, RabbitMQ password, RSA keys base64, DOMAIN, CERTBOT_EMAIL, TELEGRAM_BOT_TOKEN, VAPID keys)
      4. Run `./nginx/scripts/init-letsencrypt.sh` for first SSL certificate
      5. `docker compose -f docker-compose.prod.yml up -d`
      6. Verify: `docker compose -f docker-compose.prod.yml ps`
    - **### Обновление**: After merge to main, deploy.yml auto-pushes images. On VPS: images are pulled and services restarted automatically via SSH deploy step.
    - **### SSL сертификаты**: Certbot container handles auto-renewal. Manual renewal: `docker compose -f docker-compose.prod.yml exec certbot certbot renew`.

11. **## Документация**: Links to docs/ folder:
    - [Архитектура](docs/architecture.md)
    - [Job Stories](docs/job-stories.md)
    - [Схема БД](docs/database-schema.md)
    - [Дизайн-решения](docs/design-decisions.md)

12. **## Лицензия**: "MIT" (or note that license file does not exist yet — check if LICENSE file exists).

Do NOT include emojis anywhere in the README. Use plain markdown.
  </action>
  <verify>
    <automated>grep -c "## " README.md | xargs -I{} test {} -ge 10 && echo "PASS: 10+ sections" || echo "FAIL: fewer than 10 sections"; wc -l README.md | awk "{print ($1 >= 250) ? \"PASS: \" $1 \" lines\" : \"FAIL: only \" $1 \" lines\"}"; grep -q "## Архитектура" README.md && echo "PASS: Architecture section" || echo "FAIL: no Architecture section"; grep -q "docker compose" README.md && echo "PASS: docker compose referenced" || echo "FAIL: no docker compose"; grep -q "swagger" README.md && echo "PASS: Swagger referenced" || echo "FAIL: no Swagger"; grep -q "init-letsencrypt" README.md && echo "PASS: certbot init referenced" || echo "FAIL: no certbot init"; grep -q "docker-compose.prod.yml" README.md && echo "PASS: prod compose referenced" || echo "FAIL: no prod compose"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains "## Архитектура" heading
    - README.md contains "## Быстрый старт" heading
    - README.md contains "## API документация" or "## API" heading
    - README.md contains "## Развёртывание" heading
    - README.md contains "docker-compose.prod.yml"
    - README.md contains "swagger-ui" (case-insensitive)
    - README.md contains "init-letsencrypt.sh"
    - README.md contains "ghcr.io" (GHCR image references)
    - README.md contains "VPS_HOST" or "VPS_SSH_KEY" (GitHub Secrets)
    - README.md contains port table with 8080, 9090, 9091, 9092, 9093, 9094
    - README.md is at least 250 lines
    - README.md contains "## Роли" heading
    - README.md contains "## Тестирование" heading
    - README.md contains "## CI/CD" heading
  </acceptance_criteria>
  <done>README.md is a complete, self-contained developer onboarding document. A developer can understand architecture, run locally, find Swagger UI, and deploy to production — all from the README alone. All 4 phase success criteria are met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — this plan only creates documentation (README.md). No code, no endpoints, no data processing.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | I (Information Disclosure) | README.md | mitigate | Do not include actual passwords, tokens, or RSA keys in README. Use placeholder values like `your_secure_password_here`. Reference .env.prod as the secret store. |
</threat_model>

<verification>
1. README.md exists and is 250+ lines
2. All 12 sections present with correct headings
3. Architecture diagram included (ASCII art or text description)
4. Local dev setup instructions reference `docker compose up -d` and `./gradlew build`
5. Swagger UI link present (`/swagger-ui.html`)
6. Production deploy guide includes: VPS requirements, GitHub Secrets list, init-letsencrypt.sh, docker-compose.prod.yml
7. No real secrets or passwords in the file
</verification>

<success_criteria>
A developer cloning the repository for the first time can:
1. Understand what each microservice does, what database it uses, and how they communicate
2. Run `docker compose up -d` and `./gradlew build` to get the system running locally
3. Navigate to Swagger UI to explore API endpoints
4. Follow the deploy guide to set up a VPS with SSL and automated deployments
</success_criteria>

<output>
After completion, create `.planning/phases/48-readme/48-01-SUMMARY.md`
</output>
