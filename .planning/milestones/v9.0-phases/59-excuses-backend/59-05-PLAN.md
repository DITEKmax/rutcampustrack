---
phase: 59-excuses-backend
plan: 05
type: execute
wave: 3
depends_on:
  - 59-02
files_modified:
  - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/excuse/ExcuseEventPublisher.java
  - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/excuse/ExcuseService.java
  - services/attendance-service/attendance-app/src/test/java/ru/rutcampustrack/attendance/excuse/ExcuseEventPublisherTest.java
  - services/attendance-service/attendance-app/src/test/java/ru/rutcampustrack/attendance/excuse/ExcuseEventContractIT.java
  - services/attendance-service/attendance-app/src/test/resources/fixtures/excuse_requested.json
  - services/attendance-service/attendance-app/src/test/resources/fixtures/excuse_decided.json
autonomous: true
requirements:
  - FR-2
  - FR-6
  - NFR-1
  - NFR-5
  - AC-7

must_haves:
  truths:
    - "ExcuseEventPublisher.publishRequested() публикует в RabbitMQ exchange 'rut-uit.events' с event_type 'excuse.requested'"
    - "ExcuseEventPublisher.publishDecided() публикует event_type 'excuse.decided'"
    - "JSON-схема excuse.requested совпадает с ожиданиями notification-bot headman_alerts.py (payload.group_id, payload.user_id, payload.student_name, payload.excuse_type)"
    - "ExcuseService.createExcuse() вызывает publishRequested() после сохранения тикета"
    - "ExcuseService.updateStatus() вызывает publishDecided() после сохранения решения"
    - "Контракт-тест ExcuseEventContractIT: публикует event и проверяет JSON-схему"
  artifacts:
    - path: "services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/excuse/ExcuseEventPublisher.java"
      provides: "RabbitMQ event publishing для excuse.requested и excuse.decided"
    - path: "services/attendance-service/attendance-app/src/test/resources/fixtures/excuse_requested.json"
      provides: "Канонический event payload (source of truth для контракт-теста)"
    - path: "services/attendance-service/attendance-app/src/test/resources/fixtures/excuse_decided.json"
      provides: "Канонический decided payload"
    - path: "services/attendance-service/attendance-app/src/test/java/ru/rutcampustrack/attendance/excuse/ExcuseEventContractIT.java"
      provides: "Contract test AC-7"
  key_links:
    - from: "ExcuseService.createExcuse"
      to: "ExcuseEventPublisher.publishRequested"
      via: "after repository.save()"
      pattern: "excuseEventPublisher.publishRequested"
    - from: "ExcuseEventPublisher"
      to: "RabbitTemplate.convertAndSend(exchange, '', envelope)"
      via: "fanout exchange rut-uit.events"
      pattern: "rut-uit.events"
---

<objective>
Реализовать RabbitMQ event publishing для excuse.requested и excuse.decided, плюс контракт-тест с notification-bot JSON-схемой (Wave 3, параллельно с планом 04).

Purpose: D-19/D-20 требуют event publishing. Notification-bot уже слушает excuse.requested — нужно убедиться что JSON-структура совместима (D-27). Контракт-тест обязателен (AC-7).

Output:
- `ExcuseEventPublisher.java` — публикует два типа событий
- `ExcuseService.java` — добавить вызовы publisher в createExcuse и updateStatus
- `ExcuseEventPublisherTest.java` — unit-тест с mock RabbitTemplate
- `ExcuseEventContractIT.java` — контракт-тест с Testcontainers RabbitMQ
- Fixture JSON файлы в test/resources/fixtures/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-excuses-backend/59-CONTEXT.md
@.planning/phases/59-excuses-backend/59-RESEARCH.md
@.planning/phases/59-excuses-backend/59-02-SUMMARY.md

<interfaces>
<!-- Существующий паттерн event publishing -->

AttendanceEventPublisher.java (образец):
```java
@Service
public class AttendanceEventPublisher {
    private static final String EXCHANGE = "rut-uit.events";
    private final RabbitTemplate rabbitTemplate;

    public void publishMarked(AttendanceDocument doc) {
        Map<String, Object> payload = Map.of(
                "lesson_id", doc.getLessonId(),
                "user_id", doc.getUserId(),
                // ... другие поля
        );
        Map<String, Object> envelope = new LinkedHashMap<>();
        envelope.put("event_type", "attendance.marked");  // <- KEY: event_type, не type
        envelope.put("event_id", UUID.randomUUID().toString());
        envelope.put("occurred_at", Instant.now().toString());
        envelope.put("payload", payload);
        rabbitTemplate.convertAndSend(EXCHANGE, "", envelope);
    }
}
```

КРИТИЧНО: bot/consumers/event_dispatcher.py читает event.get("event_type"), не event.get("type")!
bot/notifications/headman_alerts.py читает:
```python
event_type = event.get("event_type")
payload = event.get("payload", {})
group_id = payload["group_id"]
user_id = payload["user_id"]
student_name = payload.get("student_name")
excuse_type_label = payload.get("excuse_type", "не указан")
```

ПОЭТОМУ envelope структура для excuse.requested:
```json
{
  "event_type": "excuse.requested",
  "event_id": "<uuid>",
  "occurred_at": "<ISO>",
  "payload": {
    "ticket_id": "<mongo objectId>",
    "student_id": <long>,
    "user_id": <long>,        // <- дублировать как user_id (бот читает payload.user_id)
    "student_name": "<string>",
    "group_id": <long>,
    "lesson_ids": [<long>, ...],
    "excuse_type": "<lowercase>",
    "comment": "<string|null>",
    "created_at": "<ISO>"
  }
}
```

envelope для excuse.decided:
```json
{
  "event_type": "excuse.decided",
  "event_id": "<uuid>",
  "occurred_at": "<ISO>",
  "payload": {
    "ticket_id": "<mongo objectId>",
    "student_id": <long>,
    "user_id": <long>,
    "decision_by": <long>,
    "status": "<approved|rejected>",
    "decision_comment": "<string|null>",
    "decided_at": "<ISO>"
  }
}
```

RabbitConfig.java (существует, не менять):
```java
@Bean
public FanoutExchange attendanceEventsExchange() { return new FanoutExchange("rut-uit.events", true, false); }
@Bean
public RabbitTemplate rabbitTemplate(ConnectionFactory cf, Jackson2JsonMessageConverter conv) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ExcuseEventPublisher + unit-тест</name>
  <files>
    services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/excuse/ExcuseEventPublisher.java
    services/attendance-service/attendance-app/src/test/java/ru/rutcampustrack/attendance/excuse/ExcuseEventPublisherTest.java
    services/attendance-service/attendance-app/src/test/resources/fixtures/excuse_requested.json
    services/attendance-service/attendance-app/src/test/resources/fixtures/excuse_decided.json
  </files>
  <read_first>
    - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/event/AttendanceEventPublisher.java (образец — event_type в envelope, payload nested)
    - services/notification-bot/bot/consumers/event_dispatcher.py (строка 69: ключ "excuse.requested")
    - services/notification-bot/bot/notifications/headman_alerts.py (строки 28-55: что читает бот — payload.group_id, payload.user_id, payload.student_name, payload.excuse_type)
    - .planning/phases/59-excuses-backend/59-CONTEXT.md (D-19, D-20)
    - .planning/phases/59-excuses-backend/59-RESEARCH.md (§ Event Contract Verification)
  </read_first>
  <behavior>
    - Test 1 (AC-7): publishRequested(ticket) → RabbitTemplate.convertAndSend вызван с exchange="rut-uit.events", routingKey="", envelope содержит event_type="excuse.requested", payload.group_id, payload.user_id, payload.student_name, payload.excuse_type (lowercase)
    - Test 2: publishRequested с excuseType=FREE_ATTENDANCE → payload.excuse_type="free_attendance"
    - Test 3: publishDecided(ticket, decisionBy) → event_type="excuse.decided", payload.status="approved"|"rejected", payload.decision_by присутствует
    - Test 4: payload.user_id == ticket.studentId (бот использует user_id, не student_id)
  </behavior>
  <action>
    1. Создать ExcuseEventPublisher.java в пакете ru.rutcampustrack.attendance.excuse:
       - @Service
       - private static final String EXCHANGE = "rut-uit.events"
       - Конструктор инъекция RabbitTemplate
       - publishRequested(ExcuseTicket ticket):
         ```java
         Map<String, Object> payload = new LinkedHashMap<>();
         payload.put("ticket_id", ticket.getId());
         payload.put("student_id", ticket.getStudentId());
         payload.put("user_id", ticket.getStudentId());  // бот читает user_id
         payload.put("student_name", ticket.getStudentName());
         payload.put("group_id", ticket.getGroupId());
         payload.put("lesson_ids", ticket.getLessonIds());
         payload.put("excuse_type", ticket.getExcuseType().name().toLowerCase());
         payload.put("comment", ticket.getComment());
         payload.put("created_at", ticket.getCreatedAt().toString());
         
         Map<String, Object> envelope = new LinkedHashMap<>();
         envelope.put("event_type", "excuse.requested");
         envelope.put("event_id", UUID.randomUUID().toString());
         envelope.put("occurred_at", Instant.now().toString());
         envelope.put("payload", payload);
         
         rabbitTemplate.convertAndSend(EXCHANGE, "", envelope);
         ```
       - publishDecided(ExcuseTicket ticket):
         ```java
         Map<String, Object> payload = new LinkedHashMap<>();
         payload.put("ticket_id", ticket.getId());
         payload.put("student_id", ticket.getStudentId());
         payload.put("user_id", ticket.getStudentId());
         payload.put("decision_by", ticket.getDecisionBy());
         payload.put("status", ticket.getStatus().name().toLowerCase());
         payload.put("decision_comment", ticket.getDecisionComment());
         payload.put("decided_at", ticket.getDecisionAt() != null ? ticket.getDecisionAt().toString() : null);
         
         Map<String, Object> envelope = new LinkedHashMap<>();
         envelope.put("event_type", "excuse.decided");
         envelope.put("event_id", UUID.randomUUID().toString());
         envelope.put("occurred_at", Instant.now().toString());
         envelope.put("payload", payload);
         
         rabbitTemplate.convertAndSend(EXCHANGE, "", envelope);
         ```
    2. Создать fixture файлы в src/test/resources/fixtures/:
       - excuse_requested.json — канонический пример с реальными значениями
       - excuse_decided.json — канонический пример
    3. Создать ExcuseEventPublisherTest.java с @ExtendWith(MockitoExtension.class), mock RabbitTemplate, 4 теста из behavior.
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && ./gradlew.bat :services:attendance-service:attendance-app:test --tests "*ExcuseEventPublisherTest" 2>&1 | tail -30</automated>
  </verify>
  <done>4 unit-теста зелёные; ExcuseEventPublisher.java создан; оба fixture JSON файла существуют</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Интеграция publisher в ExcuseService + контракт-тест</name>
  <files>
    services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/excuse/ExcuseService.java
    services/attendance-service/attendance-app/src/test/java/ru/rutcampustrack/attendance/excuse/ExcuseEventContractIT.java
  </files>
  <read_first>
    - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/excuse/ExcuseService.java (РЕДАКТИРОВАТЬ — добавить publisher вызовы)
    - services/attendance-service/attendance-app/src/test/java/ru/rutcampustrack/attendance/ (найти образец IT теста с Testcontainers RabbitMQ)
    - services/attendance-service/attendance-app/src/test/resources/fixtures/excuse_requested.json (fixture из task 1)
    - .planning/phases/59-excuses-backend/59-RESEARCH.md (§ Event Contract Verification)
  </read_first>
  <behavior>
    - Test 1 (AC-7 / contract): создать тикет через ExcuseService → из RabbitMQ тест-очереди получить сообщение → парсить JSON → проверить: event_type="excuse.requested", payload.group_id присутствует, payload.user_id присутствует, payload.excuse_type = ожидаемый lowercase string
    - Test 2: approve тикета → из RabbitMQ получить excuse.decided → payload.status="approved"
  </behavior>
  <action>
    1. Открыть ExcuseService.java. Добавить ExcuseEventPublisher в конструктор. В createExcuse() — после repository.save() вызвать excuseEventPublisher.publishRequested(savedTicket). В updateStatus() — после repository.save() вызвать excuseEventPublisher.publishDecided(updatedTicket).
    2. Создать ExcuseEventContractIT.java:
       - @SpringBootTest + Testcontainers MongoDB + RabbitMQ (RabbitMQContainer из testcontainers)
       - В setUp(): создать временную тест-очередь и сбиндить на fanout exchange "rut-uit.events"
       - test_createExcuse_publishesRequested(): вызвать excuseService.createExcuse(), подождать (Awaitility или Thread.sleep(500)), взять сообщение из тест-очереди, распарсить JSON через ObjectMapper, проверить поля
       - test_updateStatus_publishesDecided(): create + approve, получить оба сообщения, проверить второе
       - Использовать RabbitTemplate для создания тест-очереди и получения сообщений
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && ./gradlew.bat :services:attendance-service:attendance-app:test --tests "*ExcuseEventContractIT" 2>&1 | tail -30</automated>
  </verify>
  <done>ExcuseEventContractIT зелёный; grep "excuseEventPublisher.publishRequested" ExcuseService.java находит вызов; grep "excuseEventPublisher.publishDecided" ExcuseService.java находит вызов</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| attendance-service → RabbitMQ fanout | Сообщения читают все подписчики — нельзя включать чувствительные данные |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-05-01 | Information Disclosure | RabbitMQ event: чувствительные данные | mitigate | Проверить payload: нет JWT токенов, паролей, telegram chat_id. Только бизнес-поля из D-19/D-20 |
| T-59-05-02 | Denial of Service | RabbitMQ down: createExcuse падает | mitigate | RabbitMQ persistent messages. При недоступности RabbitMQ — AMQP exception → 503. Тикет не создаётся (транзакционно: оба или ничего) |
| T-59-05-03 | Tampering | Event replay: бот получает устаревший event повторно | accept | event_id уникален (UUID); бот не реализует идемпотентность пока — приемлемо для v1 |
</threat_model>

<verification>
- `./gradlew.bat :services:attendance-service:attendance-app:test --tests "*ExcuseEventPublisherTest"` → зелёный
- `./gradlew.bat :services:attendance-service:attendance-app:test --tests "*ExcuseEventContractIT"` → зелёный
- `grep -n "excuse.requested" services/attendance-service/attendance-app/src/main/java/` → ExcuseEventPublisher.java
- `grep -n "excuse.decided" services/attendance-service/attendance-app/src/main/java/` → ExcuseEventPublisher.java
- `grep "excuseEventPublisher.publishRequested" services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/excuse/ExcuseService.java` → находит
</verification>

<success_criteria>
- ExcuseEventPublisher.java: publishRequested() и publishDecided() с правильной envelope структурой (event_type + event_id + occurred_at + payload nested)
- payload.user_id присутствует (бот читает user_id, не student_id)
- ExcuseService.createExcuse() вызывает publishRequested после сохранения
- ExcuseService.updateStatus() вызывает publishDecided после сохранения
- 4 unit-теста ExcuseEventPublisherTest зелёные
- ExcuseEventContractIT зелёный с Testcontainers RabbitMQ
</success_criteria>

<output>
После завершения создать `.planning/phases/59-excuses-backend/59-05-SUMMARY.md`
</output>
