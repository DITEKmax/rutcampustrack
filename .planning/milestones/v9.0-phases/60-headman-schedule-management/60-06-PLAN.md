---
phase: 60-headman-schedule-management
plan: 06
type: execute
wave: 3
depends_on: [60-01]
files_modified:
  - frontends/web-panel/src/app/features/headman/subjects/subject-dialog.component.ts
  - frontends/web-panel/src/app/features/headman/subjects/subject-dialog.component.spec.ts
  - frontends/web-panel/src/app/features/headman/shared/headman-api.service.ts
autonomous: true
requirements: [AC-01, AC-11]

must_haves:
  truths:
    - "Диалог создания/редактирования предмета содержит multi-select для выбора нескольких преподавателей"
    - "POST /api/academic/subjects вызывается с телом {name, type, teacherIds: number[]}"
    - "Пустой teacherIds=[] допустим (предмет без преподавателей)"
    - "Существующий предмет в edit-mode отображает текущих преподавателей предзаполненными"
    - "subject-dialog.component.spec.ts покрывает multi-select + submit"
  artifacts:
    - path: "frontends/web-panel/src/app/features/headman/subjects/subject-dialog.component.ts"
      provides: "обновлённый диалог с teacherIds FormControl (multi-select)"
    - path: "frontends/web-panel/src/app/features/headman/subjects/subject-dialog.component.spec.ts"
      provides: "spec тесты multi-select"
  key_links:
    - from: "SubjectDialogComponent.onSubmit"
      to: "headmanApi.createSubject({name, type, teacherIds})"
      via: "form.value.teacherIds (массив ID)"
      pattern: "teacherIds"
---

<objective>
Обновить `/headman/subjects` Angular компонент `subject-dialog.component.ts`: заменить single-select `teacherId` на multi-select `teacherIds: number[]`, добавить выбор `type` (LECTURE/PRACTICE/LAB), обновить `headman-api.service.ts` методы `createSubject` и `updateSubject` для передачи `teacherIds[]`. Написать/обновить spec тесты.

Purpose: Реализовать D-19 — диалог создания предмета назначает N преподавателей. Реализовать D-02 — при создании type обязателен (сейчас не передаётся).

Output: Обновлённые subject-dialog.component.ts, headman-api.service.ts и spec.
</objective>

<execution_context>
@$HOME/.claire/get-shit-done/workflows/execute-plan.md
@$HOME/.claire/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60-headman-schedule-management/60-CONTEXT.md

<interfaces>
From frontends/web-panel/src/app/features/headman/subjects/subject-dialog.component.ts:
```typescript
// Текущее состояние: single teacherId FormControl, нет type поля
form = new FormGroup({
  name: new FormControl('', [Validators.required, Validators.maxLength(120)]),
  teacherId: new FormControl<number | null>(null),  // → ЗАМЕНИТЬ на teacherIds: number[]
});
// Нет type выбора — ДОБАВИТЬ

// Текущий submit body:
const body = { name: ..., teacherId: ... };  // → { name, type, teacherIds: number[] }
```

From frontends/web-panel/src/app/features/headman/shared/headman-api.service.ts:
```typescript
// Текущие сигнатуры (обновить):
createSubject(body: { name: string; teacherId?: number | null }): Observable<any>
updateSubject(id: number, body: { name: string; teacherId?: number | null }): Observable<any>
// → обновить на teacherIds: number[]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: subject-dialog.component.ts — multi-select teachers + type field</name>
  <read_first>
    - frontends/web-panel/src/app/features/headman/subjects/subject-dialog.component.ts (полностью)
    - frontends/web-panel/src/app/features/headman/shared/headman-api.service.ts (createSubject, updateSubject, listTeachers)
    - .planning/phases/60-headman-schedule-management/60-CONTEXT.md (D-02: name+type+teacherIds; D-15: равноправные наблюдатели; D-19: multi-select)
  </read_first>
  <files>
    frontends/web-panel/src/app/features/headman/subjects/subject-dialog.component.ts,
    frontends/web-panel/src/app/features/headman/subjects/subject-dialog.component.spec.ts,
    frontends/web-panel/src/app/features/headman/shared/headman-api.service.ts
  </files>
  <action>
    1. В `subject-dialog.component.ts`:
    - Заменить `teacherId: new FormControl<number | null>(null)` на `teacherIds: new FormControl<number[]>([], { nonNullable: true })` — multi-select, массив ID
    - Добавить `type: new FormControl<string>('', [Validators.required])` для выбора SubjectType (LECTURE / PRACTICE / LAB)
    - Добавить `multiple` атрибут в `<mat-select>`:
      ```html
      <mat-select formControlName="teacherIds" multiple aria-label="Выберите преподавателей">
        @for (t of teachers; track t.id) {
          <mat-option [value]="t.id">{{ t.lastName ?? t.fullName }}</mat-option>
        }
      </mat-select>
      ```
    - Добавить select для type:
      ```html
      <mat-select formControlName="type">
        <mat-option value="LECTURE">Лекция</mat-option>
        <mat-option value="PRACTICE">Практика</mat-option>
        <mat-option value="LAB">Лабораторная</mat-option>
      </mat-select>
      ```
    - В `onSubmit()` изменить body:
      ```typescript
      const body = {
        name: this.form.value.name!,
        type: this.form.value.type!,
        teacherIds: this.form.value.teacherIds ?? [],
      };
      ```
    - В edit mode: `patchValue` с `teacherIds: this.data.subject?.teacherIds ?? []` и `type: this.data.subject?.type ?? ''`
    - Удалить старую логику `teacherId` (singular)

    2. В `headman-api.service.ts`:
    - Обновить `createSubject(body: { name: string; type: string; teacherIds: number[] }): Observable<any>`
    - Обновить `updateSubject(id: number, body: { name: string; type: string; teacherIds: number[] }): Observable<any>`
    - Добавить новый метод:
      ```typescript
      /** Добавить преподавателя к предмету (POST /api/academic/subjects/{id}/teachers/{teacherId}). */
      addTeacherToSubject(subjectId: number, teacherId: number): Observable<void> {
        return this.http.post<void>(`/api/academic/subjects/${subjectId}/teachers/${teacherId}`, {});
      }
      /** Удалить преподавателя из предмета (DELETE /api/academic/subjects/{id}/teachers/{teacherId}). */
      removeTeacherFromSubject(subjectId: number, teacherId: number): Observable<void> {
        return this.http.delete<void>(`/api/academic/subjects/${subjectId}/teachers/${teacherId}`);
      }
      ```

    3. Создать/обновить `subject-dialog.component.spec.ts`:
    - Тест 1: `createMode_submitWithTwoTeachers_callsApiWithTeacherIds` — заполнить форму name+type+teacherIds=[1,2], submit → проверить headmanApi.createSubject вызван с правильным body
    - Тест 2: `editMode_prefillsTeacherIds` — data.subject.teacherIds=[3,4] → form.teacherIds contains [3,4]
    - Тест 3: `typeField_required_preventsSubmit` — type пустой → форма invalid → submit disabled
    - Тест 4: `emptyTeacherIds_isValid` — teacherIds=[] → форма valid (преподаватели не обязательны)
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack/frontends/web-panel && npm run test -- --watch=false --include="src/app/features/headman/subjects/subject-dialog.component.spec.ts" 2>&1 | tail -20</automated>
  </verify>
  <done>
    subject-dialog.component.ts содержит multi-select teacherIds и type поле; headman-api.service.ts обновлён; spec тесты зелёные; `cd frontends/web-panel && npm run test -- --watch=false` не регрессирует.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Angular browser→API Gateway | teacherIds[] передаются в POST body; JWT в Authorization header |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Broken Access Control | SubjectDialogComponent → createSubject | mitigate | Backend проверяет requireHeadman() + groupId из JWT (план 60-01); frontend не может передать чужой groupId |
| T-60-06 | Injection | teacherIds[] form input | accept | teacherIds — number[], Angular form validators; backend принимает List<Long> — не строки |
</threat_model>

<verification>
1. `grep -rn "teacherIds" frontends/web-panel/src/app/features/headman/subjects/subject-dialog.component.ts` — содержит multi-select
2. `grep -rn "teacherIds\|type.*LECTURE" frontends/web-panel/src/app/features/headman/shared/headman-api.service.ts` — обновлённые сигнатуры
3. `cd frontends/web-panel && npm run test -- --watch=false` — все тесты зелёные (должно быть >346 ранее)
</verification>

<success_criteria>
- subject-dialog.component.ts: multi-select для N преподавателей, поле type (LECTURE/PRACTICE/LAB)
- headman-api.service.ts: createSubject/updateSubject принимают `{name, type, teacherIds: number[]}`
- spec тесты зелёные
- Нет регрессии в существующих Angular тестах
</success_criteria>

<output>
После завершения создать `.planning/phases/60-headman-schedule-management/60-06-SUMMARY.md`.
</output>
