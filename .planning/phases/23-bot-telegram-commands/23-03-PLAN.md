---
phase: 23-bot-telegram-commands
plan: 03
type: execute
wave: 2
depends_on:
  - 23-01
  - 23-02
files_modified:
  - services/notification-bot/bot/__main__.py
  - services/notification-bot/bot/handlers/__init__.py
  - services/notification-bot/bot/handlers/start.py
  - services/notification-bot/bot/handlers/login.py
  - services/notification-bot/bot/handlers/status.py
  - services/notification-bot/bot/grpc_client/academic_pb2.py
  - services/notification-bot/bot/grpc_client/academic_pb2_grpc.py
  - services/notification-bot/bot/grpc_client/academic_client.py
  - services/notification-bot/tests/test_start_handler.py
  - services/notification-bot/tests/test_login_handler.py
  - services/notification-bot/tests/test_status_handler.py
autonomous: true
requirements:
  - BOT-01
  - BOT-02
  - BOT-03
must_haves:
  truths:
    - "Student with linked telegram_id who sends /start sees greeting with login and initial_password (first login) or login and group name (password changed)"
    - "Student with unknown telegram_id who sends /start sees instruction to contact headman"
    - "Student who sends /login receives OTP code in Telegram and can enter it to authenticate"
    - "Student who enters wrong OTP code sees error message and can try again"
    - "Student who sends /status with stored JWT sees current lesson info and attendance status"
    - "Student who sends /status without JWT sees prompt to /login first"
    - "Student who sends /status with no active lesson sees 'Нет активной пары'"
    - "All bot messages are in Russian"
  artifacts:
    - path: "services/notification-bot/bot/handlers/start.py"
      provides: "/start command handler with account linking"
      contains: "cmd_start"
    - path: "services/notification-bot/bot/handlers/login.py"
      provides: "/login command handler with FSM for OTP flow"
      contains: "LoginStates"
    - path: "services/notification-bot/bot/handlers/status.py"
      provides: "/status command handler"
      contains: "cmd_status"
    - path: "services/notification-bot/bot/__main__.py"
      provides: "Bot + Dispatcher integration with existing health server and watchdog"
      contains: "dp.start_polling"
  key_links:
    - from: "bot/handlers/start.py"
      to: "AcademicGrpcClient.get_user_by_telegram_id()"
      via: "dependency injection through dp['academic_client']"
      pattern: "academic_client"
    - from: "bot/handlers/login.py"
      to: "AuthHttpClient + JwtRedisClient"
      via: "dependency injection through dp['auth_client'] and dp['jwt_redis']"
      pattern: "auth_client.*jwt_redis"
    - from: "bot/handlers/status.py"
      to: "ScheduleGrpcClient + AttendanceHttpClient + AcademicGrpcClient"
      via: "dependency injection"
      pattern: "schedule_client.*attendance_client"
    - from: "bot/__main__.py"
      to: "Dispatcher + routers"
      via: "dp.include_router() and dp.start_polling()"
      pattern: "include_router"
---

<objective>
Implement all three bot command handlers (/start, /login, /status), regenerate academic pb2 files (for new GetUserByTelegramId RPC), and integrate Aiogram Bot+Dispatcher into existing __main__.py.

Purpose: This is the user-facing plan — students interact with the bot through these three commands. /start links accounts, /login authenticates via OTP, /status checks attendance.

Output: Three command handler modules, updated __main__.py with Dispatcher, regenerated academic pb2, comprehensive unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-bot-telegram-commands/23-CONTEXT.md
@.planning/phases/23-bot-telegram-commands/23-RESEARCH.md
@.planning/phases/23-bot-telegram-commands/23-01-SUMMARY.md
@.planning/phases/23-bot-telegram-commands/23-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 (Java changes) -->

New academic.proto RPC (added by Plan 01):
```protobuf
rpc GetUserByTelegramId (UserByTelegramIdRequest) returns (UserByTelegramIdResponse);

message UserByTelegramIdRequest { int64 telegram_id = 1; }
message UserByTelegramIdResponse {
  bool found = 1; int64 user_id = 2; string login = 3;
  string display_name = 4; string role = 5; int64 group_id = 6;
  string group_name = 7; bool is_headman = 8; int64 telegram_id = 9;
  string initial_password = 10; bool password_changed = 11;
}
```

Auth Service OTP endpoints (modified by Plan 01):
- POST /auth/otp/request — body: {"telegramId": N} — response: {"code": "123456"}
- POST /auth/otp/verify — body: {"telegramId": N, "code": "123456"} — response: {"accessToken": "...", "refreshToken": "...", "expiresIn": 900}

<!-- From Plan 02 (Python clients) -->

```python
# JwtRedisClient
class JwtRedisClient:
    async def save(self, telegram_id: int, access_token: str, refresh_token: str, expires_in: int) -> None
    async def get(self, telegram_id: int) -> dict | None  # {"access_token": ..., "refresh_token": ...}
    async def delete(self, telegram_id: int) -> None

# AuthHttpClient
class AuthHttpClient:
    async def start(self) -> None
    async def close(self) -> None
    async def request_otp(self, telegram_id: int) -> str  # returns code
    async def verify_otp(self, telegram_id: int, code: str) -> dict  # {"accessToken", "refreshToken", "expiresIn"}

# AttendanceHttpClient
class AttendanceHttpClient:
    async def start(self) -> None
    async def close(self) -> None
    async def get_student_records(self, access_token: str) -> list[dict]  # raises TokenExpiredError on 401

# ScheduleGrpcClient
class ScheduleGrpcClient:
    async def get_active_lesson(self, group_id: int) -> LessonResponse | None  # None = no active lesson
    async def close(self) -> None

# AcademicGrpcClient (existing + new method after pb2 regen)
class AcademicGrpcClient:
    async def get_group_members(self, group_id: int) -> list  # existing
    async def get_user_by_telegram_id(self, telegram_id: int) -> UserByTelegramIdResponse  # NEW
    async def close(self) -> None

# schedule_pb2.LessonResponse fields: id, subject_id, group_id, room, start_time, end_time, status
# academic_pb2.SubjectsByIdsRequest, SubjectsByIdsResponse for resolving subject_id -> name
```

<!-- From existing bot code -->

```python
# bot/__main__.py current structure:
async def main():
    await run_health_server()
    _consumer_task = asyncio.create_task(run_with_watchdog(config.rabbitmq_url))
    await _consumer_task

# bot/config.py (after Plan 02):
config.bot_token, config.schedule_grpc_host, config.schedule_grpc_port,
config.auth_service_host, config.auth_service_port, config.api_gateway_url,
config.jwt_key_prefix, config.jwt_ttl
```

<!-- Aiogram 3 patterns -->
```python
# Dependency injection: dp["key"] = value -> handler receives as named argument
# FSM: StatesGroup + State, FSMContext.set_state(), FSMContext.clear()
# Router: login_router = Router(), @login_router.message(Command("login"))
# Polling: dp.start_polling(bot, handle_signals=False)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate academic pb2 + add get_user_by_telegram_id to AcademicGrpcClient</name>
  <files>
    services/notification-bot/bot/grpc_client/academic_pb2.py,
    services/notification-bot/bot/grpc_client/academic_pb2_grpc.py,
    services/notification-bot/bot/grpc_client/academic_client.py
  </files>
  <read_first>
    services/notification-bot/bot/grpc_client/academic_client.py,
    services/notification-bot/bot/grpc_client/academic_pb2_grpc.py,
    proto/academic.proto
  </read_first>
  <action>
**Step 1: Regenerate academic pb2 from updated proto**

Plan 01 added `GetUserByTelegramId` to `proto/academic.proto`. Regenerate Python bindings:

```bash
cd services/notification-bot
python -m grpc_tools.protoc -I../../proto --python_out=bot/grpc_client --grpc_python_out=bot/grpc_client ../../proto/academic.proto
```

Then fix the import in `bot/grpc_client/academic_pb2_grpc.py`: replace the generated `import academic_pb2 as academic__pb2` with `from bot.grpc_client import academic_pb2 as academic__pb2`. Check the existing file before regeneration to see the exact import fix pattern.

**Step 2: Add get_user_by_telegram_id to AcademicGrpcClient**

Add new method to `AcademicGrpcClient` class in `academic_client.py`:

```python
async def get_user_by_telegram_id(self, telegram_id: int):
    """Look up user by Telegram ID. Returns UserByTelegramIdResponse proto.

    Response has .found bool — if False, user doesn't exist.
    Does NOT raise on not-found (uses found flag instead of gRPC error).
    """
    request = academic_pb2.UserByTelegramIdRequest(telegram_id=telegram_id)
    return await self._stub.GetUserByTelegramId(request)
```

Also add a method to resolve subject names (needed by /status):

```python
async def get_subjects_by_ids(self, subject_ids: list[int]):
    """Resolve subject IDs to names. Returns SubjectsByIdsResponse proto."""
    request = academic_pb2.SubjectsByIdsRequest(subject_ids=subject_ids)
    return await self._stub.GetSubjectsByIds(request)
```

Add import at top if not already present: `from bot.grpc_client import academic_pb2, academic_pb2_grpc` (already exists).
  </action>
  <verify>
    <automated>cd services/notification-bot && python -c "from bot.grpc_client.academic_client import AcademicGrpcClient; print('OK: academic_client imports')" && python -c "from bot.grpc_client import academic_pb2; print(hasattr(academic_pb2, 'UserByTelegramIdRequest'))" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - academic_pb2.py contains `UserByTelegramIdRequest`
    - academic_pb2.py contains `UserByTelegramIdResponse`
    - academic_pb2_grpc.py contains `from bot.grpc_client import academic_pb2` (fixed import)
    - academic_pb2_grpc.py contains `GetUserByTelegramId`
    - academic_client.py contains `async def get_user_by_telegram_id(self, telegram_id: int)`
    - academic_client.py contains `async def get_subjects_by_ids(self, subject_ids: list[int])`
    - `python -c "from bot.grpc_client.academic_client import AcademicGrpcClient"` exits 0
  </acceptance_criteria>
  <done>Academic pb2 regenerated with GetUserByTelegramId. AcademicGrpcClient has get_user_by_telegram_id() and get_subjects_by_ids() methods. All imports work.</done>
</task>

<task type="auto">
  <name>Task 2: /start, /login, /status command handlers with tests</name>
  <files>
    services/notification-bot/bot/handlers/__init__.py,
    services/notification-bot/bot/handlers/start.py,
    services/notification-bot/bot/handlers/login.py,
    services/notification-bot/bot/handlers/status.py,
    services/notification-bot/tests/test_start_handler.py,
    services/notification-bot/tests/test_login_handler.py,
    services/notification-bot/tests/test_status_handler.py
  </files>
  <read_first>
    services/notification-bot/bot/handlers/__init__.py,
    services/notification-bot/bot/grpc_client/academic_client.py,
    services/notification-bot/bot/grpc_client/schedule_client.py,
    services/notification-bot/bot/services/jwt_redis_client.py,
    services/notification-bot/bot/services/auth_http_client.py,
    services/notification-bot/bot/services/attendance_http_client.py,
    services/notification-bot/tests/test_academic_client.py,
    services/notification-bot/tests/conftest.py
  </read_first>
  <action>
**Step 1: Create /start handler** (per D-01, D-02, D-03)

Create `services/notification-bot/bot/handlers/start.py`:

```python
import logging

from aiogram import Router
from aiogram.filters import Command
from aiogram.types import Message

logger = logging.getLogger(__name__)

start_router = Router()


@start_router.message(Command("start"))
async def cmd_start(message: Message, academic_client) -> None:
    """Handle /start command — account linking (D-02, D-03)."""
    telegram_id = message.from_user.id
    try:
        response = await academic_client.get_user_by_telegram_id(telegram_id)

        if not response.found:
            # D-03: unknown telegram_id
            await message.answer(
                "Ваш Telegram не привязан к системе. "
                "Обратитесь к старосте вашей группы для привязки аккаунта."
            )
            return

        # D-02: known user
        if response.initial_password:
            # First login — show credentials
            await message.answer(
                f"Добро пожаловать, {response.display_name}!\n\n"
                f"Ваш логин: {response.login}\n"
                f"Ваш пароль: {response.initial_password}\n\n"
                "Используйте эти данные для входа в веб-панель.\n"
                "После входа смените пароль."
            )
        else:
            # Password already changed
            await message.answer(
                f"Добро пожаловать, {response.display_name}!\n\n"
                f"Логин: {response.login}\n"
                f"Группа: {response.group_name}"
            )

    except Exception:
        # D-14: service unavailability
        logger.warning("Academic gRPC unavailable for /start", exc_info=True)
        await message.answer("Сервис временно недоступен. Попробуйте позже.")
```

**Step 2: Create /login handler with FSM** (per D-04, D-05, D-06, D-07)

Create `services/notification-bot/bot/handlers/login.py`:

```python
import logging

import aiohttp
from aiogram import Router
from aiogram.filters import Command
from aiogram.fsm.context import FSMContext
from aiogram.fsm.state import State, StatesGroup
from aiogram.types import Message

logger = logging.getLogger(__name__)

login_router = Router()


class LoginStates(StatesGroup):
    waiting_for_code = State()


@login_router.message(Command("login"))
async def cmd_login(message: Message, state: FSMContext,
                    auth_client, jwt_redis) -> None:
    """Handle /login command — request OTP (D-05, D-06)."""
    telegram_id = message.from_user.id

    # Check if already logged in
    existing = await jwt_redis.get(telegram_id)
    if existing:
        await message.answer("Вы уже вошли в систему. Используйте /status для проверки.")
        return

    try:
        code = await auth_client.request_otp(telegram_id)
        await message.answer(
            f"Ваш код для входа: {code}\n\n"
            "Введите его в следующем сообщении:"
        )
        await state.set_state(LoginStates.waiting_for_code)

    except aiohttp.ClientResponseError as e:
        if e.status == 429:
            # D-13: rate limit
            await message.answer("Слишком много попыток. Подождите.")
        elif e.status == 401:
            await message.answer(
                "Ваш аккаунт не найден. Обратитесь к старосте."
            )
        else:
            # D-14: service unavailable
            logger.warning("OTP request failed: %s", e)
            await message.answer("Сервис временно недоступен. Попробуйте позже.")
    except Exception:
        logger.warning("OTP request failed unexpectedly", exc_info=True)
        await message.answer("Сервис временно недоступен. Попробуйте позже.")


@login_router.message(LoginStates.waiting_for_code)
async def process_otp_code(message: Message, state: FSMContext,
                           auth_client, jwt_redis) -> None:
    """Handle OTP code input in FSM state (D-05, D-07)."""
    code = message.text.strip() if message.text else ""
    telegram_id = message.from_user.id

    if not code or len(code) != 6 or not code.isdigit():
        await message.answer("Код должен содержать 6 цифр. Попробуйте ещё раз.")
        return

    try:
        tokens = await auth_client.verify_otp(telegram_id, code)
        # D-07: store JWT in Redis
        await jwt_redis.save(
            telegram_id,
            tokens["accessToken"],
            tokens["refreshToken"],
            tokens.get("expiresIn", 0),
        )
        await state.clear()
        await message.answer("Вы успешно вошли в систему! Используйте /status для проверки посещаемости.")

    except aiohttp.ClientResponseError as e:
        if e.status == 401:
            # D-13: invalid code
            await message.answer("Код неверный. Попробуйте ещё раз.")
        else:
            await state.clear()
            logger.warning("OTP verify failed: %s", e)
            await message.answer("Сервис временно недоступен. Попробуйте позже.")
    except Exception:
        await state.clear()
        logger.warning("OTP verify failed unexpectedly", exc_info=True)
        await message.answer("Сервис временно недоступен. Попробуйте позже.")
```

**Step 3: Create /status handler** (per D-08, D-09, D-10, D-11)

Create `services/notification-bot/bot/handlers/status.py`:

```python
import logging

from aiogram import Router
from aiogram.filters import Command
from aiogram.types import Message

from bot.services.attendance_http_client import TokenExpiredError

logger = logging.getLogger(__name__)

status_router = Router()

# Russian status labels
_STATUS_LABELS = {
    "present": "Присутствует",
    "absent": "Отсутствует",
    "excused": "Уважительная причина",
    "free_attendance": "Свободное посещение",
}


@status_router.message(Command("status"))
async def cmd_status(
    message: Message,
    jwt_redis,
    schedule_client,
    attendance_client,
    academic_client,
) -> None:
    """Handle /status command — show current lesson and attendance (D-08 to D-11)."""
    telegram_id = message.from_user.id

    # D-11: check for stored JWT
    tokens = await jwt_redis.get(telegram_id)
    if not tokens:
        await message.answer("Сначала войдите через /login.")
        return

    try:
        # Look up user to get group_id
        user_response = await academic_client.get_user_by_telegram_id(telegram_id)
        if not user_response.found:
            await message.answer("Ваш аккаунт не найден. Обратитесь к старосте.")
            return

        group_id = user_response.group_id
        if not group_id:
            await message.answer("Вы не привязаны к группе.")
            return

        # D-08: get active lesson
        lesson = await schedule_client.get_active_lesson(group_id)
        if lesson is None:
            await message.answer("Нет активной пары.")
            return

        # Resolve subject name via Academic gRPC (Pitfall 7 from RESEARCH.md)
        subject_name = "Неизвестный предмет"
        try:
            subjects_resp = await academic_client.get_subjects_by_ids([lesson.subject_id])
            if subjects_resp.subjects:
                subject_name = subjects_resp.subjects[0].subject_name
        except Exception:
            logger.warning("Failed to resolve subject name for id=%d", lesson.subject_id)

        # D-09: get attendance status
        attendance_status = "Не отмечен"
        try:
            records = await attendance_client.get_student_records(tokens["access_token"])
            for record in records:
                if record.get("lessonId") == lesson.id:
                    raw_status = record.get("status", "")
                    attendance_status = _STATUS_LABELS.get(raw_status, raw_status)
                    break
        except TokenExpiredError:
            # Expired JWT — delete from Redis, ask to re-login (Pitfall 8)
            await jwt_redis.delete(telegram_id)
            await message.answer("Токен истёк. Войдите снова через /login.")
            return
        except Exception:
            logger.warning("Failed to fetch attendance records", exc_info=True)
            attendance_status = "Не удалось получить"

        # D-10: format status message
        time_range = f"{lesson.start_time} — {lesson.end_time}"
        await message.answer(
            f"Текущая пара:\n\n"
            f"Предмет: {subject_name}\n"
            f"Аудитория: {lesson.room}\n"
            f"Время: {time_range}\n"
            f"Статус: {attendance_status}"
        )

    except Exception:
        # D-14: service unavailability
        logger.warning("Error in /status handler", exc_info=True)
        await message.answer("Сервис временно недоступен. Попробуйте позже.")
```

**Step 4: Update handlers/__init__.py**

```python
from bot.handlers.start import start_router
from bot.handlers.login import login_router
from bot.handlers.status import status_router

__all__ = ["start_router", "login_router", "status_router"]
```

**Step 5: Write tests for all three handlers**

Create test files following the existing test pattern (AsyncMock for dependencies, direct handler call or Aiogram test utils):

**`tests/test_start_handler.py`:**
- `test_start_known_user_first_login` — mock academic_client.get_user_by_telegram_id returns found=True, initial_password="pass123" → message contains "Ваш пароль: pass123"
- `test_start_known_user_password_changed` — found=True, initial_password="" → message contains "Группа:" and login
- `test_start_unknown_user` — found=False → message contains "Обратитесь к старосте"
- `test_start_grpc_error` — academic_client raises Exception → message contains "Сервис временно недоступен"

**`tests/test_login_handler.py`:**
- `test_login_requests_otp` — auth_client.request_otp returns "123456", jwt_redis.get returns None → message contains "Ваш код для входа: 123456" and state set to waiting_for_code
- `test_login_already_logged_in` — jwt_redis.get returns tokens → message contains "Вы уже вошли"
- `test_process_code_success` — auth_client.verify_otp returns tokens, jwt_redis.save called → message contains "Вы успешно вошли", state cleared
- `test_process_code_invalid` — auth_client.verify_otp raises ClientResponseError(status=401) → message contains "Код неверный"
- `test_login_rate_limited` — auth_client.request_otp raises ClientResponseError(status=429) → message contains "Слишком много попыток"

**`tests/test_status_handler.py`:**
- `test_status_no_jwt` — jwt_redis.get returns None → message contains "Сначала войдите через /login"
- `test_status_no_active_lesson` — schedule_client.get_active_lesson returns None → message contains "Нет активной пары"
- `test_status_with_lesson_present` — lesson returned with subject_id, records include matching lessonId with status "present" → message contains "Присутствует"
- `test_status_not_marked` — lesson returned, no matching record → message contains "Не отмечен"
- `test_status_token_expired` — attendance_client raises TokenExpiredError → message contains "Токен истёк", jwt_redis.delete called

For mocking Aiogram Message objects: use `unittest.mock.AsyncMock` for message.answer(). Create a mock Message with `from_user.id = 12345`. For FSMContext: use `AsyncMock` with `set_state` and `clear` methods.
  </action>
  <verify>
    <automated>cd services/notification-bot && python -m pytest tests/test_start_handler.py tests/test_login_handler.py tests/test_status_handler.py -x -q 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - start.py contains `start_router = Router()`
    - start.py contains `async def cmd_start(message: Message, academic_client)`
    - start.py contains `"Ваш Telegram не привязан к системе"`
    - start.py contains `response.initial_password`
    - login.py contains `class LoginStates(StatesGroup)` with `waiting_for_code = State()`
    - login.py contains `async def cmd_login(message: Message, state: FSMContext`
    - login.py contains `async def process_otp_code(message: Message, state: FSMContext`
    - login.py contains `await jwt_redis.save(`
    - login.py contains `"Код неверный. Попробуйте ещё раз."`
    - status.py contains `status_router = Router()`
    - status.py contains `async def cmd_status(`
    - status.py contains `"Сначала войдите через /login."`
    - status.py contains `"Нет активной пары."`
    - status.py contains `TokenExpiredError`
    - status.py contains `_STATUS_LABELS`
    - handlers/__init__.py contains `from bot.handlers.start import start_router`
    - test_start_handler.py contains at least 4 test functions
    - test_login_handler.py contains at least 4 test functions
    - test_status_handler.py contains at least 4 test functions
    - `python -m pytest tests/test_start_handler.py tests/test_login_handler.py tests/test_status_handler.py` exits 0
  </acceptance_criteria>
  <done>All three command handlers implemented with full error handling, Russian messages per D-12/D-13/D-14. All unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Bot+Dispatcher into __main__.py</name>
  <files>
    services/notification-bot/bot/__main__.py
  </files>
  <read_first>
    services/notification-bot/bot/__main__.py,
    services/notification-bot/bot/config.py,
    services/notification-bot/bot/handlers/__init__.py,
    services/notification-bot/bot/grpc_client/academic_client.py,
    services/notification-bot/bot/grpc_client/schedule_client.py,
    services/notification-bot/bot/services/jwt_redis_client.py,
    services/notification-bot/bot/services/auth_http_client.py,
    services/notification-bot/bot/services/attendance_http_client.py
  </read_first>
  <action>
**Integrate Aiogram Bot + Dispatcher into the existing `__main__.py` alongside health server and watchdog.**

The current `main()` function starts health server and consumer watchdog. Add Bot+Dispatcher startup as a third asyncio task running in parallel.

Modify `services/notification-bot/bot/__main__.py` to:

1. **Add imports at top:**
```python
from aiogram import Bot, Dispatcher
from aiogram.fsm.storage.memory import MemoryStorage

from bot.handlers import start_router, login_router, status_router
from bot.grpc_client.academic_client import AcademicGrpcClient
from bot.grpc_client.schedule_client import ScheduleGrpcClient
from bot.services.jwt_redis_client import JwtRedisClient
from bot.services.auth_http_client import AuthHttpClient
from bot.services.attendance_http_client import AttendanceHttpClient
```

2. **Add global reference for bot task (alongside existing `_consumer_task`):**
```python
_bot_task: asyncio.Task | None = None
```

3. **Update `health_handler` to also check bot task:**
```python
async def health_handler(request: web.Request) -> web.Response:
    if _consumer_task is None or _consumer_task.done():
        raise web.HTTPServiceUnavailable(text='{"status":"DOWN","reason":"watchdog_dead"}')
    if _bot_task is None or _bot_task.done():
        raise web.HTTPServiceUnavailable(text='{"status":"DOWN","reason":"bot_dead"}')
    return web.Response(text='{"status":"UP"}', content_type="application/json")
```

4. **Create async setup function for clients:**
```python
async def create_clients():
    """Create and start all service clients for bot handlers."""
    academic_client = AcademicGrpcClient(config.academic_grpc_host, config.academic_grpc_port)
    schedule_client = ScheduleGrpcClient(config.schedule_grpc_host, config.schedule_grpc_port)
    jwt_redis = JwtRedisClient(
        key_prefix=config.jwt_key_prefix,
        ttl=config.jwt_ttl,
        host=config.redis_host,
        port=config.redis_port,
    )
    auth_client = AuthHttpClient(
        base_url=f"http://{config.auth_service_host}:{config.auth_service_port}"
    )
    attendance_client = AttendanceHttpClient(base_url=config.api_gateway_url)

    # Start HTTP sessions (must be in async context — Pitfall 3)
    await auth_client.start()
    await attendance_client.start()

    return academic_client, schedule_client, jwt_redis, auth_client, attendance_client
```

5. **Update `main()` to create Bot+Dispatcher:**
```python
async def main() -> None:
    global _consumer_task, _bot_task

    await run_health_server()

    # Create service clients
    academic_client, schedule_client, jwt_redis, auth_client, attendance_client = await create_clients()

    # Create Bot and Dispatcher
    bot = Bot(token=config.bot_token)
    storage = MemoryStorage()
    dp = Dispatcher(storage=storage)

    # Inject dependencies via dp workflow data (Aiogram 3 DI pattern)
    dp["academic_client"] = academic_client
    dp["schedule_client"] = schedule_client
    dp["jwt_redis"] = jwt_redis
    dp["auth_client"] = auth_client
    dp["attendance_client"] = attendance_client

    # Register routers
    dp.include_router(start_router)
    dp.include_router(login_router)
    dp.include_router(status_router)

    # Start watchdog (existing)
    _consumer_task = asyncio.create_task(run_with_watchdog(config.rabbitmq_url))

    # Start bot polling (handle_signals=False — Pitfall 5: signals managed by main loop)
    _bot_task = asyncio.create_task(dp.start_polling(bot, handle_signals=False))

    logger.info("notification-bot started (health + watchdog + bot polling)")

    try:
        # Wait for either task to complete (whichever finishes first)
        done, pending = await asyncio.wait(
            [_consumer_task, _bot_task],
            return_when=asyncio.FIRST_EXCEPTION,
        )
        for task in done:
            if task.exception():
                logger.exception("Task failed: %s", task.get_name(), exc_info=task.exception())
    except asyncio.CancelledError:
        logger.info("Main cancelled, shutting down")
    finally:
        # Cleanup
        for task in [_consumer_task, _bot_task]:
            if task and not task.done():
                task.cancel()
        await auth_client.close()
        await attendance_client.close()
        await jwt_redis.close()
        await schedule_client.close()
        await academic_client.close()
```

**Key design choices:**
- `handle_signals=False` on `dp.start_polling()` — signals managed by asyncio.run(), not Aiogram (Pitfall 5)
- HTTP client sessions created in `create_clients()` async function (Pitfall 3)
- `asyncio.wait(return_when=FIRST_EXCEPTION)` — if watchdog or bot crashes, we detect it
- Graceful cleanup of all clients in finally block
  </action>
  <verify>
    <automated>cd services/notification-bot && python -c "import ast; ast.parse(open('bot/__main__.py').read()); print('Syntax OK')" && python -c "from bot.handlers import start_router, login_router, status_router; print('Imports OK')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - __main__.py contains `from aiogram import Bot, Dispatcher`
    - __main__.py contains `from aiogram.fsm.storage.memory import MemoryStorage`
    - __main__.py contains `from bot.handlers import start_router, login_router, status_router`
    - __main__.py contains `dp.include_router(start_router)`
    - __main__.py contains `dp.include_router(login_router)`
    - __main__.py contains `dp.include_router(status_router)`
    - __main__.py contains `dp.start_polling(bot, handle_signals=False)`
    - __main__.py contains `dp["academic_client"]`
    - __main__.py contains `dp["jwt_redis"]`
    - __main__.py contains `dp["auth_client"]`
    - __main__.py contains `dp["attendance_client"]`
    - __main__.py contains `dp["schedule_client"]`
    - __main__.py contains `await auth_client.close()`
    - __main__.py contains `_bot_task`
    - `python -c "import ast; ast.parse(open('bot/__main__.py').read())"` exits 0 (valid syntax)
  </acceptance_criteria>
  <done>Bot+Dispatcher integrated into __main__.py running alongside health server and watchdog. All service clients created, injected into Dispatcher, and cleaned up on shutdown.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram user -> Bot | Untrusted input from Telegram users (message.text, from_user.id) |
| Bot -> Auth Service | Internal HTTP with user-provided OTP code |
| Bot -> API Gateway | Student JWT in Authorization header |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07 | Spoofing | /start telegram_id | accept | Telegram guarantees from_user.id integrity — bot trusts Telegram API. Cannot be spoofed by end user. |
| T-23-08 | Tampering | OTP code input (/login) | mitigate | Validate code is exactly 6 digits before sending to Auth Service. Auth Service has rate limiting (OtpRateLimitException) and TTL on stored codes. FSM state limits input window. |
| T-23-09 | Information Disclosure | OTP code shown in Telegram | accept | OTP is delivered directly to the user's private chat. Telegram encrypts client-server. Code has short TTL. This is the intended delivery mechanism (per D-04). |
| T-23-10 | Denial of Service | /login FSM state leak | mitigate | MemoryStorage auto-clears on bot restart. FSM state cleared on success, error, or any unexpected exception (state.clear() in except blocks). No persistent state leak. |
| T-23-11 | Elevation of Privilege | JWT stored in Redis | mitigate | JWT TTL matches token expiry. Redis on internal network. Key prefix `bot:jwt:` namespaced. Tokens are user-scoped — a compromised token only accesses that user's data. |
</threat_model>

<verification>
1. `python -m pytest tests/ -x -q` — all bot tests pass (existing + new)
2. `python -c "from bot.handlers import start_router, login_router, status_router"` — imports work
3. `__main__.py` syntax valid and contains Bot+Dispatcher integration
4. All handler files contain Russian user-facing messages per D-12
</verification>

<success_criteria>
- /start handler shows credentials for known users or instruction for unknown users
- /login handler implements full OTP FSM flow with JWT storage in Redis
- /status handler shows current lesson info with attendance status
- All messages in Russian, error messages user-friendly per D-13
- Bot+Dispatcher runs alongside existing health server and watchdog
- 12+ unit tests across three handler test files, all passing
</success_criteria>

<output>
After completion, create `.planning/phases/23-bot-telegram-commands/23-03-SUMMARY.md`
</output>
