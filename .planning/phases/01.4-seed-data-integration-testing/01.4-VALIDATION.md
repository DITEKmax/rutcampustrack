---
phase: 01.4
slug: seed-data-integration-testing
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-30
---

# Phase 01.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | JUnit 5 + Spring Boot Test + Testcontainers |
| **Config file** | `services/auth-service/src/test/resources/application-test.yml` (Wave 0 installs) |
| **Quick run command** | `./gradlew.bat :services:auth-service:test` |
| **Full suite command** | `./gradlew.bat :services:auth-service:test :services:api-gateway:test` |
| **Estimated runtime** | ~45 seconds (Testcontainers startup + tests) |

---

## Sampling Rate

- **After every task commit:** Run `./gradlew.bat :services:auth-service:test`
- **After every plan wave:** Run `./gradlew.bat :services:auth-service:test :services:api-gateway:test`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 45 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 01.4-01-01 | 01 | 0 | FR-10.1, FR-10.2 | infra | `./gradlew.bat :services:auth-service:compileTestJava` | yes (W0) | ⬜ pending |
| 01.4-01-02 | 01 | 1 | FR-10.1, FR-10.2 | integration | `./gradlew.bat :services:auth-service:test --tests "*.AuthIntegrationTest"` | yes (W0) | ⬜ pending |
| 01.4-01-03 | 01 | 1 | FR-10.1, FR-10.2 | integration | `./gradlew.bat :services:auth-service:test --tests "*.OtpIntegrationTest"` | yes (W0) | ⬜ pending |
| 01.4-01-04 | 01 | 1 | FR-10.2 | e2e-script | `bash -n scripts/verify-gateway-e2e.sh` | yes (W0) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `services/auth-service/src/test/resources/application-test.yml` — test profile for Testcontainers PostgreSQL + Redis
- [x] `services/auth-service/src/test/resources/db/migration/V1__*.sql` — copy of academic schema migration
- [x] `services/auth-service/src/test/resources/db/migration/V2__seed_test_data.sql` — copy of seed data migration
- [x] Testcontainers dependencies in `auth-service/build.gradle.kts` (if not present)
- [x] Base integration test class with `@SpringBootTest` + Testcontainers setup

All Wave 0 requirements are addressed by Task 1 of Plan 01.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Gateway E2E login flow | FR-10 verification | Gateway requires live auth-service for PublicKeyConfig startup | Run `scripts/verify-gateway-e2e.sh` with both services running |
| OTP Telegram delivery | FR-10 verification | Telegram bot delivery is stubbed | Verify OTP code stored in Redis, actual delivery deferred |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 45s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
