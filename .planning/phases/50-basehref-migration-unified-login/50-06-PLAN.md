---
phase: 50
plan: 06
type: execute
wave: 5
depends_on: ["50-01", "50-02", "50-03", "50-04", "50-05"]
files_modified: []
autonomous: false
requirements:
  - AUTH-v9-07
must_haves:
  truths:
    - "Все 129+ baseline vitest тестов проходят после полной миграции baseHref (AUTH-v9-07)"
    - "Никаких новых failing тестов после wave 4"
    - "ng build production успешно собирает web-panel с baseHref: /"
    - "Весь phase 50 end-to-end проверен через human UAT (6 ROADMAP success criteria)"
    - "50-VALIDATION.md per-task verification map заполнен для всех plans 01-05"
  artifacts:
    - path: ".planning/phases/50-basehref-migration-unified-login/50-VALIDATION.md"
      provides: "Заполненная per-task verification map + phase gate sign-off"
  key_links:
    - from: "phase 50 completion"
      to: "ROADMAP success criteria 1-6"
      via: "human UAT checklist"
      pattern: "UAT"
---

<objective>
Финальная регрессионная проверка и human UAT phase 50. Запустить полный vitest suite, production ng build, заполнить `50-VALIDATION.md` per-task verification map, провести human-verify checkpoint по всем 6 ROADMAP success criteria.

Purpose: AUTH-v9-07 требует "все 129+ existing web-panel vitest tests continue passing". Этот plan — explicit regression gate. Также AUTH-v9-01..06 требуют проверки в реальном running контексте (с backend) — что невозможно через vitest unit тесты. Human UAT checkpoint закрывает этот gap.
Output:
- Полный vitest suite зелёный
- Production ng build успешен и `dist/browser/index.html` содержит `<base href="/">`
- Обновлённый `50-VALIDATION.md` с заполненной Per-Task Verification Map
- Human sign-off 6 ROADMAP success criteria
- Phase 50 готов к `/gsd-verify-work` / `/gsd-complete-phase`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-basehref-migration-unified-login/50-CONTEXT.md
@.planning/phases/50-basehref-migration-unified-login/50-RESEARCH.md
@.planning/phases/50-basehref-migration-unified-login/50-VALIDATION.md
@.planning/phases/50-basehref-migration-unified-login/50-01-PLAN.md
@.planning/phases/50-basehref-migration-unified-login/50-02-PLAN.md
@.planning/phases/50-basehref-migration-unified-login/50-03-PLAN.md
@.planning/phases/50-basehref-migration-unified-login/50-04-PLAN.md
@.planning/phases/50-basehref-migration-unified-login/50-05-PLAN.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full vitest regression + production ng build + заполнить 50-VALIDATION.md</name>
  <files>.planning/phases/50-basehref-migration-unified-login/50-VALIDATION.md</files>
  <read_first>
    - .planning/phases/50-basehref-migration-unified-login/50-VALIDATION.md (текущий template)
    - .planning/phases/50-basehref-migration-unified-login/50-01-PLAN.md (для task → requirement mapping)
    - .planning/phases/50-basehref-migration-unified-login/50-02-PLAN.md
    - .planning/phases/50-basehref-migration-unified-login/50-03-PLAN.md
    - .planning/phases/50-basehref-migration-unified-login/50-04-PLAN.md
    - .planning/phases/50-basehref-migration-unified-login/50-05-PLAN.md
  </read_first>
  <action>
    **ШАГ 1 — Полный vitest regression.**

    ```
    cd frontends/web-panel && npx vitest run
    ```

    Записать точный счёт прошедших и упавших тестов. Ожидается: exit code 0, все тесты passed. Если что-то падает — ПРЕКРАТИТЬ и проанализировать failing test, вернуться в соответствующий plan (01-05) для исправления. Не продолжать UAT пока vitest не зелёный.

    Сохранить счёт тестов (например "153 tests passed") для задокументирования в summary.

    **ШАГ 2 — Production build.**

    ```
    cd frontends/web-panel && npx ng build --configuration production
    ```

    Ожидается: build успешен (exit 0), никаких budget warnings errors (возможны warnings, но не errors). Проверить output:
    ```bash
    grep -c 'base href="/"' frontends/web-panel/dist/browser/index.html
    ```
    → должно быть ≥ 1.

    ```bash
    grep -c 'base href="/admin/"' frontends/web-panel/dist/browser/index.html
    ```
    → должно быть 0.

    **ШАГ 3 — Заполнить 50-VALIDATION.md Per-Task Verification Map.**

    Открыть `.planning/phases/50-basehref-migration-unified-login/50-VALIDATION.md`.

    Заменить секцию "## Per-Task Verification Map" (строки 37-45) на полную заполненную таблицу:

    ```markdown
    ## Per-Task Verification Map

    | Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
    |---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
    | 01-T1   | 01   | 1    | AUTH-v9-02, AUTH-v9-03 | T-50-01, T-50-03 | AuthUser расширен + resolveDashboardFor + JWT is_headman safe default | unit TDD | cd frontends/web-panel && npx vitest run --reporter=dot src/app/core/auth/auth.service.spec.ts | ✅ | ⬜ pending |
    | 01-T2   | 01   | 1    | AUTH-v9-03 | — | Sidebar типы расширены, D-06 no-entries-for-STUDENT проверен | unit | cd frontends/web-panel && npx vitest run --reporter=dot | ✅ | ⬜ pending |
    | 02-T1   | 02   | 2    | AUTH-v9-05 | T-50-06 | studentGuard пропускает STUDENT/headman, блокирует TEACHER/ADMIN/unauth | unit TDD | cd frontends/web-panel && npx vitest run --reporter=dot src/app/core/auth/student.guard.spec.ts | ✅ | ⬜ pending |
    | 02-T2   | 02   | 2    | AUTH-v9-04 | T-50-05 | headmanGuard пропускает только STUDENT+isHeadman, plain STUDENT → /student/dashboard | unit TDD | cd frontends/web-panel && npx vitest run --reporter=dot src/app/core/auth/headman.guard.spec.ts | ✅ | ⬜ pending |
    | 02-T3   | 02   | 2    | AUTH-v9-04, AUTH-v9-05 | T-50-07 | guestGuard редиректит authenticated, пропускает unauth; full regression | unit TDD | cd frontends/web-panel && npx vitest run --reporter=dot | ✅ | ⬜ pending |
    | 03-T1   | 03   | 3    | INFRA-v9-04 | T-50-14 | StudentPlaceholderComponent + HeadmanPlaceholderComponent standalone | unit | cd frontends/web-panel && npx vitest run --reporter=dot | ✅ | ⬜ pending |
    | 03-T2   | 03   | 3    | INFRA-v9-04, AUTH-v9-04, AUTH-v9-05 | T-50-10, T-50-11, T-50-12 | 3 новых маршрута под ShellComponent + guestGuard на /login + schedule route для criterion 4 | unit | cd frontends/web-panel && npx vitest run --reporter=dot | ✅ | ⬜ pending |
    | 04-T1   | 04   | 3    | AUTH-v9-02 | T-50-16 | role.guard.ts fallback использует resolveDashboardFor, STUDENT/headman покрыты | unit TDD | cd frontends/web-panel && npx vitest run --reporter=dot src/app/core/auth/auth.guard.spec.ts | ✅ | ⬜ pending |
    | 04-T2   | 04   | 3    | AUTH-v9-01, AUTH-v9-02, AUTH-v9-03 | T-50-15 | login.component.ts использует resolveDashboardFor + navigateByUrl, 4 role редиректа | unit TDD | cd frontends/web-panel && npx vitest run --reporter=dot | ✅ | ⬜ pending |
    | 05-T1   | 05   | 4    | INFRA-v9-04 | T-50-21 | angular.json baseHref: / в обоих местах + landing footer replaces /admin/ с /login | build + grep | cd frontends/web-panel && npx ng build --configuration development && grep -c 'base href="/"' dist/browser/index.html | ✅ | ⬜ pending |
    | 05-T2   | 05   | 4    | INFRA-v9-04 | T-50-19, T-50-20, T-50-22 | Prod nginx: /admin/ удалён, catch-all / добавлен в конец, /login 301 сохранён, prefix-match порядок соблюдён | grep + manual curl после deploy | Manual: curl -I https://ruttrack.site/ → 301 /login; curl -I https://ruttrack.site/login → 200 | ❌ manual | ⬜ pending |
    | 06-T1   | 06   | 5    | AUTH-v9-07 | — | Full suite regression + prod build | unit regression | cd frontends/web-panel && npx vitest run | ✅ | ⬜ pending |
    | 06-T2   | 06   | 5    | AUTH-v9-01..07, INFRA-v9-04 | — | Human UAT — 6 ROADMAP criteria | manual UAT | Human | ❌ manual | ⬜ pending |
    ```

    Также обновить frontmatter статус:
    ```yaml
    ---
    phase: 50
    slug: basehref-migration-unified-login
    status: ready
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-04-09
    ---
    ```

    И обновить раздел `## Validation Sign-Off` в конце файла:
    ```markdown
    ## Validation Sign-Off

    - [x] All tasks have `<automated>` verify or Wave 0 dependencies
    - [x] Sampling continuity: no 3 consecutive tasks without automated verify
    - [x] Wave 0 covers all MISSING references (Plan 01 Task 1 baseline step)
    - [x] No watch-mode flags
    - [x] Feedback latency < 30s
    - [x] `nyquist_compliant: true` set in frontmatter

    **Approval:** pending — ждёт human UAT checkpoint (Task 2 of Plan 06)
    ```

    **ШАГ 4 — Записать baseline + final счёт тестов в summary.** (Это будет использовано при создании 50-06-SUMMARY.md автоматически execute-plan workflow.)
  </action>
  <verify>
    <automated>cd frontends/web-panel &amp;&amp; npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontends/web-panel && npx vitest run` → exit 0, все тесты зелёные
    - `cd frontends/web-panel && npx ng build --configuration production` → exit 0
    - `grep -c 'base href="/"' frontends/web-panel/dist/browser/index.html` → ≥ 1
    - `grep -c 'base href="/admin/"' frontends/web-panel/dist/browser/index.html` → 0
    - `grep -c "nyquist_compliant: true" .planning/phases/50-basehref-migration-unified-login/50-VALIDATION.md` → 1
    - `grep -c "^| 01-T1" .planning/phases/50-basehref-migration-unified-login/50-VALIDATION.md` → 1
    - `grep -c "^| 06-T2" .planning/phases/50-basehref-migration-unified-login/50-VALIDATION.md` → 1
    - Весь per-task verification map заполнен (13 rows total для 5 execute plans + 2 tasks в plan 06)
    - Total regression tests ≥ 146 (baseline ~131 + 15 новых из Plan 02 + ~10 из Plan 01 + 2 из Plan 04)
  </acceptance_criteria>
  <done>
    Полный vitest suite зелёный. Production build успешен и содержит правильный base href. 50-VALIDATION.md заполнен полной per-task map. Всё готово к human UAT checkpoint (Task 2).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — 6 ROADMAP success criteria для Phase 50</name>
  <files>frontends/web-panel/src/**/*, nginx/conf.d/default.conf, frontends/landing/dist/index.html — all prior plan artefacts combined</files>
  <action>Перед началом checkpoint-а выполнить deploy prerequisites (rebuild image, restart nginx). Затем пройти пошагово все 6 ROADMAP success criteria + smoke checks. Подробные шаги в <how-to-verify> ниже. Cкорее это manual Q&A, не код — execute-plan покажет эту checkpoint карточку оператору.</action>
  <verify><automated>echo "Human checkpoint — proceed through how-to-verify steps below"</automated></verify>
  <done>Все 6 ROADMAP criteria отмечены как passed, оператор ответил "approved" на resume-signal. Phase 50 ready for /gsd-complete-phase.</done>
  <what-built>
    Все плановые изменения Phase 50 выполнены:
    - `AuthService.currentUser()` парсит role/is_headman/group_id из JWT
    - `AuthService.resolveDashboardFor()` — единый источник истины для post-login редиректов
    - `studentGuard`, `headmanGuard`, `guestGuard` — 3 новых CanActivateFn
    - `StudentPlaceholderComponent`, `HeadmanPlaceholderComponent` — placeholder shells
    - `app.routes.ts` — `/student/dashboard`, `/student/schedule`, `/headman/dashboard` зарегистрированы; `/login` защищён guestGuard
    - `role.guard.ts` fallback → resolveDashboardFor (закрывает Pitfall 2)
    - `login.component.ts` → resolveDashboardFor + navigateByUrl
    - `angular.json` baseHref: `/admin/` → `/` (оба вхождения)
    - `nginx/conf.d/default.conf` — `/admin/` блок удалён, catch-all `/` добавлен, `/login` 301 сохранён
    - `frontends/landing/dist/index.html:1330` — `/admin/` footer link → `/login`
    - Все 129+ baseline vitest тестов остаются зелёными + новые тесты для STUDENT/headman сценариев
  </what-built>
  <how-to-verify>
    **Предварительно (если ещё не сделано):**
    1. Rebuild web-panel docker image: `docker compose build rct-web-panel-nginx`
    2. Reload prod nginx: `docker compose up -d --force-recreate rct-web-panel-nginx` и `docker compose exec nginx nginx -s reload` (или restart nginx контейнер)
    3. Убедиться что все контейнеры healthy: `docker compose ps`

    **ROADMAP Success Criterion 1 — 4-role post-login routing:**
    Для каждой из 4 ролей открыть `https://ruttrack.site/login` в incognito:
    - Залогиниться как `admin` (ADMIN роль) → ожидаемый URL после логина: `/admin/dashboard` ✓
    - Залогиниться как `teacher` (TEACHER роль) → ожидаемый URL после логина: `/teacher/dashboard` ✓
    - Залогиниться как `student` (plain STUDENT без is_headman) → ожидаемый URL: `/student/dashboard` ✓, страница должна показать "Кабинет студента появится в Фазе 51"
    - Залогиниться как student с `is_headman=true` (например `student00002` если он староста — уточнить у seed data) → ожидаемый URL: `/headman/dashboard` ✓, страница "Кабинет старосты появится в Фазе 54"

    **ROADMAP Success Criterion 2 — AuthService.currentUser() signal:**
    В DevTools console на залогиненной странице student'a выполнить:
    ```javascript
    // Если signal экспонируется через Angular devtools, проверить значение
    // Альтернатива: просмотреть payload JWT через console.log(atob(document.cookie))
    ```
    Убедиться что currentUser() возвращает объект с полями `id`, `role`, `isHeadman`, `groupId`. (Опционально — если Angular devtools установлен.)

    **ROADMAP Success Criterion 3 — headmanGuard блокирует plain STUDENT:**
    Будучи залогиненным как plain STUDENT, ввести в адресную строку `/headman/dashboard`. Ожидаемо: URL автоматически меняется на `/student/dashboard` (headmanGuard → resolveDashboardFor → /student/dashboard). НЕТ показа headman placeholder.

    Альтернативно: логаут, затем `/headman/dashboard` → ожидаемо редирект на `/login`.

    **ROADMAP Success Criterion 4 — studentGuard пропускает headman:**
    Залогиниться как headman STUDENT, затем ввести `/student/schedule` в адресной строке. Ожидаемо: страница открывается (placeholder "Кабинет студента появится в Фазе 51"), никаких редиректов. Headman успешно получает доступ к /student/* роутам.

    **ROADMAP Success Criterion 5 — Logout flow:**
    Находясь на любом dashboard (любая роль), нажать кнопку logout в sidebar. Ожидаемо:
    - Redirect на `/login`
    - Token очищен (попытаться открыть `/admin/dashboard` → редирект на `/login`)
    - В DevTools Network tab: был `POST /api/auth/logout` запрос с refresh token в теле → 200 OK (backend revoked refresh token)

    **ROADMAP Success Criterion 6 — 129 existing vitest tests green:**
    Уже автоматически проверено в Task 1 — записать число прошедших тестов (из output `cd frontends/web-panel && npx vitest run`).

    **Дополнительные smoke checks (Plan 05 Task 2 manual):**
    ```bash
    curl -I https://ruttrack.site/
    # Ожидается: HTTP/1.1 301 Moved Permanently, Location: /login

    curl -I https://ruttrack.site/login
    # Ожидается: HTTP/1.1 200 OK, Content-Type: text/html (web-panel SPA)

    curl -I https://ruttrack.site/api/health 2>&1 | head -5
    # Ожидается: proxy на api-gateway, не на web-panel

    curl -I https://ruttrack.site/admin/dashboard
    # Ожидается: 200 OK (SPA fallback, Angular router раскроет /admin/dashboard)
    ```

    **Landing footer check:**
    Открыть `https://ruttrack.site/presentation/` в браузере, прокрутить до футера. Ссылка "Войти" (не "Панель администратора") должна вести на `/login`.
  </how-to-verify>
  <resume-signal>
    Если все 6 ROADMAP criteria passed — ответить "approved" для завершения Phase 50.
    Если что-то упало — описать проблему, planner / executor починят.
    Если нужна дополнительная информация — задать вопрос.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite ↔ production behavior | Unit тесты покрывают контракты, но E2E с реальным backend проверяет integration |
| Human UAT ↔ security gate | Ручная проверка что guards реально блокируют нежелательный доступ в running app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-24 | Elevation of Privilege | Unit тесты зелёные, но в runtime guards обходятся | mitigate | Task 2 Human UAT — criterion 3 (plain STUDENT → /headman/dashboard blocked) — явная проверка в реальном flow. |
| T-50-25 | Denial of Service | Production build ломается из-за budget error | mitigate | Task 1 Step 2 — explicit `ng build --configuration production`. Если падает — откатываемся. |
| T-50-26 | Tampering | Stale cached index.html с `<base href="/admin/">` ломает routing у существующих пользователей | accept | Angular CLI добавляет content-hash к bundle names (`outputHashing: all`). Пользователи получат новый `index.html` при HTTP-запросе. Cache-busting работает автоматически. |
</threat_model>

<verification>
Phase 50 gate:
- `cd frontends/web-panel && npx vitest run` → exit 0, 146+ тестов green
- `cd frontends/web-panel && npx ng build --configuration production` → exit 0
- Human UAT approved (все 6 ROADMAP success criteria)
- `50-VALIDATION.md` content filled, frontmatter status: ready, nyquist_compliant: true
</verification>

<success_criteria>
- Все 129+ baseline vitest тестов зелёные (AUTH-v9-07 CLOSED)
- Production build успешен с baseHref: /
- 50-VALIDATION.md заполнен и имеет статус ready
- Human UAT подтверждает все 6 ROADMAP success criteria
- Phase 50 готов к `/gsd-complete-phase` (git commit + state update + retrospective)
</success_criteria>

<output>
После завершения создать `.planning/phases/50-basehref-migration-unified-login/50-06-SUMMARY.md` с:
- Финальным числом тестов (e.g. "153/153 passed")
- Подтверждением 6 ROADMAP criteria
- Ссылкой на всю Phase 50 в ROADMAP.md как shipped
</output>
