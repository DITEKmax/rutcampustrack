---
phase: 40-web-panel-admin
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - frontends/web-panel/src/app/features/admin/users/users-page.component.ts
  - frontends/web-panel/src/app/features/admin/users/users-page.component.html
  - frontends/web-panel/src/app/features/admin/users/users-page.component.spec.ts
  - frontends/web-panel/src/app/features/admin/users/user-dialog/user-dialog.component.ts
  - frontends/web-panel/src/app/features/admin/users/user-dialog/user-dialog.component.html
  - frontends/web-panel/src/app/features/admin/users/user-dialog/user-dialog.component.spec.ts
  - frontends/web-panel/src/app/features/admin/users/archive-user-dialog/archive-user-dialog.component.ts
autonomous: true
requirements:
  - WPAN-09
must_haves:
  truths:
    - "Admin sees paginated user list with login, name, role chip, group, status chip columns"
    - "Admin can search users by name/login with debounced input"
    - "Admin can filter users by role and status"
    - "Admin can create a user and sees the auto-generated login in a snackbar"
    - "Admin can edit a user via PATCH with partial fields"
    - "Admin can archive a user with confirmation dialog"
    - "Admin can restore an archived user with one click (no dialog)"
  artifacts:
    - path: "frontends/web-panel/src/app/features/admin/users/users-page.component.ts"
      provides: "User list page with mat-table, search, filters, pagination"
      exports: ["UsersPageComponent"]
    - path: "frontends/web-panel/src/app/features/admin/users/user-dialog/user-dialog.component.ts"
      provides: "Create/edit user dialog with reactive form"
      exports: ["UserDialogComponent"]
    - path: "frontends/web-panel/src/app/features/admin/users/archive-user-dialog/archive-user-dialog.component.ts"
      provides: "Archive confirmation dialog"
      exports: ["ArchiveUserDialogComponent"]
  key_links:
    - from: "users-page.component.ts"
      to: "AdminApiService"
      via: "inject(AdminApiService)"
      pattern: "adminApi.listUsers"
    - from: "user-dialog.component.ts"
      to: "AdminApiService"
      via: "inject(AdminApiService)"
      pattern: "adminApi.createUser|adminApi.patchUser"
---

<objective>
User CRUD page for admin: paginated mat-table with search/filter, create/edit dialog with reactive forms, archive confirmation dialog, restore action. Delivers WPAN-09 fully.

Purpose: Admin can manage all system users — create with auto-generated logins, edit profiles, archive/restore accounts.
Output: UsersPageComponent + UserDialogComponent + ArchiveUserDialogComponent, all wired to AdminApiService from Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-web-panel-admin/40-RESEARCH.md
@.planning/phases/40-web-panel-admin/40-UI-SPEC.md
@.planning/phases/40-web-panel-admin/40-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — types and API service -->

From frontends/web-panel/src/app/features/admin/shared/types.ts:
```typescript
export type UserRole = 'admin' | 'teacher' | 'student';
export type AccountStatus = 'active' | 'expelled' | 'suspended' | 'archived';
export interface UserResponse {
  id: number; login: string; displayName: string; role: UserRole;
  status: AccountStatus; groupId: number | null; headman: boolean;
  employeeNumber: string | null; telegramId: number | null; createdAt: string;
}
export interface CreateUserRequest {
  displayName: string; role: UserRole;
  groupId?: number; employeeNumber?: string; telegramId?: number;
}
export interface UserCreatedResponse extends UserResponse { initialPassword: string; }
export interface PatchUserRequest {
  displayName?: string; isHeadman?: boolean; groupId?: number;
  employeeNumber?: string; telegramId?: number; status?: AccountStatus;
}
export interface GroupResponse { id: number; name: string; code: string; active: boolean; createdAt: string; }
```

From frontends/web-panel/src/app/features/admin/shared/admin-api.service.ts:
```typescript
listUsers(params: { page?: number; size?: number; role?: string; status?: string; search?: string }): Observable<{ items: UserResponse[]; total: number }>
createUser(req: CreateUserRequest): Observable<UserCreatedResponse>
patchUser(id: number, req: PatchUserRequest): Observable<UserResponse>
deleteUser(id: number): Observable<void>
listGroups(): Observable<GroupResponse[]>
```

From frontends/web-panel/src/app/features/admin/shared/role-chip/role-chip.component.ts:
```typescript
export class RoleChipComponent { role = input.required<UserRole>(); }
```

From frontends/web-panel/src/app/features/admin/shared/status-chip/status-chip.component.ts:
```typescript
export class StatusChipComponent { status = input.required<AccountStatus>(); }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UsersPageComponent — mat-table with search, role/status filters, pagination</name>
  <files>
    frontends/web-panel/src/app/features/admin/users/users-page.component.ts,
    frontends/web-panel/src/app/features/admin/users/users-page.component.html,
    frontends/web-panel/src/app/features/admin/users/users-page.component.spec.ts
  </files>
  <read_first>
    frontends/web-panel/src/app/features/admin/shared/types.ts,
    frontends/web-panel/src/app/features/admin/shared/admin-api.service.ts,
    frontends/web-panel/src/app/features/admin/shared/role-chip/role-chip.component.ts,
    frontends/web-panel/src/app/features/admin/shared/status-chip/status-chip.component.ts,
    frontends/web-panel/src/app/features/teacher/stats/stats-page.component.ts,
    frontends/web-panel/src/app/features/teacher/journal/journal-page.component.ts,
    .planning/phases/40-web-panel-admin/40-UI-SPEC.md
  </read_first>
  <action>
    1. Create `UsersPageComponent` — standalone, signal-based state:
    ```
    Signals: users = signal<UserResponse[]>([]), loading = signal(false), error = signal<string | null>(null),
             totalElements = signal(0), pageIndex = signal(0), pageSize = signal(20),
             groups = signal<GroupResponse[]>([])
    Injected: adminApi = inject(AdminApiService), dialog = inject(MatDialog),
              snackBar = inject(MatSnackBar), destroyRef = inject(DestroyRef)
    Search: searchControl = new FormControl('')
    Filters: roleFilter = signal<string>(''), statusFilter = signal<string>('')
    ```

    2. On `ngOnInit()`:
    - Load groups via `adminApi.listGroups()` for the group name lookup
    - Load initial users via `loadUsers()`
    - Wire `searchControl.valueChanges.pipe(debounceTime(300), distinctUntilChanged(), takeUntilDestroyed(this.destroyRef))` to call `loadUsers()`

    3. `loadUsers()` method: build params from `pageIndex()`, `pageSize()`, `roleFilter()`, `statusFilter()`, `searchControl.value`, call `adminApi.listUsers(params)`, set `users`, `totalElements`, `loading`, `error` signals.

    4. Template (`users-page.component.html`):
    - Page header row: `<h1>Пользователи</h1>` + `<button mat-flat-button color="primary" (click)="openCreateDialog()">Создать пользователя</button>`
    - Filter row: `<mat-form-field>` with `matInput` placeholder "Поиск по имени или логину…" bound to `searchControl` + two `<mat-select>` for role (options: empty="Все роли", "admin"="Админ", "teacher"="Преподаватель", "student"="Студент") and status (options: empty="Все", "active"="Активные", "archived"="Архивные"). On `(selectionChange)` set the respective signal and call `loadUsers()`.
    - `<mat-table [dataSource]="users()">` with columns: login, displayName, role (use `<app-role-chip [role]="row.role">`), group (lookup `groups().find(g => g.id === row.groupId)?.name ?? '—'`), status (use `<app-status-chip [status]="row.status">`), actions.
    - Actions column: two `<button mat-icon-button>` — edit (Phosphor `ph-pencil-simple` icon, tooltip "Редактировать", `(click)="openEditDialog(row)"`) and archive/restore (`ph-archive` if active, `ph-arrow-counter-clockwise` if archived; tooltip "Архивировать"/"Восстановить"; `(click)="archiveUser(row)"` or `(click)="restoreUser(row)"`).
    - Archived user rows: apply class `opacity-60` on the `<mat-row>` when `row.status === 'archived'`.
    - `<mat-paginator [length]="totalElements()" [pageSize]="pageSize()" [pageSizeOptions]="[20, 50, 100]" (page)="onPageChange($event)">`.
    - Empty state: `@if (users().length === 0 && !loading()) { <div class="text-center py-12"><p class="text-lg font-semibold">Пользователи не найдены</p><p class="text-sm text-[var(--mat-sys-on-surface)]/60">Измените фильтр или создайте первого пользователя.</p></div> }`
    - Loading: `@if (loading()) { <mat-progress-bar mode="indeterminate"></mat-progress-bar> }`

    5. Methods:
    - `onPageChange(event: PageEvent)`: set `pageIndex` and `pageSize` signals, call `loadUsers()`
    - `openCreateDialog()`: open `UserDialogComponent` with `{ mode: 'create' }`. On `afterClosed()` with truthy result: show snackbar `"Пользователь создан. Логин: ${result.login}"` (4000ms), call `loadUsers()`
    - `openEditDialog(user: UserResponse)`: open `UserDialogComponent` with `{ mode: 'edit', user }`. On close with truthy: snackbar `"Данные пользователя обновлены."`, call `loadUsers()`
    - `archiveUser(user: UserResponse)`: open `ArchiveUserDialogComponent` with `{ user }`. On close with truthy: call `adminApi.patchUser(user.id, { status: 'archived' })`, snackbar `"Пользователь архивирован."`, call `loadUsers()`. Note: use PATCH with status='archived' (not DELETE, per research — DELETE is soft archive but PATCH gives more control)
    - `restoreUser(user: UserResponse)`: directly call `adminApi.patchUser(user.id, { status: 'active' })`, snackbar `"Пользователь восстановлен."`, call `loadUsers()`

    6. `users-page.component.spec.ts`:
    - Test: component creates
    - Test: ngOnInit triggers loadUsers and listGroups calls
    - Test: openCreateDialog opens MatDialog with mode 'create'
    - Test: restoreUser calls patchUser with status 'active'

    Standalone imports: CommonModule, ReactiveFormsModule, MatTableModule, MatPaginatorModule, MatSortModule, MatFormFieldModule, MatInputModule, MatSelectModule, MatButtonModule, MatIconButtonModule (mat-icon-button), MatProgressBarModule, MatTooltipModule, MatSnackBarModule, RoleChipComponent, StatusChipComponent
  </action>
  <verify>
    <automated>cd frontends/web-panel && npm run test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontends/web-panel/src/app/features/admin/users/users-page.component.ts contains `export class UsersPageComponent`
    - frontends/web-panel/src/app/features/admin/users/users-page.component.ts contains `inject(AdminApiService)`
    - frontends/web-panel/src/app/features/admin/users/users-page.component.ts contains `inject(MatDialog)`
    - frontends/web-panel/src/app/features/admin/users/users-page.component.ts contains `inject(MatSnackBar)`
    - frontends/web-panel/src/app/features/admin/users/users-page.component.ts contains `debounceTime(300)`
    - frontends/web-panel/src/app/features/admin/users/users-page.component.html contains `Пользователи`
    - frontends/web-panel/src/app/features/admin/users/users-page.component.html contains `mat-paginator`
    - frontends/web-panel/src/app/features/admin/users/users-page.component.html contains `app-role-chip`
    - frontends/web-panel/src/app/features/admin/users/users-page.component.html contains `app-status-chip`
    - frontends/web-panel/src/app/features/admin/users/users-page.component.html contains `Создать пользователя`
    - npm run test exits 0
  </acceptance_criteria>
  <done>UsersPageComponent renders paginated mat-table with search, role/status filters, edit/archive/restore actions, empty state. Spec passes.</done>
</task>

<task type="auto">
  <name>Task 2: UserDialogComponent (create/edit) + ArchiveUserDialogComponent</name>
  <files>
    frontends/web-panel/src/app/features/admin/users/user-dialog/user-dialog.component.ts,
    frontends/web-panel/src/app/features/admin/users/user-dialog/user-dialog.component.html,
    frontends/web-panel/src/app/features/admin/users/user-dialog/user-dialog.component.spec.ts,
    frontends/web-panel/src/app/features/admin/users/archive-user-dialog/archive-user-dialog.component.ts
  </files>
  <read_first>
    frontends/web-panel/src/app/features/admin/shared/types.ts,
    frontends/web-panel/src/app/features/admin/shared/admin-api.service.ts,
    frontends/web-panel/src/app/features/admin/users/users-page.component.ts,
    .planning/phases/40-web-panel-admin/40-UI-SPEC.md
  </read_first>
  <action>
    1. Create `UserDialogComponent` — standalone, with `inject(MatDialogRef)` and `inject(MAT_DIALOG_DATA)`:

    Data interface:
    ```typescript
    export interface UserDialogData {
      mode: 'create' | 'edit';
      user?: UserResponse;
      groups: GroupResponse[];
    }
    ```

    Reactive form (FormGroup):
    - `displayName: FormControl<string>` — required, minLength 2
    - `role: FormControl<UserRole | ''>` — required
    - `groupId: FormControl<number | null>` — shown when role is 'student' or 'teacher'
    - `employeeNumber: FormControl<string>` — shown when role is 'teacher'
    - `isHeadman: FormControl<boolean>` — shown when role is 'student' (checkbox)

    In edit mode: patch form with user data. `displayName` = user.displayName, `role` = user.role (disabled in edit mode — role cannot be changed), `groupId` = user.groupId, `isHeadman` = user.headman.

    Template (`user-dialog.component.html`):
    - Dialog title: `@if (data.mode === 'create') { Новый пользователь } @else { Редактировать пользователя }`
    - `<mat-dialog-content>` with form fields:
      - displayName: `<mat-form-field><mat-label>ФИО</mat-label><input matInput formControlName="displayName"><mat-error>ФИО обязательно</mat-error></mat-form-field>`
      - role: `<mat-form-field><mat-label>Роль</mat-label><mat-select formControlName="role"><mat-option value="admin">Админ</mat-option><mat-option value="teacher">Преподаватель</mat-option><mat-option value="student">Студент</mat-option></mat-select></mat-form-field>`
      - In create mode: hint text below role field: "Логин будет сгенерирован автоматически"
      - In edit mode: read-only login display: `<mat-form-field><mat-label>Логин</mat-label><input matInput [value]="data.user!.login" readonly></mat-form-field>`
      - Group select (conditionally shown with `@if (form.get('role')?.value === 'student' || form.get('role')?.value === 'teacher')`): `<mat-select formControlName="groupId"><mat-option [value]="null">—</mat-option>@for (g of data.groups; track g.id) { <mat-option [value]="g.id">{{g.name}}</mat-option> }</mat-select>`
      - isHeadman checkbox (conditionally shown with `@if (form.get('role')?.value === 'student')`): `<mat-checkbox formControlName="isHeadman">Является старостой</mat-checkbox>`
      - Error area: `@if (apiError()) { <p class="text-sm text-[var(--mat-sys-error)] mt-2">Не удалось сохранить. Проверьте данные и попробуйте снова.</p> }`
    - `<mat-dialog-actions align="end">`:
      - `<button mat-button mat-dialog-close>Отмена</button>`
      - `<button mat-flat-button color="primary" (click)="save()" [disabled]="saving()">{{ data.mode === 'create' ? 'Создать пользователя' : 'Сохранить изменения' }}</button>`

    2. `save()` method:
    - Mark all controls as touched. If form invalid, return.
    - Set `saving` signal to true.
    - If create mode: build `CreateUserRequest` from form values (omit empty optional fields), call `adminApi.createUser()`. On success: `dialogRef.close(result)` (result has `login` and `initialPassword`). On error: set `apiError` signal, `saving` false.
    - If edit mode: build `PatchUserRequest` from changed form values only. Call `adminApi.patchUser(data.user!.id, req)`. On success: `dialogRef.close(true)`. On error: set `apiError`, `saving` false.

    3. Create `ArchiveUserDialogComponent` — standalone, simple confirmation dialog:
    ```typescript
    @Component({
      selector: 'app-archive-user-dialog',
      standalone: true,
      imports: [MatDialogModule, MatButtonModule],
      template: `
        <h2 mat-dialog-title>Архивировать пользователя?</h2>
        <mat-dialog-content>
          <p>Пользователь {{ data.user.displayName }} будет деактивирован. Войти в систему он не сможет.</p>
        </mat-dialog-content>
        <mat-dialog-actions align="end">
          <button mat-button mat-dialog-close>Отмена</button>
          <button mat-flat-button color="warn" [mat-dialog-close]="true">Архивировать</button>
        </mat-dialog-actions>
      `,
    })
    export class ArchiveUserDialogComponent {
      data = inject<{ user: UserResponse }>(MAT_DIALOG_DATA);
    }
    ```

    4. Create `user-dialog.component.spec.ts`:
    - Test: dialog opens in create mode with empty form
    - Test: dialog opens in edit mode with form pre-filled
    - Test: save in create mode calls adminApi.createUser with correct body
    - Test: form validation prevents save when displayName is empty
    - Test: isHeadman checkbox visible only when role = student
  </action>
  <verify>
    <automated>cd frontends/web-panel && npm run test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontends/web-panel/src/app/features/admin/users/user-dialog/user-dialog.component.ts contains `inject(MatDialogRef`
    - frontends/web-panel/src/app/features/admin/users/user-dialog/user-dialog.component.ts contains `inject(MAT_DIALOG_DATA)`
    - frontends/web-panel/src/app/features/admin/users/user-dialog/user-dialog.component.ts contains `FormControl`
    - frontends/web-panel/src/app/features/admin/users/user-dialog/user-dialog.component.ts contains `adminApi.createUser`
    - frontends/web-panel/src/app/features/admin/users/user-dialog/user-dialog.component.ts contains `adminApi.patchUser`
    - frontends/web-panel/src/app/features/admin/users/user-dialog/user-dialog.component.html contains `Новый пользователь`
    - frontends/web-panel/src/app/features/admin/users/user-dialog/user-dialog.component.html contains `Создать пользователя`
    - frontends/web-panel/src/app/features/admin/users/user-dialog/user-dialog.component.html contains `Является старостой`
    - frontends/web-panel/src/app/features/admin/users/archive-user-dialog/archive-user-dialog.component.ts contains `Архивировать пользователя`
    - frontends/web-panel/src/app/features/admin/users/archive-user-dialog/archive-user-dialog.component.ts contains `mat-dialog-close`
    - npm run test exits 0
  </acceptance_criteria>
  <done>UserDialogComponent handles create/edit with reactive form, role-conditional fields, API integration. ArchiveUserDialogComponent confirms archive action. All specs pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dialog form -> API | User-submitted form data passes through AdminApiService to backend |
| Admin role enforcement | Backend @RequireRole(ADMIN) on all user management endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-04 | Tampering | UserDialogComponent form | mitigate | Reactive form validators (required, minLength) prevent empty submissions; backend validates all fields server-side |
| T-40-05 | Information Disclosure | UserCreatedResponse.initialPassword | mitigate | Password shown only in snackbar (4s auto-dismiss), not persisted in DOM or state; admin is authorized to see it |
| T-40-06 | Denial of Service | Search debounce | mitigate | 300ms debounceTime + distinctUntilChanged prevents rapid API calls from typing |
</threat_model>

<verification>
1. `cd frontends/web-panel && npm run test` — all tests pass
2. `grep "UsersPageComponent" frontends/web-panel/src/app/features/admin/users/users-page.component.ts` — exported
3. `grep "UserDialogComponent" frontends/web-panel/src/app/features/admin/users/user-dialog/user-dialog.component.ts` — exported
4. `grep "ArchiveUserDialogComponent" frontends/web-panel/src/app/features/admin/users/archive-user-dialog/archive-user-dialog.component.ts` — exported
</verification>

<success_criteria>
- UsersPageComponent shows paginated user table with search, role/status filters
- UserDialogComponent handles create mode (auto-login hint) and edit mode (prefilled form)
- ArchiveUserDialogComponent shows destructive confirmation
- Restore action calls patchUser with status='active' directly (no dialog)
- Snackbar feedback for all CRUD operations
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/40-web-panel-admin/40-02-SUMMARY.md`
</output>
