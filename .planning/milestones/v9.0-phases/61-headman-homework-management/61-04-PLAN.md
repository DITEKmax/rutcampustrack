---
phase: 61-headman-homework-management-ui-homeworkapi-controller-homewo
plan: 04
type: execute
wave: 4
depends_on: [61-03]
files_modified:
  - event-schemas/homework.published.json
  - event-schemas/homework.updated.json
  - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/event/HomeworkPublishedEvent.java
  - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/event/HomeworkUpdatedEvent.java
  - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/homework/HomeworkService.java
  - services/notification-bot/bot/notifications/homework.py
  - services/notification-bot/tests/test_homework_handler.py
  - services/academic-service/academic-app/src/test/java/ru/rutcampustrack/academic/homework/HomeworkEventPublishIT.java
autonomous: true
requirements: [HW-EVENT]

must_haves:
  truths:
    - "Event `homework.published` payload содержит lessonDate (ISO date) + lessonNumber (int)"
    - "Event `homework.updated` payload содержит subjectId + lessonDate + lessonNumber"
    - "JSON Schemas в event-schemas/ соответствуют новому payload (required поля обновлены)"
    - "notification-bot handler `handle_homework` для event_type=homework.updated резолвит subject_name через academic_client.get_subjects_by_ids и формирует текст «ДЗ изменено: {subject} — {title}»"
    - "pytest покрывает handler для homework.updated (subject lookup + формат сообщения)"
  artifacts:
    - path: "event-schemas/homework.published.json"
      provides: "JSON schema с lesson_date, lesson_number"
      contains: "lesson_date"
    - path: "event-schemas/homework.updated.json"
      provides: "JSON schema с subject_id, lesson_date, lesson_number"
      contains: "subject_id"
    - path: "services/notification-bot/bot/notifications/homework.py"
      provides: "расширенный handler: ветка homework.updated резолвит subject"
      contains: "homework.updated"
    - path: "services/notification-bot/tests/test_homework_handler.py"
      provides: "pytest для handler"
  key_links:
    - from: "HomeworkService.createHomework"
      to: "HomeworkPublishedEvent с lessonDate + lessonNumber"
      via: "publishEvent(new HomeworkPublishedEvent(..., saved.getLessonDate().toString(), saved.getLessonNumber()))"
      pattern: "HomeworkPublishedEvent"
    - from: "HomeworkService.updateHomework"
      to: "HomeworkUpdatedEvent с subjectId + lessonDate + lessonNumber"
      via: "publishEvent"
      pattern: "HomeworkUpdatedEvent"
    - from: "notification-bot dispatcher.py строка 83 (homework.updated)"
      to: "handle_homework function"
      via: "существующий routing (researcher VERIFIED)"
      pattern: "homework\\.updated"
---

<objective>
Расширить payload событий `homework.published` и `homework.updated` полями `lesson_date`, `lesson_number` (+ `subject_id` для updated). Обновить JSON Schemas. Расширить notification-bot handler для `homework.updated` чтобы он резолвил subject_name и формировал читаемый текст уведомления.

Purpose: Реализует D-07 (push при create И update, с полным payload). Бот уже маршрутизирует `homework.updated` (researcher VERIFIED: `event_dispatcher.py:83`), но handler для updated ветки пишет generic «Домашнее задание обновлено» без subject. Нужно расширить.

Output: Обновлённые Java events, обновлённые JSON Schemas, расширенный Python handler, pytest + Java IT для публикации.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v9.0-phases/61-headman-homework-management/61-CONTEXT.md
@.planning/milestones/v9.0-phases/61-headman-homework-management/61-RESEARCH.md

<interfaces>
From services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/event/HomeworkPublishedEvent.java:
```java
// Текущий Payload record — НЕТ lesson_date / lesson_number
public record Payload(
    @JsonProperty("homework_id") Long homeworkId,
    @JsonProperty("group_id") Long groupId,
    @JsonProperty("subject_id") Long subjectId,
    @JsonProperty("title") String title,
    @JsonProperty("has_link") Boolean hasLink
) {}
// ADD: @JsonProperty("lesson_date") String lessonDate, @JsonProperty("lesson_number") Integer lessonNumber
```

From services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/event/HomeworkUpdatedEvent.java:
```java
// Текущий Payload — часто только {homework_id, group_id, title}. Нужно добавить
// subject_id (обязателен для bot lookup), lesson_date, lesson_number.
```

From services/notification-bot/bot/notifications/homework.py:
```python
# Функция handle_homework принимает event_type и payload.
# Ветка if event_type == "homework.published" — уже резолвит subject через
# academic_client.get_subjects_by_ids([subject_id]).
# Ветка elif event_type == "homework.updated" — сейчас text = f"Домашнее задание обновлено\n\n{title}"
# Нужно сделать аналогично published: резолвить subject и писать «ДЗ изменено: {subject} — {title}»
```

From services/notification-bot/bot/event_dispatcher.py (строка 83 VERIFIED):
```python
# Уже регистрирует handle_homework для homework.updated. Менять диспетчер не надо.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Расширить Java events + JSON Schemas + HomeworkService publish calls + Java IT</name>
  <read_first>
    - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/event/HomeworkPublishedEvent.java
    - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/event/HomeworkUpdatedEvent.java
    - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/event/DomainEvent.java (базовый класс)
    - services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/homework/HomeworkService.java (места где вызывается publishEvent — после Task 1 PLAN-03)
    - event-schemas/homework.published.json (текущая схема)
    - event-schemas/homework.updated.json
    - event-schemas/lesson.one_off.created.json (шаблон стиля JSON Schema из Phase 60)
  </read_first>
  <files>
    services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/event/HomeworkPublishedEvent.java,
    services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/event/HomeworkUpdatedEvent.java,
    services/academic-service/academic-app/src/main/java/ru/rutcampustrack/academic/homework/HomeworkService.java,
    event-schemas/homework.published.json,
    event-schemas/homework.updated.json,
    services/academic-service/academic-app/src/test/java/ru/rutcampustrack/academic/homework/HomeworkEventPublishIT.java
  </files>
  <action>
    1. **HomeworkPublishedEvent.Payload** — добавить поля:
    ```java
    public record Payload(
        @JsonProperty("homework_id") Long homeworkId,
        @JsonProperty("group_id") Long groupId,
        @JsonProperty("subject_id") Long subjectId,
        @JsonProperty("title") String title,
        @JsonProperty("has_link") Boolean hasLink,
        @JsonProperty("lesson_date") String lessonDate,     // ISO-8601
        @JsonProperty("lesson_number") Integer lessonNumber
    ) {}
    ```
    Обновить конструктор `HomeworkPublishedEvent(source, ..., lessonDate, lessonNumber)`.

    2. **HomeworkUpdatedEvent.Payload** — добавить:
    ```java
    public record Payload(
        @JsonProperty("homework_id") Long homeworkId,
        @JsonProperty("group_id") Long groupId,
        @JsonProperty("subject_id") Long subjectId,     // NEW (per D-07 bot lookup)
        @JsonProperty("title") String title,
        @JsonProperty("has_link") Boolean hasLink,
        @JsonProperty("lesson_date") String lessonDate,
        @JsonProperty("lesson_number") Integer lessonNumber
    ) {}
    ```

    3. **HomeworkService** — в `createHomework` после save:
    ```java
    eventPublisher.publishEvent(new HomeworkPublishedEvent(
        this, saved.getId(), saved.getGroupId(), saved.getSubjectId(),
        saved.getTitle(), saved.getLink() != null,
        saved.getLessonDate().toString(), saved.getLessonNumber()));
    ```
    В `updateHomework` после сохранения — аналогично `HomeworkUpdatedEvent` с теми же полями.

    4. **event-schemas/homework.published.json** — обновить required + properties:
    ```json
    {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "homework.published",
      "type": "object",
      "required": ["event_type", "payload"],
      "properties": {
        "event_type": { "type": "string", "const": "homework.published" },
        "payload": {
          "type": "object",
          "required": ["homework_id", "group_id", "subject_id", "title", "lesson_date", "lesson_number"],
          "properties": {
            "homework_id":   { "type": "integer" },
            "group_id":      { "type": "integer" },
            "subject_id":    { "type": "integer" },
            "title":         { "type": "string" },
            "has_link":      { "type": "boolean" },
            "lesson_date":   { "type": "string", "format": "date" },
            "lesson_number": { "type": "integer", "minimum": 1, "maximum": 8 }
          }
        }
      }
    }
    ```
    Удалить `lesson_id` из схемы если был (несоответствие давнее, researcher VERIFIED).

    5. **event-schemas/homework.updated.json** — аналогично: добавить `subject_id`, `lesson_date`, `lesson_number` в required + properties.

    6. **HomeworkEventPublishIT.java** — Testcontainers (Postgres + RabbitMQ) + `@SpringBootTest`:
    - Создать Homework через сервис (mock ScheduleGrpcClient → валидный LessonResponse) → перехватить сообщение в RabbitMQ (test queue bound к fanout) → проверить payload содержит `lesson_date` и `lesson_number`.
    - Update → тот же перехват для `homework.updated`, проверить что есть `subject_id`.
    - Шаблон — существующий `OneOffLessonEventPublisherIT.java` из Phase 60.
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && ./gradlew.bat :services:academic-service:academic-app:test --tests 'HomeworkEventPublishIT'</automated>
  </verify>
  <done>
    - 2 IT теста зелёные (published + updated)
    - JSON Schemas валидны (валидатор проходит)
    - Java events компилируются
  </done>
</task>

<task type="auto">
  <name>Task 2: notification-bot — расширить handle_homework для homework.updated + pytest</name>
  <read_first>
    - services/notification-bot/bot/notifications/homework.py (текущий handle_homework целиком)
    - services/notification-bot/bot/event_dispatcher.py (строка ~83 подтвердить route homework.updated → handle_homework)
    - services/notification-bot/bot/clients/academic_client.py (метод get_subjects_by_ids)
    - services/notification-bot/tests/ (ls — найти существующие test_*.py для паттерна)
    - services/notification-bot/tests/test_one_off_created_handler.py (шаблон pytest для bot handler, если есть из Phase 60)
  </read_first>
  <files>
    services/notification-bot/bot/notifications/homework.py,
    services/notification-bot/tests/test_homework_handler.py
  </files>
  <action>
    1. **homework.py** — в ветке `elif event_type == "homework.updated":` сейчас, вероятно, выглядит так:
    ```python
    elif event_type == "homework.updated":
        text = f"Домашнее задание обновлено\n\n{title}"
    ```
    Заменить на:
    ```python
    elif event_type == "homework.updated":
        subject_id = payload.get("subject_id")
        subject_name = "предмет"
        if subject_id:
            subjects = await academic_client.get_subjects_by_ids([subject_id])
            if subjects:
                subject_name = subjects[0].get("name", subject_name)
        text = f"ДЗ изменено: {subject_name} — {title}"
        # опционально: добавить строку «Пара {lesson_number}, дата {lesson_date}»
        lesson_date = payload.get("lesson_date")
        lesson_number = payload.get("lesson_number")
        if lesson_date and lesson_number:
            text += f"\nПара {lesson_number}, {lesson_date}"
    ```

    Для ветки `homework.published` — аналогично можно добавить строку «Пара X, дата Y», если в existing реализации её нет (не критично для D-07; планировщик может пропустить, если тест-ожидания чёткие).

    2. **test_homework_handler.py** — pytest (AsyncMock aiogram + AsyncMock academic_client):
    ```python
    @pytest.mark.asyncio
    async def test_updated_uses_subject_name():
        academic_client = AsyncMock()
        academic_client.get_subjects_by_ids = AsyncMock(return_value=[{"id": 42, "name": "Математика"}])
        payload = {
            "homework_id": 1, "group_id": 10, "subject_id": 42,
            "title": "Задача 3", "has_link": False,
            "lesson_date": "2026-05-01", "lesson_number": 2
        }
        bot = AsyncMock()
        await handle_homework(bot, academic_client, ..., event_type="homework.updated", payload=payload)
        # Assert: bot.send_message вызван с text содержащим "Математика" и "Задача 3"
        call = bot.send_message.call_args_list[0]
        assert "Математика" in call.kwargs["text"]
        assert "Задача 3" in call.kwargs["text"]

    @pytest.mark.asyncio
    async def test_published_still_works():
        # Regression — существующая ветка homework.published с добавленным lesson_date/number
        # в payload не должна сломаться.
    ```

    Точные сигнатуры handle_homework — проверить в `homework.py` перед написанием теста, адаптировать вызов.

    **НЕ делать:** не менять диспетчер event_dispatcher.py — он уже правильно маршрутизирует (researcher).
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack/services/notification-bot && python -m pytest tests/test_homework_handler.py -v</automated>
  </verify>
  <done>
    - pytest тесты зелёные
    - existing notification-bot тесты проходят без regression
    - Handler для homework.updated пишет сообщение с subject_name + title
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RabbitMQ event payload → notification-bot handler | trusted (внутренняя шина) но payload может содержать некорректный subject_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-12 | Tampering | malformed subject_id в payload | mitigate | `if subject_id:` + fallback `subject_name = "предмет"` |
| T-61-13 | DoS | academic_client таймаут | accept | существующий HTTP-клиент в bot-е имеет retry/timeout |
| T-61-14 | Info disclosure | title содержит PII | accept | title вводит сам староста, не PII |
</threat_model>

<verification>
- academic-app test suite зелёный
- notification-bot pytest зелёный
- JSON Schemas валидны
</verification>

<success_criteria>
1. `homework.published` payload содержит `lesson_date`, `lesson_number`
2. `homework.updated` payload содержит `subject_id`, `lesson_date`, `lesson_number`
3. JSON Schemas обновлены
4. Bot handler для `homework.updated` формирует «ДЗ изменено: {subject} — {title}»
5. Java IT + Python pytest зелёные
</success_criteria>

<output>
После завершения создать `.planning/milestones/v9.0-phases/61-headman-homework-management/61-04-SUMMARY.md`.
</output>
