---
phase: 60-headman-schedule-management
plan: 08
type: execute
wave: 5
depends_on: [60-01, 60-02, 60-03, 60-04, 60-05, 60-06, 60-07]
files_modified:
  - docs/phase-60-report.md
autonomous: true
requirements: [AC-01, AC-02, AC-03, AC-04, AC-05, AC-06, AC-07, AC-08, AC-09, AC-10, AC-11]

must_haves:
  truths:
    - "Все 11 AC покрыты тестами и зелёные"
    - "./gradlew.bat build BUILD SUCCESS (academic-service + schedule-service + attendance-service)"
    - "cd services/notification-bot && pytest -q — все тесты зелёные"
    - "cd frontends/web-panel && npm run test -- --watch=false — все тесты зелёные"
    - "phase-60-report.md создан с описанием всех реализованных фич"
  artifacts:
    - path: "docs/phase-60-report.md"
      provides: "отчёт фазы 60 — итог реализации"
  key_links:
    - from: "full backend build"
      to: "all Flyway migrations applied"
      via: "./gradlew.bat build"
      pattern: "BUILD SUCCESS"
---

<objective>
Финальная верификация Phase 60: запустить полный build всех сервисов, проверить все тесты (Java + Python + Angular), убедиться что Flyway миграции (V12 academic, V3+V4 schedule) применяются без ошибок. Написать итоговый отчёт фазы.

Purpose: Завершающий gate для Phase 60 — все 11 AC верифицированы перед merge.

Output: BUILD SUCCESS, зелёные тесты, phase-60-report.md.
</objective>

<execution_context>
@$HOME/.claire/get-shit-done/workflows/execute-plan.md
@$HOME/.claire/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60-headman-schedule-management/60-CONTEXT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build + regression suite</name>
  <read_first>
    - .planning/phases/60-headman-schedule-management/60-01-SUMMARY.md
    - .planning/phases/60-headman-schedule-management/60-02-SUMMARY.md
    - .planning/phases/60-headman-schedule-management/60-03-SUMMARY.md
    - .planning/phases/60-headman-schedule-management/60-04-SUMMARY.md
    - .planning/phases/60-headman-schedule-management/60-05-SUMMARY.md
    - .planning/phases/60-headman-schedule-management/60-06-SUMMARY.md
    - .planning/phases/60-headman-schedule-management/60-07-SUMMARY.md
  </read_first>
  <files>
    docs/phase-60-report.md
  </files>
  <action>
    1. Запустить полный Java build:
    ```bash
    ./gradlew.bat build 2>&1 | tail -30
    ```
    Если ошибки — исправить. BUILD SUCCESS обязателен.

    2. Запустить Python bot тесты:
    ```bash
    cd services/notification-bot && python -m pytest -q 2>&1 | tail -20
    ```

    3. Запустить Angular тесты:
    ```bash
    cd frontends/web-panel && npm run test -- --watch=false 2>&1 | tail -20
    ```

    4. Проверить специфические тесты Phase 60:
    ```bash
    ./gradlew.bat :services:academic-service:academic-app:test --tests "ru.rutcampustrack.academic.subject.*" -x javadoc
    ./gradlew.bat :services:schedule-service:schedule-app:test --tests "ru.rutcampustrack.schedule.oneoff.*" -x javadoc
    ./gradlew.bat :services:attendance-service:attendance-app:test --tests "ru.rutcampustrack.attendance.event.OneOffLessonCancelledConsumerIT" -x javadoc
    ```

    5. Проверить отсутствие teacher_id в schedule-service:
    ```bash
    grep -rn "teacherId\|teacher_id" services/schedule-service/schedule-app/src/main/java/ | grep -v "\.sql" | grep -v "// old"
    ```
    Должно быть 0 результатов.

    6. Проверить новые endpoints доступны (если Docker запущен):
    ```bash
    curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/api/academic/subjects  # 200 или 401
    curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/api/schedule/one-off-lessons  # 200 или 401
    ```

    7. Создать `docs/phase-60-report.md` с разделами:
    - ## Цели фазы (из ROADMAP)
    - ## Реализовано (все 11 AC с статусом Полностью/Частично)
    - ## Изменения по сервисам (academic-service, schedule-service, attendance-service, notification-bot, web-panel)
    - ## Новые Flyway миграции (V12 academic, V3+V4 schedule)
    - ## Новые API endpoints
    - ## События RabbitMQ
    - ## Тесты (количество по сервису)
    - ## Known Limitations (если read-path merge не полностью реализован)
    - ## Next Steps (если есть незавершённое)
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && ./gradlew.bat build 2>&1 | grep -E "BUILD|FAIL|ERROR" | tail -10</automated>
  </verify>
  <done>
    BUILD SUCCESS; pytest зелёные; Angular тесты зелёные; docs/phase-60-report.md создан; все 11 AC подтверждены в отчёте.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (финальная верификация — нет новых boundaries) | — |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Broken Access Control | всё phase-60 | verified | unit + IT тесты покрывают: headman wrong group → 403; non-headman → 403; ADMIN bypass |
| T-60-02 | Tampering | SubjectService atomicity | verified | SubjectServiceIT.rollbackOnTeacherSaveFail подтверждает транзакцию |
| T-60-03 | Tampering | UNIQUE one-off | verified | OneOffLessonControllerIT.create_whenDuplicateOneOff_returns409 |
| T-60-04 | Idempotency | cancelled event | verified | OneOffLessonCancelledConsumerIT.cancellation_idempotent |
| T-60-05 | Conflict | template + one-off | verified | OneOffLessonControllerIT.create_whenTemplateSlotActive_returns409 |
</threat_model>

<verification>
1. `./gradlew.bat build` → BUILD SUCCESS
2. `cd services/notification-bot && python -m pytest -q` → all passed
3. `cd frontends/web-panel && npm run test -- --watch=false` → all passed
4. `ls docs/phase-60-report.md` — файл существует
5. `grep -c "AC-" docs/phase-60-report.md` — упомянуты все 11 AC
</verification>

<success_criteria>
- `./gradlew.bat build` BUILD SUCCESS
- Все pytest тесты зелёные
- Все Angular тесты зелёные (нет регрессии существующих 346+)
- docs/phase-60-report.md создан с AC-01..AC-11
- Явно задокументированы Known Limitations если какой-то AC реализован частично
</success_criteria>

<output>
После завершения создать `.planning/phases/60-headman-schedule-management/60-08-SUMMARY.md`.
Обновить `.planning/STATE.md` — Phase 60 Complete.
</output>
