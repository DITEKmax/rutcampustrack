---
phase: 59-excuses-backend
plan: 07
type: execute
wave: 4
depends_on:
  - 59-01
files_modified:
  - frontends/web-panel/src/app/features/student/excuses/excuse-form-dialog/excuse-form-dialog.component.ts
  - frontends/web-panel/src/app/features/student/excuses/excuse-form-dialog/excuse-form-dialog.component.html
  - frontends/web-panel/src/app/features/student/excuses/student-excuses.component.ts
  - frontends/web-panel/src/app/features/student/excuses/student-excuses.component.html
  - frontends/web-panel/src/app/features/student/shared/student-api.service.ts
  - frontends/web-panel/src/app/features/student/shared/student-schedule.types.ts
  - frontends/web-panel/src/app/features/student/excuses/student-excuses.component.spec.ts
autonomous: true
requirements:
  - FR-1
  - FR-3
  - AC-9
  - D-21
  - D-22

must_haves:
  truths:
    - "ExcuseFormDialog содержит dropdown 'Причина пропуска' с 6 значениями ExcuseType на русском"
    - "submitExcuse() отправляет POST /api/attendance/excuses с {lessonIds, excuseType, comment} (JSON, не FormData)"
    - "student-excuses.component загружает данные через GET /api/attendance/excuses/me"
    - "getExcuseTickets() использует /api/attendance/excuses/me вместо /api/attendance/excuses"
    - "Список тикетов отображает excuseType на русском языке"
    - "student-excuses.component.spec.ts зелёный"
  artifacts:
    - path: "frontends/web-panel/src/app/features/student/excuses/excuse-form-dialog/excuse-form-dialog.component.ts"
      provides: "Форма с ExcuseType dropdown + JSON submit"
    - path: "frontends/web-panel/src/app/features/student/shared/student-api.service.ts"
      provides: "submitExcuse() и getExcuseTickets() с правильными URL"
    - path: "frontends/web-panel/src/app/features/student/excuses/student-excuses.component.spec.ts"
      provides: "Vitest тесты (AC-9)"
  key_links:
    - from: "ExcuseFormDialogComponent.submit()"
      to: "StudentApiService.submitExcuse(lessonIds, excuseType, comment)"
      via: "Observable subscribe"
      pattern: "submitExcuse"
    - from: "StudentExcusesComponent.loadTickets()"
      to: "StudentApiService.getExcuseTickets()"
      via: "Observable subscribe"
      pattern: "getExcuseTickets"
---

<objective>
Подключить student-excuses UI к рабочему API: добавить ExcuseType dropdown в форму, заменить DEFERRED-заглушки на реальные вызовы (Wave 4, параллельно с план 06, 08).

Purpose: D-21/D-22 — форма создания тикета должна иметь dropdown причины и отправлять JSON через реальный POST endpoint. Список тикетов должен загружаться из /excuses/me.

Output:
- Обновлённый ExcuseFormDialogComponent с ExcuseType dropdown
- submitExcuse() → JSON POST (не FormData, файлы out of scope D-03)
- getExcuseTickets() → GET /api/attendance/excuses/me
- Обновлённые Vitest тесты
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-excuses-backend/59-CONTEXT.md
@.planning/phases/59-excuses-backend/59-01-SUMMARY.md

<interfaces>
<!-- API контракт из плана 01 -->

POST /api/attendance/excuses:
```json
{
  "lessonIds": [123, 456],
  "excuseType": "illness",
  "comment": "болел"
}
```

GET /api/attendance/excuses/me?page=0&size=20:
```json
{
  "_embedded": {
    "excuseTicketList": [...]
  },
  "_links": { ... },
  "page": { ... }
}
```

ExcuseTicket response (из ExcuseTicketResponse.java, план 01):
```typescript
// Добавить в student-schedule.types.ts
export interface ExcuseTicket {
  id: string;
  studentId: number;
  groupId: number;
  studentName: string;
  lessonIds: number[];
  excuseType: ExcuseType;  // lowercase string
  comment: string | null;
  status: ExcuseTicketStatus;  // lowercase
  decisionBy: number | null;
  decisionComment: string | null;
  decisionAt: string | null;
  createdAt: string;
  updatedAt: string;
}

export type ExcuseType = 'illness' | 'summons' | 'university_order' | 'exemption' | 'free_attendance' | 'other';
export type ExcuseTicketStatus = 'submitted' | 'approved' | 'rejected' | 'draft';
```

ExcuseType русские подписи (D-21):
```typescript
const EXCUSE_TYPE_LABELS: Record<ExcuseType, string> = {
  illness: 'Болезнь',
  summons: 'Повестка',
  university_order: 'Приказ университета',
  exemption: 'Освобождение',
  free_attendance: 'Свободное посещение',
  other: 'Другое',
};
```

Существующий student-excuses.component.ts уже имеет statusLabels с 'pending' но ExcuseTicketStatus enum SUBMITTED, не PENDING.
ВАЖНО: статус 'submitted' будет возвращаться как 'submitted' из backend. Обновить statusLabels.

Существующая форма уже имеет:
- lessonIds selection (checkbox список)
- comment поле
- submitExcuse() вызов

Нужно добавить:
- excuseType select/dropdown
- убрать file upload (D-03 — out of scope)
- изменить submitExcuse() — передавать excuseType, убрать files
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StudentApiService обновления + типы</name>
  <files>
    frontends/web-panel/src/app/features/student/shared/student-api.service.ts
    frontends/web-panel/src/app/features/student/shared/student-schedule.types.ts
  </files>
  <read_first>
    - frontends/web-panel/src/app/features/student/shared/student-api.service.ts (РЕДАКТИРОВАТЬ — полный файл)
    - frontends/web-panel/src/app/features/student/shared/student-schedule.types.ts (РЕДАКТИРОВАТЬ — обновить ExcuseTicket тип)
    - .planning/phases/59-excuses-backend/59-CONTEXT.md (D-21, D-22)
  </read_first>
  <behavior>
    - getExcuseTickets() делает GET /api/attendance/excuses/me (а не /api/attendance/excuses)
    - submitExcuse(lessonIds, excuseType, comment) отправляет JSON body {lessonIds, excuseType, comment} через http.post (НЕ FormData)
    - Ошибки 404 → of([]) для getExcuseTickets(), 404 → of(undefined) для submitExcuse()
    - ExcuseTicket тип обновлён — поле excuseType добавлено, status может быть 'submitted' (не 'pending')
    - ExcuseType тип обновлён до 6 значений в виде lowercase string union
  </behavior>
  <action>
    1. Открыть student-schedule.types.ts. Добавить/обновить:
       ```typescript
       export type ExcuseType = 'illness' | 'summons' | 'university_order' | 'exemption' | 'free_attendance' | 'other';
       export type ExcuseTicketStatus = 'submitted' | 'approved' | 'rejected' | 'draft';
       
       export interface ExcuseTicket {
         id: string;
         studentId: number;
         groupId: number;
         studentName: string;
         lessonIds: number[];
         excuseType: ExcuseType;
         comment: string | null;
         status: ExcuseTicketStatus;
         decisionBy: number | null;
         decisionComment: string | null;
         decisionAt: string | null;
         createdAt: string;
         updatedAt: string;
       }
       ```
    2. Открыть student-api.service.ts. Обновить getExcuseTickets():
       - URL: /api/attendance/excuses/me (добавить ?page=0&size=20 через HttpParams)
       - embedded key: excuseTicketList (или первый массив из embedded — defensive)
    3. Обновить submitExcuse() сигнатуру: `submitExcuse(lessonIds: number[], excuseType: ExcuseType, comment: string | null): Observable<void>`
       - Тело: `{ lessonIds, excuseType, comment }` через http.post как JSON
       - Убрать FormData и files параметр
       - 404 → of(undefined as unknown as void) как прежде
       - Убрать HTTP 404 graceful degradation у submitExcuse — при backend готовом 404 должен быть ошибкой (или оставить для безопасности — на усмотрение исполнителя)
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack/frontends/web-panel && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript компилируется без ошибок; grep "/excuses/me" student-api.service.ts находит строку; submitExcuse больше не принимает files параметр</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ExcuseFormDialog с dropdown + StudentExcusesComponent обновление + тесты</name>
  <files>
    frontends/web-panel/src/app/features/student/excuses/excuse-form-dialog/excuse-form-dialog.component.ts
    frontends/web-panel/src/app/features/student/excuses/excuse-form-dialog/excuse-form-dialog.component.html
    frontends/web-panel/src/app/features/student/excuses/student-excuses.component.ts
    frontends/web-panel/src/app/features/student/excuses/student-excuses.component.html
    frontends/web-panel/src/app/features/student/excuses/student-excuses.component.spec.ts
  </files>
  <read_first>
    - frontends/web-panel/src/app/features/student/excuses/excuse-form-dialog/excuse-form-dialog.component.ts (РЕДАКТИРОВАТЬ — полный файл)
    - frontends/web-panel/src/app/features/student/excuses/excuse-form-dialog/excuse-form-dialog.component.html (РЕДАКТИРОВАТЬ)
    - frontends/web-panel/src/app/features/student/excuses/student-excuses.component.ts (РЕДАКТИРОВАТЬ)
    - frontends/web-panel/src/app/features/student/excuses/student-excuses.component.spec.ts (РЕДАКТИРОВАТЬ — обновить тесты)
    - frontends/web-panel/src/app/features/headman/excuses/headman-excuses.component.spec.ts (образец теста этой фичи)
    - .planning/phases/59-excuses-backend/59-CONTEXT.md (D-21, D-22, D-24)
  </read_first>
  <behavior>
    - Test 1 (AC-9 dropdown): компонент ExcuseFormDialogComponent отрендерен → select/mat-select с 6 опциями ExcuseType присутствует
    - Test 2 (AC-9 submit): форма заполнена (lessonId выбран, excuseType выбран) → submit() вызывает studentApiService.submitExcuse(lessonIds, excuseType, comment)
    - Test 3: submit() без выбранного excuseType → validationError.set('Выберите причину пропуска')
    - Test 4 (AC-9 list): StudentExcusesComponent отрендерен → getExcuseTickets() вызывается; при наличии тикетов в списке → отображаются карточки
    - Test 5: statusLabels['submitted'] === 'На рассмотрении'
  </behavior>
  <action>
    1. Обновить ExcuseFormDialogComponent:
       a. Добавить в форму excuseType FormControl (required) с FormBuilder
       b. Добавить readonly excuseTypeLabels = EXCUSE_TYPE_LABELS map (6 пар)
       c. Добавить readonly excuseTypes = Object.keys(EXCUSE_TYPE_LABELS) as ExcuseType[]
       d. В submit(): валидировать selectedLessonIds.length > 0 И excuseType !== null; если нет → validationError
       e. Вызвать apiService.submitExcuse(ids, this.excuseTypeControl.value, comment) (убрать files)
       f. Убрать file upload логику (D-03 out of scope) — убрать drag-and-drop, файловый input, MAX_FILE_SIZE_BYTES/MAX_FILE_COUNT
    2. Обновить excuse-form-dialog.component.html:
       - Добавить mat-select или native select с [ngModel] или FormControl для excuseType
       - Убрать файловый блок (drag-and-drop зону)
    3. Обновить StudentExcusesComponent:
       - statusLabels: заменить 'pending' → 'submitted': 'На рассмотрении'
       - При необходимости добавить excuseTypeLabels для отображения причины в списке
    4. Обновить student-excuses.component.spec.ts:
       - Обновить mock StudentApiService
       - 5 тестов из behavior-блока
    5. Запустить тесты.
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack/frontends/web-panel && npx vitest run --reporter=verbose 2>&1 | grep -E "excuse|PASS|FAIL|Tests" | tail -20</automated>
  </verify>
  <done>Vitest тесты для excuse компонентов зелёные; grep "excuseType" excuse-form-dialog.component.ts находит поле формы; grep "submitExcuse" и "excuseType" в вызове — верно передаётся</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Angular form → API | excuseType должен быть одним из 6 допустимых значений; валидация на бэкенде |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-07-01 | Tampering | excuseType: произвольная строка в JSON | mitigate | Backend валидирует ExcuseType enum; frontend select ограничен 6 значениями |
| T-59-07-02 | Information Disclosure | список тикетов показывает чужие данные | mitigate | API GET /excuses/me возвращает только тикеты текущего пользователя (JWT.sub) |
</threat_model>

<verification>
- `cd frontends/web-panel && npx tsc --noEmit` → без ошибок
- `cd frontends/web-panel && npx vitest run --reporter=verbose 2>&1 | grep -E "excuse"` → тесты зелёные
- `grep "excuses/me" frontends/web-panel/src/app/features/student/shared/student-api.service.ts` → находит
- `grep "excuseType" frontends/web-panel/src/app/features/student/excuses/excuse-form-dialog/excuse-form-dialog.component.ts` → находит FormControl
- `cd frontends/web-panel && npm test -- --run` → все 346+ тестов зелёные
</verification>

<success_criteria>
- ExcuseFormDialog: dropdown с 6 ExcuseType значениями на русском; submit() передаёт excuseType в JSON (не FormData); file upload убран
- StudentExcusesComponent: loadTickets() вызывает GET /excuses/me; statusLabels обновлены (submitted → 'На рассмотрении')
- StudentApiService: getExcuseTickets() → /excuses/me; submitExcuse(lessonIds, excuseType, comment) → JSON POST
- student-excuses.component.spec.ts: 5 тестов зелёные (AC-9)
</success_criteria>

<output>
После завершения создать `.planning/phases/59-excuses-backend/59-07-SUMMARY.md`
</output>
