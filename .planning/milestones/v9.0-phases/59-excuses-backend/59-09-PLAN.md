---
phase: 59-excuses-backend
plan: 09
type: execute
wave: 5
depends_on:
  - 59-04
  - 59-05
  - 59-06
  - 59-07
  - 59-08
files_modified:
  - services/attendance-service/attendance-app/src/test/java/ru/rutcampustrack/attendance/excuse/ExcuseControllerIT.java
  - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/excuse/ExcuseService.java
  - docs/phase-59-report.md
  - .planning/phases/59-excuses-backend/59-VALIDATION.md
autonomous: false
requirements:
  - AC-1
  - AC-2
  - AC-3
  - AC-4
  - AC-5
  - AC-6
  - AC-7
  - AC-8
  - AC-11
  - AC-12

must_haves:
  truths:
    - "ExcuseControllerIT: AC-1..AC-6 покрыты интеграционными тестами (Testcontainers MongoDB + RabbitMQ)"
    - "ExcuseService валидирует lessonIds через ScheduleGrpcClient.getLessonsByIds (интегрирован из плана 03)"
    - "./gradlew :attendance-service:attendance-app:test → BUILD SUCCESS"
    - "cd services/notification-bot && pytest tests/ → all passed"
    - "cd frontends/web-panel && npm test -- --run → все тесты зелёные"
    - "./gradlew :services:schedule-service:schedule-app:test → BUILD SUCCESS (нет регрессий)"
    - "phase-59-report.md создан"
  artifacts:
    - path: "services/attendance-service/attendance-app/src/test/java/ru/rutcampustrack/attendance/excuse/ExcuseControllerIT.java"
      provides: "Полный IT тест (AC-1..AC-6)"
    - path: "docs/phase-59-report.md"
      provides: "Phase completion report"
  key_links:
    - from: "ExcuseControllerIT"
      to: "ExcuseController + ExcuseService + MongoDB + RabbitMQ"
      via: "@SpringBootTest + Testcontainers"
      pattern: "@SpringBootTest"
---

<objective>
Финальная интеграция: ExcuseService + ScheduleGrpcClient validateLessonIds, полный ExcuseControllerIT, регрессионные проверки всех сервисов и report (Wave 5).

Purpose: Все предыдущие планы независимы друг от друга — этот план соединяет их: добавляет gRPC-валидацию lessonIds в сервис, создаёт интеграционный тест полного flow, проверяет регрессии и документирует фазу.

Output:
- ExcuseService: validateLessonIds через ScheduleGrpcClient (план 03)
- ExcuseControllerIT: 6 интеграционных тестов AC-1..AC-6
- Полная регрессия всех 3 компонентов
- docs/phase-59-report.md
- 59-VALIDATION.md: nyquist_compliant: true
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/59-excuses-backend/59-CONTEXT.md
@.planning/phases/59-excuses-backend/59-RESEARCH.md
@.planning/phases/59-excuses-backend/59-VERIFICATION.md
@.planning/phases/59-excuses-backend/59-02-SUMMARY.md
@.planning/phases/59-excuses-backend/59-03-SUMMARY.md
@.planning/phases/59-excuses-backend/59-04-SUMMARY.md
@.planning/phases/59-excuses-backend/59-05-SUMMARY.md

<interfaces>
<!-- Существующий паттерн CheckinIntegrationTest как образец -->

CheckinIntegrationTest (найти в attendance-app/src/test/) — образец @SpringBootTest IT с Testcontainers.

ExcuseControllerIT тесты по AC:
- AC-1: POST /attendance/excuses как STUDENT → 201 + ExcuseTicketResponse
- AC-2: POST с повторным lessonId → 409
- AC-3: POST с is_headman=true → 409
- AC-4: GET /attendance/excuses/{id} от другого студента → 403
- AC-5: PATCH /attendance/excuses/{id}/status {approved} → 200 + AttendanceDocument обновлён
- AC-6: PATCH от headman на свой тикет → 409

ExcuseService.createExcuse() — добавить вызов validateLessonIds перед сохранением:
```java
// После проверки headman и duplicate:
List<LessonInfo> lessons = scheduleGrpcClient.getLessonsByIds(request.lessonIds());
validateAllLessonsInGroup(lessons, requestContext.getGroupId(), request.lessonIds());
// Если любой урок не принадлежит группе → BadRequestException
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Интегрировать gRPC валидацию lessonIds в ExcuseService</name>
  <files>
    services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/excuse/ExcuseService.java
  </files>
  <read_first>
    - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/excuse/ExcuseService.java (РЕДАКТИРОВАТЬ)
    - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/grpc/ScheduleGrpcClient.java (метод getLessonsByIds из плана 03)
    - .planning/phases/59-excuses-backend/59-03-SUMMARY.md (подтвердить сигнатуру getLessonsByIds)
    - .planning/phases/59-excuses-backend/59-CONTEXT.md (D-10, D-25)
  </read_first>
  <action>
    1. Открыть ExcuseService.java. В createExcuse() после проверки headman и duplicate lessonId — добавить:
       ```java
       // D-25: Validate lessonIds belong to student's group via gRPC
       List<LessonInfo> lessons = scheduleGrpcClient.getLessonsByIds(request.lessonIds());
       Set<Long> returnedIds = lessons.stream().map(LessonInfo::getLessonId).collect(Collectors.toSet());

       // Проверить: все запрошенные ids вернулись
       for (Long requestedId : request.lessonIds()) {
           if (!returnedIds.contains(requestedId)) {
               throw new BadRequestException("Урок с id=" + requestedId + " не найден");
           }
       }

       // D-25: Проверить что все уроки принадлежат группе студента
       Long studentGroupId = requestContext.getGroupId();
       for (LessonInfo lesson : lessons) {
           if (!Objects.equals(lesson.getGroupId(), studentGroupId)) {
               throw new BadRequestException("Урок с id=" + lesson.getLessonId() + " не принадлежит вашей группе");
           }
       }
       ```
       Импортировать: LessonInfo из ru.rutcampustrack.schedule.grpc.LessonInfo.
    2. Если ScheduleGrpcClient ещё не инъецирован в ExcuseService конструктор — добавить его.
    3. Компилировать.
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && ./gradlew.bat :services:attendance-service:attendance-app:compileJava 2>&1 | tail -10</automated>
  </verify>
  <done>Компиляция зелёная; grep "getLessonsByIds" ExcuseService.java находит вызов</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ExcuseControllerIT — полный интеграционный тест</name>
  <files>
    services/attendance-service/attendance-app/src/test/java/ru/rutcampustrack/attendance/excuse/ExcuseControllerIT.java
    .planning/phases/59-excuses-backend/59-VALIDATION.md
  </files>
  <read_first>
    - services/attendance-service/attendance-app/src/test/java/ru/rutcampustrack/attendance/ (найти существующий IT тест — образец @SpringBootTest + Testcontainers)
    - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/excuse/ExcuseController.java (эндпоинты для HTTP вызовов)
    - .planning/phases/59-excuses-backend/59-RESEARCH.md (§ AC-1..AC-6 test commands)
    - .planning/phases/59-excuses-backend/59-VALIDATION.md (заполнить per-task verification map)
  </read_first>
  <behavior>
    - AC-1: POST /attendance/excuses с валидным запросом (STUDENT, не headman) → 201 + body.id != null, body.status = "submitted"
    - AC-2: POST с lessonId уже в активном тикете того же студента → 409
    - AC-3: POST с X-Is-Headman: true → 409
    - AC-4: GET /attendance/excuses/{id} от студента2 (другой userId) → 403
    - AC-5: PATCH /attendance/excuses/{id}/status {status:"approved"} от headman → 200; проверить AttendanceRepository что документ существует с status="excused"
    - AC-6: PATCH headman пытается approve тикет где ticket.studentId == headman.userId → 409
  </behavior>
  <action>
    1. Создать ExcuseControllerIT.java:
       - @SpringBootTest(webEnvironment = RANDOM_PORT)
       - @Testcontainers
       - Testcontainers: MongoDBContainer (mongo:6) + RabbitMQContainer (rabbitmq:3-alpine)
       - @DynamicPropertySource для MongoDB URI и RabbitMQ URL
       - inject TestRestTemplate или MockMvc (предпочтительно TestRestTemplate с @SpringBootTest RANDOM_PORT)
       - Хелпер buildHeaders(userId, role, groupId, isHeadman): возвращает HttpHeaders с X-User-Id, X-User-Role, X-Group-Id, X-Is-Headman
       - Для D-25 (gRPC): mock ScheduleGrpcClient.getLessonsByIds() через @MockBean возвращать тестовые LessonInfo с groupId совпадающим
       - 6 тестов из behavior-блока
    2. Запустить тест.
    3. Открыть 59-VALIDATION.md. Заполнить per-task verification map таблицу для всех 9 планов на основе выполненных задач. Установить nyquist_compliant: true в frontmatter.
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && ./gradlew.bat :services:attendance-service:attendance-app:test --tests "*ExcuseControllerIT" 2>&1 | tail -30</automated>
  </verify>
  <done>ExcuseControllerIT зелёный (6 тестов); 59-VALIDATION.md содержит заполненную таблицу; nyquist_compliant: true</done>
</task>

<task type="auto">
  <name>Task 3: Полная регрессия + phase-59-report.md</name>
  <files>
    docs/phase-59-report.md
  </files>
  <read_first>
    - docs/phase-58-report.md (образец формата отчёта)
    - .planning/phases/59-excuses-backend/59-VERIFICATION.md (AC список для отчёта)
    - .planning/phases/59-excuses-backend/59-01-SUMMARY.md
    - .planning/phases/59-excuses-backend/59-02-SUMMARY.md
    - .planning/phases/59-excuses-backend/59-03-SUMMARY.md
    - .planning/phases/59-excuses-backend/59-04-SUMMARY.md
    - .planning/phases/59-excuses-backend/59-05-SUMMARY.md
    - .planning/phases/59-excuses-backend/59-06-SUMMARY.md
    - .planning/phases/59-excuses-backend/59-07-SUMMARY.md
    - .planning/phases/59-excuses-backend/59-08-SUMMARY.md
  </read_first>
  <action>
    1. Запустить полную регрессию:
       ```bash
       # Backend
       ./gradlew.bat :services:attendance-service:attendance-app:test
       ./gradlew.bat :services:schedule-service:schedule-app:test
       # Bot
       cd services/notification-bot && pytest tests/ -v
       # Frontend
       cd frontends/web-panel && npm test -- --run
       ```
       Убедиться что все зелёные. Если есть fail → починить до создания отчёта.
    2. Создать docs/phase-59-report.md по образцу phase-58-report.md:
       - Phase: 59 Excuse Tickets Backend
       - Status: COMPLETE
       - Date: текущая дата
       - What was done: перечень реализованных компонентов
       - AC coverage: таблица AC-1..AC-12 со статусом
       - Tests: количество новых тестов по компоненту
       - Known limitations / UAT pending: AC-9, AC-10 требуют ручной UAT
       - Architecture decisions: ExcuseTicketStatus.SUBMITTED используется как pending; AttendanceWritePort паттерн; envelope структура для notification-bot совместимости
    3. Не обновлять CLAUDE.md — это делает оркестратор.
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && ./gradlew.bat :services:attendance-service:attendance-app:test :services:schedule-service:schedule-app:test 2>&1 | grep -E "BUILD|tests|passed|failed" | tail -15</automated>
  </verify>
  <done>Все тесты зелёные; docs/phase-59-report.md создан; AC-1..AC-12 задокументированы</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: UAT — сквозной сценарий тикет студента → одобрение старостой</name>
  <action>Автоматизированные тесты завершены. Требуется ручная проверка UI flow и Telegram интеграции.</action>
  <verify>
    <automated>MISSING — AC-9 и AC-10 требуют ручного UAT (Telegram alerts нельзя автоматизировать)</automated>
  </verify>
  <done>UAT сценарии пройдены: студент создаёт тикет → Telegram alert старосте → головной одобряет → студент получает уведомление → attendance record обновлён</done>
  <what-built>
    Полный backend для тикетов о пропуске:
    - ExcuseTicket MongoDB коллекция
    - REST API 5 эндпоинтов через /api/attendance/excuses
    - gRPC LessonsByIds в schedule-service
    - Cascade на attendance при approve
    - RabbitMQ events: excuse.requested + excuse.decided
    - Bot handler для excuse.decided (уведомление студенту)
    - Frontend: ExcuseType dropdown в форме студента
    - Frontend: Headman excuses page с approve/reject
  </what-built>
  <how-to-verify>
    UAT сценарии из 59-VERIFICATION.md:
    1. Войти как student → /student/excuses → нажать "Новый запрос" → форма имеет dropdown "Причина пропуска" с 6 опциями
    2. Выбрать урок + причину "Болезнь" → отправить → тикет появляется в списке со статусом "На рассмотрении"
    3. Старосте должен прийти alert в Telegram с типом причины
    4. Войти как headman → /headman/excuses → pending тикет виден
    5. Одобрить тикет → статус у студента меняется на "Одобрено" + в журнале урок помечается уважительным
    6. Студенту должно прийти уведомление в Telegram об одобрении
    7. Попытаться создать второй тикет на тот же урок → получить ошибку 409
  </how-to-verify>
  <resume-signal>Введите "approved" если UAT пройден, или опишите найденные проблемы</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IT Test → ExcuseController | MockMvc с X-User-* headers имитирующими JWT claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-09-01 | Repudiation | Нет аудит-лога всех операций | accept | ExcuseTicket.decisionBy + decisionAt + updatedAt достаточно для аудита |
| T-59-09-02 | Information Disclosure | phase-59-report.md в репозитории | accept | Документация без секретов; стандартная практика проекта |
</threat_model>

<verification>
- `./gradlew.bat :services:attendance-service:attendance-app:test` → BUILD SUCCESS
- `./gradlew.bat :services:schedule-service:schedule-app:test` → BUILD SUCCESS
- `cd services/notification-bot && pytest tests/` → all passed
- `cd frontends/web-panel && npm test -- --run` → все тесты зелёные
- `./gradlew.bat :services:attendance-service:attendance-app:test --tests "*ExcuseControllerIT"` → 6 тестов зелёные
- `ls docs/phase-59-report.md` → файл существует
- `grep "nyquist_compliant: true" .planning/phases/59-excuses-backend/59-VALIDATION.md` → находит
</verification>

<success_criteria>
- AC-1..AC-6 покрыты ExcuseControllerIT
- AC-7 покрыт ExcuseEventContractIT (план 05)
- AC-8 покрыт test_excuse_decided.py (план 06)
- AC-11 покрыт LessonsByIdsGrpcIT (план 03)
- AC-12: все backend тесты + бот-тесты + frontend тесты зелёные
- docs/phase-59-report.md создан
- 59-VALIDATION.md: nyquist_compliant: true, per-task map заполнена
- UAT checkpoint пройден (AC-9, AC-10)
</success_criteria>

<output>
После завершения создать `.planning/phases/59-excuses-backend/59-09-SUMMARY.md`
</output>
