---
phase: 33-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api-gateway/src/main/resources/application.yml
  - services/api-gateway/src/main/java/ru/rutcampustrack/gateway/filter/JwtAuthenticationFilter.java
autonomous: true
requirements: [INFRA-04, INFRA-05]

must_haves:
  truths:
    - "OPTIONS preflight from Mini App dev origin (localhost:5174) returns Access-Control-Allow-Origin: http://localhost:5174"
    - "OPTIONS preflight from Web Panel origin (localhost:4200) returns Access-Control-Allow-Origin: http://localhost:4200"
    - "Unauthenticated POST to /api/auth/tma does not return 401 (path is whitelisted)"
    - "Unauthenticated POST to /api/auth/refresh-body does not return 401 (path is whitelisted)"
    - "Existing CORS for PWA origins (localhost:5173, localhost:80) still works"
  artifacts:
    - path: "services/api-gateway/src/main/resources/application.yml"
      provides: "CORS allowed-origins with 5 origins"
      contains: "http://localhost:5174"
    - path: "services/api-gateway/src/main/java/ru/rutcampustrack/gateway/filter/JwtAuthenticationFilter.java"
      provides: "PUBLIC_PATHS with 5 entries including /api/auth/tma and /api/auth/refresh-body"
      contains: "/api/auth/tma"
  key_links:
    - from: "application.yml"
      to: "Gateway CORS filter"
      via: "Spring Cloud Gateway globalcors configuration"
      pattern: "allowed-origins"
    - from: "JwtAuthenticationFilter.java"
      to: "isPublicRoute method"
      via: "PUBLIC_PATHS Set.of()"
      pattern: "/api/auth/tma"
---

<objective>
Expand Gateway CORS to accept Mini App and Web Panel origins, and whitelist two new auth paths in the JWT filter's PUBLIC_PATHS.

Purpose: Mini App (Phase 36) and Web Panel (Phase 38) need CORS approval to call the Gateway. Auth TMA and body-refresh endpoints (Phase 34) must be reachable without a JWT.
Output: Updated application.yml with 5 CORS origins, updated JwtAuthenticationFilter.java with 5 PUBLIC_PATHS entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@services/api-gateway/src/main/resources/application.yml
@services/api-gateway/src/main/java/ru/rutcampustrack/gateway/filter/JwtAuthenticationFilter.java

<interfaces>
<!-- Current CORS config (application.yml lines 14-16): -->
```yaml
allowed-origins:
  - "http://localhost:5173"
  - "http://localhost:80"
```

<!-- Current PUBLIC_PATHS (JwtAuthenticationFilter.java lines 32-36): -->
```java
private static final Set<String> PUBLIC_PATHS = Set.of(
        "/api/auth/login",
        "/api/auth/refresh",
        "/api/auth/public-key"
);
```

<!-- OPTIONS bypass already in place (line 50-52): -->
```java
if (exchange.getRequest().getMethod() == HttpMethod.OPTIONS) {
    return chain.filter(exchange);
}
```

<!-- DedupeResponseHeader filter already configured (line 28): -->
```yaml
default-filters:
  - DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials, RETAIN_UNIQUE
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand CORS origins and PUBLIC_PATHS</name>
  <files>services/api-gateway/src/main/resources/application.yml, services/api-gateway/src/main/java/ru/rutcampustrack/gateway/filter/JwtAuthenticationFilter.java</files>
  <read_first>
    - services/api-gateway/src/main/resources/application.yml (full file -- see current allowed-origins list at lines 14-16)
    - services/api-gateway/src/main/java/ru/rutcampustrack/gateway/filter/JwtAuthenticationFilter.java (full file -- see PUBLIC_PATHS at lines 32-36)
  </read_first>
  <action>
1. Edit `services/api-gateway/src/main/resources/application.yml` -- replace the `allowed-origins` list (lines 14-16) with:

```yaml
            allowed-origins:
              - "http://localhost:5173"
              - "http://localhost:80"
              - "http://localhost:5174"
              - "http://localhost:3000"
              - "http://localhost:4200"
```

The first two entries are existing (PWA dev + PWA prod). The three new entries are:
- `http://localhost:5174` -- Mini App dev (Vite on port 5174, one above PWA's 5173)
- `http://localhost:3000` -- Mini App prod (mini-app-nginx container)
- `http://localhost:4200` -- Web Panel dev/prod (Angular CLI default and web-panel-nginx)

Do NOT add a Landing origin -- Landing is a static page that makes no API calls.
Do NOT use wildcard `"*"` -- `allow-credentials: true` requires explicit origins per CORS spec.
Do NOT change any other part of the CORS config (allowed-methods, allowed-headers, allow-credentials, max-age, default-filters).

2. Edit `services/api-gateway/src/main/java/ru/rutcampustrack/gateway/filter/JwtAuthenticationFilter.java` -- replace the `PUBLIC_PATHS` Set (lines 32-36) with:

```java
    private static final Set<String> PUBLIC_PATHS = Set.of(
            "/api/auth/login",
            "/api/auth/refresh",
            "/api/auth/public-key",
            "/api/auth/tma",
            "/api/auth/refresh-body"
    );
```

The first three entries are existing. The two new entries are:
- `/api/auth/tma` -- Telegram Mini App initData authentication (Phase 34 will implement as POST)
- `/api/auth/refresh-body` -- Body-based token refresh for environments where httpOnly cookies are unavailable (Phase 34)

Do NOT change PUBLIC_PREFIXES, the OPTIONS bypass, or any other part of the filter.
  </action>
  <verify>
    <automated>grep -c "localhost:5174\|localhost:3000\|localhost:4200" services/api-gateway/src/main/resources/application.yml | grep -q "3" && grep "api/auth/tma" services/api-gateway/src/main/java/ru/rutcampustrack/gateway/filter/JwtAuthenticationFilter.java && grep "api/auth/refresh-body" services/api-gateway/src/main/java/ru/rutcampustrack/gateway/filter/JwtAuthenticationFilter.java && echo "GATEWAY_UPDATED"</automated>
  </verify>
  <acceptance_criteria>
    - application.yml contains exactly 5 entries under allowed-origins: "http://localhost:5173", "http://localhost:80", "http://localhost:5174", "http://localhost:3000", "http://localhost:4200"
    - application.yml does NOT contain wildcard "*" in allowed-origins
    - application.yml still contains "allow-credentials: true"
    - application.yml still contains "DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials, RETAIN_UNIQUE"
    - JwtAuthenticationFilter.java PUBLIC_PATHS contains "/api/auth/tma"
    - JwtAuthenticationFilter.java PUBLIC_PATHS contains "/api/auth/refresh-body"
    - JwtAuthenticationFilter.java PUBLIC_PATHS still contains "/api/auth/login", "/api/auth/refresh", "/api/auth/public-key"
    - JwtAuthenticationFilter.java OPTIONS bypass block is unchanged (line 50-52)
    - JwtAuthenticationFilter.java PUBLIC_PREFIXES is unchanged
  </acceptance_criteria>
  <done>Gateway CORS config lists 5 origins (2 existing + 3 new). JwtAuthenticationFilter.PUBLIC_PATHS lists 5 paths (3 existing + 2 new). No existing functionality broken.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Gateway compiles with updated config</name>
  <files>services/api-gateway/src/main/resources/application.yml, services/api-gateway/src/main/java/ru/rutcampustrack/gateway/filter/JwtAuthenticationFilter.java</files>
  <read_first>
    - services/api-gateway/src/main/java/ru/rutcampustrack/gateway/filter/JwtAuthenticationFilter.java (verify PUBLIC_PATHS change compiled correctly)
  </read_first>
  <action>
Run the Gradle build for the api-gateway module to confirm the Java change compiles and existing tests pass:

```bash
./gradlew.bat :services:api-gateway:build
```

If the build fails, read the error output and fix the issue. The only change is adding two strings to a `Set.of()` call -- compilation failure would indicate a syntax error (missing comma, unclosed parenthesis).

If existing tests fail, read the test output. The CORS and PUBLIC_PATHS changes should not break existing tests since:
- CORS is YAML config, not Java code
- PUBLIC_PATHS only adds entries (does not remove)
- The OPTIONS bypass is unchanged
  </action>
  <verify>
    <automated>cd . && ./gradlew.bat :services:api-gateway:build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew.bat :services:api-gateway:build` exits with code 0 (BUILD SUCCESSFUL)
    - No compilation errors in JwtAuthenticationFilter.java
    - All existing api-gateway tests pass
  </acceptance_criteria>
  <done>api-gateway module builds successfully with the CORS and PUBLIC_PATHS changes. No regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Gateway CORS | Cross-origin requests from new frontend origins |
| unauthenticated request -> PUBLIC_PATHS | Requests to /api/auth/tma and /api/auth/refresh-body bypass JWT check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04 | Spoofing | CORS allowed-origins | mitigate | Use explicit origin list (5 exact URLs), never wildcard; `allow-credentials: true` enforces browser-side origin validation; DedupeResponseHeader prevents duplicate ACAO headers |
| T-33-05 | Elevation of Privilege | PUBLIC_PATHS /api/auth/tma | mitigate | Path whitelisted for unauthenticated access, but Phase 34 will implement HMAC-SHA256 validation of Telegram initData -- the endpoint is not "open" but validates identity via a different mechanism than JWT |
| T-33-06 | Elevation of Privilege | PUBLIC_PATHS /api/auth/refresh-body | mitigate | Path whitelisted, but Phase 34 will validate the refresh token cryptographically -- same security as existing /api/auth/refresh (cookie-based) but via request body |
| T-33-07 | Tampering | CORS config change | accept | YAML change is additive (3 new origins); existing origins unchanged; DedupeResponseHeader already configured to prevent header duplication |
</threat_model>

<verification>
1. `./gradlew.bat :services:api-gateway:build` passes (BUILD SUCCESSFUL)
2. `grep -c "http://localhost" services/api-gateway/src/main/resources/application.yml` in allowed-origins section returns 5
3. `grep "/api/auth/tma" services/api-gateway/src/main/java/ru/rutcampustrack/gateway/filter/JwtAuthenticationFilter.java` finds the path
4. `grep "/api/auth/refresh-body" services/api-gateway/src/main/java/ru/rutcampustrack/gateway/filter/JwtAuthenticationFilter.java` finds the path
5. After starting Gateway: `curl -i -X OPTIONS -H "Origin: http://localhost:5174" http://localhost:8080/api/auth/login` returns `Access-Control-Allow-Origin: http://localhost:5174`
</verification>

<success_criteria>
- Gateway CORS accepts requests from 5 origins (2 existing PWA + 3 new)
- /api/auth/tma and /api/auth/refresh-body are in PUBLIC_PATHS
- Gateway builds and all existing tests pass
- No wildcard origins, no removed entries, no broken OPTIONS bypass
</success_criteria>

<output>
After completion, create `.planning/phases/33-infrastructure/33-02-SUMMARY.md`
</output>
