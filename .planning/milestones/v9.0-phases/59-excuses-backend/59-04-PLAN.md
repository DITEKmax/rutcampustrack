---
phase: 59-excuses-backend
plan: 04
type: execute
wave: 3
depends_on:
  - 59-02
  - 59-03
files_modified:
  - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/shared/port/AttendanceWritePort.java
  - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/checkin/AttendanceWritePortImpl.java
  - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/excuse/ExcuseService.java
  - services/attendance-service/attendance-app/src/test/java/ru/rutcampustrack/attendance/excuse/ExcuseServiceApproveIT.java
autonomous: true
requirements:
  - FR-5
  - NFR-2
  - AC-5

must_haves:
  truths:
    - "AttendanceWritePort интерфейс создан в shared/port/ — изолирован от checkin/ пакета"
    - "При одобрении тикета (APPROVED) все lessonIds получают AttendanceRecord со статусом EXCUSED или FREE_ATTENDANCE"
    - "ExcuseType.FREE_ATTENDANCE → AttendanceStatus.FREE_ATTENDANCE; все остальные ExcuseType → EXCUSED"
    - "Если AttendanceDocument для lessonId+studentId уже существует — обновить статус"
    - "При отклонении (REJECTED) — attendance записи НЕ изменяются"
    - "AttendanceWritePortImpl находится в пакете checkin/ — реализует порт; excuse/ не импортирует checkin/ напрямую"
  artifacts:
    - path: "services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/shared/port/AttendanceWritePort.java"
      provides: "Порт для записи attendance из excuse домена"
    - path: "services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/checkin/AttendanceWritePortImpl.java"
      provides: "Имплементация порта — upsert AttendanceDocument"
    - path: "services/attendance-service/attendance-app/src/test/java/ru/rutcampustrack/attendance/excuse/ExcuseServiceApproveIT.java"
      provides: "Интеграционный тест каскада (AC-5)"
  key_links:
    - from: "ExcuseService.updateStatus (APPROVED path)"
      to: "AttendanceWritePort.mark(studentId, lessonId, status)"
      via: "порт инъекции — НИКАКОГО прямого импорта из checkin/"
      pattern: "attendanceWritePort.mark"
    - from: "AttendanceWritePortImpl.mark"
      to: "AttendanceRepository.findByLessonIdAndUserId"
      via: "upsert логика"
      pattern: "attendanceRepository"
---

<objective>
Реализовать каскад на attendance записи при одобрении тикета (Wave 3).

Purpose: D-16 требует что при approve → AttendanceRecord обновляется/создаётся для каждого lessonId. Изоляция доменов из CLAUDE.md — порт в shared/port/, имплементация в checkin/.

Output:
- `AttendanceWritePort.java` — новый интерфейс в shared/port/ с методом mark()
- `AttendanceWritePortImpl.java` — имплементация в checkin/ пакете (upsert AttendanceDocument)
- `ExcuseService.java` — дополнить updateStatus: inject AttendanceWritePort, вызвать mark() при APPROVED
- `ExcuseServiceApproveIT.java` — интеграционный тест с Testcontainers MongoDB
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-excuses-backend/59-CONTEXT.md
@.planning/phases/59-excuses-backend/59-RESEARCH.md
@.planning/phases/59-excuses-backend/59-02-SUMMARY.md

<interfaces>
<!-- Существующие порты и паттерны -->

AttendanceReadPort.java (образец порта в shared/port/):
```java
// shared/port/ — ZERO imports from checkin/ package
public interface AttendanceReadPort {
    List<AttendanceRecord> findByLessonId(Long lessonId);
    // ...
}
```

AttendanceDocument.java (MongoDB document в checkin/):
```java
@Data @Builder @NoArgsConstructor @AllArgsConstructor
@Document(collection = "attendances")
public class AttendanceDocument {
    @Id private String id;
    @Field("lesson_id") private Long lessonId;
    @Field("user_id") private Long userId;
    @Field("group_id") private Long groupId;
    @Field("subject_id") private Long subjectId;
    @Field("semester_id") private Long semesterId;
    @Field("lesson_number") private Integer lessonNumber;
    @Field("lesson_date") private LocalDate lessonDate;
    @Field("status") private AttendanceStatus status;
    @Field("source") private AttendanceSource source;
    @Field("marked_by") private Long markedBy;
    @Field("created_at") private Instant createdAt;
    @Field("updated_at") private Instant updatedAt;
}
```

AttendanceStatus enum (уже существует):
```java
public enum AttendanceStatus { PRESENT, ABSENT, EXCUSED, FREE_ATTENDANCE, CANCELLED; }
```

AttendanceSource enum:
```java
public enum AttendanceSource { STUDENT_GEO, TEACHER_MANUAL, AUTO_CLOSED, HEADMAN_EXCUSE; }
// Использовать HEADMAN_EXCUSE для записей созданных через тикет (если не существует — добавить)
```

ExcuseType → AttendanceStatus маппинг (D-16):
```
ILLNESS           → EXCUSED
SUMMONS           → EXCUSED
UNIVERSITY_ORDER  → EXCUSED
EXEMPTION         → EXCUSED
OTHER             → EXCUSED
FREE_ATTENDANCE   → FREE_ATTENDANCE
```

ExcuseService.updateStatus (результат плана 02):
```java
// При APPROVED статусе:
// attendanceWritePort.mark(ticket.getStudentId(), lessonId, mappedStatus) — для каждого lessonId
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AttendanceWritePort интерфейс + AttendanceWritePortImpl</name>
  <files>
    services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/shared/port/AttendanceWritePort.java
    services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/checkin/AttendanceWritePortImpl.java
    services/attendance-service/attendance-api-contract/src/main/java/ru/rutcampustrack/attendance/contract/enums/AttendanceSource.java
  </files>
  <read_first>
    - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/shared/port/AttendanceReadPort.java (образец порта)
    - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/checkin/AttendanceReadPortImpl.java (образец имплементации)
    - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/checkin/AttendanceDocument.java (уже существует)
    - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/checkin/AttendanceRepository.java (доступные методы)
    - services/attendance-service/attendance-api-contract/src/main/java/ru/rutcampustrack/attendance/contract/enums/AttendanceSource.java (проверить наличие HEADMAN_EXCUSE)
    - .planning/phases/59-excuses-backend/59-CONTEXT.md (D-16)
  </read_first>
  <action>
    1. Проверить AttendanceSource.java — если нет HEADMAN_EXCUSE, добавить его в enum.
    2. Создать AttendanceWritePort.java в пакете ru.rutcampustrack.attendance.shared.port:
       ```java
       // ZERO imports from checkin/ package
       public interface AttendanceWritePort {
           /**
            * Upsert an attendance record for a student/lesson with the given status.
            * Called from excuse domain upon ticket approval (D-16).
            * Source is set to HEADMAN_EXCUSE.
            */
           void mark(Long studentId, Long lessonId, Long groupId, AttendanceStatus status);
       }
       ```
    3. Создать AttendanceWritePortImpl.java в пакете ru.rutcampustrack.attendance.checkin:
       - @Component implements AttendanceWritePort
       - Конструктор инъекция: AttendanceRepository
       - mark() метод:
         a. Найти существующий документ: attendanceRepository.findByLessonIdAndUserId(lessonId, studentId) — если нет такого метода в репозитории, добавить Optional&lt;AttendanceDocument&gt; findByLessonIdAndUserId(Long, Long)
         b. Если существует — обновить status, source=HEADMAN_EXCUSE, updatedAt=Instant.now(), сохранить
         c. Если не существует — создать новый AttendanceDocument с builder: userId=studentId, lessonId, groupId, status, source=HEADMAN_EXCUSE, createdAt/updatedAt=Instant.now(). Поля semesterId/subjectId/lessonNumber/lessonDate заполнить null или 0 (они заполняются полностью только при реальном checkin — для excuse достаточно минимального набора). Сохранить.
    4. Компилировать модуль.
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && ./gradlew.bat :services:attendance-service:attendance-app:compileJava 2>&1 | tail -20</automated>
  </verify>
  <done>Компиляция зелёная; AttendanceWritePort.java в shared/port/; AttendanceWritePortImpl.java в checkin/; grep "HEADMAN_EXCUSE" attendance-api-contract/ выдаёт AttendanceSource.java</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Интеграция AttendanceWritePort в ExcuseService + тест каскада</name>
  <files>
    services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/excuse/ExcuseService.java
    services/attendance-service/attendance-app/src/test/java/ru/rutcampustrack/attendance/excuse/ExcuseServiceApproveIT.java
  </files>
  <read_first>
    - services/attendance-service/attendance-app/src/main/java/ru/rutcampustrack/attendance/excuse/ExcuseService.java (РЕДАКТИРОВАТЬ — добавить инъекцию и вызов)
    - services/attendance-service/attendance-app/src/test/java/ru/rutcampustrack/attendance/ (найти AbstractAttendanceIntegrationTest или образец Testcontainers теста)
    - .planning/phases/59-excuses-backend/59-CONTEXT.md (D-16, D-17)
    - .planning/phases/59-excuses-backend/59-RESEARCH.md (§ AC-5, failure modes)
  </read_first>
  <behavior>
    - Test 1 (AC-5): тикет с 3 lessonIds, excuseType=ILLNESS → updateStatus(APPROVED) → в MongoDB найти 3 AttendanceDocument со status=excused, source=headman_excuse (проверить через MongoDB lowercase)
    - Test 2: excuseType=FREE_ATTENDANCE → updateStatus(APPROVED) → attendances status=free_attendance
    - Test 3: REJECTED → attendances НЕ изменились (0 новых записей для этих lessonIds)
    - Test 4: attendanceDocument уже существует с status=present → approve → status обновлён до excused
  </behavior>
  <action>
    1. Открыть ExcuseService.java. Добавить в конструктор AttendanceWritePort attendanceWritePort (@Autowired или конструктор инъекция). В методе updateStatus: после сохранения обновлённого тикета со статусом APPROVED — вызвать для каждого lessonId в ticket.getLessonIds():
       ```java
       if (request.getStatus() == ExcuseTicketStatus.APPROVED) {
           AttendanceStatus attendanceStatus = mapExcuseTypeToAttendanceStatus(ticket.getExcuseType());
           for (Long lessonId : ticket.getLessonIds()) {
               attendanceWritePort.mark(ticket.getStudentId(), lessonId, ticket.getGroupId(), attendanceStatus);
           }
       }
       ```
       Добавить private метод mapExcuseTypeToAttendanceStatus(ExcuseType):
       - FREE_ATTENDANCE → AttendanceStatus.FREE_ATTENDANCE
       - всё остальное → AttendanceStatus.EXCUSED
    2. Создать ExcuseServiceApproveIT.java:
       - @SpringBootTest(webEnvironment=NONE) + Testcontainers MongoDB (по образцу существующих IT тестов)
       - Настроить тестовый RequestContext mock с headman-правами
       - Создать ExcuseTicket напрямую в MongoDB через ExcuseRepository.save()
       - Вызвать excuseService.updateStatus(id, UpdateExcuseStatusRequest(APPROVED, "OK"))
       - Проверить через AttendanceRepository что документы созданы/обновлены с нужным статусом
       - 4 теста из behavior-блока
  </action>
  <verify>
    <automated>cd /c/Users/maksd/IntelliJIDEA/rutcampustrack && ./gradlew.bat :services:attendance-service:attendance-app:test --tests "*ExcuseServiceApproveIT" 2>&1 | tail -30</automated>
  </verify>
  <done>ExcuseServiceApproveIT зелёный (4 теста); grep "attendanceWritePort.mark" ExcuseService.java находит вызов; mapExcuseTypeToAttendanceStatus метод существует</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ExcuseService → AttendanceWritePort | Trusted internal call (same JVM); no external input after validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-04-01 | Tampering | AttendanceWritePortImpl.mark: race condition | mitigate | MongoDB findAndModify или upsert atomic; повторный approve блокирован D-18 в ExcuseService |
| T-59-04-02 | Tampering | Идемпотентность: approve дважды изменит attendance | mitigate | D-18: ExcuseService.updateStatus проверяет status == SUBMITTED перед изменением; повторный вызов невозможен |
| T-59-04-03 | Repudiation | Нет аудит-лога кто сделал attendance запись | accept | source=HEADMAN_EXCUSE + decisionBy в ExcuseTicket — достаточно для аудита |
</threat_model>

<verification>
- `./gradlew.bat :services:attendance-service:attendance-app:compileJava` → BUILD SUCCESSFUL
- `grep -r "implements AttendanceWritePort" services/attendance-service/` → AttendanceWritePortImpl.java
- `grep -r "attendanceWritePort" services/attendance-service/attendance-app/src/main/java/` → ExcuseService.java
- `./gradlew.bat :services:attendance-service:attendance-app:test --tests "*ExcuseServiceApproveIT"` → зелёный
- `./gradlew.bat :services:attendance-service:attendance-app:test` → все тесты (включая существующие) зелёные
</verification>

<success_criteria>
- AttendanceWritePort.java в shared/port/ с методом mark(studentId, lessonId, groupId, status)
- AttendanceWritePortImpl.java в checkin/ с upsert логикой
- ExcuseService.updateStatus: при APPROVED вызывает mark() для каждого lessonId с правильным AttendanceStatus
- mapExcuseTypeToAttendanceStatus: FREE_ATTENDANCE → FREE_ATTENDANCE; остальное → EXCUSED
- ExcuseServiceApproveIT: 4 теста зелёные (AC-5)
</success_criteria>

<output>
После завершения создать `.planning/phases/59-excuses-backend/59-04-SUMMARY.md`
</output>
